name: Test Conversation Service

on:
  push:
    branches: [main]
    paths:
      - "apps/conversation-service/**"
      - ".github/workflows/test-conversation-service.yml"
  pull_request:
    branches: [main]
    paths:
      - "apps/conversation-service/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm install -g pnpm
      - run: pnpm install
      - run: pnpm --filter @mcpfactory/conversation-service test
        env:
          CONVERSATION_SERVICE_DATABASE_URL: ${{ secrets.CONVERSATION_SERVICE_DATABASE_URL }}
          SERVICE_SECRET_KEY: test-secret
