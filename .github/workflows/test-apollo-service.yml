name: Test Apollo Service

on:
  push:
    branches: [main]
    paths:
      - "apps/apollo-service/**"
      - ".github/workflows/test-apollo-service.yml"
  pull_request:
    branches: [main]
    paths:
      - "apps/apollo-service/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - uses: pnpm/action-setup@v4
      - run: pnpm install
      - name: Build shared packages
        run: pnpm --filter @mcpfactory/runs-client build
      - name: Run unit tests
        run: pnpm --filter @mcpfactory/apollo-service test:unit

      - name: Create Neon branch for PR
        if: github.event_name == 'pull_request'
        id: neon
        uses: neondatabase/create-branch-action@v6
        with:
          project_id: ${{ vars.NEON_PROJECT_ID_APOLLO }}
          branch_name: pr-${{ github.event.number }}-apollo
          api_key: ${{ secrets.NEON_API_KEY }}

      - name: Set database URL
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "APOLLO_SERVICE_DATABASE_URL=${{ steps.neon.outputs.db_url_pooled }}" >> "$GITHUB_ENV"
          else
            echo "APOLLO_SERVICE_DATABASE_URL=${{ secrets.APOLLO_SERVICE_DATABASE_URL_DEV }}" >> "$GITHUB_ENV"
          fi

      - name: Push schema to DB
        run: pnpm --filter @mcpfactory/apollo-service exec drizzle-kit push --force
        env:
          APOLLO_SERVICE_DATABASE_URL: ${{ env.APOLLO_SERVICE_DATABASE_URL }}

      - name: Run integration tests
        run: pnpm --filter @mcpfactory/apollo-service test:integration
        env:
          APOLLO_SERVICE_DATABASE_URL: ${{ env.APOLLO_SERVICE_DATABASE_URL }}
          SERVICE_SECRET_KEY: test-secret
