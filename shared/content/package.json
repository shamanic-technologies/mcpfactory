{
  "name": "@mcpfactory/content",
  "version": "0.1.0",
  "description": "Single source of truth for MCP Factory content (pricing, MCPs, URLs, features)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "generate:readme": "tsx scripts/generate-readme.ts"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.3.0"
  }
}
