{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "./apps/mcp-service/Dockerfile",
    "watchPatterns": [
      "/apps/mcp-service/**",
      "/shared/**",
      "/pnpm-lock.yaml"
    ]
  },
  "deploy": {
    "healthcheckPath": "/health",
    "healthcheckTimeout": 10
  }
}
