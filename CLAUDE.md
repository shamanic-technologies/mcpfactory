# Project: MCP Factory

DFY (Done-For-You), BYOK (Bring Your Own Keys) automation platform on MCP. User gives URL + budget; the platform handles lead finding, outreach, email generation, reporting.

## Commands

```bash
pnpm dev                    # All services via Turbo
pnpm dev:dashboard          # Dashboard only (Next.js, port 3001)
pnpm dev:<service-name>     # Any individual service
pnpm build                  # Build all (Turbo-orchestrated)
pnpm lint                   # Lint all packages
pnpm generate:readme        # Regenerate README.md from shared/content

# Per-package testing — scripts use `test`/`test:unit`, NOT `vitest` directly
pnpm --filter @distribute/<package> test                              # all tests
pnpm --filter @distribute/<package> test:unit                         # unit only
pnpm --filter @distribute/<package> test tests/unit/specific.test.ts  # single file
```

**Verify frontend with `tsc --noEmit`, NOT lint — `next lint` is GONE in Next 16.** Any `next lint` invocation reads `lint` as a directory arg and fails; no standalone `eslint` binary in the repo. Real check: `cd apps/<app> && node_modules/.bin/tsc --noEmit -p tsconfig.json` (dashboard has ~3 pre-existing test-file `TS1501` regex errors — ignore, only care about your files). (#1223) **The suite passing is NOT a substitute: the guards are source-substring, so a file that does not even PARSE can keep them all green.** A `{/* comment */}` dropped directly inside ANY single-child JSX expression above the root element is two children in one expression — 10 `TS1005`/`TS1382`/`TS17015` errors, 1702 tests still passing. It is NOT only the `{cond && ( … )}` form: a ternary branch (`) : ( {/* … */} <div>`) fails identically, and that one reads as safe because the branch already has parentheses around it. Put the comment ABOVE the expression or INSIDE the element, and run `tsc` on every JSX edit however cosmetic (2026-08-28, the campaign Outcome chart: a comment explaining a new wrapper class, added between `) : (` and its `<div>`).

**Don't trust RTK's `next build` summary** — the hook replaces stdout with a `Errors: N | Warnings: M` one-liner that can be wrong (phantom `Errors: 1` on a clean run). Real run: `rtk proxy -- npx next build` (the `--` is required), trust the log + exit code.

**Running `pnpm dev:dashboard` (or any `next dev`) REWRITES the tracked `apps/<app>/next-env.d.ts`**, flipping its import from `./.next/types/routes.d.ts` to `./.next/dev/types/routes.d.ts`. A `git add -A` after a local render check therefore commits a dev-mode type path into a file whose own header says *"This file should not be edited"*, and nothing goes red — `tsc` and the suite pass either way, because the path only matters to whichever mode generated it. `git checkout -- apps/<app>/next-env.d.ts` before staging. Generalise: after booting the app to LOOK at a change (the render-it-and-look rule), read `git status` for files you never opened — a dev server writes to the tree, and the screenshot step is the one place in the workflow where that is easy to miss.

**Build shared workspace packages before app tests/build.** Vitest/Vite resolve workspace deps via `dist/`; an unbuilt `shared/*` surfaces as `Failed to resolve entry for package "@distribute/<name>"`. Run `pnpm -r build` after install or after pulling `shared/` changes.

**Source-substring page tests: a `not.toMatch(/X/)` guard trips on the fix's own explanatory comment** (a comment that writes the forbidden literal fails the test). Reword the comment, or assert against a comment-stripped copy (`pr-expert-public-report.test.ts` ships `stripComments(src)`). **Before adding a `not.toContain("<literal>")` guard, grep the WHOLE target file for that literal, not just the JSX you edited — a big page carries PRE-EXISTING comments mentioning it far from the code you touched, and each one fails the guard.** Reword every hit in the same commit. (#1235, DIS-112; #3033 removed an "AI-suggested" badge from `onboarding.tsx` and two unrelated comments ~1600 and ~300 lines away still wrote the literal.) **When the grep shows the literal also lives in LEGITIMATE code elsewhere, rewording is wrong — SCOPE the guard to a slice of the function you actually changed** (`sliceFrom("async function saveX(", N)` then `expect(body).not.toContain(...)`), because a file-wide `not.toContain` on a common expression asserts something you do not mean. #3039 forbade restating `replyToMeetingPct: nextRates.r2m` in the two economics SAVE paths, but the same expression legitimately builds the projection helper's econ overrides one screen away — a read, not a write; the file-wide guard failed on correct code until it was scoped to the two save functions. **The trap also runs in the OTHER direction, and that one has no target file to grep: some guards SCAN ALL of `src/` for a co-occurrence heuristic, so a DOC COMMENT you add to any file can trip a guard you never opened.** `api-proxy.test.ts` flags any file containing both `getToken` and `apiCall` as token-passing to the api client; the SWR rewrite of `use-tenant-switcher.ts` (which legitimately calls `getToken` for the org-switch re-mint) failed it purely because a comment explained that its reads "go through `apiCall`" — the code passes no token. Rewording the comment ("the shared api client") fixed it. So when a guard you didn't touch goes red on a file you only commented, read the guard's predicate before assuming a real violation. **Third face: the `sliceFrom(marker)` ANCHOR itself collides with a doc comment, silently slicing prose instead of code.** `indexOf` takes the FIRST hit, and a good doc comment above a component names the very token you want to anchor on (`role="button"`, a field name, a class), so the guard then asserts against explanatory English and fails with a confusing "expected 'role=\"button\">`, NOT a `<button>`. Se…' to contain 'tabIndex={0}'" — the quoted haystack is your own comment. Use `lastIndexOf`, or anchor on a JSX-only string (`aria-label="More info"`, `onKeyDown={(e) => {`). This bit twice on 2026-07-30 alone, once as a `not.toContain` on a comment and once as a mis-anchored slice. **Fifth face: a `const` declaration is a bad anchor, because a later change can HOIST or duplicate it and `indexOf` then slices from somewhere else entirely — silently, on a guard nobody touched.** A guard anchored on `const onePath = selectedFunnels.length === 1;` inside the budget step kept passing until that flag was hoisted to the top of the flow for a second reader; the first hit moved ~2800 lines up, so every assertion (including a `not.toContain("—")`) ran against unrelated code. Anchor on something only the target can contain: the function signature, or a string literal from its own copy. **Fourth face, the slice LENGTH: compute the offset, never guess it.** A `slice(at, at + N)` whose `N` falls short cuts the assertion's target out of the haystack, and the failure message quotes the SLICE (which starts with the function signature you anchored on), so it reads as "the code is missing" for code that is right there — and every comment you add to the function pushes the boundary further, so a guard that passed at commit time breaks on the next edit. Measure it once (`python3 -c "s=open(f).read(); a=s.index(anchor); print(s.index(target)-a)"`) and give the real distance real headroom. Cost 2026-07-30 (leads-table mobile fit): a 5000-then-7000-char slice and a 900-char slice each dropped their last assertion, two wasted suite runs. **For a `toContain`-only guard there is a length you never have to maintain: bound the slice to the NEXT declaration (`src.slice(at, src.indexOf("function <theNextOne>("))`) rather than a number.** A too-long slice cannot hurt a `toContain`, and the boundary then moves with the file instead of expiring on the next comment — which is what a measured-with-headroom number keeps failing to survive. Keep the measured number ONLY where the guard is a `not.toContain`, where a slice running past the function into its neighbour is the failure mode. Recurrence 2026-08-30 (#3793): `friendly-datetime.test.ts` sliced 11000 chars at a target sitting at 10996, so three added comment lines pushed it out and the run reported the delivery row's `friendlyDateTime` as missing from code that had not changed. **And the danger flips with the assertion's POLARITY, so "when unsure, give it headroom" is wrong half the time: a `toContain` fails when the slice is too SHORT (the target got cut out), a `not.toContain` fails when it is too LONG (the slice ran past the function into a NEIGHBOUR that legitimately contains the forbidden literal — very often the next function's doc comment).** So for a `not.toContain` guard, measure to the function's CLOSING BRACE and do not pad; write the measured number in a comment beside it so the next editor does not "fix" it by widening. Cost 2026-07-31 (#3167): a 400-char slice from `function continueFunnelStats()` — asserting the step persists nothing — reached the next function's comment *"Saved via sales-economics"* and failed on correct code; the body is 188 chars. **Fifth face, the slice DIRECTION: `sliceFrom` only ever looks FORWARD, so an anchor that sits AFTER its target can never see it.** Gating existing copy behind a new condition puts the gate BEFORE the string, so a guard anchored on the string and asserting the gate reads the wrong side of the file and fails no matter how long the slice is — widening it looks like the fix and is not. Anchor on the GATE and assert the copy, not the reverse. The same `python3` measurement that sizes a slice also reveals the sign (`print(s.index(target) - s.index(anchor))` going NEGATIVE is the tell), so measure before writing either direction. Cost 2026-08-24 (service-suggest-loading): a guard anchored on `We drafted these from` asserting `servicesDrafted` — the gate is 70 chars EARLIER. **Seventh face, and it fires on a file you are ADDING to rather than editing: a guard that COUNTS occurrences or takes the FIRST `indexOf` over a whole file is silently scoped to "there is one component in here", so landing a SECOND component in that file breaks it — and the failure reads as a shape regression in the component you did not touch.** `expect((src.match(/hidden md:table-cell/g) ?? []).length).toBe(10)` is a statement about ONE table; with two in the file it counts 23 and reports the original table as having grown seven columns. `src.indexOf("<thead>")` finds whichever table is declared first, which is the new one, so an ordering assertion about the old table's columns runs against the new table's header and fails on correct code. Both are ordinary consequences of co-locating a sibling, and neither is visible until the suite runs. So when you add a component beside a guarded one, bound EVERY existing count/first-index guard to the component it means (a `const listTable = src.slice(src.indexOf("export function X("))` at the top of the test file, used by each guard), and give the new component its own bounded slices in its own test. Cost 2026-08-28 (distribute.you #3683): four guards in `campaigns-page.test.ts` went red on a table whose markup had not changed by one character. **Eighth face, and it is the one that bites when you FOLLOW the bound-to-the-next-declaration advice on JSX: an ELEMENT cannot be bounded by its own closing token, because a self-closing CHILD carries that token first.** `PAGE.slice(at, PAGE.indexOf("/>", at))` on a `<Component … />` call site reads as the obvious stable bound and cuts the element off at its first child prop that happens to be a self-closing tag (`delivery={<StatusBadge … />}`), so every assertion about a prop below that line fails as "the code is missing" on code that is right there. Bound a JSX call site by the SECTION THAT FOLLOWS it (a heading string, the next component's tag) rather than by `/>`, `)` or `}` — all three appear inside almost every non-trivial element. Cost 2026-09-04 (#3896): five guards in `lead-funnel-stage-panel.test.ts` had expired measured slices, and the first bounded rewrite replaced one expiring failure mode with another in the same run.

**The `hidden` ATTRIBUTE loses to a display CLASS, so `hidden={cond}` on a `flex`/`grid`/`block` element does nothing.** `[hidden] { display: none }` lives in the UA stylesheet and any author `display` declaration outranks it, so `<div className="flex …" hidden={boardOnly}>` renders exactly as it did before — no error, no red test, the control simply stays reachable to anyone who looks. It reads as obviously correct, which is what makes it dangerous on a gate. Conditionally RENDER (`{!cond && (<div …>)}`) — which is the right shape anyway, since markup a page still carries is a control anybody can still reach — or put the state in the class (`${cond ? "hidden" : ""}`), where Tailwind's own `hidden` utility is a real `display: none` at author specificity. (2026-08-28, the campaign leads board.)

**Sixth, cheapest face: a guard asserting user-facing COPY must use the SOURCE spelling, not the rendered one.** JSX escapes an apostrophe as `&apos;`, so `toContain("We couldn't read your site")` fails against source reading `We couldn&apos;t read your site` while the rendered page is exactly what you typed. Copy-paste the literal out of the file rather than out of the design.

**Run `pnpm --filter @distribute/dashboard test` before pushing a dashboard change — `tsc` alone misses source-substring guards** (many tests assert a page contains a literal expression/copy string). `tsc` stays green while the guard goes red in CI and silently blocks auto-merge. Run it (~3s, source-only) and update the matching guard in the same commit. (#1252) **Run the package SCRIPT, never the raw `vitest` binary — the script is `vitest run --exclude 'tests/e2e/**'`, so a bare `node_modules/.bin/vitest run` also collects the three Playwright specs under `tests/e2e/`, which cannot run under vitest and report as 3 FAILED FILES. Every actual test still passes (`1504 passed / 0 failed` beside `3 failed` files), so the summary reads as a regression you just introduced when it is only the missing exclude.** Same shape in `apps/admin`. Tell them apart by the failed-FILE list being entirely `tests/e2e/*.spec.ts` and the failed-TEST count being zero.

**`apps/admin` tests are NOT a CI merge gate — only `apps/dashboard`'s are (`.github/workflows/test.yml` = build + `--filter @distribute/dashboard test` + lint; NO `--filter admin test` job).** So admin source-substring guards ROT SILENTLY: a rename/nav change that doesn't update the matching admin test ships red and nothing blocks it (#2689's Signups-tab rename left 4 stale assertions across `public-metrics-page.test.ts` + `context-sidebar.test.ts` + `last-brand.test.ts`, all red on main). Because there's no gate, you MUST run `pnpm --filter @distribute/admin test` locally before pushing an admin change and update every matching guard in the same commit — CI will not catch it for you. When editing an admin test file, also fix any pre-existing red assertions in THAT file (they're this same rot); sibling test files reading code you didn't touch are follow-ups, not scope. (#2698)

## `apps/admin` is the STAFF GOD-MODE console — a dashboard fork, NOT a customer surface, and it runs on the HETZNER BOX

**It is a container on `167.233.196.79` beside the whole backend fleet (#3298), reachable at `admin.distribute.you`.** It left Vercel on 2026-08-07 (#3293/#3297) because the allowlist held one person's two addresses and a hosted build cost 1,231 build minutes a month to serve nobody outside the team; `apps/admin/vercel.json` is vestigial, kept in case something goes back. `pnpm dev:admin` (port 3002) still works locally — seed `apps/admin/.env.local` from the box: `ssh -i ~/.ssh/oracle-distribute root@167.233.196.79 'cat /root/distribute/env/admin-app.env'` (and `admin-app.build.env` for the `NEXT_PUBLIC_*`), which carries every var admin needs including `ADMIN_DISTRIBUTE_API_KEY`.

- **`apps/admin/Dockerfile`, built from the MONOREPO ROOT.** `output: "standalone"` with `outputFileTracingRoot` pinned to the repo root — `next build` runs with `apps/admin` as its cwd, so without it the output silently misses everything pnpm hoisted above the package. The image installs only admin's subtree (`--filter @distribute/admin...`).
- **`NEXT_PUBLIC_*` is inlined at BUILD time** from `apps/admin/.env.production` (written on the box, gitignored). Changing one needs a rebuild, not a restart. Server-side secrets arrive at run time via the compose `env_file`.
- **Two runtime secrets did NOT survive the Vercel cutover, and a `process.env.X` grep cannot find that class of hole — `requireEnv("X")` reads the name as a STRING.** `env/admin-app.env` reached the box with 16 keys while `apps/admin/src` needs 18: `POSTHOG_PERSONAL_API_KEY` (the `/metrics` PostHog visitor + signup queries) and `STRIPE_SECRET_KEY` (the paid-users tab's per-customer payment-method fan-out) were both absent. They fail LOUD and correctly, so **four of the five `/metrics` tabs threw on render** — every one but `?view=active-users`, which is the only tab that skips `fetchPublicStatsSummary`. The symptom is a bare `digest` on screen and nothing else; the actual line is in `docker logs distribute-admin-app-1` (`⨯ Error: [public-stats] POSTHOG_PERSONAL_API_KEY is required`). Both values already sat on the same box in `env/dashboard-app.env`, so the recovery was two appended lines plus `docker compose up -d admin-app` — a RECREATE, not a rebuild, since neither is `NEXT_PUBLIC_*`. **Generalise: after moving an app between platforms, diff what it READS against what landed, and enumerate the reads with `grep -rhoE 'requireEnv\("[A-Z0-9_]+"\)'` ALONGSIDE the `process.env.` grep** — the env-var name inside a `requireEnv()` helper is a string literal, invisible to the obvious search, so the obvious search reports a clean bill of health on a container that is missing credentials. The existing "a deleted platform takes its secrets with it" rule below covers the values with no second source; this is the other half, where a second source existed and nobody checked whether the copy had been made.
- **The box is NOT a git repo** — `/root/distribute` is hand-managed like the rest of the fleet: `docker-compose.override.yml` holds `admin-app` (the base compose file is generated from the Railway bundle and says not to hand-edit it), `env/admin-app{,.build}.env`, a Caddy vhost, and `deploy-admin.sh` (fetch main → refresh `.env.production` → rebuild → restart).
- **Two gates, and the second one is the load-bearing one.** A Cloudflare Access application allows `kevin.lourd@gmail.com` only, so the email allowlist in middleware is now the second gate rather than the only one. But **Access protects a HOSTNAME, not an origin** — a request straight to the box's IP carrying the right `Host` and SNI sails past it, and it did (verified 200 before the fix). The Caddy vhost therefore 404s any peer outside Cloudflare's published ranges. Residual gap: an attacker proxying through their OWN Cloudflare zone arrives from a Cloudflare IP. Closing that needs a managed Transform Rule injecting a shared header (or Authenticated Origin Pulls), and **no token on this machine has the Transform Rules scope** — the MCP and the `cloudflare` skill's token both 403. **Every other service on that box has the same origin bypass and no allowlist at all.**
- **Admin REGISTERS NOTHING — `apps/admin/src/instrumentation.ts` is DELETED, and the dashboard is the single registrar for every shared platform store.** Do NOT re-add one. Both apps used to write email templates, platform keys, chat configs, the `cold-email` prompt and the welcome-gift pin, keyed by name and last-writer-wins; 6 of the 9 template names differed, so each boot silently overwrote the other app's copy (read at the destination: the dashboard wrote its 14 at 10:17:52, admin replaced 9 of them at 10:19:53). The dashboard's set is a strict SUPERSET for templates and platform keys and byte-identical for the rest, so deleting admin's copy removed nothing from the store. **A change to a chat config, the platform prompt, an email template or the gift amount therefore goes in `apps/dashboard`, even when the surface consuming it is admin-only** — admin's duplicate guards for those were deleted with it (the dashboard's own `workflow-chat-context` / `platform-config-startup` / `welcome-gift-pinned` cover the same ground). Owner-decided when the clobber was found: **the customer dashboard wins any conflict between the two.** (#3302) ⚠️ **That sentence settles admin-vs-dashboard and says nothing about chat-service, which SEEDS THREE CHAT CONFIGS OF ITS OWN** — `audience-editor`, `persona-editor` and `brand-profile-editor` live in chat-service's `src/lib/seed-platform-configs.ts` ("owned BY chat-service", its own words) and are absent from this repo's `PLATFORM_CHAT_CONFIGS`, which carries only `workflow` / `press-kit` / `feature` / `campaign-prefill`. So editing an editor-chat's system prompt or its tool descriptions is a chat-service PR, not a dashboard one, and a `grep` of `apps/dashboard/src/instrumentation.ts` for one of those keys comes back EMPTY — which reads as "this config does not exist" rather than "it is owned elsewhere". Verify at the DESTINATION either way (`docker exec … psql -U postgres -d chat_service` → `platform_configs`, keyed on `key`), never by reading a registrar.

⚠️ **Before deleting a deployment, diff its boot registrations against the app that survives.** `apps/admin/src/instrumentation.ts` was the ONLY registrar of the `posthog` platform key, which features-service consumes — a staff console with one user was supplying a backend credential. That entry moved to the dashboard first (#3292). The same class of gap applies to email templates and platform prompts: a registration that stops running fails SILENTLY, because the stored row survives and nothing re-writes it until the store moves. Verify at the DESTINATION (`SELECT … FROM platform_keys` on key-service's Neon), never by reading the registration code.

`apps/admin` is the dashboard cloned for internal staff. Two load-bearing facts before touching it:
- **The admin BRAND MARK is intentionally DISTINCT from the customer dashboard's GREEN charter dot — do NOT re-sync it in a fleet-wide rebrand.** Admin uses a WHITE mark on the theme-neutral surfaces (tab favicon `src/app/icon.svg`, `src/app/apple-icon.png`, `public/favicon.jpg`) and a theme-responsive header mark (`components/header.tsx`, inline SVG dot — **blue in light mode, white in dark mode**, no dark square). This is so a staff tab reads distinct from a customer tab at a glance. A "swap the mark across all apps" PR (like #2614 which green-ified everything) MUST skip admin or re-clobbers this — keep the customer dashboard green, admin white/blue. The green `text-brand-600` "distribute" wordmark text was left as-is (only the mark differs). (#2625)
- **Access = email allowlist, single source `apps/admin/src/lib/admin-allowlist.ts`** (`ADMIN_ALLOWED_EMAILS` + `isAdminEmail`). Edge-gated in `proxy.ts` off the session `email` claim (403 for any signed-in non-staff on every non-public route); the `/api/admin/*` routes re-check it (defense-in-depth). Add/remove staff in that ONE file — don't re-inline the list. **The api-service `requireStaff` gate (staff-only `/v1/billing/credits/*` grant proxies + `/v1/billing/usage-discount` + admin-brands/features-admin, on the forwarded `x-email`) HARDCODES the same allowlist in `src/middleware/auth.ts` — the `STAFF_EMAILS` Railway env var was REMOVED (api-service v0.84.2, #698) because Kevin doesn't want a Railway var for this + it silently drifted. It is still a THIRD copy that must stay byte-equal with the admin/dashboard allowlists, but now via a code edit (no env). Whenever you add/remove staff here, edit the hardcoded array in api-service `src/middleware/auth.ts` to the SAME set + ship it, or the missing staff get a 403 on credit-granting / discount-setting. Do NOT re-introduce a `STAFF_EMAILS` env read.** (admin free-credits #2059; hardcode #698)
- **Cross-org "god-mode" = option A (real auto-join), NOT read-only impersonation.** The breadcrumb org switcher lists EVERY platform org via `GET /api/admin/orgs?q=` (Clerk Backend `getOrganizationList`, `CLERK_SECRET_KEY`), not `useOrganizationList({ userMemberships })`. On switch, `handleOrgSwitch` first `POST /api/admin/orgs/:orgId/join` (makes the admin a real `org:admin` member, idempotent) THEN `setActive` — because Clerk `setActive` rejects a non-member org. This keeps the whole **DIS-143 membership-scoped data path unchanged** (Clerk active org → JWT → gateway `x-external-org-id`); brands/features/writes all follow the active org with zero extra change. Accepted tradeoff: staff emails appear in each customer's Clerk member list. Why A over a read-only `x-admin-view-org-id` impersonation header: the gateway admin path (`api-service auth.ts`) trusts `x-external-org-id` with no membership check BUT resolves the (org,user) PAIR via client-service `/internal/resolve`, which **upserts the caller's `users.orgId` to the viewed org** (single org per user) — so a naive "forward URL org + staff user id" would churn/mis-attribute the staff user across customer orgs. Real membership (A) makes that churn legitimate + lets staff act, which is what's wanted. (#1643)
- **The SAME god-mode also lives in the customer `apps/dashboard`, but STAFF-GATED — and there the gating model flips.** `apps/dashboard/src/lib/admin-allowlist.ts` (byte-equal copy) + the same `GET /api/admin/orgs` + `POST /api/admin/orgs/:orgId/join` routes; breadcrumb shows the all-orgs switcher only when `isAdminEmail(useUser().email)` (else the unchanged `userMemberships` list — customers see only their own orgs). CRITICAL DIFFERENCE vs the admin app: the dashboard is the customer surface with **NO edge allowlist**, so the route-level `isAdminEmail` **403** is the SOLE security boundary — it's what stops a logged-in customer from enumerating/joining every other tenant. The client `isStaff` flag is cosmetic (hides the UI); never rely on it for access. Keep the dashboard allowlist + routes byte-equal with admin's. (#1655)
- **The god-mode join route MUST be idempotent via Clerk's RESPONSE, never via a `getOrganizationMembershipList` pre-check — that pre-check reads only the FIRST PAGE and silently breaks for staff who joined many orgs.** Symptom: god-mode switch into a (real, onboarded) customer org lands on a **blank org page** (`/orgs/:id` renders, sidebar + header fine, main empty). Causal chain (took a full session to trace, documented so it's instant next time): `POST /api/admin/orgs/:id/join` 502s → `OrgActivator` (dashboard, the URL-driven god-mode variant) does `if (!res.ok) throw` BEFORE `setActive`, so the active org never switches → URL org ≠ Clerk active org → `useAuthQuery`'s org-consistency gate (`urlOrg === activeOrg`) DISABLES every org-scoped read → the org-landing page's `["brands"]` query stays `undefined` → `if (!brandsData) return null` → blank; meanwhile every `/api/v1/*` returns **409 org_desync** (proxy `checkProxyOrg`: client-sent URL org ≠ JWT active org). The 502 root cause: the join route did `getOrganizationMembershipList({userId})` (FIRST PAGE only) → for a staff member of many orgs an existing membership beyond page 1 read as absent → re-`createOrganizationMembership` → Clerk **400 `already_a_member_in_organization`** → route wrapped it 502. **Tell it apart from the legit case:** an org the staff is NOT meant to enter (incomplete/never-onboarded org, no brand) bouncing to `/onboarding` is CORRECT (that's the first-run gate, not this bug) — THIS bug is a blank page on a COMPLETE org (has brands). **Fix (KevinLourd/godmode-join-idempotent):** drop the paged pre-check; `createOrganizationMembership` directly and treat a Clerk error whose `errors[].code === "already_a_member_in_organization"` as success (`{ok:true}`) — trust Clerk (server-side source of truth) over a paged list. Applied byte-equal to BOTH `apps/dashboard` + `apps/admin` join routes. Hardening in the same PR: `OrgActivator` now `await session?.getToken({skipCache:true})` AFTER `setActive` (mirrors `handleOrgSwitch`, CLAUDE.md DIS-143 item 3) to close the stale-token 409 window. **Diagnostic approach that nailed it:** the route only returned `err.message` ("Bad Request", Clerk's generic) — the real `errors[].code` was in the **Vercel runtime logs** (`get_runtime_logs query:"admin-join"` → `already_a_member_in_organization`); and the "is this org actually complete?" question was answered by querying **brand-service** (`org_brands` for the org → 2 brands = onboarded → blank is a bug, not the onboarding gate). Org NAMES live only in Clerk (client-service `orgs.name` is null for everyone), and the prod `CLERK_SECRET_KEY` Backend API is IP-restricted from a dev machine (403 code 1010) — so map a Clerk-org-name to data via brand-service, not Clerk. (godmode-join-idempotent)

- **The admin WORKFLOW-EDIT chat (`apps/admin/.../workflows/[workflowId]/page.tsx` → `WorkflowChat`) MUST NOT inline the full workflow DAG (`workflow.dag`) in the chat `context` — api-service caps the JSON request body (`express.json({ limit: "10mb" })` today; it was the 100KB Express default when this shipped, and a large workflow's DAG serializes to >300KB) → `PayloadTooLargeError: request entity too large` on api-service → 500 → `{"error":"Internal server error"}` in the chat panel on EVERY message.** The `context` is re-sent on every turn (chat-service `buildSystemPrompt` JSON.stringifies it fresh each turn from the client payload — it is NOT persisted per session), and it travels through the api-service gateway, so the 100KB cap bites on the very first message. The model reads the DAG ON DEMAND via its existing `get_workflow_details(<uuid>)` tool (already the HARD RULE for edits); the page's `instructions` point at that tool, and only small fields (`summary`, `feature`, identity) stay inline for a tool-free overview. Guarded by `apps/admin/tests/workflow-chat-context.test.ts` (`not.toContain("dag: workflow.dag")`). RULE for ANY chat/Edit-with-AI surface (admin OR dashboard): never inline a large payload (DAG, full lead list, big profile) into `context` — fetch it via a tool on demand. Persisting context server-side by session does NOT fix this (the first message already carries the full payload). (#2978)

- **An IDENTIFIER a downstream service validates against a closed set (`model`, prompt-template `type`, `service`, an endpoint `path`) is READ from that service's schema, NEVER transcribed from the user's words — and `validate_workflow` does not catch the difference, so a validated DAG is not a proven one.** The user speaks product language ("DeepSeek Flash v4"); the service accepts an exact token; turning one into the other produces a slug that looks right, passes every check, and 400s on the first live send. **Answering from memory is the specific failure mode** — training data lags the platform, so a model/template/provider shipped last week is absent from what the LLM "knows" and present in the schema, one `get_endpoint_details` call away. The rule lives in the `workflow` chat's system prompt (`apps/dashboard/src/instrumentation.ts`, the single registrar, so it covers the org-scoped editor AND the feature-stats panel) and in the panel's own context (`workflowCatalogueInstructions`): read the permitted set, copy the exact string, and if what the user named is absent **say so BEFORE writing anything**. Do NOT "fix" this by inlining a model list into the prompt — it goes stale on the next alias and reproduces the bug with extra steps. **`validate_workflow` checks DAG structure, body field NAMES against the request schema, and referenced output paths — never whether a VALUE is one the service accepts**, so it returns `valid: true` on a DAG that cannot run; never report such a workflow as "tested". Cost 2026-08-15: asked to duplicate three cold-email workflows onto DeepSeek, the chat wrote `deepseek-flash-v4` (real alias: `deepseek-flash`, provider `vercel`, chat-service #376) into three PRODUCTION DAGs, called them "testé, 100% valide", and three turns later asserted DeepSeek has no Flash V4 — wrong in both directions. Zero blast radius only because no campaign referenced them; all three dynasties were deprecated. Guards: `apps/dashboard/tests/platform-config-startup.test.ts` + `apps/admin/tests/feature-stats-workflow-ai.test.ts`. Sibling fix spawned in workflow-service (make the validator reject a static body literal outside the target schema's enum) and content-generation-service (accept the DeepSeek alias at all — its `/generate` model set is its OWN, and had no DeepSeek entry on either branch, so the request was unsatisfiable however the DAG was authored).

- **An image in an investor update is UPLOADED, never linked — staff pick a file and it lands on our own R2.** `InvestorUpdateComposer` reads the file as a data URL and posts it to api-service `POST /v1/platform-uploads` (staff-gated) → cloudflare-service `POST /internal/upload/base64` → back comes the public URL, which is what goes in the markdown. An image hosted somewhere else can be moved, expire or start blocking hotlinking long after the update has landed in forty inboxes, so there is no URL field left (guarded: the composer must carry no `type="url"`). PLATFORM-owned on purpose — the file is ours, so it is billed to no customer org and the route carries no org identity. Three gates, each catching a different failure: `imageFileProblem` before the upload (SVG is refused by Gmail, Outlook and Yahoo; WebP is deliberately absent because Outlook on Windows renders through Word; 5MB cap), `imageUrlProblem` on the URL that came BACK (the public domain is resolved at upload time from key-service, so a misconfiguration surfaces as an unusable link rather than a broken picture), then an actual decode. The accept list and the gate read ONE catalogue (`ACCEPTED_IMAGE_TYPES` in `lib/investor-update-html.ts`, alias-free so it carries real unit tests). Guards: `tests/investor-update-html.test.ts` + `tests/investors-surface.test.ts`.

- **CHOOSING the file is the whole action, and the send is BLOCKED while a picked file is not in the body.** A separate confirm button stood between the two, and that is how the first real update went out with no picture at all: the file sat chosen in the form, the button was never pressed, nothing was ever uploaded, and the send said nothing. Diagnosed against prod, not the code — `body_markdown` carried no `![…]`, `html_body` no `<img>`, and R2 `investor-updates/` held only the two verification files, so the producer (which renders `![alt](url)` through a dedicated renderer) was never in it. Generalise: **a two-step control whose FIRST step looks like the action will be left half-done, and if the surrounding form can then be submitted, the omission is silent.** Fix both halves — make step 1 do the work, and let the entity's own gate refuse while the half-state exists (`investorUpdateBlocker`'s `pendingImageName`). The insertion states that it landed (the line drops below the fold of a 14-row textarea) and alt text falls back to the filename, which is what an investor reads while their client blocks remote images.
- **The update AUTOSAVES to `localStorage` (`lib/investor-update-draft.ts`, alias-free, versioned key), and the hydration latch is STATE, never a ref.** With a ref the saving effect's next pass still holds the EMPTY values of the render it was created in, decides they differ from what was restored, and schedules a write of the blank form over a stored draft — only the effect cleanup happens to cancel it, and the line sits on `Saving draft...` on a page nobody typed into. React batches the restore's four updates into one commit, so a state flag in the deps sees the restored values and the flag together. Verified by REPRODUCTION (esbuild-bundled harness + Playwright over a real http origin — `localStorage` is unavailable on `about:blank` and `file://`, which would make every assertion vacuously pass), not by reading the classes. Draft never reaches the network, which is what makes autosaving safe beside a button that mails forty investors; it is purged on a successful send, and the line says it is local to that browser. NOT a cookie: the remembered-state rule is about the FIRST frame and pays for it on every request, which a multi-KB markdown body has no business doing. Guards: `tests/investor-update-draft.test.ts`.
- **A filename the author picked becomes a PUBLIC URL, so it is sanitized at the upload boundary (`sanitizeUploadFilename`) — and `imageMarkdown` wraps a destination containing a space or a parenthesis in angle brackets.** cloudflare-service builds the object key from the filename we send, and macOS names every screenshot `Screenshot 2026-08-02 at 10.46.50.png`; a bare markdown destination ends at the FIRST SPACE, so the line emitted no `<img>` at all and the update went out displaying its own markup, in the preview and the send alike. Angle brackets rather than `encodeURI`: the body doubles as the email's plain-text part so it stays readable, and re-inserting an already-encoded URL cannot turn `%20` into `%2520`. Alt text keeps reading the ORIGINAL name, not the slug. **Generalise, and this is the deeper miss: proving an artifact was PRODUCED is not proving it WORKS — reproduce it through the real consumer.** The insertion was verified end to end (the file uploaded, the line landed in the body) and the one thing never checked was the line through the producer's own `marked`, which is where it failed. Whenever a surface emits input for another system to parse, run the emitted value through that system's actual parser in the guard, not a shape assertion.

## A competitor clone is a PHOTOGRAPH served at the root of its own host, and NOTHING of ours is injected into it

`apps/landing/clones/<slug>/` holds a mirrored competitor landing, served at `lab-<slug>.distribute.you` behind one password and `noindex`. Four exist today (`explee`, `revid`, `outrank`, `trustmrr`) and they are copies: the bytes are exactly what the origin sent, logo included, so a design conversation happens against the real thing rather than a description of it. `brandised: false` on each catalogue entry (`src/lib/clone-catalogue.ts`) records that, and it is the flag a future change reads before injecting anything of ours.

- **Root of its own SUBDOMAIN, never a path prefix.** A captured page is full of root-absolute references (`/_next/static/…`, `url(/img/hero.png)`) and they resolve on a subdomain exactly as on the origin. Under a path prefix every one of them would have to be rewritten, which is a change to bytes we deliberately have not read. One level deep, so Universal SSL already covers it and no wildcard certificate is involved — a proxied wildcard is Enterprise-only anyway, and a two-level wildcard is not in the Universal cert. Adding a clone is one directory, one hostname on the Caddy line, one proxied A record.
- **`src/proxy.ts` exists for this and nothing else, and its FIRST statement is the exit for every ordinary landing request.** It runs on every request the app serves — there is no `config.matcher`, because a clone host legitimately needs `/_next/*` and any matcher excluding static paths would exclude the clone's own assets. No work of any kind may be added above the host check.
- ⚠️ **The route segment is `internal-clone`, NOT `_clone`.** A leading underscore is a PRIVATE folder in the app router: excluded from routing entirely, so every rewrite onto it falls through to the 404 page. Being routable means it is also addressable, which is why the proxy 404s that prefix on any host that is not a clone — otherwise it is a second door onto the clones with no password on it.
- ⚠️ **`request.url` inside a route handler is the request as it ARRIVED, not the rewritten one**, so it already holds the path the origin was asked for. Slicing an internal prefix off it is what a rewrite invites and it is silently wrong: a path SHORTER than the prefix collapses to `/` (so `/version.json` served the home page) and a longer one loses its first characters (so every asset 404'd while sitting on disk). `originPathFor` holds the rule and is pinned by unit tests.
- **The route serves raw bytes.** No token substitution, no analytics injection, no Organization JSON-LD rewrite — every rewrite `staticResponse()` does for OUR pages is exactly what must NOT happen to a copy. A guard asserts that helper is not reachable from the route, so explain that history in the TEST, never in the component.
- **An unset `CLONE_BASIC_AUTH` LOCKS the clones rather than opening them.** A missing secret is a visible 401, never a silent publication. `/robots.txt` on a clone host answers `Disallow: /` BEFORE the password, because a crawler arriving without credentials must still be told to stay away. The host is resolved against the CATALOGUE, so a guessed `lab-anything` never reaches a directory read, and two independent gates stop a traversal (segment rejection in `clonePathFor`, then a resolved-path containment check).
- **Capture is TWO passes, because a static crawl and a browser see different things.** `scripts/clone-site.mjs` walks the markup, the stylesheets and the bundles — ASSET references only, since following document links turns a one-page mirror into a site crawl (explee links a page per country: 839 files and 171MB against the 87 it needs). `scripts/capture-live.mjs` then loads the page in Chromium at four viewport/DPR combinations and records what it actually fetches, which is the only way to reach a chunk whose URL the bundle assembles at runtime or an image that loads on scroll. A response the browser served from its own cache hands back no body, so it is REFETCHED rather than skipped — treating it as already-written is how four of outrank's stylesheets went missing from a clone that looked complete.
- **A query string is part of a URL's identity when the server generates the response from it** (`/_next/image?w=96` and `?w=48` are two pictures), so such a URL is stored as a SIBLING `<stem>.__q<hash><ext>` — a child directory would need the same name to be a file and a directory at once. An origin can also answer ONE url with different bytes per `Accept`, so the reader lists what is stored and honours the caller's `Accept` the way the origin did.
- **`scripts/verify-clone.mjs` is what proves a capture, and a number is not enough — look at the screenshots.** It renders the clone and the origin side by side and reports what failed on one and not the other. Measured after both passes: outrank and trustmrr render with zero same-origin gaps, explee and revid with three each, every one an analytics beacon or a prefetch of a page we do not mirror. Pixel diff at 1440x900 is 0.67% (outrank) and 1.15% (explee); revid at 13% and trustmrr at 25% differ ONLY in a playing hero video and a randomised sponsor rail, both non-deterministic on the origin itself.
- ⚠️ Two things that read as clone defects and are not: `next dev` refuses a request whose `Origin` is not its own, which is every CORS-mode subresource on a clone host (hence `allowedDevOrigins` in `next.config.ts`, dev-only); and Playwright's `httpCredentials` answers a 401 CHALLENGE, which a crossorigin preload never gets the chance to retry — verify with an explicit `authorization` header instead.
- The Dockerfile carries an explicit `COPY` for `clones/`: nothing imports them, so Next's file tracing never sees them and the standalone output would ship without them while the container stayed healthy.
- ⚠️ **`localisedHosts` is a claim the on-disk guard checks, and the localiser does NOT walk references inside a copied third-party script** — gojiberry's `files.tlt-cdn.com/tlt.js` references `cdn.jsdelivr.net`, nothing was mirrored for it, and listing the host left `clone-serving.test.ts` red on `main` for a day (the landing suite is not a CI gate, so nothing blocked). Claim only a host whose `__external/<host>/` directory exists.
- Guards: `tests/unit/clone-serving.test.ts`, which also pins the reader against the capture script's own path function — a disagreement there is a 404 on an asset that is on disk, which reads as a bad capture rather than as drift.

**The lab also holds OUR next landing, `clones/distribute/`, served at `lab-distribute.distribute.you` — an `authored: true` catalogue entry, not a photograph.** Same host machinery, same password, same noindex, same raw-bytes route; what differs is that it is hand-written (`index.html` + `styles.css` + `main.js` + `assets/`, no framework, no build) and `brandised: true` from the start. It exists to be worked on beside the competitor copies it borrows from before it replaces the apex page. Rules while it lives there: (a) **every visual recipe is lifted from a clone, not reinvented** — the file header of `styles.css` names which (explee: hero, launch field, showcase cards, pricing slider, FAQ; gojiberry: pill nav, section hairline with corner brackets, label pills, two-column feature cards, dashed timeline, stat band, plan cards, closing CTA); a new section starts by extracting the sibling's DOM + matched CSS with Playwright over CDP (`CSS.getMatchedStylesForNode`), never from memory. (b) **The clone route serves raw bytes, so no `__TOKEN__` works there** — figures are hardcoded from the owner's notes until the design is frozen; the guard fails on any unresolved token. (c) Owner-decided copy: the outcome is REVENUE (`Get revenue in 24h. From $1/day`), `First $30 free` + `Start free` carry the conversion and the $400 match is never mentioned; the FULL offer is presented as live everywhere (fake it until you make it), with the one exception that a pricing item not in prod carries a `data-coming-soon` (i) — and only the pricing list may say "coming soon". (d) Light theme, charter blue, Fustat display + Inter body (gojiberry's pairing). Guards: `tests/unit/lab-landing.test.ts`. Verify by rendering (viewport-stepped screenshots, never `fullPage` — the hero's `100dvh` inflates to the whole page under a full-page capture and every section reads blank).

## `apps/docs` is a STATIC EXPORT whose ROOT LAYOUT must declare no canonical, and whose routes live in ONE list

`apps/docs` builds with `output: "export"` and ships to Cloudflare Pages through `.github/workflows/deploy-static-sites.yml`, so a merge to `main` reaches it through that Actions run and NOT through the box's deploy cron. It has no server: no route handler, no middleware, no request-time API. Content negotiation (`Accept: text/markdown`) is therefore impossible there by construction, and the answer to "serve markdown" is a published file, never a handler.

**A canonical declared in the root layout is INHERITED by every page, so it tells search engines that the home page is the real document and all the others are duplicates of it.** `alternates: { canonical: SITE_URL }` sat in `src/app/layout.tsx` for months and every one of the 28 pages emitted `<link rel="canonical" href="https://docs.distribute.you/">`. Nothing errors, every page renders, the sitemap lists all of them, and the pages simply do not surface: an Is Agentic audit reported "name search surfaced no pages on distribute.you, although developer resources exist on the site". The same trap exists for any Next app whose layout sets `alternates` — a canonical belongs to the page it names and to no other page.

- **`src/lib/docs-routes.ts` is the ONE list**: path, title and description for every route. The `<title>`, the meta description, the canonical and `sitemap.ts` all read it, because those four have to agree and a second hand-written array drifts on the next page added. `docsMetadata(path)` (`src/lib/docs-metadata.ts`) throws on an unknown path, so a new page cannot ship with the layout's default title and no canonical of its own.
- **Canonicals carry the trailing slash**, because `trailingSlash: true` means `/api/brands` is served at `/api/brands/` and the bare form points at a redirect.
- **The product is named `distribute.you`, never the bare word `distribute`** — that word is an ordinary English verb and matches nothing a person searching for the product would type. A title that already contains the name opts out of the layout's `%s | distribute.you Docs` template so the tab does not say it twice.
- **`/openapi` is the fixed address for the machine-readable entry points**: `api.distribute.you/openapi.json`, the explorer at `api.distribute.you/docs`, and the hosted MCP server at `mcp.distribute.you/mcp`. All three existed before and were named only inside prose, which is not an address an agent can guess.
- **`public/llms.txt` indexes every page** with a one-line summary plus those three URLs, and `robots.txt` announces it. A guard asserts it covers every route in the list, so a page added without an llms.txt line fails the suite.
- **`src/app/not-found.tsx` is what gives `404.html` a body.** Static export writes that file and Cloudflare Pages serves it with a real 404, so the status was never the problem; the default body was the empty app shell, which tells an agent following a stale link nothing.
- **Per-page `.md` mirrors are deliberately NOT published.** The markdown exists per page as the `Copy for AI/LLM` payload, but it is heterogeneous (most pages hold a template literal, `/mcp/tools` computes it from a data structure), so mirroring it needs either an extractor over page source or a second copy that rots. If that changes, move the markdown into one module first and generate from there.
- Guards: `apps/docs/tests/unit/agent-discoverability.test.ts`. Note the docs suite is NOT a CI merge gate (only `apps/dashboard`'s is), so run `pnpm --filter @distribute/docs test` locally before pushing a docs change. (#3582)

**BEING FOUND AND BEING USABLE are two different findings, and closing the first can leave the second wide open — check that a developer resource RUNS before doing any more work on making it discoverable.** A discoverability audit reports what a name search surfaced, so a repeat "partial" reads as "publish more, link harder, name it better". Twice now that half was already shipped (#3582, #3619) and the real defect was that every resource the search DOES surface told a developer to paste something that fails: an `npx` command for a package that has never existed on npm, an auth header the API answers `401` to (it is api-service's separate ADMIN path), a TypeScript client package that 404s, a catalogue of 35 MCP tools against a server that exposes 6, and four whole pages documenting REST routes that are in none of the 185 paths the API serves. Every one renders perfectly, so nothing is red and nothing is missing; a reader only finds out at the shell. That is WORSE than not being found, and it is what a partial score is measuring.

The checks are seconds each and none of them can be done by reading the page: `npm view <pkg>` (or the registry JSON) for every package name printed; the deployed `openapi.json` `components.securitySchemes` for the auth scheme, which is the only authority on it; a connected MCP client's own tool list for the tool catalogue; and the deployed path set for every REST route a page prints (extract them with a regex, diff against `openapi.json`, it is one script). Do all four BEFORE writing copy, because each one changes what the copy should say.

**A catalogue of ANOTHER system's capabilities is read back from that system, never maintained by hand.** The tools list, the auth scheme, the endpoint set and the package names all have an authority that answers on request, and a hand-written copy of any of them rots silently in the direction that looks fine. `apps/docs/src/lib/developer-surfaces.ts` is the one home for every value a reader PASTES (header, key prefix, MCP endpoint, client configs, CLI package, tool catalogue) precisely because 22 files spelled them out separately and went stale together. A page that prints a command imports it from there; `agent-discoverability.test.ts` fails if any retired literal reappears anywhere in `src` or `public`, and if a command-printing page carries its own copy.

⚠️ **Two mechanical-sweep traps, both hit on this ship.** (a) A find-and-replace across the app REWRITES YOUR OWN EXPLANATORY COMMENTS: the `X-API-Key` -> `Authorization` sweep edited the doc comment that explained why `X-API-Key` was wrong, inverting its meaning into a claim that the API rejects the correct header. Same family as the guard-trips-on-its-own-comment rule; re-read every comment the sweep touched, and word the module's comments so they DESCRIBE a retired literal rather than spelling it (otherwise the guard fails on the file that explains the guard). (b) An import-insert `str.replace` no-ops silently when the target line differs by one symbol (`import { URLS }` vs `import { URLS, DISTRIBUTION_FEATURES }`), and `tsc` is the only thing that catches it. Assert EVERY scripted replace including the import insert, not just the body edits.

⚠️ **Two more sweep traps, from the vocabulary sweep that retired the second word for a sales funnel (#3647).** (c) **A banned word usually carries SEVERAL SENSES, and a word-level find-and-replace flattens them into degenerate English.** Replacing every occurrence of one noun with one replacement produced *"a funnel is a funnel"*, *"the funnel's own funnel"*, *"walks each funnel's own funnel"* and *"the prewarm funnels ICP -> suggestAudiences"* (that last one was a VERB). None of it fails `tsc` and none of it fails a test, so the only thing that catches it is reading the diff — budget for a corrective pass, and grep the result for the replacement word appearing TWICE in one short span (`grep -inE "funnel[^.]{0,45}funnel"`), which finds nearly all of them in one command. Classify the senses BEFORE the sweep (here: a sales funnel, a sequence of service calls, a chained verb, a blockchain, a JS operator) and apply the narrow phrases first, the generic rule last. **And scope the sweep to PROSE, because a source file's identifiers carry the word too.** Running the replacement list over a whole `.ts` file renamed a local variable and produced `const funnel = funnel.stepKeys` — a self-shadowing declaration whose every later use silently read the wrong thing. That one `tsc` does catch, which is the lucky half; the unlucky half is the same sweep flattening a doc comment into `"at most one of them is ever in a given funnel's funnel"`, which nothing catches. So run the sweep, then read the whole diff, then `tsc` — and when the file is mostly new, rewriting it by hand beats patching a mangled sweep. (2026-08-28, the leg rename: both happened in one pass over three files.) (d) **A sweep scoped by FILE EXTENSION silently breaks a pairing that spans extensions.** `static-html.ts` emits the class names that `public/landing/index-v1.html` styles and animates; a sweep over `*.ts,*.tsx` renamed the emitter's half and left the stylesheet's, so the hero console would have rendered unstyled with nothing red anywhere. Whenever a sweep touches a CSS class, a `data-` attribute, a keyframe name or a DOM id, run it over the HTML/CSS/JS in the same pass and diff the two sides for parity (`grep -o "<token>" both-files | sort | uniq -c`).

**A page documenting a surface the API no longer serves is deleted, not footnoted.** Outlets, journalists, articles and press kits left the product (the dashboard cluster went in #2196/#2593) and their docs pages outlived them by months. Deleting a page means its route entry, its sidebar entry and its `llms.txt` line go in the same commit, and the guard pins all four as absent. (#3632)

## `/model` states the acquisition model, and it KEEPS no catalogue of its own

`admin.distribute.you/model` (sidebar section Features) answers "what are all these objects, and which of them can actually be sold together". One band is documentation and everything under it is read live, which is the whole design: a local copy of another service's list rots silently, and the tell is always that the copy is where the missing entries would go (`acquisition-channels.ts` named 2 channels of 33 for months).

- **A channel states a LEG, not just what it produces.** `stepTransitions` is a list of `{from, to}`; `from: null` means the lead was not on the chain at all, which is what every entry channel does. That is what makes a chain sellable one leg at a time, and `operatedBy` (`platform` / `customer`) says who puts the hours in — a customer-operated channel spends none of our money, so its daily operating cost is a stated ZERO rather than a blank, and what such a leg costs THEM is declared per lead against lead-service. The page renders the leg and the operator; a channel with no leg reads `—`.
- ⚠️ **The root step vocabulary is `steps`, NOT `producibleSteps`, and `producibleSteps` per channel is deliberately NOT read.** It survived the widening as a DERIVED value (the legs whose `from` is null), so it is EMPTY for all 8 channels that convert an internal step — precisely the set this page exists to show. Reading it would render a dash for them and nobody would notice, which is the silent twin of the rename that took the page down on 2026-08-27. Guards pin both wire names in the reader and in the page.
- **The two reads are `GET /v1/public/channels` and `GET /v1/public/channel-funnel-economics`** — public, no auth, no org scope, so plain `useQuery` (there is no active org to gate on). features-service publishes the join itself: a channel states the first signals it can PRODUCE (`producibleSteps`), a funnel states the signal it STARTS on, and `salesFunnels` is DERIVED from the two upstream, so the pairs on sale can never drift from what a channel can do. Nothing here decides which pairs exist.
- **The step, family and operator vocabularies are read as PLAIN STRINGS, never `z.enum`** (guarded). The step set already grew from 4 entry signals to 9 (it gained `meeting_booked`, `meeting_attended`, `signup`, `form_filled`, `paid_client`) and the families gained `conversion`, both within a day of this page shipping. Both are expected to GROW — a channel that converts a step in the MIDDLE of a chain (a human closer taking `Meeting attended` to `Paid client`) has no token today — and a reader that closes the set throws on the catalogue the day it widens. `channelFamilyLabel` / `unmeasuredReasonLabel` / `unpricedStepLabel` render an unknown token verbatim rather than blank.
- **A matrix cell has FOUR states and they must not collapse into one dash**: `not_sellable` (the channel produces no signal this funnel starts on), `unmeasured` (sellable, and features-service names WHICH ingredient is missing), `unknown` (sellable, and the economics read carries no row for it — its own state, because reading a missing row as "not sellable" would contradict the channel table one band up), `measured` (the price). Prod today: 107 sellable pairs, 7 measured.
- **Every figure is rendered verbatim** through the shared `fmtRoi` / `fmtUsd`; the page divides nothing (guarded). The one derived number is `summariseCells`, a count of the cells ON SCREEN — that describes the table, it is not a metric off served fields. `dailyOperatingCostCents` renders in WHOLE dollars: it is a commercial per-day TERM we set, the same reading as a daily budget, where cents are noise.
- **`lib/acquisition-model.ts` is alias-free** (its only import is type-only and erased at build) so it carries real unit tests. Keep it that way, and keep it data-pure — formatting belongs to the page.
- ⚠️ **`text-gray-200` has NO `html.dark` remap in admin**, so it renders BRIGHT on the dark surface: the blank marker would be the loudest thing in the matrix. The remap covers `text-gray-900` down to `text-gray-300`; a quiet tone means gray-300 or gray-400. Same class of gap as the `text-gray-950` one below, in mirror image. Verified by reproduction (compiled Tailwind off the app's own `globals.css` + Playwright at 1440 and 390, light and dark), not by grepping the class.
- Guards: `tests/acquisition-model.test.ts` (real unit tests) + `tests/model-page.test.ts`. Admin tests are NOT a CI gate, so run `pnpm --filter @distribute/admin test` locally before pushing.

## The Claude config console edits a REPO, not a machine — and markdown is the only thing it writes

`/audit/config` (`audit/config/page.tsx`, rules in `lib/config-files.ts`, GitHub in `lib/github-config.ts`) reads and edits the global CLAUDE.md, RTK.md, the 26 skills, the hooks and each repo's own CLAUDE.md. The config Claude Code actually loads lives on a laptop under `~/.claude` and `~/.agents/skills`, which are SYMLINKS into a clone of `shamanic-technologies/agent-config`; admin is a container on the Hetzner box and cannot see that laptop. So every read and write goes through the GitHub Contents API against the repo, and a save is a commit. **That indirection is the design, not a limitation to route around** — do not add a filesystem path, and do not try to reach the machine.

- **A save does NOT reach the laptop until it pulls.** The page prints `git -C ~/conductor/repos/agent-config pull` with a copy button for exactly that reason: a commit that lands on the remote and never reaches the machine that reads it is the one way this feature quietly does nothing. The durable fix is a `SessionStart` hook in agent-config that pulls; it lives in that repo, so it is a separate ship.
- **Hooks and `scripts/` are READ ONLY, and that is a security decision, not an omission.** A hook is code that runs on the laptop, unattended, on every Bash call, so editing one from a browser is arbitrary local execution one click away. Cloudflare Access and the email allowlist bound WHO reaches the page; they do not make the two actions the same class. `settings.template.json` is read-only for a different reason: `install.sh` copies it into `~/.claude`, so an edit here would not reach the live settings. Everything unwritable is still fully readable.
- **The write rule is markdown-only in agent-config, and exactly `CLAUDE.md` in a product repo. It is enforced in the ROUTE, never by the UI** — a control the page does not render is still a request anyone can send, so `configWriteProblem` runs before any network call and 403s. Read is narrow too: widening it past one file per product repo turns a config console into a source browser for the whole organisation.
- **A save sends the blob `sha` it read**, so a file that moved in between is REFUSED rather than overwritten. That 409 is the feature; do not retry past it. `configFile` is therefore in `EPHEMERAL_QUERY_ROOTS` — a restored copy carries a stale sha, so every save in that session would conflict.
- **Saving a product repo's CLAUDE.md commits to its default branch, so the box rebuilds and redeploys that service** within ~5 min (health-checked with rollback, so bounded). The page states it on those files.
- **MCP servers, the live `~/.claude/settings.json` and `.credentials.json` are machine-local and unreachable.** The page says so in its own card rather than omitting them — an absent section reads as a gap someone forgot.
- Auth is `GITHUB_CONFIG_TOKEN` (fine-grained PAT) read from admin's own container env, like `CLERK_SECRET_KEY` in the god-mode routes; not a key-service platform key, which exists for secrets several services resolve. **Absent token is a first-class state**: the page names what is missing and the command to supply it. With a fine-grained PAT the repo list IS the grant, so what shows up is exactly what can be touched.
- `config-files.ts` is alias-free (real unit tests) and carries no em-dash anywhere, comments included: nearly every string in it is copy a person reads. Guards: `tests/config-files.test.ts` + `tests/audit-config-surface.test.ts`.

**A SCOPE sold by campaigns — an offer, or the brand — states `Learning` on its RATIOS while EVERY campaign selling it is still learning, and clears the moment ONE is measured.** `scopeIsLearning` (`lib/learning-threshold.ts`, real unit tests) reads the rows `useCampaignRows` already built, on the same query keys, so it costs no network and a header can never state a return that every row beneath it is declining to state. Owner-decided rule: *"show Learning except at least one of the offer's campaigns is NOT in learning stage"* — deliberately NOT "does the scope's TOTAL clear the bar", because three campaigns at five outcomes each are three unreliable prices and adding them does not make one reliable. A scope with NO campaigns is UNMEASURED, not learning: there is nothing to have an opinion about, so it reads exactly as it does today.

**The same rule states itself PER ROW in the brand Overview's Offers table** (`useOfferLearning` -> `offerLearningFor`, `lib/use-offer-learning.ts`): `ROI` and `% CAC` read `Learning` while every campaign selling that offer is, and a learning row sinks below the measured ones rather than being ranked on a return it declines to show. `$ Revenue` and `$ Invested` are never gated — a total that grows with each outcome, and money already spent, neither divided by an outcome count (there is no `$ CAC` column here). The rows come from `useCampaignRows(brandId, featureSlug, ALL_OFFERS)`: **the offer-scoped branch run once for every offer, never the brand-grain one** — that branch is pinned to ONE channel, and an offer is routinely sold through several, so an offer's single measured campaign on a second channel would be invisible and the row would read `Learning` for good. Per-offer hook calls are impossible (hooks are not loopable), which is what `ALL_OFFERS` exists for; every query key is one the page already polls, so the second question costs no network. ⚠️ An offer with NO campaigns is UNMEASURED, not learning — absent from the map reads `false`, the same default `scopeIsLearning` takes on an empty list, and the opposite of `audienceLearningFor` one level down. Guards: `tests/brand-offers-page.test.ts`. (#3778)

**One level down, an AUDIENCE clears the bar the moment ONE of the scope's campaigns has priced IT** (`audienceIsLearning` + `useAudienceLearning`). Counts from different campaigns are deliberately NOT pooled: five replies in each of two campaigns is two unreliable prices, and the table states a price per audience. The read is per CAMPAIGN — features-service answers per-audience evidence scoped to one — and every key is byte-equal to the one that campaign's own Audiences page polls, so drilling in costs no second request. ⚠️ **The default here is the OPPOSITE of `scopeIsLearning`'s**: an audience no campaign reports on IS learning (the campaigns exist and have no outcomes from it), while a scope with no campaigns at all is UNMEASURED and gates nothing. `audienceLearningFor` holds both: unsettled or no-campaigns → false, present-and-thin → true, absent-with-campaigns → true. It gates `ROI` / `% CAC` / `$ CAC` on the Audiences table and the Top-3 card's return — from the ONE map, so the card and the table cannot disagree about the same row — and never `$ Invested`.

⚠️ **The Return-on-spend gate shipped BROKEN for a day because the prop was never threaded**, and the guard did not catch it: it asserted `roi-trend-card.tsx` HANDLES `learning` and never that the page PASSES it, so the card's branch was correct while the page kept drawing the chart. The edit that added it used a `str.replace` with no assertion, so the no-op was silent. Two rules from that: assert every scripted replace, and **a guard for a threaded prop must pin the CALL SITE, not only the component that would honour it** (`overview.slice(indexOf("<RevenueOverviewSection"), …)`).

Gated: `ROI` / `$ CAC` / `% CAC` on the stat row (`economicsLearning`), the **Return on spend** line, which is drawn PROVISIONAL rather than withheld — the same curve in dotted grey, with NO plotted points and no fill under it, so a reader sees the shape their money has traced without reading it as a trend to act on (the paragraph this replaced made people read a sentence to find out there was nothing to see). ⚠️ **A placeholder curve states its SHAPE and no value, so every way of taking a reading off it is off: no plotted dot, no per-point label, NO HOVER TOOLTIP (and therefore no `activeDot`, which was only ever its anchor), and a y-axis stating only its two ENDS.** The tooltip is the one that reads as harmless — it is on demand, so it feels like the reader asking rather than the chart telling — and it is exactly the same number the dots and the labels were removed for, wearing an interaction. **A scope still under break-even gets a FIXED `LEARNING_CEILING` of 10x rather than an axis scaled to its own best day**: at 0.0x a self-scaled axis draws the flat line across the top of a 0-to-1 band, which reads as a result, while against the multiple a return is judged on it reads as barely started with break-even visible above it as the thing still to reach (owner-picked, twice: *"je ne veux pas voir cette card"* and *"quand le user est < 1x et en learning, mets le max Y axis value a 10x"*). Those ends (`learningBounds`) span the data AND break-even, so the dashed break-even line is never clipped out of the domain; a degenerate domain — every point exactly at break-even — is padded and states the one real value rather than the padding. The marked dots and the per-point `LabelList` were BOTH shipped first and both removed after the owner asked twice; do not re-add either. The hover tooltip is the one place a value is stated, on demand, which is why the `activeDot` stays. The measured chart is untouched (solid, filled, five ticks — `tickCount={learning ? undefined : 5}`), and that surviving `activeDot` needs its OWN colour class: recharts renders it outside the `<Area>`'s element, so a bare `fill: "currentColor"` there resolves against the root and came back BLACK against the grey line — measured with a Playwright probe, not assumed. Its `LearningTag` is the ONE that keeps its `(i)`, since the prose that explained the muting is gone. And on the Campaigns header both `Cost per acquisition` and `#1 acquisition channel` — the second being a RANKING BY that price, which is exactly as thin as the price. NOT gated: `Pipeline revenue` and `Pipeline generated`, per the ratio-vs-total rule. It applies at BRAND level as well as offer, because the two pages state the same money for a single-offer brand and tagging one of them would be one number described two ways.

## A brand Overview states MONEY; a channel- or funnel-scoped figure belongs to the campaign

The brand Overview answers one question — what did the whole brand return — and a brand runs several acquisition channels and several sales funnels AT ONCE. So a figure whose meaning is scoped to one channel or one funnel is narrower than the page it sits on, and putting it there labels a sum with one member's vocabulary. The campaign Overview is where those belong: it runs exactly one channel and, since #3344, states exactly one funnel.

Concretely, the brand Overview shows `Outreach` plus four money cards — **Pipeline revenue** (`headline.totalPipelineUsd`), **ROI** (`costEconomics.roiMultiple`), **$ CAC** (`costEconomics.costPerAcquisitionUsd`), **% CAC** (`costEconomics.costOfAcquisitionPct`) — charts **Return on spend** (`roiHistory`) where the one-funnel signal line used to be, and ranks its **Top-audiences** card on `projection.returnPerDollar`. It does NOT show the per-day `Outreach activity` bars (they count the emails cold outreach sends) nor the funnel step pairs (`Website Visits` / cost per visit, and the goal's outcome pair). All of it is the SAME components, prop-gated (`showActivityChart`, `showEconomics`, `showFunnelMetrics`, `showRoiTrend`) — never a second copy, or the two Overviews drift into describing one result two ways.

⚠️ **`$ CAC` reads `costPerAcquisitionUsd`, NOT the lens-only `costPerConversionUsd`.** The latter is served on a `?lens=` response only and is ABSENT on the brand Overview's un-lensed read, which is what left the card on a dash for a while; features-service pins the two equal for the same scope. Whichever is absent, do NOT "fix" a dash by dividing — neither `committedCostUsd ÷ an outcome count` nor `pipeline ÷ roiMultiple` is the figure features-service computes, and putting either on screen is the compute-a-stat-in-the-browser bug.

**`roiHistory` is CUMULATIVE on both legs, its spend leg is COMMITTED and its pipeline leg is what actually landed** (spend dated by runs' own cost buckets, pipeline by the per-lead event timestamps). The spend leg rides the same single basis `costEconomics.committedCostUsd` does, so the curve's last point reconciles with the ROI card above it rather than charting a different currency. Do not re-derive it per-period: spend on a day buys outcomes that land days later, so a period-grain ratio oscillates between 0 and absurd. A day whose cumulative spend is still 0 carries `roiMultiple: null` and is DROPPED, never plotted at 0. Pipeline whose outcome has no timestamp (`undatedPipelineUsd`) counts in the ROI card but cannot be in the line, so the card STATES that gap rather than letting the two disagree silently. `roiHistory` is fail-soft server-side (null on a blip) and null on `?lens=` — render the reason, never a skeleton.

**Every ROI figure in the dashboard reads ONE function, `formatRoi` (`lib/format-roi.ts`) — one decimal under 10×, a whole number from 10× up.** `2.4×`, `30×`. Under ten the decimal changes a decision (2.4 and 2.9 are different answers about whether a campaign pays); at thirty it does not, and it reads as false precision on a figure that moves several points a day. Same adaptive shape as `formatUsdAdaptive`. ⚠️ **The threshold is not the load-bearing part — the SINGLE source is.** Coarsening was tried once on the chart ALONE and correctly reverted: at a real 11.7 the headline read `12×` two inches under a stat card still reading `11.7×`, one number disagreeing with itself on one screen. That reverted attempt is why the helper exists rather than a convention. Callers: the Audiences table at every scope, the Top-3 audiences card, the ROI stat card, the Return-on-spend chart (headline AND y-axis ticks), the Campaigns table (`RoiCell`, which the Offers table also renders), the best-model card, the workflow scorecard, and the daily digest email. A new ROI cell, tick, headline or email token calls `formatRoi` — never its own `toFixed`, which is what a guard in `tests/format-roi.test.ts` pins. `null` means the producer could not measure a return, so the caller passes its own word for that (`-` in the two audience surfaces, `—` everywhere else); the helper never fabricates a zero.

**Where an ROI reads GREEN is the same module's `roiIsGood`, and the single source matters more than the threshold.** Above break-even the money is coming back and that reads green; below it stays the ordinary text colour and NEVER red, because a campaign that has not finished learning is under 1x by construction and red is a verdict. Three surfaces sit within inches of each other on one screen — the ROI stat card, the Return-on-spend headline and the Campaigns table's `RoiCell` — so three copies of `multiple > 1` is exactly how they come to disagree about where green starts, the same failure the formatter beside it exists to prevent. An unmeasurable return is not a bad one: `null` reads false and renders in the ordinary colour beside the caller's own word for "we have no figure". The stat card takes it through `ScoreCard`'s `valueClassName`, which defaults to the ordinary `text-gray-800` every other card wears — a card passes it only when the number carries a reading on its own, never as decoration. ⚠️ The card reads `costEconomics.roiMultiple` while the chart headline reads `roiHistory`'s last point, so the two can legitimately differ by the undated pipeline and flip green at different moments; that is the documented gap, not a drift. Guard: `tests/format-roi.test.ts` pins the rule, every caller, and the card's CALL SITE.

**Rank audiences on RETURN, never on cost per outcome** — cost per outcome ranks by cheapness, so an audience converting to nothing outranks an expensive one that pays. `projection.returnPerDollar` is the same definition `funnel-ranking` uses, so an audience's return and the brand's are one statistic at two grains.

**The Top-3 card is BRAND-scoped by default and states NOTHING but the return there** (`TopAudiencesCard`, `campaignScoped` opts the campaign Overview out). Two things were removed from it, and both are the same rule:
- **It states no brand-wide return of its own.** It used to render `brandProjection.returnPerDollar` above the rows, so a reader could see an audience beating the brand or not. That number is **PROJECTED** (conversion rates × lifetime revenue) while the ROI stat card two inches above is **REALIZED** (`costEconomics.roiMultiple`, actual pipeline ÷ actual spend) — so one page carried two different numbers under one word (prod: `2.5×` under `2.7×`) and read as contradicting itself. Both were correct; printing both was the bug. **One return per page.** Do NOT re-add a second brand-grain figure here, and do NOT "reconcile" them — they answer different questions and a card is the wrong place to explain that.
- **No cost per outcome at brand level.** `$45` per sales interest under an audience names ONE funnel's step on a surface that sums several — the same reason the brand Overview drops the funnel step pairs. A row with no projection prints `-` rather than falling back to a funnel cost.

**And the mirror at CAMPAIGN scope: the card LEADS with the cost per outcome and never states a return at all** (`ranksByReturn = brandLevelMoney`, guarded). A campaign buys ONE outcome and is run to make that outcome cheaper, so a projected return is a brand's question wearing a campaign's name — and the Audiences table one click away already leads with that cost, so a return above it made one screen rank the same audiences two ways. The cost was previously only a FALLBACK for a row features-service could not project, which meant the card flipped to `Return` the moment any row carried a projection, i.e. always. It ranks cheapest-first, its subtitle states the outcome COUNT that price divides by, and the header reads the funnel's own words (`Cost per sales interest`) byte-equal with the table's column. ⚠️ Under a CAMPAIGN the metric is keyed on that campaign's own LEG (`legRankMetric`, below), and `audienceRankMetric` — which is keyed on the retired GOAL — is only the fallback for a leg we cannot place.

Guard: `tests/brand-overview-roi-focus.test.ts`. (#3349, #3355; features-service v0.127.0)

**The rule runs one level down too: the brand AUDIENCES table and the brand Overview's campaigns section.**

`customer-audiences-page.tsx` is shared by the brand route and `campaigns/[id]/audiences`, and `brandLevelMoney = !campaignScoped` is the switch. At BRAND level it shows **ROI / % CAC / $ CAC** (all three read off `projection.{returnPerDollar, costOfAcquisitionPct, costPerPaidClientUsd}`, features-service v0.128.0) sorted by RETURN DESC, and every funnel-scoped pair is off — `Positive replies`/`CPPR`, `Website Visits`/`Cost per website visit`, and the signup / form-submission / sale variants. Cost per outcome ranks by CHEAPNESS, so an audience converting to nothing outranks an expensive one that pays; that is why the default flips to desc-on-return rather than asc-on-cost. The campaign route keeps every column: it sells exactly one funnel, so its own steps ARE what it buys. ⚠️ **`% CAC` is READ, never derived by inverting the return beside it** — `100/cacPct` is on the banned-browser-math list, and the producer implements it AS the reciprocal so the two null together. It is PROJECTED (like the return) and must not be paired with `/revenue`'s REALIZED `costEconomics.costOfAcquisitionPct`.

**The pairs are keyed on the CAMPAIGN's own FUNNEL now, not on the goal — `showVisitCols = hasStep("website_visits") && !brandLevelMoney`, read off the same `stepsFor(goal, funnelKey)` the stat cards use.** The paragraph below was written when the gate was `!isPositiveReplies`, and that flag is the retired GOAL's answer: it is true for every campaign whose goal is not the reply-terminal one, INCLUDING `sales_meetings_from_conversation`, whose chain is reply -> meeting -> paid and buys no website visit at all. So a customer's reply-led campaign listed `Website Visits` / `Cost per website visit` beside its audiences for months, and the reply pair was hidden on a visit-led one by the mirror error. `reply_meeting` and `visit_meeting` both answer to `sales_meetings`, so no goal-keyed test can separate them — this is the same reason the stat cards moved to the funnel. `isPositiveReplies` and `showReplyColsForGoal` are DELETED rather than left unused, and the table's `min-w` narrow branch reads `!showVisitCols` for the same reason. Guards: `customer-audiences-table.test.ts` + `brand-overview-roi-focus.test.ts`.

**A chat mounted on a SCOPED page must carry that scope in its `context`, or it WRITES at a wider scope than the page can read.** `EditWithAIChat` sends a free-form `context` object to chat-service, which resolves the subject of every tool call from it — so a field the page omits is a question the chat cannot answer, and the failure is silent in the worst direction: the tool succeeds, the row is created, and the page that asked for it filters the row out. The audiences chat carried `brandId` and `audienceId` and not `offerId`, so an audience asked for on one offer's page was created brand-wide (`offer_id` NULL) and was invisible on the page it was requested from — reported as *"le chat dit qu'il a créé l'audience mais je ne vois rien"*. Nothing was red anywhere: the chat's own confirmation was truthful about the write, human-service and api-service had accepted and forwarded an offer for months, and only the dashboard never sent one.

- **The guard pins the CALL SITE, never the component.** A chat perfectly able to carry a scope is the feature entirely absent if the page never passes it — the same reason the threaded-prop rules elsewhere in this file pin `<Component` call sites.
- **An absent scope keeps the wider behaviour, never a guessed one.** Off an offer route there is no offer to state, and the chat must read exactly as it did before rather than inferring one from the brand's offer list.
- ⚠️ **The mirror of it: a chat that CREATES something the customer cannot yet see must not report it as created.** `suggest_audiences` persists candidates at status `suggested`, which NO customer surface renders (`VISIBLE_AUDIENCE_STATUSES` is active / paused / archived), and its tool description said the candidates were *"ALREADY PERSISTED"* — which is what licensed the model to announce a creation for a row that exists nowhere. Fixed by DELETION-and-sharpening in chat-service (the tool description and the seeded system prompt now state that `suggested` is invisible until activated), per the fix-an-LLM-step-by-removing-an-instruction rule. Same shape as the status-label rule above: **a surface states what HAPPENED, never what was attempted.**
- Generalise for any new chat surface: list what the page's own reads are scoped BY (`brandId`, `offerId`, `campaignId`, a funnel key), and make the context carry every one of them — that list is exactly the set of ways a write can land somewhere the reader cannot see. (#3823, chat-service v0.61.1)

**A per-outcome COST states `Learning` until ten of that outcome have landed** (`lib/learning-threshold.ts`, the same bar the stat cards use — see the UPDATE 6 block above). `COST_COL_OUTCOME` maps each gated column to the count it divides by (`cppr`->positiveReplies, `cpc`->websiteClicks, `cps`->signups, `cpfs`->formSubmissions, `cpsale`->sales), so a new cost column is gated by adding one line rather than by remembering to. Two invariants: an ABSENT stats row is NOT learning (the table already prints `-` for a row the producer returned nothing for, and replacing one honest answer with a different one helps nobody), and a learning row has NO RANK under that column — it sinks below every measured one and orders among the rest by that outcome COUNT descending, which is the column immediately beside it. Sorting them by the price they are deliberately not showing is the display-one-field-order-by-another bug. The COUNT columns and `$ Invested` are never gated: the counts are what show the bar approaching, and invested money already happened.

**Each funnel-scoped pair carries its own `&& !brandLevelMoney`, so a NEW pair added here is off by default only if you remember to write it — and the visit pair proves that is not a safe assumption.** `Website Visits` / `Cost per website visit` shipped gated on `isPositiveReplies` ALONE, which reads as a complete rule (a click is genuinely not in the reply→paid funnel) and hides the second, larger reason: at brand level a visit names one funnel's first step while the rows beside it are attributed across every funnel the brand sells. So the pair kept rendering for every brand on any other goal, months after #3358 dropped its four siblings. It is now ONE named flag, `showVisitCols = !isPositiveReplies && !brandLevelMoney`, pinned by name in `brand-overview-roi-focus.test.ts` + `customer-audiences-table.test.ts`. Generalise: when a sweep turns N sibling conditions off at a scope, grep for the ones that were ALREADY conditional on something else — those read as done and are the ones it misses.

**`$ Invested` sits right of `$ CAC` and is the one REALIZED figure in that group.** It renders `evidence.totalCostInUsdCents` verbatim (features-service, `pricing=net`, the same net spend billing charges and the same field the CPPR floor reads), sorted on the field it displays. The three columns to its left are PROJECTIONS of what the audience is worth going forward; this is what it has cost so far. That is not the one-return-per-page contradiction — the labels differ and they answer different questions — but the tooltip states it outright, because a reader who assumes one basis will try to multiply `$ Invested × ROI`.

**It renders at BOTH scopes, and at both it sits immediately left of `Outreach`** — at brand level that falls out for free, since every funnel pair above it is off there. So the campaign copy is a separate `{campaignScoped && …}` header/cell pair placed before `Outreach` rather than a widening of the `brandLevelMoney` block: moving the brand column would have re-ordered a table nobody asked to change. Two things differ from the brand column and only two — the campaign read is `campaignId`-scoped (its own cache key), and the TOOLTIP is its own string, because the brand copy says "the three columns to its left are projections", which is false beside per-outcome costs. The rest is byte-equal: same `evidence.totalCostInUsdCents`, same `col="invested"` sort case, same skeleton. ⚠️ The campaign `min-w` widths count this column — a new campaign-only column means bumping all four campaign branches, and the brand branch stays put. Guard: `tests/brand-overview-roi-focus.test.ts` pins it at both scopes AND asserts the campaign header precedes `Outreach`. (#3377, #3407)

**A FEATURE IS AN ACQUISITION CHANNEL, so `/features/{slug}/revenue` answers for ONE channel — every surface asks at the grain it PRESENTS (campaign → offer → brand), and repointing one surface means sweeping every sibling that reads the same thing.** The per-feature read is correct for a campaign, which runs exactly one channel, and wrong the moment a surface presents its answer as an offer's or a brand's: it describes whichever channel it happened to name and understates the subject by whatever the others did. Nothing errors, every figure is real, and the two surfaces simply disagree — so it is reported as "the stats don't match" rather than as a bug, and the first instinct is to look for a caching lag that is not there.

The grains and their reads: campaign → `getFeatureRevenue(slug, brandId, {campaignId})`; offer → `getOfferRevenue(offerId, brandId)`; brand → `getBrandRevenue(brandId)`; the brand's offers one row each → `getBrandOfferMoney(brandId)`. Keys carry the grain (`["offerRevenue", brandId, offerId]` / `["brandRevenue", brandId]` / `["brandOfferMoney", brandId]`), so surfaces at one grain dedupe to a single poll and cannot print different money for one subject.

**The sweep is the part that gets missed.** #3468 moved the brand Overview onto the brand and offer reads and left three siblings on the per-feature one: the Campaigns header (whose own comment said *"Brand-level (ungrouped) revenue"*), `OutreachStatCardsAuto` (which renders on every brand- and offer-scoped page, so its cost cards described one channel throughout), and the Offers table. Measured on the brand that surfaced it, whose single offer runs four channels — `sales-cold-email-outreach` $2,625.44 / 2.67×, plus $40.94, $2.24 and $0 — against $2,670.44 / 2.62× for the brand and for that offer. The customer read `2.6× / $2,669` on the cards and `2.7× / $2,625` in the table directly beneath. So when you repoint ONE surface to a new grain, grep every remaining caller of the old read in the same PR and decide each one's grain explicitly; a comment claiming a grain is not evidence of one. Guard: `tests/money-asked-at-page-grain.test.ts`.

**WHICH CHANNEL a surface is about is the same question one level down, and `useSoleFeatureSlug()` is the wrong answer under a campaign — `useScopedFeatureSlug(campaignId)` (`lib/scoped-feature-slug.ts`) is the ONE narrowing.** That hook returns the brand's single GA feature, which is right for a brand-scoped surface and silently wrong for every campaign that is not on it: a campaign is (offer x funnel x channel) and states its channel on its own row. Nothing errors, every read fires, every figure is real, and the page simply describes a channel the reader is not looking at. Two shapes, and the first is the one that reads as a missing feature rather than a wrong number:

- **A row list filtered by `featureSlug === soleSlug` does not CONTAIN the open campaign**, so anything derived from those rows derives nothing. The campaign Leads page took its funnel that way, so `panelFunnel` was null and the lead panel drew **no `Funnel progress` section at all** — reported as the status control being absent, not as a wrong figure.
- **`/revenue?campaignId=` fired under the sole slug answers for the WRONG CHANNEL's money**, so the stat row above states one channel's spend under another's name — the `$2,625 vs $2,668` shape one grain up, inside a single screen.

Three surfaces share that screen and all three now read the hook: `engaged-leads-page`, `customer-audiences-page`, `OutreachStatCardsAuto`. The campaign Overview had already been fixed by hand (`campaign?.featureSlug`) and its comment describes the same incident. Four invariants: the slug is **NULL while the campaign read is in flight**, never the sole slug as a stand-in (a read on a guessed channel lands in that channel's cache entry); the money gate under a campaign is the **channel CATALOGUE**, never `isRevenueFeature` (the brand's GA set, which blanks every campaign on another channel); a campaign's revenue is asked with **`campaignId` ALONE** so the args are byte-equal to the Overview's under the same key; and the **sole slug survives only where the question is genuinely brand-level** (`useCampaignRows` at brand grain, `useAudienceLearning`), spelled `soleFeatureSlug` so a campaign-scoped read cannot pick it up by accident. Measured on `feedback-request-cold-email-outreach`: one campaign, one offer, and a Leads page that fetched `sales-cold-email-outreach` throughout. Guard: `tests/campaign-channel-scope.test.ts`.

**Rows do NOT sum to their parent, deliberately, and that is not this bug.** Money adds across an offer's channels because a run belongs to exactly one, but people do not (a lead worked through two channels is one lead) and no ratio does (a ratio of sums is neither the sum nor the average of ratios). Across several offers the rows likewise do not add to the brand, and a campaign stating no offer is in no row at all. features-service combines the parts at each grain; summing them in the browser would be wrong on top of being the client-computed metric this repo forbids.

⚠️ **The brand's per-offer money is `GET /v1/features/brands/:brandId/offers`, NOT `/v1/brands/:id/offers`** — the latter is already the brand's offer CATALOG from brand-service, and `brandRoutes` mounts first, so a same-path declaration would be dead code. Two different questions sharing a noun. (#3473, #3474; features-service#816 → v0.140.0, api-service#855)

**The Campaigns table is a COMPONENT (`components/campaigns/campaigns-table.tsx`), rendered by the Campaigns page AND full-width under the brand Overview's chart** — never a second copy, or a campaign reads one way on the Overview and another one click over. The ordering lives in the shared `useCampaignRows` hook because the Campaigns page's "#1 acquisition channel" tile reads the same sorted list; two sorts would let the tile and the first row name different campaigns. Both queries keep their original keys, so the extra surface costs no network. (#3358, #3359)

**It lists PAUSED campaigns too, one line per IDENTITY, ordered status → ROI desc → last-updated desc.** A campaign the customer paused is still one of their campaigns, and a list that drops it tells them they have none — the controls modal exists precisely so they can turn it back on. What must NOT come back with it is campaign-service's history: a campaign IS (offer × funnel × channel), that service keeps every superseded row (it used to mint a fresh one on each workflow switch), and features-service totals the identity server-side — so the stored rows are many where the campaign is one. Measured on the brand that surfaced this: 1 `ongoing`, 1 `stopped` with `stop_reason: manual`, and **45 `stopped` ancestors of the ongoing one**. The old active-only filter was right about the 45 and wrong about the 1. `listedCampaigns` collapses on the identity key (`offerId|funnelKey|featureSlug`) — the live row wins outright, else the latest by `updatedAt` — so the ancestors keep riding on their live row (their money is already in it) while an identity with no live row states its paused campaign. Status leads the sort so a paused row never sits between two live ones on a return it no longer earns; `updatedAt` breaks the tie between rows with no figures at all. ⚠️ **The surfaces whose question is about LIVE campaigns read the hook's `activeRows`, never `rows`** — the "#1 acquisition channel" tile and the brand-level Leads tabs, because naming a channel or offering a funnel tab off a campaign that stopped months ago describes something the brand no longer sells. `activeRows` is derived FROM `rows` rather than from a second filter, so one collapse and one ordering serve both. A campaign's OWN Leads page still reads its own row whatever its status. Consequence: the empty state is one sentence again — every campaign belongs to an identity and every identity states a row, so the list is empty exactly when the brand has no campaign on this feature. Guard: `tests/campaigns-page.test.ts`.

**Its money block reads `ROI · % CAC · $ Revenue · $ Invested`, and `$ Invested` is the one already-happened figure among them** — `costEconomics.committedCostUsd` off the `groupBy=campaignId` group the table already fetches with `pricing=net`, i.e. net spend already billed PLUS the holds open on sends the campaign has queued, AND the exact number `ROI` and `% CAC` divide by, so a row cannot contradict its own return. Rendered verbatim (`fmtUsd(revenue?.committedCostUsd)`); a campaign with no group reads `—` rather than `$0`, because "we have no figure" and "it cost nothing" are different statements. Same field, same net basis and near-identical tooltip as the `$ Invested` on both Audiences scopes (#3377, #3407) — one cost question, one answer fleet-wide. The tooltip states the projection-vs-realized split outright, since a reader who assumes one basis will try to multiply `$ Invested × ROI`. `Revenue` was renamed `$ Revenue` in the same pass so the two money columns read as a pair. ⚠️ A new column here means bumping `md:min-w-[760px]` and BOTH `colSpan`s (7 today, after the Sales-funnel and Channel columns merged into `Campaign`) — the skeleton and the empty state each carry one, and a stale `colSpan` silently narrows those rows. Guard: `tests/campaigns-page.test.ts` pins the column order, the tooltip copy and that the cell reads the served field rather than deriving one. (#3419)

**A campaign IS (offer × funnel × channel), so EVERY surface that names one states BOTH halves in ONE place, in the same order and the same words — `components/campaigns/campaign-identity.tsx` holds that vocabulary and nothing else may re-spell it.** The funnel LEADS because it is what the campaign buys; the channel follows quieter behind **Via** because it is where it buys it. Two layouts, differing only in stacking: `CampaignIdentity` stacks them wherever a campaign occupies a ROW (the Campaigns table's first column, and every row of the budget modal at all three grains — brand, offer and campaign Overview all mount the SAME `CampaignControlsModal`), and `CampaignIdentityInline` says the same two halves on ONE line for the top bar, where a two-line crumb would set the bar's height off one crumb and leave every other floating in it. ⚠️ **There is NO separator between the halves** — a middle dot makes peers of them and they are not peers; it was removed from the crumb for exactly that. `FunnelCell` / `ChannelCell` are DELETED, not merely unused: two spellings of one identity is what this removes, and re-adding either brings the drift back. **The stacked block's height is the funnel MARK's** (`h-8` = the `sm` tile's 32px) via two leadings that add to it (`leading-[14px]` + `leading-[18px]`, the 18 being the `xs` channel mark), so a row is the height of its icon rather than of whatever the text needs; the inline layout pins BOTH its marks to `xs` itself, so the crumb lines up with the offer tile by construction rather than by a `size` prop a call site can get wrong (`CampaignTitle` therefore takes none). Measured rather than grepped, per the reproduce-the-render rule: stacked reads 32/32, and the crumb's funnel mark, channel mark and offer tile all read 18px on one 20px row. Consequence for the TABLE: `Campaign` is the FIRST column and there are no separate `Sales funnel` / `Channel` columns at any width — below `md` only `Campaign` + `ROI` render (`w-[70%]` / `w-[30%]`, released at `md:w-auto`), the four money columns and `Status` fold away, and the floor is `md:min-w-[760px]`. A new column means bumping that floor and BOTH `colSpan`s (currently 7 — the skeleton and the empty state each carry one, and a stale one silently narrows those rows). Guards: `tests/campaigns-page.test.ts` + `tests/campaign-title.test.ts` + `tests/header-breadcrumb.test.ts`. (#3526, #3529)

**A campaign is named for the LEG it performs, not for the whole funnel — `campaignLegLabel` (`lib/campaign-leg.ts`) states the arrow and `CampaignIdentity` renders it.** A funnel is sold leg by leg, so naming a row after the entire chain overstates almost every campaign and outright misdescribes some: `founder-led-closing` converts only *Meeting attended* into *Paid client*, and its row read "Sales Meeting from Conversation", which is the name of the funnel it closes the last arrow of. Eight published channels perform an internal leg and four are already fundable, since a customer-operated channel passes `channelIsFundable`.

- **The legs are WIRE data**: features-service states them on the channel's own feature row and `acquisition-channels.ts` has read them since #3638, as bare step tokens (`{from, to}`, `from: null` meaning onto the funnel from nothing).
- **`SALES_FUNNELS` carries `stepKeys` purely to JOIN against them** — the same steps under the producer's tokens, parallel to `steps` and NEVER rendered. The two lists cannot be one: the producer calls the reply funnel's first step `conversation` while the customer reads "Positive reply", so matching on labels finds nothing. A guard pins the two arrays to the same length.
- **The WORDS come from the funnel's own `steps`**, so this introduces no vocabulary at all: an entry leg reads `Positive reply`, an internal one `Meeting attended → Paid client`.
- **The funnel disambiguates a multi-leg channel.** Cold email produces a conversation AND a website visit; an in-house closing team converts a signup AND a filled form; at most one of those belongs to any given funnel. An entry leg counts only when its `to` is the funnel's FIRST step; an internal leg only when `from`/`to` are ADJACENT, in that order — a shortcut past a step is not one of the funnel's own arrows and no funnel prices it.
- **A channel stating no placeable leg falls back to the funnel's NAME**, never a dash: that is the sentence this surface read before legs existed, so a feature row predating the field loses nothing.
- ⚠️ **Consequence at BRAND grain**, where the table lists campaigns across several funnels: two campaigns buying the same step of different funnels read the same words. The funnel MARK still tells them apart and the funnel's own name is on the `title`. `lib/campaign-leg.ts` is alias-free so it carries real unit tests — keep it that way. Guards: `tests/campaign-leg.test.ts` + `campaign-title.test.ts` + `campaigns-page.test.ts`. (#3664)

**The two RATIOS state `Learning` under ten outcomes; the three MONEY TOTALS never do.** ROI and % CAC are one statement in two units, a return and its reciprocal, so they move together — showing one beside a tag would let a reader trust the number we just said we could not stand behind. `$ Revenue`, `$ Invested` and `$ Budget` are NOT gated, and `$ Revenue` is the one worth stating outright because it IS a projection: a total GROWS with each outcome instead of being decided by whichever one landed, so a campaign with two outcomes has a SMALL pipeline rather than an unreliable one, and withholding it hides a real figure behind a word about a precision problem it does not have. The test is not projected-vs-realized, it is **ratio vs total** — only a figure that DIVIDES by the outcome count is unstable at a low count. The other two are money already spent and a ceiling the customer set, neither derived from an outcome count. A learning row also has NO RANK under the return: it sits below the measured rows of its status and falls through to last-updated, because ordering a table by a number it is deliberately not showing reads as unordered.

**The count rides the grouped read the table already makes** — features-service answers the volume half on `?groupBy=campaignId` (v0.143.0, features-service#827), the same `outcomes` block it already served per workflow, read under its own names (`recipientsRepliesPositive` / `recipientsClicked`) and declared `.optional()` so an older payload still parses. ⚠️ **Do NOT reintroduce a per-row `/revenue?campaignId=` fan-out.** That was the stopgap while the group answered with money and no volume, and it cost one extra read per listed row on every poll; a consumer fanning out to a second, heavier endpoint to patch a lean group is the workaround this repo names, not the fix. **The gate is the funnel's first MEASURED step** (`campaignSignalCount`: a positive reply on the reply-led chains, a website visit on the visit-led ones) — never its terminal outcome, which needs the brand's conversion tracker live and is legitimately 0 for most campaigns, so gating on it would print `Learning` forever on a campaign that is measurably working. An ABSENT payload is "cannot tell", never zero: the row then reads exactly as it does today. Guards: `tests/learning-threshold.test.ts`.

**The `Learning` tag says a figure is withheld; the band at the top of the Campaigns surfaces says WHEN it stops — one number, in DAYS.** `lib/learning-progress.ts` (alias-free, real unit tests) reads the campaign as a reservoir: it needs about ten outcomes' worth of spend (`LEARNING_MIN_OUTCOMES` x the expected cost per outcome, the same projection the monthly forecast prices), it has spent some of it, and it spends a known amount each day. Everything else is that division. Days rather than dollars because days are what a person acts on, and the CTA follows from it: doubling the ceiling halves the SPENDING half.

- **The GATE is unchanged and stays the outcome count.** The band renders only while the campaign is learning, so a bar approaching its end can never sit beside a figure that has already been stated. Nothing here re-derives whether a row is thin.
- ⚠️ **A threshold that MULTIPLIES a price must price the SAME step the gate COUNTS — this is the general trap, and nothing goes red when you get it wrong.** The gate counts the funnel's first measured step (a sales interest, a website visit); the threshold multiplied `LEARNING_MIN_OUTCOMES` by `outcomeUnitCostUsd`, which is the cost of the funnel's TERMINAL outcome (a booked meeting) and is the figure the monthly forecast is about. Both are real served numbers, both parse, both typecheck, and the band simply promised a spend target for ten of one thing under a sentence counting ten of another — out by the reply-to-meeting rate, in whichever direction that rate happens to point. Reported by the owner as *"le calcul est faux"*, not as a mismatch, because a plausible wrong number reads as a bad estimate. `learningSignalUnitCostUsd` (`workflow-projection-choice.ts`) reads the step's own served cost off the projection the page already fetches; its precedence is byte-equal to the gate's. **Generalise: whenever a surface states `N x <price>` beside a count of N, name the step ONCE and derive both from it.**
- **The band states the number and offers the raise; it explains NOTHING.** It carried a line under the bar spelling out the reservoir (the spend target, the daily rate, the settling window) and the owner cut it: three clauses on a surface whose whole job is a glance. The lever likewise states what the raise BUYS in the band's own unit (`Invest $16/day instead of $8/day -> save 27 days of learning`), never what it would LEAVE (`about 42 days`, which makes the reader subtract), and it opens `CampaignControlsModal` with the daily budget ALREADY SET to the figure it named (`prefillBudgetUsd`, honoured only at campaign grain — at brand or offer grain there are several rows and no single one it belongs to). A control that promises an amount and then hands over a blank field asks the question twice.
- **ONE learning callout per surface, and it is the BAND — the first-outcome reassurance banner is DELETED from the fleet** (`first-outcome-reassurance-banner.tsx` + `lib/first-outcome-reassurance.ts` are gone, guarded by a `src/`-wide check in `campaign-funnel-steps.test.ts`). It promised "2 to 4 weeks" on a window it was not counting, priced that window in the funnel's terminal outcome while the band counted the first step, and sat under the band on the campaign Overview: one state in two boxes, disagreeing about what they were both waiting for. The band answers the same question with a date, so it replaced the banner at BRAND and OFFER grain too. Do not re-add a reassurance banner anywhere.
- **Cold email carries a 14-day settling tail** (`REPLY_SETTLING_DAYS`, `REPLY_SETTLING_CHANNEL_SLUGS` as a CLOSED set, never a substring test on "email"). A send bought today is not answered today, so the last dollar of the learning budget buys outcomes that appear well after it is spent. The tail only starts once the spend is in, dated off `roiHistory.daily`'s cumulative committed leg (the same basis the threshold is measured against). An UNKNOWN elapsed is read as the whole window still ahead, never as a finished one: shortening a promise on no evidence is the one direction that costs trust.
- **Any missing input renders NOTHING.** No expected price means there is no tank; no daily budget means nothing is filling it. Substituting the nearest figure we hold (total spent, the fleet average) would state a date nobody can stand behind, on a surface whose whole job is to state one.
- **The CTA offers nothing when doubling buys no day back** (`learningProgressIfDoubled` returns null at one spending day or none), and it opens the SAME `CampaignControlsModal` every other budget surface opens: several windows onto one number are fine, a second narrowing is not.
- **Every surface speaks for the campaign that finishes SOONEST, through ONE derivation (`lib/use-scope-learning-lead.ts` -> `ScopeLearningBand`).** The scope clears the moment one campaign is measured, so the honest subject is the soonest finisher — NOT the one with the most outcomes so far, which ranks by a proxy for the answer: two campaigns at the same count are a week apart if one is funded at twice the other's ceiling or prices a different step. ⚠️ The rule that matters more than the pick is the SINGLE derivation: the price, the committed spend, the daily ceiling, the settling tail and how much of it has run were assembled by hand at four call sites, and that is exactly how one campaign came to read `13 days` on its own page and `27 days` one click up (prod 2026-08-29, same $144.39 spent, same $24/day, same $44.97 per sales interest — the campaign page simply never added the 14-day settling tail, which is also why its CTA vanished). A band is a promise about a date, so two of them for one campaign is the self-contradictory-surface bug wearing a countdown. Every read the hook makes is a key the surrounding surfaces already poll: `useCampaignRows` for the campaigns and their money, the projection keyed on the FUNNEL so campaigns selling one share an answer, and the lead's own `/revenue?campaignId=` (the only carrier of the dated spend curve) byte-equal to the campaign Overview's key.
- ⚠️ `bg-purple-200` had NO `html.dark` remap, so the bar's unfilled TRACK painted the loudest thing on the dark surface, brighter than its own fill. One line in `globals.css`, added with the band, same class of gap as `text-gray-200`. Verified by REPRODUCTION (compiled Tailwind off the app's own `globals.css` + Playwright at 1280 and 390, light and dark), not by grepping the class.
- Guards: `tests/learning-progress.test.ts`, which pins the CALL SITE on both pages, not only the component.

**A number on a customer surface is explained in ONE short sentence a child could restate, or it is not explained at all.** The reflex on a figure with real machinery behind it is to write the machinery down — the basis, the window, the caveat — and each clause is individually true, which is what makes the paragraph feel earned. It is not: a reader who has to parse three clauses to learn what a bar means reads none of them, so the honest measure of the copy is whether someone could say the rule back after one pass. The learning band went through exactly this: a first draft explained the threshold, the projection behind it and the settling tail, and the owner cut it to *"affiche simplement le % ou le nombre de jours restants"*. What survived is a reservoir anyone can restate (this much to spend, this much a day, plus two weeks for replies) and the machinery moved into the module's doc comments, where it belongs and costs a reader nothing. Same instinct as the plain-language rule for explaining architecture, applied to what ships on screen rather than to what is said in chat: lead with the thing, drop the qualifiers that change no decision, and let the tooltip or the surface underneath carry the rest.

**`$ Budget` is the row's fifth number and it sits with the STATUS, not with that money block** — the four above are charges and projections of charges, and a ceiling is neither, so parking it beside them invites a reader to add it to spend. Beside the pill it answers the other half of one question: is this campaign running, and how hard. It is billing's own `(org, brand, funnel, channel, offer)` row — a campaign IS that triple — narrowed by **the ROW's own `campaign.offerId`**, never by the surface's: billing's per-pair figure spans every offer selling that pair, so a row that borrowed the pair total would print a sibling offer's money under this campaign's name, and the brand-scoped list and the offer-scoped one would disagree about one campaign. Whole dollars always (a ceiling is a configured whole-dollar value); `$0` is a real answer meaning stopped, while `—` means billing had none — different statements. The read is `["brandFunnelBudgets", brandId]`, the key Offer Settings and Campaign Settings already poll, so it dedupes to no new network, and it joins the reveal-on-settle gate so a failed read shows dashes rather than an eternal skeleton. Guard: `tests/campaigns-page.test.ts`.

**At brand level there is NO goal — only the return.** The brand goal (`salesEconomics.optimizationGoal`) is a RETIRED brand-service column: `NOT NULL` with a server default, so it reads `websitePurchase` for a brand that stated nothing (`schema.ts`: *"Nothing reads it"*), and it collapses `reply_meeting` and `visit_meeting` onto one word. Do NOT key a brand-level surface on it, and do NOT replace it by PICKING one declared funnel — that answers "which funnel", which is not the brand's question.

- **Leads tabs are the UNION of the funnels the brand's ACTIVE campaigns sell** (`leadTabsForFunnels`, built on #3362's `funnelSteps` so one chain definition serves both scopes). A campaign filters that same list to itself; a brand takes the union. `outreach` is always present and always last — every lead we contacted is in it whatever the funnel, so a brand with no live campaign still has one truthful tab. The auto-select latch is one-shot, so it waits on the CAMPAIGNS query, not the economics one.
- **(RETIRED) The brand Overview's reassurance banner** counted outcomes of every kind and retired on TIME (28 days from the brand's first spend). It is deleted: the band states the same thing with a date, and a promise nobody is counting down is worse than no promise. Its one durable lesson survives above — a surface that NAMES an outcome must also disappear on that outcome, so the sentence and the gate are one decision.
- **The brand Overview resolves NO workflow.** `/workflow-projection` answers "how many of the GOAL's outcome does this budget buy", and it fed only the Outcome line's dashed forecast (replaced by the return chart) and that spend cap. `getWorkflowProjection` / `salesObjectiveForOptimizationGoal` / `selectWorkflowForOptimizationGoal` / `workflowOutcomeUnitCost` are all off this page; the campaign Overview still resolves one, correctly.
- **Brand-level audience reads name NEITHER a funnel NOR a goal** — features-service v0.129.0 treats that as a first-class request and prices every audience through the best-returning declared funnel (`max`, never a blend), sorting on return. A campaign names its `funnel`. Each row carries its own `basisFunnelKey`, because an audience's best chain is routinely not the brand's.
- **The daily digest fires on the RETURN**: it sends only when the brand's return went UP on the day (two SERVED points of `roiHistory.daily`, never a browser-computed ratio), and names every kind that landed (`"3 positive replies and 1 signup"`), each person badged for what THEY did. A flat or falling day sends nothing — an inbox that gets the email whatever happened learns to ignore the one that matters. An absent curve means "could not measure", never "improved". `OutcomeGoal` / `fetchBrandGoal` / the goal-keyed `GOAL_CONFIG` are deleted. (#3364)

Open follow-up: `audience-stats` now prefers a `funnel` param and marks `goal` deprecated — the dashboard still sends `goal`, which features-service keeps working.

## A REFUSED charge is on the wire already, and two independent gates made it invisible

`lib/payment-failure.ts` (alias-free, real unit tests) answers one question off the payments list the Billing page already polls: has a charge been refused since the last one that went through. `PaymentFailedBanner` renders it at the top of `/billing` with Stripe's own reason and a retry.

- **`last_payment_error` has always been served.** stripe-service mirrors the raw PaymentIntent and api-service's `/v1/billing/payments` is a documented passthrough, so the decline code and the sentence Stripe wrote for the cardholder arrive on every poll. `PaymentIntentSchema` simply did not declare it, which is the too-narrow-schema bug in its PLANNING form: a field the reader strips is invisible to `git grep`, so it reads as absent from the wire and the obvious next move is a producer PR. Issue #2109 filed exactly that (a new billing-service `topup_status`) and was wrong; one authenticated call to the endpoint the surface already polls settled it. **"The producer does not serve X" is a claim about a payload — verify it against a payload.**
- **Two gates hid it, and neither is a bug on its own.** The Payments card filters to `status === "succeeded"` (an abandoned checkout husk is noise), so a decline was not merely unexplained, it was absent. And the page's "Top Up Credits" button renders only while auto-topup is OFF — correct when the balance refills itself, and exactly wrong for an org whose armed auto-topup keeps declining, which then has no recharge control at all. The banner reads the UNFILTERED list for the first reason and carries its own Retry for the second.
- **Two exclusions carry the whole rule, and both are about Stripe's own semantics.** A SUCCEEDED payment is never a failure even when it carries an error — Stripe keeps the error of an earlier attempt on an intent a retry later accepted, so reading it would report a failure the customer already fixed. An intent with NO error object is not one either: an abandoned checkout also rests at `requires_payment_method`, and telling someone their payment failed because they closed a tab is worse than saying nothing. Everything is measured against the newest SUCCEEDED payment rather than the newest payment overall, because the question is whether the money is flowing now.
- **The REASON is Stripe's `message`, verbatim** — never a decline-code map of ours. `decline_code` (`insufficient_funds`) is preferred over the coarser `code` (`card_declined`) for diagnostics only.
- **"Your campaigns are stopped" is gated on the BALANCE, not on the decline** (`availableCreditCents(account) <= 0`). A refused top-up on an org still holding credit has stopped nothing, and the banner is derived from payments alone, so that sentence needs its own evidence.
- **Retry opens the SAME modal a hard 402 opens** (`showPaymentRequired`), in its `depleted` variant when the balance is out, so two doors onto one recharge never tell different stories. ⚠️ The refused amount is deliberately NOT passed as `required_cents`: the modal renders that as **Required**, and it is what auto-topup tried to charge, not a debt the customer owes.
- Measured on the org that surfaced it: 12+ declines since Aug 29, an open `credit_depletion_episodes` row, and no signal anywhere in the product. Scope is the billing page only — `CreditAlerts` (the global banner) fires only when auto-topup is OFF, so a failing-card org still gets no warning on other pages; making it global costs a payments poll on every route. Guards: `tests/payment-failure.test.ts`. (#3828, closes #2109)

## The Revenue tab counts THREE different monies and must never merge them

`/metrics?view=revenue` (`components/revenue-view.tsx`) shows three figures that are all "revenue" in English and are not the same number. Prod 2026-08-02: **cash $10,351.46**, **consumed $5,758.88**, **committed MRR $1,980**. Someone reading two of them together will try to reconcile them; the page is laid out in named bands so they can't.

- **Cash collected** — payments minus settled refunds and lost disputes, **across every acquirer**, never one of them. Served ready-made by `/public/stats/billing`: `total_paid_cents` (gross), `total_returned_cents`, `total_revenue_cents` (net), and `revenue_cents` per period on `monthly_growth`/`weekly_growth`. Nothing here recomputes it. A return is attributed to the period it HAPPENED in, so a past bucket is never rewritten and a net-negative period is real.
- **Revenue consumed** — `getFleetRevenue()` → features-service, summing net actualized cold-email spend off the runs cost ledger. **No acquirer is in this chain.** It trails cash by the prepaid credit not yet burned. **Refunds must NOT be netted into it**: a refund reverses a payment, it cannot un-send an email.
- **Committed run-rate** — Σ active daily budget × 30, a daily snapshot recorded going forward. It does not reach back to inception, so its growth labels say "since the first snapshot".

⚠️ **"Acquirer" is load-bearing in that first bullet and the copy on the page says so — do NOT let any of it drift back to naming one vendor.** stripe-service is named after the first acquirer and mirrors a second one (Revolut Merchant) beside it; its per-org payment summary and per-org payment history have counted both since #149, and for a while its PLATFORM read did not, so the company's own cash number silently excluded a live acquirer. Measured 2026-09-04: **$12,553.27** of Stripe against **$506.00** of Revolut, of which **$500 was a real customer credit top-up** that the org was correctly credited for and that no revenue figure anywhere ever saw. Nothing was red — the endpoint answered 200, the shape never changed, every number rendered — and the only tell was a doc comment in the producer claiming its total equalled the sum of the per-org totals, which it could not. `tests/revenue-view.test.ts` now fails if `revenue-view.tsx` names ANY acquirer, which is safe as a whole-file check because the component names none; so if you need to explain this history near that file, explain it in the TEST, never in the component. The paid-users tab is the deliberate exception and stays Stripe-only in its own words: it fans out that vendor's saved payment methods directly, and answering it for a second acquirer needs a live per-org call that has no business on a public unauthenticated route.

Two traps:
- **Every "since inception" label holds only while the producer's TRAILING window still contains the first billed day** (2026-03-12 / 2026-W11) — at its defaults the weekly window stopped doing so around Sept 2026. `getFleetRevenue` requests the maximum windows (36 months / 104 weeks) and `trimLeadingZeroBuckets` drops the pre-inception buckets. A zero INSIDE the history is kept; dropping it makes the growth line skip a period.
- **`Number("")` is `0`, not `NaN`** — `centsStringToUsd` rejects blank before finiteness, or an empty field renders "$0" and reads as "we took in nothing".

**NRR** (`retentionSeries`) is features-service-owned (v0.120.0, `netRevenueRetention`), read `.optional()`. Cohort fixed at period start, new logos excluded from both sides — the industry definition, so it is comparable to the >100 / >120 bands. A `retentionPct: null` period is UNMEASURABLE and is dropped, never charted as 0; the headline is the last COMPLETE period (mid-period retention always reads low) with its cohort size beside it. Guards: `tests/revenue-view.test.ts`. (#3243, #3244, features-service #727)

**Chart chrome (`period-compound-chart.tsx`, and any recharts chart with NAMED y-axes): NO `CartesianGrid`, and the only horizontal is the one that means something.** `CartesianGrid` defaults to `yAxisId` 0; these charts name their axes (`"value"` / `"growth"`, or `"capacity"` on `audit/capacity-history-chart.tsx`), so recharts resolves no ticks for it and degenerates into ONE dashed line across the top of the plot — a reference the chart does not have. Reported as *"cette barre horizontale en pointillé blanc dans tous mes graphes qui ne veut rien dire"*. Do not re-add it; a chart on a DEFAULT axis renders a real grid and keeps its own. On retention the 100% line takes that role (`referenceValue`), dashed, coloured from `currentColor` off a `text-gray-400` wrapper — a hardcoded hex is invisible on one of the two themes, and the `html.dark` remap does not reach an SVG `stroke` attribute. **Y ticks are ANCHORED on that reference** (`referenceTicks`, walking up from it rather than from zero) so 100% carries its own label and coarsening a tall axis can never drop it. `niceCeiling`'s steps include 1.5/2.5/3/4 on purpose: under the classic 1/2/5/10 grammar a 255% series rounds to a 500% ceiling and every bar renders half as tall as it should. Guards: `tests/chart-domain.test.ts`. (#3251, #3256)

## The feature-stats surface is THREE pages behind a SECOND sidebar — and "Outreach" is people contacted, never a run count

**UPDATE — the WORKFLOW page is SHARED across features, and each feature lists only the sub-nav rows it has.** `components/feature-stats/feature-workflows-page.tsx` is the one implementation (`FeatureWorkflowsPage({ featureSlug })`); every `/feature-stats/<slug>/workflows` route is a one-line wrapper naming its feature. `AI Meeting Booking` (`ai-meeting-booking`) is the second one and its base path REDIRECTS to `/workflows`, because Economics and Cost details read the cold-email-shaped cross-org endpoints (cost per positive reply, per website visit) and have no page there — so `FeatureStatsSidebar` takes a `nav` prop (defaulting to all three rows) and that feature passes `["workflows"]`. A nav row whose page does not exist is a 404 nobody catches. ⚠️ The guards for the table and the AI panel read the SHARED COMPONENT, never the route wrappers: a guard pinned to a wrapper passes while the table it no longer contains regresses. Adding a third feature is a route pair plus a nav row, never a copy of the page.

⚠️ **A full-viewport overlay in `apps/admin` needs `z-[60]`, because the app header is `sticky top-0 z-50` — at `z-40` the header paints over the overlay's own title and close button and the top of the panel is unreachable.** The `relative` ancestor between them (`<div className="flex flex-1 overflow-hidden relative">` in the dashboard layout) carries no `z-index`, so it creates no stacking context and the two really do compete in the root one; the header's own account dropdown is `z-[60]` but lives INSIDE the header's stacking context, so it cannot collide. The `workflow-ai-panel` shipped at `z-40` and was reported as "le right panel est caché en haut par la top bar". Verify a fix by REPRODUCTION (`document.elementFromPoint` in the top strip, old markup beside new) — a class grep proves the spelling and says nothing about paint order.

`/feature-stats/sales-cold-email-outreach` was one 641-line page holding two unrelated questions. It is now Economics (`page.tsx`, the cross-org price of every outcome), Cost details (`details/`, the objective-selectable trend + per-workflow cost split) and Workflow (`workflows/`, every workflow scored across all client brands), with a sub-nav column of its own.

- **The second sidebar renders BESIDE the Platform one, not in place of it.** `getNavigationLevel` leaves `/feature-stats/...` at nav level `app`, so `ContextSidebar` keeps drawing `AppLevelSidebar`; the route's `layout.tsx` mounts `FeatureStatsSidebar` next to `{children}` in a `md:flex-row`. The campaign layout looks identical and is the OTHER case — its level returns `null`, so its sidebar replaces. Adding a `segments[0] === "feature-stats"` branch to `getNavigationLevel` would silently drop the Platform nav (guarded).
- **`SidebarSection` / `SidebarLink` / `SidebarItem` are EXPORTED from `context-sidebar.tsx` for this.** Two sidebar columns on screen at once cannot each own their row markup, or they read as two products. Same reason every shared helper moved out of the page: `lib/feature-stats-format.ts` (the one money format, the null-sinking comparator, the sort toggle, the trend derivations), `lib/feature-stats-objectives.ts` (the outcome catalogue) and `components/feature-stats/primitives.tsx` (`SortableTh` / `GrowthBadge` / `Sparkline` / `OutcomeCard` / `TrendTooltip`). The guard fails if any page redeclares one.
- **Query keys are unchanged across the split**, so the per-objective trend fetched on Economics is the same entry Cost details reads, and Workflow's `positiveReply` / `websiteVisit` cost reads dedupe with the Cost details selector. Splitting a page must not multiply its network.
- **Every Workflow cell is a SERVED field — the join is a display lookup, not a metric.** `buildWorkflowPerfRows` (alias-free, real unit tests) merges three cross-org reads on `workflowDynastySlug`: `workflow-cost-per-outcome?objective=positiveReply` (positive replies + CPPR + spend), the same route at `objective=websiteVisit` (website visits + CPWV) and `public/features/ranked?groupBy=workflow` (outreach). Two calls are needed because that endpoint prices ONE objective at a time. Verified equal on the shared fields in prod (Azalea: `$209.88 / 1 click / 1 positive reply` on both), so the row cannot contradict itself. The rows are the UNION of the three sources — a workflow one source has not caught up on still shows its money, with `—` where a source is silent.
- **⚠️ `completedRuns` is NOT an outreach volume.** A workflow that contacted 638 people logs ~15,000 runs, so labelling the run count "Outreach" overstates it ~23×. The column reads `recipientsContacted`, which is what "outreach" means on every other surface in the fleet (the customer Leads page's Outreach tab is contacted leads). `getCrossOrgWorkflowOutreach` states this and the guard fails if `completedRuns` reaches the page.
- **`limit` on the ranked read is a ceiling, not a page size** — that endpoint defaults to the top 3, and the Workflow page needs every workflow, not a leaderboard.
- The table reveals on SETTLE (`isPending && !isError` per query), so one slow cross-org read cannot skeleton the page forever. Guards: `tests/feature-stats-subnav.test.ts`.
- **The Workflow page EDITS its workflows — `Edit with AI` mounts the SAME `WorkflowChat` the org-scoped editor runs, in a right-side overlay (`components/feature-stats/workflow-ai-panel.tsx`). Never a second chat implementation.** Two things differ, and both follow from this page having no workflow in its URL. (1) The context carries a **name → UUID catalogue** (`lib/feature-stats-workflow-catalogue.ts`, alias-free so it carries real unit tests) instead of one `workflowId`, because the user names the workflow in the sentence ("duplicate Ballad but swap the LLM"). (2) The instructions route a duplication through `get_workflow_details` + `fork_workflow` **explicitly**, because the platform prompt reads an ABSENT `workflowId` as "this feature has no workflow yet, generate one from scratch" — which would discard the very pipeline the user asked to copy. No DAG is inlined (the context is re-sent every turn; same rule as the single-workflow editor). The panel stays MOUNTED while closed — hidden, not unmounted — or closing it to look at the table aborts a streaming answer and loses the thread.
- **The CATALOGUE is the FOURTH row source, and that is what makes a just-created workflow visible.** All three cross-org stats reads are keyed on spend and outcomes, so a workflow that has NEVER RUN is absent from every one of them — without the catalogue in the union, a workflow created from this page would be invisible on it until its first billed run and the page would read as if the creation had not happened. It lands with every earned column blank, which is the honest reading. Its query key is `["workflows", FEATURE_SLUG]` on purpose: `WorkflowChat.onFinish` already invalidates the `workflows` root, so the new row appears with zero extra wiring — do NOT add a bespoke post-chat refresh. Guards: `tests/feature-stats-workflow-ai.test.ts`.
- ⚠️ The chat proxy bills the LLM spend to the staff member's **currently-active Clerk org** — on a platform page that is whatever customer org was last god-moded into. Same as every admin chat surface today; a platform-billed chat route is a separate backend ask.

**The same scorecard exists one grain down, at `.../brands/:id/features/:slug/workflow-stats` — and there EVERY column is THIS brand's own realized figure. There is no fleet half.** Eleven columns: ROI, % CAC, $ CAC, Revenue, Positive replies, CPPR, Website visits, CPWV, Outreach, $ Invested — all features-service fields for this brand.

- **The cross-brand benchmark used to occupy the right six columns of this same table, and taking it out is the fix, not a simplification.** Both halves carried the SAME six labels, so one label meant this brand on one side and every brand on the other, and reading a row across compared one brand against the fleet — two different questions under one heading. The fleet answer keeps its own page (`/feature-stats/<slug>/workflows`), where every row IS the fleet, so nothing was lost. `tests/brand-workflow-scorecard.test.ts` fails if `All client brands`, any `getCrossOrgWorkflow*` read or `buildWorkflowPerfRows` reappears here. The cross-org readers stay — the fleet page still uses them.
- **`$ Invested` is `costEconomics.committedCostUsd` — billed spend PLUS the open provisioned holds, never the billed-only sibling.** ROI and % CAC divide by that same number, so a spend column showing anything else would make one row contradict its own ROI. This line said the exact opposite until features-service#779: ROI rode billed-only while the `spend` block rode committed, so ONE payload answered "how much did this cost" two ways and a brand with a single campaign read **$202** on its Overview beside **$191** here. There is now exactly one basis and it is COMMITTED. The producer still serves `actualCostUsd` (honestly billed-only, so consumers had a gap-free path); nothing in this repo reads it and nothing may fall back to it.
- **Positive replies / CPPR / Website visits / CPWV / Outreach have no per-workflow answer from features-service yet** — it serves them for the WHOLE brand on the un-grouped read. They are declared `.nullish()` under the producer's own established names (`recipientsRepliesPositive`, `recipientsClicked`, `recipientsContacted`, `spend.cpprCents`, `spend.totalCpcCents`) so the columns fill in on their own when it ships, and the page STATES why they are blank rather than leaving five unexplained columns of dashes. Do NOT derive them here: a group is a dynasty folding several versioned slugs, so it would mean summing and re-dividing in the browser.
- **The brand half CANNOT be rolled up from the per-campaign grouping.** `/features/:slug/revenue?groupBy=campaignId` is the only breakdown that existed, and a workflow routinely carries several campaigns (prod brand `75d7e3e8`: 20 campaigns over ~15 workflows, `bronze-2` alone on four). Summing pipeline and re-dividing ROI/CAC in the browser is the compute-a-stat-in-the-browser bug AND would contradict the Overview. features-service owns it: **`groupBy=workflow`** (features-service #772 → v0.130.0), one lean group per workflow. A group is a DYNASTY, keyed `workflowDynastySlug` with `workflowSlugs` listing the versions folded in — the same key the cross-org benchmark uses, so the two halves join directly. Both legs read the producer that FROZE the attribution (runs-service spend by `workflowSlug`, and the `workflowSlug` lead-service froze on each `leads_campaigns` row), never the campaign row's CURRENT workflow: campaign-service switches a live campaign's workflow while keeping its id, so that would mis-attribute everything spent before the switch. Groups deliberately do NOT sum to the brand — a lead served under two workflows belongs to both, and the engine's per-organisation combination is not additive across partitions (the per-campaign grain has the same property). The api-service gateway already forwards `groupBy`, so no proxy PR — but note its whitelist is a fixed list (`brandId, campaignId, workflowSlug, groupBy, lens, pricing`) and `funnel` is MISSING from it, which is the gateway-strips-a-field bug waiting to bite a future reader.
- **The route is `workflow-stats`, not `workflows`, on purpose**: `getNavigationLevel` sends `segments[6] === "workflows"` to the featureSettings level, so a `/workflows/...` sub-route would draw the Settings sidebar instead of the feature one. Guarded.
- **The nav entry reads `Workflows`; the alpha workflow EDITOR one level down under Feature Settings was relabelled `Workflow editor`.** One nav tree with two entries reading the same word for a scorecard and an editor is a nav contradicting itself. The app-level `/features/:id/workflows` entry keeps `Workflows` — different tree, no collision.
- **`buildBrandWorkflowRows` reads the workflow key under either spelling** (dynasty preferred, versioned accepted) because the producer owns how it identifies a workflow; a group carrying neither is DROPPED rather than rendered under a fabricated name. Guards: `tests/brand-workflow-scorecard.test.ts`.

⚠️ **A field the reader's schema never declared can also make a fix look CROSS-REPO when it is one line of frontend — so probe the payload the consumer ALREADY fetches before asking a producer for anything.** The too-narrow-schema rule below is written as a rendering bug (a field reads `undefined`, a cell shows `—`), and its more expensive face is a PLANNING one: a figure the producer serves and the reader strips is invisible to `git grep`, so it reads as absent from the wire and the obvious next move is a producer PR, a deploy gate and a wave. The cheap check is one authenticated call to the exact endpoint the surface already polls, read field by field — from inside the producer's own container if Cloudflare 1010s a scripted request. Cost 2026-08-29 (#3770, the funnel leg row's price): I offered the owner two options, one needing a features-service change for a per-campaign cost, and `?groupBy=campaignId` had been serving `outcomes.cpprCents` all along — declaring two fields turned it into a frontend-only fix with no new request. Generalise: **"the producer does not serve X" is a claim about a payload, so verify it against a payload, never against a reader.**

**A field a producer RENAMED, left declared under its old name and `.nullish()`, reads `undefined` forever and nothing anywhere goes red.** This is the quiet twin of the too-narrow-schema rule above (a narrow `z.object()` STRIPS a new field): here the schema is wide enough, the name is simply dead. `safeParse` passes (optional-and-absent is valid), `tsc` passes (the property exists on the inferred type), and every guard passes — the only symptom is a cell that is always `—`, which is indistinguishable from "the producer could not measure it". The brand workflow reader carried `costEconomics.totalCostUsd` for however long after features-service#402 renamed it to `actualCostUsd`, and it surfaced only because a new column finally needed to render the value. So when a producer's own docs describe a field as *renamed from X*, grep your readers for X in the same breath, and prefer reading BOTH (`new ?? old`) so an older body still fills the column. Corollary for a renamed REQUIRED field: declaring it `.nullish()` is what hides it — a field the producer marks required should be required in the reader too, so a rename fails the parse loudly instead of going blank.

**Third face, the LOUD one: a parser shared across GRAINS must accept every grain's BODY, and a field that is required at one grain is routinely absent at another by construction.** The two rules above are silent — a stripped or dead field reads `—`, indistinguishable from "the producer could not measure it". This one throws, and the failure lands somewhere you would not think to look: `safeParse` fails, the reader throws, the query errors, and reveal-on-settle paints the section as HEADINGS WITH NOTHING UNDER THEM. No skeleton, no error UI, nothing on screen saying it broke — so it gets reported as "the stats display wrong" rather than as a crash, and the diagnosis starts at the producer instead of at the parse. The tell is a whole band of labels blank at once while the wire carries real numbers.

Sharing the parser is still right — the money block a consumer renders is identical at all three grains, so a second parser is a second place for it to drift. What has to change with it is the SCHEMA: reread which fields survive a grain change before pointing a second reader at it. Here the field is whatever names the answer's subject, and a subject name almost never survives a widening. `featureSlug` was required for as long as the only revenue read was per-feature; #3468 pointed the brand and offer Overviews at reads whose bodies carry `brandId` / `offerId` and a `channels` breakdown INSTEAD, because a feature IS an acquisition channel here and a brand runs several. Every brand and offer Overview threw for two days on `$7,000` pipeline, `2.62×` ROI and `$953` CAC that were on the wire the whole time.

**Guard it with a REAL PARSE of each grain's body, never a source-substring assertion about the call sites.** `overview-reveal-on-settle.test.ts` shipped in the same PR and stayed green: it pinned the query keys, the shared parser, `pricing=net` and the absence of browser-side summing — every claim about how the readers are CALLED, none about what the parser ACCEPTS. `tsc` is equally blind, since the property exists on the inferred type either way. `revenue-parse.ts` imports only zod and a type precisely so it can carry real unit tests; keep it that way, and add a fixture per grain plus one rotten body that must still throw, so loosening a field cannot quietly turn the parser fail-soft. (#3468 → #3470)

**Fourth face, and it lives in a hop nobody looks at: a MIDDLE service that re-declares a vocabulary an END service owns will reject values the owner accepts, and the refusal names neither of them.** The three faces above are all about a CONSUMER's schema against a PRODUCER's body. This one is a validator sitting between them. A customer clicks a control, the request is refused with a generic `Invalid request`, and both ends are innocent: the dashboard sends a value the producer's deployed openapi lists, the producer would have taken it, and a passthrough in the middle answers first from a copy of the enum it made when it was written. Nothing rots loudly — the values that were in the list when the copy was taken keep working, so the control is broken for SOME of its options and fine for the rest, which reads as a flaky feature rather than a stale schema.

Diagnose by walking EVERY hop's deployed contract, not just the two ends: `get_endpoint_details` on the producer AND on each gateway between. The tell is one service's enum being a strict subset of the other's, usually with a couple of values the owner's own description calls retired. The fix belongs in the middle hop and is a DELETION — it forwards the field and lets the owner refuse, exactly as api-service already does on the same route (`status: string.min(1)`, "instantly-service owns this vocabulary and is the only place it is authoritative"). Do NOT add the missing values to the copy: that ships the same bug with a later expiry date.

Cost 2026-08-27 (lead panel reply kind): three of the nine reply kinds the picker offers (`lead_referral`, `lead_info_requested`, `lead_meeting_requested`) were refused with `Invalid request` because email-gateway validated against its own copy of instantly-service's list, taken before v0.74.0 widened it. instantly-service and api-service both accepted them.

**Fifth face, the one-word one: `.optional()` and `.nullable()` are DIFFERENT, and a producer that declares a field REQUIRED AND NULLABLE is telling you `null` is a value it means to send.** `.optional()` accepts an ABSENT field and REFUSES a null one, so reading a required-nullable field that way parses every body except the one the producer wrote the null for. It is the loud face (the parse throws, the query errors, reveal-on-settle paints headings with nothing under them) but it reads as a producer outage rather than a reader bug, because the endpoint answers 200 with a body that is valid against its own contract. Check the openapi's `required` list and `nullable` flag together before picking the modifier, and reach for `.nullish()` whenever both are true and the consumer already spells the field `x?.y`.

It gets worse in a FAN-OUT, which is where it actually bites: a surface that reads one endpoint per member of a set (an offer's channels, a brand's funnels) throws the WHOLE read on the first member whose body carries the null, so one channel nobody was looking at blanks every row. So when a schema-mismatch is reported on a page that fans out, probe EVERY member, not the one the page is about — the failing member is routinely the one that looks least relevant. Cost 2026-08-27 (#3614, `getFeatureRevenueByCampaign`): features-service serves the per-campaign `outcomes` block required-and-nullable (null = "no funnel is wired for this channel and the leads were never read"); the reader had `.optional()`. `sales-cold-email-outreach` and `feedback-request-cold-email-outreach` both answered with a full block, and `pr-expert-quote-opportunities` — which states sales funnels and has none wired, so the channel catalogue includes it in the fan-out — answered `"outcomes": null` and took the page down.

⚠️ **Probing an internal service route as an org means the INTERNAL org UUID, never the `org_...` Clerk id the URL carries** — api-service resolves external → internal at the gateway, so a hand-probe that skips the gateway and pastes the id out of the browser address bar hands a `text` where a `uuid` column is expected. It surfaces one hop DOWNSTREAM of what you are probing (features-service `502 Failed to compute feature revenue`, whose log names a brand-service `500` on a `select … where org_brands.org_id = $1`), so it reads exactly like the real bug you came to find and sends you off diagnosing a healthy service. Resolve it first: `docker exec -i distribute-postgres-1 psql -U postgres -d client_service` → `SELECT id FROM orgs WHERE external_id = 'org_...'` (the table is `orgs`, not `organizations`). Same family as the valid-UUID `x-run-id` rule above, and it cost a full round on this fix.


⚠️ **`new ?? old` is ONLY for a RENAME — the same number under two spellings. It is FORBIDDEN the moment the two names mean different things, and a producer that keeps the old name POPULATED beside the new one is usually telling you they differ.** A rename retires the old name (it stops being served, so the fallback can only ever fire on an older body). Two live fields are a MIGRATION between two concepts, and `??` there renders one concept under the other's label on exactly the window where it matters — silently, since both parse and both are numbers. Read the producer's description before writing the `??`: "TRANSITIONAL AND REPORTED ONLY… nothing divides by it" is not a spelling, it is a second answer. Kevin, on `committedCostUsd ?? actualCostUsd`: *"ACTUAL CA VEUT DIRE ACTUAL, COMMITED VEUT DIRE COMMITED, LES DEUX CONCEPTS ONT DU SENS, NE LES MELANGE PAS."* Read the new name alone; absent → null → render `—`, which is the honest "we have no figure". (features-service#779)

## "Queued today" on the Instantly audit is the number the SEND SELECTOR decides on — a step count compared against a daily cap is a wrong number, not a rounding

`GET /internal/audit/account-health` serves two different granularities for the same queue and they are one letter apart: `queuedFirstUnsent` counts every REMAINING STEP of a lead nobody has contacted yet (a 3-step sequence contributes 3), `queuedFirstUnsentSequences` counts the LEADS. A never-started lead owes exactly ONE email today, its first; the other steps are weeks out. So the honest due-today figure — and the exact quantity instantly-service's send selection counts toward an account's daily load — is **`queuedFirstUnsentSequences + queuedNextToday`**, and that is what `queuedTodayFor()` in `audit/instantly/page.tsx` returns.

The step field is not wrong, it is a different question: it is the correct input to the partition that reconciles against `queueSize` (`queueSize === queuedFirstUnsent + queuedNextToday + queuedNextTomorrow + queuedNextLater`), which is why both survive. Mixing them is what breaks.

- **Everything that displays, sorts, buckets or colors "queued today" reads the one helper.** The column, `compareRows`, the `QUEUE_BINS` histogram and the row panel all call it — a table that shows one number and orders by another reads as broken in a new way.
- **The row panel keeps the two totals under SEPARATE headings** (`Due today` / `Queue (all remaining steps)`). They differ on purpose and must never be read as one number.
- **The two tiles above the chart count STEPS** (`queueSize`) and say so; do not relabel them as emails.
- Guard: `apps/admin/tests/instantly-queued-today.test.ts` forbids the old summation and pins a single definition of the value.

**Same table, the ceiling that number is compared against: "Daily max send" states the RAMP-AWARE cap, and a figure gated on "only when it differs from its neighbour" can be deleted by that gate forever.** instantly-service computes the cap send selection actually enforces (`capForAccount = min(configured daily_limit, rampCapForAge)`) and serves it as `effectiveDailyCap` on every row. The cell rendered it ONLY when it differed from `dailyLimit` — and it never differs: `lifecycle-limits-sync` writes the ramped value back into Instantly's own `daily_limit`, so the `min()` returns `daily_limit` by construction. Probed prod: **244/244 rows carry the field, 0/244 differ**, so the line had never rendered on any tab since it shipped. Nothing was red — the producer was correct, the reader parsed it, the component handled it, and the value was simply unreachable — and it was reported as *"je ne vois rien"*, which reads as a missing feature rather than a suppressed one.

- **`dailyMaxFor(r)` (`effectiveDailyCap ?? dailyLimit`) is the ONE definition**, same discipline as `queuedTodayFor` beside it. The cap is the column's main number on every row; the difference gate now guards the PRE-RAMP configured limit, so a mature account states one number once and a ramping one reads `23` over `ramping up from 50 configured`.
- **`Queued today` colours against that same ceiling.** It compared against `r.dailyLimit`, so a young account read green against a cap the selector was not letting it reach — one row contradicting itself about one account, the same class the step-vs-lead bug above created.
- **The column sorts on the value it displays** (it sorted the pre-ramp number it was no longer showing).
- **Generalise: before shipping a figure behind "state it only when it differs from X", check against real data whether it CAN differ.** If a producer makes the two equal by construction, the gate is not a de-duplication, it is a deletion — and the honest shape is to make the more truthful number the one you always render and gate the other. Guard: `apps/admin/tests/instantly-daily-max.test.ts`. (#3830, follow-up to #3827)

Why it mattered: summing the step field put `tatiana@fuseconnectio.com` at 102 against a 45/day cap (227% saturated) while the selector saw ~32 of 45 and correctly kept assigning it leads. Fleet-wide 29 accounts read over cap where 12 are. Every account looking saturated is why a real starvation bug (25 of 39 live mailboxes sending nothing for days) was hard to read off this page. (#3190, #3191, instantly-service#547)

## A sending DOMAIN is the unit that bills and the unit you cancel — the Instantly audit grades it above the accounts table

`DomainHealthCard` (`components/audit/domain-health-card.tsx`, model in `lib/domain-health.ts`) sits above "Sending accounts" and answers what the per-account table cannot: which domains to turn off this month. Mailboxes are sold per domain and a spent one keeps billing, so the delete decision is per domain, not per account. Both read the SAME `["instantlyAccountHealth"]` query, so they share one poll and cannot disagree about the rows.

- **A mailbox must clear `HEALTH_BAR` (95) on BOTH scores** — Instantly's Health Score AND inbox placement. Either one alone answers the wrong question: prod carries mailboxes at `98/0` (perfect health, zero inbox). Below the bar it is **dead** with an empty queue and **dying** while it still owes emails, because deleting a draining mailbox drops queued sends. **The bar between ~85 and ~95 reclassifies nothing** — the scores are bimodal, and measured over all 256 prod accounts on 2026-08-02 the two produce byte-identical verdicts. Do not re-litigate it without re-measuring.
- **The domain roll-up is ORDERED and EXHAUSTIVE** (`domainHealthState`), guarded by a test over every combination of up to three mailboxes: all dead → `To delete now`; all healthy → `Healthy`; **any healthy beside any non-healthy → `Mix state`**; otherwise → `To delete soon`. Mix is deliberately wider than "one dead + one healthy", which left `dead + dying` and `healthy + dying` matching no rule at all. `To delete soon` therefore means NOT ONE mailbox clears the bar — a domain still landing in inboxes must never sit in a tab whose whole promise is that it is on its way out (prod `axionmilestone.com`: four dying mailboxes beside one at 100/100 with 29 queued).
- **An absent score is `ungraded`, never a pass or a fail.** Ungraded mailboxes are excluded from the roll-up rather than counted either way; a domain where nothing could be graded reads `Not graded`, and **that tab renders only when it has rows** so an empty one never advertises a state the fleet is not in. Both scores are non-null for every prod account today — this is the defensive branch, and the honest answer for it is its own word, not a default.
- **Cost is MAILBOX-ONLY, and that is the point**: a registered domain is not refunded when you cancel, so the recurring mailbox subscription is exactly what deleting saves. `MAILBOX_MONTHLY_USD` carries published list prices (google $7 Workspace Business Starter annual, microsoft $6 M365 Business Basic, imap $3 Mailforge, read 2026-08-02). **A provider with no rate returns `null` for the whole domain, never a partial sum** — a partial reads as the real cost while understating it. The tab header states its own total the same way.
- **Colour is keyed on the VERDICT, never on a raw score band** (guarded): a chip coloured by score beside a pill stating the verdict is one row contradicting itself. All tints are in the `html.dark` closed set.
- Rows lead with the most expensive domain (the card exists to answer "what do I turn off"), unstateable costs last. One column per mailbox slot, sized to the widest domain in the ACTIVE tab. `lib/domain-health.ts` is alias-free apart from a type-only import, so it carries real unit tests — keep it that way. Guard: `tests/domain-health.test.ts`.

## Conversion rates and lifetime revenue belong to a SALES FUNNEL — the dashboard writes NO brand-level economics, anywhere

`brand_sales_economics` is the model the funnel model replaced: ONE set of rates and ONE lifetime revenue for every path a brand sells through, plus an `optimizationGoal` that could not tell the two meeting funnels apart. **features-service no longer reads that goal at all**, and prices its pipeline on the brand's DECLARED funnel — so a dashboard write there persists values nothing consumes. The row survives only as a legacy fallthrough for a brand that declared no funnel, and as the prefill the funnel screens seed from.

**So no dashboard surface writes it. `saveBrandSalesEconomics` has no caller left in `apps/dashboard`** — `buildEconomicsPayload`, `resolveStoredEconomics`'s payload role and `rememberSavedEconomics` are deleted with it, and `tests/onboarding-economics-write.test.ts` fails if any of the three names reappears. (`apps/admin` keeps its own copies; staff fork, deliberately not synced.)

**Why this is a DELETION and not a better restatement.** The write REQUIRED all six core metrics on every call, so a screen editing one had to restate five it never showed — and restating them from client state silently destroys what the user confirmed earlier. Cost 2026-07-29 (brand `EmailToolsHub` / `7604c385-1f02-4016-b42f-344565bcd36d`): the rates step showed the hydrated 8.4% visit→signup / 16.2% signup→paid and the user accepted them. While they were at Stripe the pending-checkout snapshot became unparseable, so `readPendingCheckoutLaunch` rebuilt it via `reconstructCheckoutOnboardingState` — which fabricated `rates: { ...DEFAULT_RATES }`. The lifetime-revenue step then PUT the full six-metric set from that rebuilt state, writing `5%` / `10%` over the confirmed values. The same reconstruction seeded a `$2,500` placeholder LTR, and since `roiMultiple = ltrUsd / costPerPaidClientUsd`, the projection printed a flattering ROI that collapsed to `0.6×` the moment the user's real `$100` landed. Restating from the WIRE fixed that instance; not writing at all removes the class.

Rules (`apps/dashboard/src/components/onboarding/onboarding.tsx`):
- **Picking the primary funnel persists NOTHING.** `savePrimaryFunnelAndContinue` is synchronous: it sets the outcome (which orders the detail screens, prices the budget step and resolves the projection) and advances. What the brand sells through was already stated one step back by `saveFunnelsAndContinue`.
- **The best-model step prices the PRIMARY FUNNEL, through the same `getBrandSalesFunnels` → `buildFunnelPatch` → `declareBrandSalesFunnel` path the funnel's own screen and the Settings card use.** Its fields come from `funnelRateFields(def)` plus the lifetime revenue, so a rate reads the same words in all three places and one funnel is priced the same way wherever it is priced. It kept an editor at all because a sub-1x ROI is almost always a small lifetime revenue rather than a bad model — a block showing the quotient and neither operand is unexplainable.
- **The old field set was the retired goal's, and it was incoherent.** `RATE_KEYS_FOR_OUTCOME[outcome]` held the ENTRY legs of DIFFERENT funnels (the meeting goal asked for reply→meeting AND visit→meeting, one from each meeting funnel) rather than the chain of the one funnel being priced. Deleted; do not reintroduce a per-goal rate list.
- **The Update button arms on a LIVE compare** (`economicsSnapshot !== modelEconomicsBaseline`), never a sticky edited latch — typing a value and undoing it must disarm it. The baseline is captured by an effect on entering the step and refreshed on write, so no entry path (funnel Continue, offer Back, a fresh page load resuming at `model`) can forget it.
- **A refusal renders `funnelWriteErrorMessage(err)`**, never `err.message` — that is the whole downstream body verbatim.
- **`DEFAULT_RATES.ltv` is `0` / `""` on purpose — do NOT re-add an invented lifetime revenue.** An LTR placeholder is divided into the fleet cost-per-outcome and rendered as a projected ROI, so it reads as a promise, not a default.
- **`prewarmStoredEconomics` stays.** The post-payment steps run on a FRESH page load (the Stripe return) and never call `hydrateOnboardingInBackground`, so the warm-up is what seeds the lifetime revenue the funnel screens prefill from. `ltvEditedRef` keeps a user-typed LTR from being overwritten by it.
- Guards: `apps/dashboard/tests/onboarding-economics-write.test.ts`. (#3039, retire-brand-level-rates)

**The run-status bar is GONE from the whole customer dashboard, and PAUSING IS DEFUNDING A FUNNEL — `brand-status-control.tsx` is DELETED, not hidden.** It stated three BRAND-level things side by side — the retired optimization goal, the brand pause flag, and the brand's daily budget — and its last home was the campaign Overview, a page scoped to ONE campaign and ONE funnel. Every one of the three was wrong there: the goal word cannot even name which of the two meeting funnels the campaign runs (`sales_meetings` covers both), and the dollar figure is billing's SUM of every funnel's ceiling, not this campaign's. Money is funded per sales funnel on Brand Settings, so **a customer stops a chain by dropping its ceiling to zero** — a brand-wide flag beside per-funnel ceilings is two ways to say one thing, and only one of them is what billing charges. Deleted with it: `setBrandPause` in `lib/api.ts` (there is NO writer left), the `api/brand-status-email` route the mutation fired, the `brand-paused` / `brand-resumed` email templates whose only sender that route was, and the already-orphaned `brand-daily-budget-card.tsx` (the second `setBrandPause` caller). **`getBrandPause` is GONE TOO, and its deletion is the lesson: a flag with no writer does not stay true, it FREEZES.** It survived one round as the first-outcome banner's read-only gate, on the reasoning that a banner must not promise results to a brand that is not running — which is right about the question and wrong about the instrument. With the control deleted nothing wrote it again, so prod held 8 rows, none touched since early August, and the banner inherited the staleness in BOTH directions: a brand marked `paused: true` in July while spending $55.69 that week was told nothing, and every brand with a funded funnel and no campaign at all read `paused: false` and was promised results from a campaign that does not exist. The banner gates on `useRunningDailyBudgetCents` now — money actually standing behind an ongoing campaign — so `BrandPause`, `BrandPauseSchema` and the `brandPause` persist root are deleted with it, and a `src/`-wide guard fails if any of them comes back. campaign-service's endpoint is left alone (its runtime gate-check may still consult it). Generalise: **when you delete the only writer of a piece of state, delete its readers in the same pass or say out loud what will keep it true** — a reader left behind reads a photograph, and it is worse than no reader because it looks like a live signal. Choosing how a brand sells, and funding it, are both the Sales Funnels card on Settings. Consequence accepted: `Restart` exists nowhere, so a brand paused by the old button resumes by funding a funnel. The no-website goal coercion (`noWebsite && storedGoal ? "positive_replies" : storedGoal`) went with the bar and is no longer needed as a display rule — it now holds by construction, since every website-led funnel refuses to be declared for a brand with no domain. Guards: `tests/campaign-funnel-steps.test.ts` + `tests/campaign-budget-once.test.ts`.

**Sibling fix, features-service v0.106.0 (#659): the workflow projection reads live economics on every request and caches only the evidence fan-out.** Before that, `servedCached` keyed the whole body on `(org, brand, objective, pricing)` with a 5s TTL / 60s hard-max and no invalidation on an economics change, so a read right after the write could still return the pre-write `roiMultiple`/`cacPct`. Do NOT re-fold economics-derived fields back into the cached body, and do NOT add a consumer-side cache-bypass param — the split (heavy LTR-independent fan-out cached, cheap LTR division live) is what makes read-your-own-write correct.

## The Sales Funnels section is what a brand STATES about how it sells, and every field on it persists per funnel

`BrandSalesFunnelsCard` (`components/settings/brand-sales-funnels-card.tsx`, model in `lib/sales-funnels.ts`) is **GA** and is the brand Settings page's ONLY sales-economics surface: it REPLACED the flat `Click Destination` and `Sales Economics` sections, whose cards were deleted from `apps/dashboard` with them (`apps/admin` keeps its own separate copies — staff fork, deliberately not synced). A funnel owns the conversion rates, the lifetime revenue and the landing page those two held ONE set of for the whole brand, so a brand selling a $200 self-serve plan and a $20k contract prices each one. It renders its own heading and offers FOUR funnels, multi-select, each with its own conversion rates, its own customer lifetime revenue and its own destinations:

| key | name | chain |
|---|---|---|
| `reply_meeting` | Sales Meeting from Conversation | Positive reply → Meeting booked → Meeting attended → Paid client |
| `visit_meeting` | Sales Meeting from Website | Website visit → Meeting booked → Meeting attended → Paid client |
| `visit_signup` | Website Purchase | Website visit → Signup → Paid client |
| `visit_form` | Form Magnet | Website visit → Form filled → Paid client |

**A funnel's NAME is what it is; its CHAIN is what it does — the two are separate fields and the card renders them on separate lines.** Before #3164 the chain WAS the title, which left the second line with nothing to say and made every card read as a wire diagram. The name leads (`text-sm font-medium text-gray-900`), the chain sits under it quieter (`text-xs text-gray-500`), and a `h-11 w-11` icon tile runs alongside BOTH (44px vs the 42px the two lines occupy — a tile sized to the title alone reads as decoration).

**There is no primary funnel.** Several run at once and none outranks another; which one a campaign optimizes for is a campaign concern, not a settings one. `tests/sales-funnels.test.ts` fails if `primary` / `Primary` / `StarIcon` reappears in the card.

**Choosing a funnel, and dropping one, are decisions about how the brand sells — so NEITHER is a checkbox.** There is no checkbox in the card at all (guarded). A closed card is opened by clicking it ANYWHERE: a `role="button"` div wraps the whole header with `tabIndex` + Enter/Space, deliberately not a `<button>`, because the form it reveals contains its own buttons and a nested button is invalid HTML. The choice itself is then a named button inside the open form — `OK` / `Update` to take the funnel, **`Remove this funnel`** to drop it — with the action row right-aligned on desktop (`sm:flex-row sm:items-center sm:justify-end`) and stacked on mobile. **One card is open at a time** (`openKey`), because the list reorders itself around the selection and several open forms would move under the cursor. Verified by reproduction rather than by reading the classes: `document.elementFromPoint` at six points across a closed card (padding, icon tile, name, chain, empty space, the Selected tag) all resolve inside the clickable container at 1280px and on a Pixel 7, and Enter + Space each fire it.

**The chosen funnels render FIRST, then the rest, as two groups** (`partitionFunnelsBySelection`, pure and unit-tested). A chosen funnel carries a green `Selected` pill on the right (`bg-green-50 text-green-700`); the rest sit under a quiet `Not selected` heading on `bg-gray-50` with a `text-gray-500` name and a dimmed tile, so the two sets read apart at a glance. The heading only renders when both groups exist — labelling a list where nothing is chosen yet is noise. The hover tint differs per group (`hover:bg-gray-50` on a white card, `hover:bg-gray-100` on the gray ones), or an unselected card would show no response to the cursor.

**A funnel that is neither chosen nor open shows its name and its chain, and NO numbers** (`showNumbers = state.declared || isOpen`, guarded). An undeclared funnel's rates and lifetime revenue are a prefill nobody has confirmed, and printing them on a row the brand never picked reads as a claim about how it sells.

**The chain is modelled as `steps` + `legs`, one leg per arrow, every arrow priced.** `legs.length === steps.length - 1` is guarded. `funnelLegPct` prints the rate under its arrow once the funnel is chosen or open, and returns `null` for a rate the brand has not filled — a rate we do not have prints nothing rather than a zero. **`Meeting booked → Meeting attended` is the show-up rate, and brand-service stores it ON THE FUNNEL and nowhere else in the fleet** — so `meetingBookedToAttendedPct` saves like every other leg but is the one `SeedlessFunnelRateKey`: `isSeedableRateKey` sends it down a branch that starts it BLANK on every brand instead of indexing `BrandSalesEconomics` (which has no such column). Rate labels live once in `RATE_FIELDS` and funnels point at them by key, so two funnels sharing `meetingToClosePct` cannot drift into two labels for one stored number.

**The lifetime revenue closes the CHAIN, after `Paid client`** (`funnelLifetimeLabel` → `$2,500 lifetime revenue`), not a line of its own: it is what the last step is worth, so it reads in the same sequence. It is dropped when absent or zero.

**It WRITES, through brand-service's own per-funnel routes and nothing else** (`getBrandSalesFunnels` / `declareBrandSalesFunnel` / `undeclareBrandSalesFunnel` in `lib/api.ts`, proxied by api-service under `/v1/brands/:id/sales-funnels`). `OK` / `Update` declares the funnel and prices it; `Remove this funnel` undeclares it, and its economics go with the declaration. The CTA is `OK` on first declare and `Update` after (`state.declared`), never `Save`. **Do NOT wire `saveBrandSalesEconomics` / `saveBrandClickDestination` here** (guarded): those hold ONE lifetime revenue and ONE destination for the whole brand, so they would take four funnels' values and persist one of each — the per-funnel model collapsing back into the brand-level one.

**The write is a PARTIAL patch diffed against what is stored** (`buildFunnelPatch(def, draft, saved)`), because brand-service leaves an omitted field as stored and CLEARS an explicit `null`. Three things follow, all guarded in `tests/sales-funnels.test.ts`: editing one rate sends only that rate, so a field confirmed elsewhere is never overwritten from a stale copy; emptying a field sends `null` and really clears it, so nothing is validated as "required" client-side any more (`validateFunnelDraft` is shape-only — a blank value is legal everywhere); and the patch can never carry a rate outside the funnel's own chain or a destination the funnel has no use for, both of which brand-service 400s. `sameUrl` compares what two destinations would NORMALIZE to, or every save would re-send an unchanged URL.

**A goal the catalogue prices under a LONGER chain is translated, never read leg by leg** (`orphanGoalSeedFor`). `positive_replies`, `website_visits` and `sales` price ONE end-to-end step; the funnel spells the same journey over several legs. So the brand's rate seeds the LAST leg (the one landing on `Paid client`) and every leg above it seeds at **100%** — the product across the chain then equals exactly what the brand gave us. `positive_replies` → `reply_meeting`, `website_visits` → `visit_signup`, `sales` → BOTH (each from its own rate); `signups` / `sales_meetings` / `form_submissions` / `website_purchase` are absent because a funnel already prices their legs by name. Reading those legs by name CANNOT work: brand-service stores every blended rate NOT NULL with a server default, so a leg the goal never configured reads back a plausible number the brand never stated. Guarded in `tests/sales-funnels.test.ts`, including that the chain multiplies back to the stated rate.

**The prefill is for a person to confirm and is never written on its own.** An UNDECLARED funnel seeds from the brand's blended economics + click destination (`funnelDraftFromBrand`); a DECLARED one shows its own stored values (`funnelDraftFromDeclared`, `null` → blank, never a zero). Because the patch omits any field still equal to what is stored, and an undeclared funnel's stored state is `NOTHING_DECLARED`, a prefilled field the user left alone is never persisted — brand-service has no defaulting layer, so a value it never received reads back `null`, which is how a consumer knows not to rank on it. Turning that into "the brand told us" is worse than the preview was. **The booking link still starts EMPTY** — nothing seeds one, and borrowing the click destination would put a URL on screen the brand never gave us.

**`declared` is read BEFORE `funnels`, and the two empty answers never collapse.** `declared: true` with no funnels means the brand STATED it sells through none; absent-or-false means it has never told us anything, and the card says so in different words. It is `.optional()` in the reader only because an older brand-service serves the set without the flag; an absent flag reads as "never stated". Undeclaring the LAST funnel does not un-state the set (brand-service keeps the marker), so a brand that dropped everything has still answered.

**A refusal from brand-service is shown, not swallowed** (`funnelWriteErrorMessage`): a 400 renders `body.error`, which is a sentence brand-service wrote for a person ("this funnel starts with a click onto the brand's website…"); 403/404 get their own line; anything else gets one generic line. **Never `err.message`** (guarded) — `apiCall` sets it to the whole downstream body verbatim. Do NOT re-implement brand-service's validation as the source of truth; the client checks exist to make typing pleasant and the server's 400 is the answer.

- **A booking link is OPTIONAL wherever a meeting sits in the chain** (`bookingLink: true` on both meeting funnels), and a page destination only where a click onto the site starts it (`pageDestination`). A brand that books over email still runs the funnel, so `validateBookingUrl("")` returns ok; a malformed one is still rejected. These are two independent fields on `FunnelDraft`, not one `kind`-switched destination — `visit_meeting` legitimately has both.
- **The line under the chain is `funnelDestinationChips`: each destination as its own favicon (`BrandLogo`, logo.dev) beside `shortUrl(...)`** rather than the raw link. A destination the brand never gave us is DROPPED, not printed empty. **`shortUrl` cuts everything from the first `?` or `#`** and the chip is width-bounded + `truncate`: a real click destination carries a UTM tail (`…/level-1-free-assessment/?utm_source=landing_page&utm_medium=email&utm_campaign=…`) long enough to fill the row on its own, and none of it identifies the page. The path stays, because that IS what distinguishes one destination from another.
- **The green tag on a collapsed funnel states what it spends TODAY — its RUNNING campaigns' ceilings — never the sum of the fields the open form edits.** Those fields are billing's ceiling per (funnel x channel x offer) and billing stores NO status, so summing them counts a paused channel exactly like a running one: measured on the brand that surfaced it, a $140/day ceiling with all 47 of the funnel's campaign rows `stopped` read a green `$140/day` for a funnel spending nothing. The join is campaign-service's and is already served — read `["brandSpendableBudget", brandId]` (the key every campaign surface polls, so it costs no request) and narrow it with `spendableCampaignsForFunnel`, the SAME exported rule the funnels table one level up reads, or a row and the card it opens disagree about one funnel's money. Both writes already call `invalidateCampaignMoney`, which re-reads that root. **Three states, and the middle one is the half that was also wrong in the other direction**: running > 0 → green `$X/day`; funded with nothing running → grey **Paused**; no ceiling at all → grey **Not funded**; read unsettled → NO tag, never a grey one ("we could not measure this" is not "this spends nothing"). A funnel whose every channel is stopped still holds the customer's amounts, so `Not funded` read as data they would have to re-enter — `offerFundedCents` survives for exactly that question (does a ceiling EXIST), which status cannot answer. ⚠️ **Generalise the miss: when a sweep moves money surfaces onto the status-aware figure, the EDITING surfaces are the ones it skips** — a read-only stat card is obviously "a money surface", while a settings card that owns the ceiling fields reads as the source of the number rather than a consumer of it, so it keeps summing what it writes. Grep every reader of the ceiling store, not just the pages that display a total. (#3838)
- **The per-channel daily budget is BELOW the funnel's inputs, full width, one row per channel — never a cell of the form's grid.** It shipped as a single cell of `sm:grid-cols-2 lg:grid-cols-4`, so a channel's mark, name and `$ /day` input had a quarter of the width and wrapped into a stack nobody could read (reported as *"l'UI est dégueulasse"*). Generalise: **a block whose content is a LIST OF ROWS is not a form field and does not belong in the field grid** — a row needs the full width to put its identity on the left and its value on the right, and the grid is sized for one label over one input. Rows are separated (`divide-y`) rather than stacked, and the input stays `w-32 shrink-0` so every amount lines up whatever the channel is called. Verify by REPRODUCTION (compiled Tailwind off the app's own `globals.css` + Playwright at 1280 and 390), not by grepping classes: measured one 56px row per channel with the name and the input on one line, and no overflow at either width. (#3831)
- **Icons are Phosphor duotone** (`@phosphor-icons/react`, imported per-icon from `dist/csr/<Name>` so the 190KB barrel never enters the bundle; the non-deprecated export is `<Name>Icon`). Heroicons is a single-weight utility set and read as flat inside a colored tile; duotone carries a tinted fill under its stroke in the same `currentColor`, so one `text-<tone>-600` class drives both layers. `ChatsCircle` / `CalendarCheck` / `ShoppingCartSimple` / `Magnet`. The four tints (purple / indigo / blue / orange) are all in the `html.dark` closed set, guarded.

- A booking link is a scheduling page on a third-party domain (Cal.com, Calendly), so it is validated as a plain URL (`validateBookingUrl`), NOT through `validateDestination`'s on-brand-domain check. Conflating the two rejects every real booking link.
- ⚠️ **The card's read is `getOfferSalesFunnels(brandId, offerId)` under `["offerSalesFunnels", brandId, offerId]`, NOT a brand-wide one — a lifetime revenue is a property of `(offer, funnel)`, so the brand grain would answer with what a DIFFERENT proposition is worth.** This line said `["brandSalesFunnels", brandId]` for months after the read moved to the offer; `getBrandSalesFunnels` survives in `api.ts` with no React Query caller at all (onboarding calls it imperatively), and `brandSalesFunnels` is NOT in `PERSISTABLE_QUERY_ROOTS`. A new surface that wants the LTR and copies the name out of this file therefore adds an unlisted root, cold-skeletons on every visit, AND borrows a sibling offer's number — and only the allowlist half goes red (`persist-query-cache.test.ts`). Read the card's own `useAuthQuery` before reusing a key named here. `offerSalesFunnels` IS allowlisted; an unlisted root is default-OFF and cold-skeletons every visit. The declare response carries ONE funnel, so `cacheDeclared` merges it into the cached set in catalogue order; the undeclare response IS the remaining set and is written whole.
- **Consequence for a consumer of the LTR: it is only resolvable where an OFFER is named.** `saleValuePrefillUsd` (`lib/lead-close-won.ts`) opens the Close won deal-value field with it, so the prefill exists on the offer, funnel and campaign leads pages and is deliberately ABSENT at brand grain — a lead there can be on any of the brand's offers, and reading the brand-wide figure to fill the gap is the borrowing this rule exists to stop. The field opens empty instead, which is what it did before.
- **A funnel carries the MONEY it is funded with, and that money is billing's, not brand-service's.** The card composes two reads — the declaration and its economics from brand-service, the daily ceiling from billing (`getBrandFunnelBudgets`) — and makes two writes on one button. The ceiling stays OUT of `FunnelDraft`: that draft is exactly what brand-service's partial patch reads, and it has no column for money. It is written only when it MOVED, and BEFORE the nothing-changed exit, because a budget edit alone is a real change even when the economics are untouched. The green tag states the funded ceiling rather than merely that the funnel was picked — the money IS the selection now — and a declared funnel at zero reads `Not funded` instead of a green tag claiming it runs. Guards: `tests/sales-funnels.test.ts`.
- **The per-funnel floor governs what a brand may NEWLY state, never what one has already been running — so `funnelBudgetBelowMinimum` reads the STORED ceiling, not the typed value alone.** Per-funnel ceilings shipped after the money did, and billing's single-funnel attribution sweep carried each brand-level ceiling over verbatim rather than raising anyone's spend, so live brands sit under their funnel's floor right now (prod: a reply-to-meeting funnel at $8/day against a $24 floor, charged daily). Checking the typed value alone left those brands two moves — leave it alone, or defund it — and refused the raise, which is the wrong direction to block. It also refused an edit to a **conversion rate** on a funnel whose money nobody was touching, because the gate runs before the whole form saves. A funnel billing funds under its floor is therefore grandfathered: keep it, raise it (even to another figure under the floor), or drop it to zero; lowering it to another funded figure under the floor stays refused, and reaching the floor spends the grandfather through the ordinary rule rather than a branch of its own. The refusal and the tip beside the field (`funnelBudgetFloorMessage` / `funnelBudgetTip`) name what the funnel may actually do instead of a floor it is not allowed to walk down to. **Onboarding passes a stored ceiling of `0` explicitly** — signup is a brand stating its budgets for the FIRST time, so the floor applies in full and no sub-floor ceiling can be created there. billing-service holds the same rule and its 400 is what decides. ⚠️ **The grandfather RATCHETS: it measures against what is stored right now, so a brand that raises $8 to $10 can no longer return to $8, and going through 0 does not help (0 clears the grandfather, so $8 reads as a fresh sub-floor statement). Only a direct DB write gets it back — do not "just test" a raise on a real customer's funnel.** (#3339, billing-service #354 → v0.62.1)

**The funnel key is becoming the fleet's ONE vocabulary, and it is the RICHER word — a surface naming what something buys reads the funnel key, never the goal.** `reply_meeting` and `visit_meeting` both collapse onto `meetingBooked`, so the goal cannot name a chain on its own; the eight-token goal set is being retired into four funnel tokens the owner fixed verbatim: `form_magnet`, `sales_meetings_from_conversation`, `sales_meetings_from_website`, `website_purchases` (renames of `visit_form` / `reply_meeting` / `visit_meeting` / `visit_signup`). `normalizeSalesFunnelKey` (`lib/sales-funnels.ts`) already reads BOTH spellings ahead of brand-service emitting the new ones, same discipline as `CANONICAL_GOALS`. A campaign carries `funnelKey` on the wire and the Campaigns table reads it and **NOTHING else — there is no goal fallback anywhere, and `primaryFunnelForGoal` was deleted rather than left unused** (`lib/campaign-funnel.ts` exports exactly one function, guarded). Deriving a funnel from a goal prints a chain the campaign never stated, and `meetingBooked` proves it: two funnels answer to it.

**Creating a campaign STATES the funnel it sells — `funnelKey` is REQUIRED on both `createCampaign` signatures, and a feature that sells through no sales funnel states an explicit `null`.** campaign-service 400s a sales campaign that states none: the funnel is what the campaign is paced on (its own daily ceiling in billing) and priced on, so a campaign without one is what left customers funding a funnel and never getting a campaign for it. Required-not-optional is the point — it makes a new caller answer the question instead of quietly shipping a null.
- **Onboarding states a funnel the customer just FUNDED** (`lib/launch-funnel.ts` `fundedLaunchFunnelKey`, alias-free so it carries real unit tests), read off the SAME per-funnel budget map billing was written from a few lines above, so the campaign and its ceiling can never name different funnels. The pick never leaves the funded set and is DETERMINISTIC — the primary funnel when it was funded, else the first funded funnel in catalogue order — because a retry that picked differently would create a second campaign for a second funnel.
- **Several funded funnels create ONE campaign here**, not one per funnel: campaign-service provisions the rest, one per funded funnel, on its next tick. Do NOT loop the client over the funded map.
- **Nothing funded STOPS the launch** rather than defaulting. A funnel nobody funded is neither paced nor priced, so inventing one is worse than the error.
- **Staff state it explicitly**: a picker on both admin create paths (`lib/sales-funnel-keys.ts`, the canonical four), required when `isRevenueFeature(featureSlug)` and absent otherwise; it rides the pending-checkout blob because the resume path creates the campaign on a fresh page load. A relaunch carries `campaign.funnelKey` off the row it relaunches.
- **Never derive it from a goal** (guarded) — same reason as the vocabulary rule above. Writes go out in the canonical spelling (`canonicalSalesFunnelKey`) while reads stay tolerant of both.
- The api-service create proxy re-declares the body, so a new field on this call needs the gateway to forward it before it reaches campaign-service. Guards: `apps/dashboard/tests/launch-funnel.test.ts` + `apps/admin/tests/campaign-funnel-key.test.ts`. (#3344, campaign-service #316)

**A brand that has been running outreach for months KEEPS THAT CAMPAIGN when it funds its funnels — campaign-service adopts it into the funnel it was already running instead of parking it and provisioning an empty twin.** The "superseded" state is gone from the fleet, front and back; nothing renders it, nothing computes it, and no campaign is alive-but-never-given-a-turn. It shipped as a 15-minute defer on the keyless pre-funnel campaign plus a new campaign for the same funnel, so the staff table showed two rows on one channel and one funnel: the row holding 100% of the history (11.6×, $1,177) read dead, and the row that read live had produced nothing. The old goal vocabulary is REWRITTEN in campaign-service's own storage, not mapped on read — `form_submissions` → Form Magnet, a booked meeting → **Sales Meeting from Conversation** (owner-decided 2026-08-02: these campaigns are cold email, so the chain that ran is reply→meeting, never the website one), `website_purchase` → Website Purchases.

**Generalise it: when the wire hands a surface two rows that contradict each other, INVENTING A WORD to explain the contradiction is not a fix — it ships the bug with a label on it.** This page did exactly that: it noticed two campaigns reading `ongoing` for one funnel, correctly called that "the page contradicting brand Settings", and then coined `superseded` — a state no service emits — instead of asking why the backend had produced two. The tell is a status/label the wire does not carry, computed client-side from a shape you find surprising, with a doc comment justifying why the shape is surprising. That comment is the diagnosis; act on it upstream. Kevin, reading the result cold: *"WTF IS SUPER SEEDED???"* — a word invented for a customer surface is jargon by construction, because nobody outside the diff has ever seen it. Same reflex to distrust: a read-time fallback that maps a retired vocabulary onto a current one. If a stored value is stale, rewrite the stored value. (#3253, #3258, #3262, campaign-service v0.36.0)

**Picking funnels IS picking goals.** features-service `/goal-arbitration` reads the declared set from brand-service's own `/internal` route (never from here), ranks it, and campaign-service runs the winner — the brand-level `optimizationGoal` is only the FALLBACK for a brand that declared nothing. That is why removing the flat goal picker was safe, and why a dashboard surface that DISPLAYS a goal must read the arbitrated one rather than the stored field: showing the stored goal beside campaigns running the arbitrated one is the self-contradictory-surface bug. The arbitration is reachable from the browser through the api-service proxy (`KevinLourd/goal-arbitration-proxy`); it 502s `authorized_goals_unavailable` for a brand that never declared, which is the signal to fall back to the stored goal, the same chain campaign-service uses. (#3146, #3164, #3169, #3149; brand-service#393 / #396, api-service#784, features-service#704)

## A channel is a FEATURE SLUG, and features-service owns which ones exist: the dashboard READS the catalogue, it does not keep one

`lib/acquisition-channels.ts` used to hold two hand-written arrays: two channels we ran, eight more under a `COMING_SOON_CHANNELS` heading with no feature slug, on the reasoning that a channel we cannot run "exists in no service yet". That reasoning expired. features-service publishes **33 channels** at `GET /public/channels`, every one seeded `implemented: true` / `status: "active"` (its own comment: *"BOOKABLE, like every other published channel. There is no half-published state to express here"*), each stating the funnels it can be sold through. So the local copy named 2 of 33 and called live ones "coming soon" — the stale-copy failure, and it had teeth: `channelsForFunnel` filtered that copy, so **a channel published upstream could not be funded here at all**, and a campaign running one had no mark and no name.

- **`acquisitionChannelsFromFeatures(features)` derives the catalogue from the features the app already fetches.** `listFeatures` runs no Zod, so `slug` / `name` / `description` / `displayOrder` / `salesFunnels` arrive intact on the `["features"]` query every session already holds. `useAcquisitionChannels()` is a `useMemo` over that context, NOT a query of its own: naming a campaign costs no request. There is deliberately no reader for `/public/channels` here, because the features list already answers.
- **A feature IS a channel when it states at least one sales funnel.** The producer's own words: every non-sales feature (PR, hiring, VC, press kits, expert quotes) states `[]`. So "sells through nothing" IS "not a channel" and no list decides it here. ABSENT stays the pre-field reading (treated as selling every funnel), which is a different statement from `[]` and is read apart from it.
- **The MARK is the only thing this app decides**, because nothing upstream states one. `CHANNEL_MARKS` keys a tile on a slug: a platform's real logo for a channel bought on somebody else's platform, a Phosphor duotone glyph in a `html.dark`-remapped tint for the media we own. **A slug missing from that map is not an error** — the channel keeps its name, its funnels and its money and simply draws no tile, and `AcquisitionChannelMark` returns null for it. A mark we would have to invent is worse than none, and gating a channel's EXISTENCE on whether we drew an icon is how the stale copy happened in the first place.
- **`acquisitionChannelForFeatureSlug(slug, channels)` takes the catalogue explicitly — there is no default argument**, so a new caller has to say where its channels came from rather than silently resolving against a list that may be a session old. An empty catalogue (features still settling, or errored) answers null, and every caller already prints the channel's own words through `channelSlugLabel` rather than guessing at a tile.
- **There is NO acquisition-channels settings card, and that is deliberate.** An earlier note here described `BrandAcquisitionChannelsCard` as GA on brand Settings; the component does not exist and `acquisition-channels.test.ts` has a guard asserting it never comes back. A channel is not CHOSEN anywhere: it is FUNDED on the funnel it feeds, on Offer Settings, and funding a (funnel, channel, offer) triple is what makes it run. So the catalogue's consumers are the campaign surfaces and the funnel budget rows, nothing else.
- **WHICH channels exist and WHICH a customer may FUND are two questions, and conflating them is what made one stale list so damaging.** features-service marks all 33 published channels bookable, which is what the agency SELLS; campaign-service provisions a campaign for a CLOSED SET of feature slugs, which is what currently RUNS. Funding a channel outside that set states a ceiling and produces no campaign at all: nothing errors, nothing is charged, and the channel simply never does anything, which is worse than not offering it. So `PROVISIONABLE_CHANNEL_SLUGS` (in `lib/funnel-channels.ts`, the FUNDING module, deliberately not in the catalogue) gates what Offer Settings offers, while identity, name and mark stay wire-driven for every channel. It is a MIRROR of campaign-service's set and temporary by construction: the day that service states which features it can provision, this reads it and the list goes. Adding a slug here without adding it there offers a dead channel; adding it there without adding it here hides a live one. ⚠️ **`google-ads` is deliberately ABSENT from it**, and the reason is one hop further out than the mirror can see: google-service wraps the Ads API and declares its spend, features-service publishes the channel, billing states its $5/day floor and campaign-service provisions and schedules the campaign, but prod holds **zero workflows** for it against 553 for cold email. Being able to PROVISION a campaign is not being able to RUN one, so funding it would produce a campaign that is scheduled and then does nothing forever. Add the slug when a workflow answers for it, not before.
- ⚠️ **Generalise the failure, because it is the one this file keeps recording**: a local copy of another service's list rots silently, and the tell is that the copy explains WHY the missing entries are missing. That explanation is a diagnosis of your own staleness. Guards: `tests/acquisition-channels.test.ts` + `tests/funnel-channels.test.ts`.

**A LEG wears ONE tone and a CHANNEL wears ONE tone, and they are the charter's secondary and tertiary — the four-colour categorical set survives only where a reader genuinely has to tell things apart.** `FUNNEL_LEG_TONE` (purple, `lib/funnel-leg-marks.ts`) and `OWN_CHANNEL_TONE` (orange, `lib/acquisition-channels.ts`) replace the four tints both catalogues used to draw from. The reason is what the tiles are FOR: a table walking one funnel arrow by arrow is a SEQUENCE, and four colours down a sequence read as four kinds of thing rather than as four steps of one; likewise four colours across the channels read as four kinds of channel, a distinction nobody needs when the glyph and the vendor logo already say which one it is. The GLYPH carries the identity — twelve distinct leg glyphs, which is the whole reason both catalogues are keyed on a glyph token — so the tone is free to say what LAYER the tile belongs to instead. A campaigns row states its leg and then the channel it buys it through, so the two tiles now read as two KINDS of thing rather than as two arbitrary colours.
- **Purple is the charter's own secondary** (~44 degrees off the primary blue, the relationship `globals.css` preserves under the `tone-tile` rotation), so on a customer's dashboard it is THEIR secondary. **Orange is the tertiary, and indigo was rejected**: it sits ~19 degrees off the charter blue, so a tile wearing it reads as the primary rather than as a third accent. Verified by REPRODUCTION (compiled Tailwind off the app's own `globals.css` + Playwright), not by grepping the class: light renders 302 / 41, and under a +40 brand tint both rotate together to 343.9 / 87.6 with their spacing intact.
- **The four SALES-FUNNEL tiles keep their four colours**, deliberately: telling one funnel from another is exactly the categorical job the rotated set exists for. Consequence accepted: the reply funnel's own mark is purple like every leg, and the form funnel's is orange like every channel. Different tiles, different glyphs, different columns.
- **A vendor channel keeps its real platform logo on a white tile and takes no tone at all** — a logo is not a tint, and tinting it would make it read as ours.
- Guards: `tests/funnel-leg-marks.test.ts` + `tests/acquisition-channels.test.ts` pin one tone per catalogue, which one, that they differ, and that the tint still rides a `tone-tile`.

**The two marks are COMPONENTS, not icon maps inlined in a card — `components/marks/{acquisition-channel-mark,sales-funnel-mark}.tsx`.** The staff Campaigns table draws the SAME tile for the channel a campaign runs on and the funnel it optimizes for, so a second copy of either icon map is how two surfaces end up disagreeing about what a channel looks like. Both take `size` (`sm` for a table row, `md` for a settings card) and `dimmed`. Resolution lives in the alias-free libs: `acquisitionChannelForFeatureSlug` (a campaign states its channel as a feature slug, never inferred from its workflow) and `campaignFunnel` (the funnel key the campaign itself states, never derived from its goal). Both return `null` for anything the catalogue does not carry, and the caller then prints a plain word with no tile rather than borrowing a mark.

## The 7 confirmed user-fields (the offer): a PUT that OMITS a key cannot clear it, so an emptied field MUST be sent

The brand offer fields (`USER_FIELD_KEYS` = `services` + the 6 Hormozi levers) are written through brand-service `PUT /orgs/brands/:brandId/user-fields`. Two backend semantics drive everything on the consumer side:
- **`upsertUserFields` REPLACES the value of each key the body CARRIES and leaves an OMITTED key untouched.** No merge, no partial-array append.
- **`buildUserFieldsView` falls back to the AI `suggested` prefill (from `brand_extracted_fields`) whenever a key has NO confirmed row.** So a key that was never confirmed keeps re-serving the AI suggestion.

Consequence — **a payload builder that omits empty values makes "clear this field" IMPOSSIBLE, and the deleted entry visibly comes BACK on the next read** (either the old confirmed value or the suggestion behind it). All three builders shipped with an `if (cleaned.length) out[key] = cleaned;` guard and a comment claiming it protected a confirmed value; that guard WAS the bug. They now send the key with `[]` / `""`, which writes a confirmed-EMPTY row — clearing the field AND stopping the suggestion resurfacing. The PUT body is `z.record(z.string(), z.unknown())` with no minimum, so empties are accepted and **no backend change is needed**. (#3027, #3028)

**THREE copies of this builder exist and must stay coherent — fixing one is not fixing the bug:**

| App | File | Function | Scope rule |
|---|---|---|---|
| admin | `src/lib/user-fields-form.ts` | `profileToPayload` | every key of the card's `defs` subset (`cloneSubset` defaults them all) |
| dashboard | `src/components/strategy/strategy-page.tsx` | `profileToUserFieldsPayload` | all 7 (`cloneFields` defaults them all) |
| dashboard | `src/components/onboarding/onboarding.tsx` | `buildUserFieldsPayload` | a lever only when `key in profile` — a lever the offer step never rendered carries no user intent |

**Onboarding keeps a non-empty guard on `services` ONLY** (`if (cleanServices.length)`): that is the services step's own picked list, so an empty one there is an incomplete step, not a deletion. Do not "unify" it away.

**An "update from my website" button is a RESET, and it MUST send `regenerateFieldKeys` or brand-service hands back the user's own input.** brand-service does two things to a CONFIRMED user-field: it injects the value into the generation prompt as authoritative (*"prefer the profile value"*) AND overwrites the model's output with it before responding (`multiBrandFieldExtractionService.ts`, unconditional overlay). So a prefill on a brand with confirmed fields **cannot return anything new** — which is why the admin console's output read as disappointing for months while onboarding's looked good (onboarding runs on a brand-new brand where nothing is confirmed yet). Diagnose this by reading brand-service, never by re-tuning the prompt.

- `regenerateFieldKeys` (brand-service v0.62.0) suppresses BOTH halves, **scoped to the keys listed**. The offer button therefore lists the 6 levers and deliberately NOT `services`: confirmed services must keep reaching the model, because the levers are written FROM them. Guarded (`SERVICES_PREFILL_KEYS` must not appear in the levers mutation).
- **Two buttons, never one** (`lib/offer-prefill.ts`, alias-free twin in both apps → real unit tests): services are read off the site, the levers are written from the services. The dashboard's single-card offer button SAVES a dirty services field first, since only confirmed fields reach the prompt.
- **Both run `mode: "suggest"`.** `extract` is site-grounded and returns the literal string `"Unknown"`, which is not a value anyone wants to read in a field. `resetCache: true` stays (redundant with regenerate, but covers a pre-v0.62.0 producer that strips the key).
- **ONE shared description set** — `USER_PROFILE_FIELDS` in each app's `api.ts`, the set onboarding already uses. The admin's own `HORMOZI_LEVER_GUIDANCE` + `buildExtractDefs` + `EXTRACT_KEY_FOR_FIELD` are DELETED: it asked for `valueProposition` and mapped the answer onto `dreamOutcome` (the prompt names the requested JSON keys verbatim, so the model wrote a generic value proposition), and it prepended the services into all six lever descriptions — six copies of one sentence, duplicating the Hormozi framing brand-service's `suggest` prompt already applies. **`valueProposition` still exists as a backend-extract field** (features-service reads it as the `extractKey` for `valueForTarget`/`valueForVC`/`valueForAccelerator`) — do not rename that one, it is a different concept.
- A RESET, not a top-up: the fields a button owns take whatever came back, one the extraction did not answer is CLEARED, and keys outside that set are untouched. Values land in the DRAFT — nothing persists until Save — which is what makes a destructive reset safe. The old fill-blanks-only guard (`isEmptyField`) is REMOVED; it made the button read as dead on a populated card. Guards: `apps/{admin,dashboard}/tests/{offer-prefill,user-fields-clear}.test.ts`. (#3213, #3215, brand-service #419)

**The two offer editors (`components/brand-profile/field-editor.tsx`, byte-equal twins in both apps) are ONE mode, and the chips are deletable from the keyboard.** Its previous shape — a permanent pencil on every text row, and a list rendered as a comma-joined sentence behind an `Edit` button whose chips only grew an `×` inside a second mode you left via `Done` — was reported as *"c'est dur à delete, je ne sais pas quoi faire avec ça"*.
- **Pencil on hover only** (`opacity-0 group-hover:opacity-100`). A pencil printed permanently beside every value reads as a control you must find and press, which is the opposite of what an inline editor is for.
- **No `Edit`, no `Done`, no `+N more`.** Chips are live the moment they render.
- **Chip keys** (the pattern Material 3 / eBay / Telerik / PatternFly / CMS.gov / Angular Material converge on): `←` `→` Home End move between chips on a SINGLE roving tab stop (tabbing past seven services must be one press, not seven); `Delete`/`Backspace` removes the focused chip and focus lands on its neighbour, or on the add field when none remain; **`Backspace` in an EMPTY add field FOCUSES the last chip rather than deleting it** — the two-step is what stops a stray keypress destroying an entry. The `×` stays for the mouse at `tabIndex={-1}`, or every stop doubles.
- **The textarea grows from measured `scrollHeight`**, capped at `MAX_TEXTAREA_HEIGHT_PX`, resetting to `auto` before each measure (or `scrollHeight` reports the previous height and the box can only grow). The old `rows={value.split("\n").length}` counted HARD newlines, so a paragraph wrapping over five visual lines opened two rows tall.
- **Verify a change here by REPRODUCING it**, per the rendered-behaviour rule: bundle the real component with esbuild (`node_modules/.pnpm/esbuild@*/…/bin/esbuild --bundle --format=iife --jsx=automatic`) into a throwaway page and drive it with Playwright. A class grep proves the handler is spelled a certain way and says nothing about what a key press does — and it will not catch the two assertion traps this cost: a chip's `×` is an SVG so `textContent` carries no `×`, and a click landing while React re-renders from an input's `onBlur` hits a detached node (blur first). Guards: `apps/{admin,dashboard}/tests/offer-field-editor-ux.test.ts`. (#3213)

**A stored user-field's SHAPE does not track its declared `kind` — normalize by kind at the ONE clone boundary, never with a bare `typeof v === "string" ? v : ""` at a render or save site.** `kind` (text vs list, `ALL_FIELDS` in `components/brand-profile/field-editor.tsx`) is a DASHBOARD-ONLY concept: brand-service takes `{key, description}` and stores whatever JSON the model emits, so the SAME key comes back a bare string on one extraction run and a `string[]` of bullet points on the next (prod `brand_extracted_fields` for one brand: `riskReversal` array at 01:37 and 02:07, string at 03:40). Both directions therefore occur in prod, and BOTH break:
- **array in a text-kind lever** (`perceivedLikelihood`, `riskReversal`, `urgency`, `scarcity`, `dreamOutcome`) → the old `typeof value === "string" ? value : ""` rendered the placeholder, i.e. the Strategy page said **"not set" on a lever the user had filled in onboarding** — and, because the SAVE path applied the same coercion and #3027 sends every key including empties, **editing any OTHER lever then wrote a confirmed-EMPTY row over it** (silent deletion of a value the user never touched). Hit 6 brands each on riskReversal/urgency, 5 on scarcity, 3 on perceivedLikelihood, 1 on dreamOutcome.
- **string in a list-kind field** (`socialProof`) → the earlier, already-fixed half of the same bug (`coerceListField`).

The fix is a mirrored pair — `coerceListField` / `coerceTextField` (dashboard `lib/strategy-model.ts`, admin `lib/user-fields-form.ts`, byte-equal twins; array→text joins on `\n`, since `TextEditor` renders `whitespace-pre-line`) — applied **by declared kind at the single clone boundary each app already has**: `cloneFields` (dashboard `field-editor.tsx`) and `cloneSubset` (admin `user-fields-form.ts`). Normalizing there fixes render AND save at once and heals the stored row on the next save. Do NOT push it down into individual editors, and do NOT reintroduce a raw `typeof … ? … : ""` on a user-field value. `coerceListField` MUST return a NEW array (the clone is what stops an edit mutating the saved baseline). `isListLeverKey` (`onboarding/offer-levers.ts`) must stay equal to `ALL_FIELDS`' `kind` (guarded). Guards: `apps/dashboard/tests/offer-lever-text-coercion.test.ts` + `apps/admin/tests/user-fields-text-coercion.test.ts`. (offer-levers-array-text-coercion)

**Testing note:** these modules import through the `@` alias, which vitest does not resolve in this repo, so the guards are source-substring over the function body (`src.slice(indexOf(marker), +1200)`), not unit calls. `lib/strategy-model.ts` and `components/brand-profile/field-editor.tsx` (dashboard) are the exceptions — they carry no runtime `@` import (types only / a relative `../../lib/strategy-model`), so they ARE runtime-importable and their helpers get real unit tests. Keep it that way: adding a runtime `@/…` import to either turns those unit tests into resolution failures.

## A brand domain belongs to whoever CHECKED OUT on it — and a backend error message is NEVER rendered raw

Two rules from the same prod incident (2026-07-29). A customer who signed up without a website tried to add it in Brand Settings and got, on screen, verbatim: `Could not save: {"error":"A brand already exists for domain \"steadyrecruitment.com\"","code":"DOMAIN_CONFLICT"}`. The blocker was their OWN abandoned brand: onboarding creates the brand row early, they abandoned twice, and one of those shells was squatting the domain. Their org held three brand rows for one business; only the third had a campaign and a daily budget.

**Ownership rule (owner-decided, enforced in brand-service).** A domain belongs to whoever has checked out on the brand holding it. If nobody ever paid on that brand, the domain is up for grabs and attaching it TAKES IT OVER, silently, no prompt. Only two refusals survive, and they are distinguishable: `DOMAIN_OWNED_BY_YOUR_PAID_BRAND` (this org already paid on a brand with that domain) and `DOMAIN_OWNED_BY_ANOTHER_ORG` (a paying org would lose its click destination). Cross-org sharing of one brand identity by domain is intentional and untouched — the rule only decides which row CARRIES the domain. Checkout state is NOT brand-service's to compute: **client-service owns it** (`GET /internal/brands/{brandId}/checkout-status` and its per-org sibling), because it owns the user journey and can join brand identity to money; brand-service throws rather than assume "nobody paid" when that answer is unavailable. Do NOT re-derive checkout state anywhere else, and do NOT reach into billing/stripe for it.

**Never render `err.message` from `apiCall` to a user.** `callExternalService` sets the thrown Error's `message` to the downstream response body VERBATIM, so any api-service route that rebuilt `res.json({ error: err.message })` stringified the whole downstream JSON INTO the message — destroying `code` for every consumer and putting a JSON blob in front of a customer. api-service now forwards the upstream status + body field-for-field (`respondUpstreamError`, `src/lib/upstream-error.ts`); the gateway does not own downstream shapes. On the dashboard side the invariant is permanent regardless: an error surface branches on the STATUS (and `code` when present) and prints its own copy, with the real error going to `console.error`. `brand-domain-card.tsx` is the reference — 409 names the conflicting brand when the org owns it (a display lookup over the shared `["brands"]` query, deduped with the tenant switcher), everything else gets one generic line. Guard: `apps/dashboard/tests/brand-domain-card-error.test.ts` (`not.toContain("error.message")`).

**Related, fixed in the same ship:** creating a no-website brand was not idempotent, so every re-onboarding minted another row. That is how one org reached three. (#3079, brand-service v0.57.1, client-service v0.24.4, api-service v0.91.1)

## `/onboarding` is ONE flow, built around the brand's SALES FUNNELS — and it WRITES them

`components/onboarding/onboarding.tsx` serves every signup. It ran for a while as a beta-gated variant beside the brand-level flow it replaced (`Onboarding({ variant })`, `?flow=ga`); both the gate and that second flow are GONE, so there is no `variant` prop and no `isV2` to branch on — a re-added branch would be a second money path nobody asked for.

- **Order:** welcome → url → loading → services → **audiences** → **funnels** → **primary** → consent → pricing → bonus → Stripe → celebrate → phone → **funnelStats** (one screen per picked funnel) → model → offer → launching.
- **`destination`, `objective`, `rates` and `ltr` are RETIRED but still in `Step` + `ALL_STEPS`.** They asked for ONE click destination, ONE goal and ONE set of rates — the brand-level model the funnels replaced. Their render blocks, `saveDestinationAndContinue`, `saveRatesAndContinue` and `saveLtrAndContinue` are deleted (guarded). The NAMES stay because removing one NARROWS what a persisted snapshot may carry, which strands a session that was mid-checkout when this shipped — the same reason `ONBOARDING_STATE_VERSION` stays at **8**, and why `clickDestinationUrl` stays on `PersistedOnboardingState` although nothing asks for it any more. `legacyStepFor` maps each retired name onto the step that asks the same thing now (`destination`→`audiences`, `objective`/`rates`→`funnels`, `ltr`→`funnelStats`) and a render fail-safe catches anything that slips past it.
- **The selection MUST survive the Stripe round-trip, which is NOT the same question as surviving a refresh.** The post-payment steps run on a FRESH page load, so state that only lives in React is gone by the time `funnelStats` renders: `detailFunnels` came back empty, the step hit its own `if (!funnel) → setStep("model")` escape and skipped itself SILENTLY, and `model` lost its primary-funnel card too ("il manque les étapes … après le checkout"). The fix is the `selectedAudienceIds` precedent, not a version bump: `selectedFunnelKeys` + `primaryFunnelKey` ride the **TOP LEVEL of the `PendingCheckoutLaunch` blob** (version-independent), and `applyRestoredFunnelSelection(pending)` puts them back on BOTH returns — success (walks the screens) and cancel (lands on pricing, where Back walks up through primary/funnels). They are read TOLERANTLY and are deliberately absent from the blob's validity check (an older blob must still LAUNCH), and `buildPendingLaunchBlob` writes them live-selection-first with the stored blob as fallback, so a re-checkout after a cancel carries what the user has picked NOW. `?from=add` never showed the bug — it launches in the SAME page session. Tell for this class: a post-payment step reads state the pre-payment steps wrote, and the wire between them is the blob, never the component.
- **TWO writes, each on the step that renders the values it sends.** (1) `saveFunnelsAndContinue` states the WHOLE set (`stateBrandSalesFunnels`, `PUT /v1/brands/:id/sales-funnels`) — distinct from declaring one funnel: it flips `declared`, removes a funnel the user unpicked, and is what features-service `/goal-arbitration` reads, so it lands BEFORE the budget step. (2) `savePrimaryFunnelAndContinue` writes NOTHING — it used to persist the primary funnel's goal on the brand row, and that goal is retired (features-service reads the declared funnel instead), so the step is now synchronous local state plus an advance. (3) `saveFunnelStatsAndContinue` prices each funnel through the SAME `getBrandSalesFunnels` → `buildFunnelPatch` → `declareBrandSalesFunnel` path the Settings card uses: the stored set is read from the wire on EVERY write and the patch is the DIFF against it, so a prefill the user left alone is never written and an emptied field really clears. A refusal STOPS the step and renders `funnelWriteErrorMessage(err)` (never `err.message` — that is the whole downstream body verbatim).
- **The brand-level click destination is MIRRORED, not asked.** A funnel owns its landing page now, but brand-service still serves `clickDestinationUrl` on the brand read and consumers link off it, so the page given to the FIRST funnel that lands a click on the site is written there too (`clickDestinationMirroredRef`, once per session, best-effort — the funnel's own page is already persisted, so a failure must not strand a paid user). Same value the user typed, never an invented one. Dropping this write entirely was the silent regression the flow would otherwise have shipped: every new brand would carry a null destination.
- **Everything the launch CREATES names the OFFER it is about, and the offer is READ, never created here** (`lib/launch-offer.ts` `soleOfferId`, alias-free so it carries real unit tests). A campaign is (offer × funnel × channel) and billing keys its ceiling on the same triple, so a launch that states no offer produces a campaign **no offer page can show** and a ceiling that addresses every offer selling that pair rather than the campaign it funds. Both were unstated until #3506: prod carried an `ongoing` campaign a customer could not see anywhere, on a brand whose single offer had existed 28 minutes before it was created. There is nothing to create — brand-service gives a brand its first offer on the first brand-scoped write (`resolveOfferForWrite`, get-or-create, race-safe), and the funnels step has already triggered one by the time the launch runs. **Zero offers and several offers both mean "we do not know" and both ship UNATTRIBUTED** — naming the wrong one files a campaign, and the money pacing it, under a proposition the customer never sold through. The read is best-effort by construction, logged loud on both branches: the customer has already been charged, and campaign-service and human-service each adopt an unattributed row on their own tick, so a failure here is a delay, not a dead end. ⚠️ **The AUDIENCES step is the one write that CANNOT name it** — it runs BEFORE the funnels step, so no offer exists yet and every audience a signup produces is born brand-wide (distribute.you#3454 filed exactly this; the same prod brand carried 12 unattributed audiences). Do NOT "fix" that by creating an offer from the browser to make the step nameable: naming a brand's first proposition is brand-service's call, and human-service attributes those rows on its own cadence. Guards: `tests/launch-offer.test.ts`.
- **`monthlyPipelineLabel` is the one browser-derived figure in the flow**, and it is on borrowed time: there is no served field for a budget-scaled monthly pipeline, so it divides. Filed as features-service#699; when that lands, delete the helper and render the field. It returns null (never a zero) when any input is missing — "we could not estimate this" and "it buys nothing" are different statements. Do NOT add a second browser-derived number beside it.
- Guards: `tests/onboarding-funnels-flow.test.ts` + `tests/onboarding-funnel-view.test.ts`. (#3167, onboarding-funnels-ga)

**A `min-height` is not a height, so a footer declared as pinned is not pinned — and the symptom is a CTA below the fold on every phone at once.** The onboarding shell is a `flex-col` column and `StepShell` splits it into a `min-h-0 flex-1 overflow-y-auto` region plus a `shrink-0` footer, which reads as a bottom-pinned CTA over a scrolling body. It was neither. `min-h-[100svh]` sets a FLOOR: the column is free to grow past the viewport, so the flex children divide the GROWN height, the scroller's content always fits inside its own box, `overflow-y-auto` never triggers, the PAGE scrolls instead, and the footer rides below the fold. The tell is exact and worth memorising: measure the scroller and find `scrollHeight === clientHeight` while `document.documentElement.scrollHeight > innerHeight` — the region you declared as the scroller is not scrolling and the document is. The fix is to make the height DEFINITE (`max-h-[100svh]` + `overflow-hidden`, released at `sm:` where the shell becomes a centred floating card and a hard cap would clip a tall step); it is one edit in `app/(authed)/onboarding/layout.tsx` and it covers all 16 steps rather than the one that was reported. Measured on the welcome step before: the CTA sat at **926px on a 375x667 screen, on a 390x844 one AND on a 412x915 one** — identical, because the number is content height, not viewport-relative, which is itself the giveaway that nothing was being capped. After: 647px on the 375x667, and the body no longer scrolls at any size. Generalise: whenever a layout claims a pinned footer / sticky region / internal scroller, check that some ancestor actually BOUNDS the axis — `min-h-*` alone never does. **And measure it: a source guard proves the classes are spelled right and says nothing about whether the CTA is on screen.** Guards: `tests/onboarding-welcome-mobile.test.ts`. (#3482)

**Mobile sizes are their own decision on the welcome step, not an afterthought — one size at every width is what put the CTA off screen.** Three cards at `p-5` with a stacked 40px icon tile were **528px of the 926**, so the compaction is the other half of the fix above: the cards are ROWS on mobile (icon beside the text, `p-4`, `h-9 w-9`) and stack again from `sm:`, and the headline, intro, grid gap and CTA margin each step down. Nothing about the desktop layout changes. The copy is pinned by `beta-onboarding-flow.test.ts` and `onboarding-funnels-flow.test.ts` — resize, never reword.

**The website typed on the landing is carried in a COOKIE, not only in `?url=` — the param does not survive the trip and the fallback silently degrades it.** `?url=` rides `redirectUrlComplete` through Clerk and `/sso-callback`, and the first-run edge gate can bounce a signed-in user to a bare `/onboarding`; either hop drops the query. The field then falls through to `businessDomainFromEmail`, which returns a bare host BY CONSTRUCTION (`kevin@voozaa.app` -> `voozaa.app`), so a visitor who typed `voozaa.app/us/` lands on `voozaa.app` and the page they actually named is gone — with nothing anywhere reporting a failure. `?via=` needed exactly this treatment for exactly this reason (`partnero-via-capture.tsx` exists BECAUSE the param does not survive the Clerk redirect), so the precedent was already in the repo. `LandingUrlCapture` (root layout, sibling of the `?via=` one) parks it; onboarding recovers it in an effect ordered ABOVE the email guess (effects run in declaration order, and that ordering is guarded) and expires it once it has landed in the field, so a later `?from=add` flow does not inherit the first brand's website. `lib/landing-url-cookie.ts` is alias-free so it carries real unit tests; it refuses a non-http scheme (a `javascript:` input parses fine and must never be rendered back into a field someone then submits) and caps length, because the cookie rides every request to the origin including each `/api/v1/*` proxy call. **Generalise: any value carried from the landing through signup needs a cookie, never a query param alone.**

**The URL field SHOWS a URL, and that is a display decision that must not leak into identity.** A bare host in a field asking for a website reads as a token someone half-remembered; `https://voozaa.app/us/` reads as the page they gave us. Both sources (the landing carry and the email guess) are normalized for display through `normalizeLandingUrl`, on the SEED only — never on a keystroke, or the field could not be typed in. `extractDomain` is untouched, so `domain`, `hostname`, the org name, `BrandStepHeader` and `upsertBrand` all resolve exactly as before. Do NOT "simplify" this by normalizing at `extractDomain` or by storing the normalized value as the brand: this is what the field shows, not what the brand is.

**A step that is WAITING on an extraction and a step whose extraction FAILED must never render the same thing — and the thing they must not render is the success copy.** Onboarding's every-step pattern is `await extract(...).catch(() => null)`, which is correct (a failed read cannot strand a paid signup on the loading screen) and is exactly what made two steps lie. The services step kept its `We drafted these from <host>` line over an empty box, and the audience step kept a sentence it had assembled locally from the picked services, which is indistinguishable on screen from an ICP brand-service drafted. Neither said anything. Reported as *"during like 20s nothing was suggested… then 2 options just appear in the text input. WTF???"* and *"this is a placeholder when the backend is down right?? WTF it was down???"* — the surface never answered either question, so both had to be answered from prod logs.

- **Three states, and the two empty ones are different statements.** `servicesDrafted` (a list exists, from the extract or typed) / `servicesPending` (nothing yet, something still running that can deliver it) / `servicesUnread` (nothing, nothing left running — a verdict, so it says so and offers `retryServicesExtract`). A still-running read is a WAIT; a settled empty read is an ANSWER; collapsing them is the whole bug. `servicesExtractFailed` + `servicesHydrating` are the state that makes the distinction possible — the `.catch` keeps swallowing, it just stops swallowing SILENTLY.
- **The success copy is gated on the success.** `We drafted these from <host>` is a claim about an extraction that returned something, so it renders only when something is there; otherwise the step asks plainly.
- **A late background write must not land on a step already in use.** The heavy hydrate resolves tens of seconds after the fast extract, so BOTH writers go through `setServices((prev) => (prev.length ? prev : nextServices))`. `servicesEditedRef` protects a list the USER curated; `prev.length` protects one the SIBLING WRITER filled. Without the second guard the hydrate swaps the list out from under whoever is reading the step — which is the "2 options just appear" half of the report.
- **The audience seed reads live state through a REF, never the effect's closure.** `promptRef` / `fallbackPromptRef` are assigned during render; the seed runs inside a promise callback created at MOUNT, so reading `prompt` there read the mount render and its "never clobber a prompt the user already edited" guard tested a stale `""`. A failing ICP is what makes the prewarm settle late, so the two bugs fire together: the ICP dies, the prompt arrives seconds later, and it overwrites whatever was typed in between. `not.toContain("onPromptChange(prompt.trim() ? prompt")` pins it.
- **A dead prewarm auto-fires the suggest rather than looking idle.** The prewarm bails the instant the ICP throws, so `suggestAudiences` never ran and the step read as one that merely wanted a click. Both branches now self-fire off `seeded`. ⚠️ `loading` is HANDED OVER to `runSuggest`, not cleared under it — a shared `finally` drops the button to its idle label for the whole call it just started AND leaves it clickable, so a second suggest fires over the first.
- **`icpFallback` is the REASON, not the absence.** A locally-assembled sentence is labelled as one (`We couldn't read enough from <host> to draft this`); a reader who assumes it was drafted off their site edits it as if we had read the site.

Guards: `tests/onboarding-extraction-visibility.test.ts`. Root cause of the incident was NOT in this repo — chat-service's `disableThinking` floor was derived per GENERATION plus a `model.includes("pro")` test, so the 2026-08-14 `flash-pro` → `gemini-3.7-flash` upgrade sent `thinkingLevel: "minimal"` to a model that rejects it: `400 INVALID_ARGUMENT`, deterministic, 10 days, killing brand-service's services extraction AND its ICP suggestion on every new signup (fixed chat-service v0.54.1, per-model floor table + a test walking every alias). The dashboard half is that none of it was visible: the fast path failed silently and the slow `url_map` fallback (~28s) looked like the product being slow. **Generalise: when a producer outage surfaces as "the product is slow", check whether the fast path is failing and the slow path is the fallback — and whether any surface said so.** (service-suggest-loading)

**Leaving the flow and coming back LATER is a different question from refreshing, and only a COOKIE can answer it — the wizard's progress is `sessionStorage`, so the tab closing takes it.** `onboardingComplete` is written only at the terminal launch, so a person who abandons mid-flow still fails the edge gate on their next visit; the gate sent them to a BARE `/onboarding`, which with no snapshot and no brand starts at the welcome screen. Everything they had typed was still in brand-service — the flow just had no way to know which brand to re-hydrate. Reported by a customer who reached the per-funnel budget step (the last screen before Stripe), left, and had to redo the whole flow.

The recovery path ALREADY EXISTED and was structurally unreachable for the case that needs it: `/onboarding?brandId=` fetches the brand, replays the loading-screen hydration and lands on `funnels` with services / funnels / rates / lifetime revenue pre-filled — but its only caller was `BrandSetupGate`, which fires for an org that is ALREADY `onboardingComplete` (the add-a-second-brand case). So the one thing that rescues a half-finished signup could never serve a first signup. **When a feature reads as absent, check whether it exists behind a gate that excludes its own use case** — the fix here was three call sites, not a new flow.

- **`lib/onboarding-brand-cookie.ts` is the model** (alias-free, so it carries real unit tests — keep it that way). The client writes it at BOTH brand-create sites (website and no-website), because the brand row is what the resume keys on; `proxy.ts`'s first-run gate reads it through one `onboardingHref()` helper used by both of its branches; `/api/onboarding/complete` clears it. ONE clear point, server-side, on the path the launch already calls — a finished user must never be sent back into the flow by a stale cookie.
- **Org-scoped (`onboarding-brand-{orgId}`), like `last-brand-{orgId}`.** Onboarding can create a brand-new org (`?new=1`), so an unscoped cookie would resume a brand abandoned under org A inside org B — landing someone on a brand that is not the one they came to create.
- **NOT httpOnly, deliberately** — the client creates the brand, so the client is what writes this; the value is a brand id the URL carries a moment later. Same call as `tenant-identity-cookie.ts`, and it mirrors that file's pure-`*CookieAssignment()`-builder split so the string stays unit-testable.
- **A week, then it expires.** An abandoned onboarding older than that is better restarted than resumed: the brand's extracted fields have gone stale and nobody remembers what they picked.
- **Audience picks and budget amounts are still lost cross-session, and that is accepted here.** Neither is persisted before payment (audiences activate at the terminal launch, per-funnel budgets are written to billing in `completeLaunchAfterCheckout`), so the resume lands on `funnels` and the user re-picks those two — 4 screens instead of 10, with everything typed earlier restored from the wire. Persisting them is a separate change; do not fake them from a snapshot that no longer exists.
- Guards: `tests/onboarding-brand-cookie.test.ts`. ⚠️ The DIS-111 gate guard (`onboarding-brand-gate.test.ts`) pinned the literal `new URL("/onboarding", req.url)`, so it goes red on any change to the redirect's destination — assert the helper, not the string.

**Gate a NEW preview on the BETA allowlist, never the staff one — the two are not interchangeable and picking the wrong one is a security decision, not a style one.** `isAdminEmail` is the PRIMARY security boundary on `/api/admin/*` in the dashboard (the god-mode org switcher, which enumerates and joins every tenant on the platform, with no edge gate behind it), and it must stay byte-equal across THREE copies (`apps/dashboard`, `apps/admin`, api-service `src/middleware/auth.ts`). So gating a preview on it means every person added for the preview also receives cross-tenant god-mode, and adding them costs a three-repo fan-out. `beta-allowlist.ts` grants nothing but UI visibility. Rule: a preview that only ever touches the VIEWER'S OWN data goes behind the beta gate; reach for the staff gate only when the surface genuinely reads across orgs. Cost 2026-07-31 (#3167 → #3170): shipped this flow behind `useIsAdminUser`, so `kevin@pressbeat.io` — already on the beta list — got the other flow and reported the click-destination step still showing.

**The funnels come from the shared catalogue through a DISPLAY ADAPTER (`lib/onboarding-funnel-view.ts`) — read the catalogue there, never field-by-field at a render site.** This is the general pattern whenever you consume a model a PARALLEL WORKSPACE is actively reshaping (the Conductor in-flight-workspaces query at O.0 is what surfaces one): put the shape-reading in a single alias-free module, map over it at the render sites, and the sibling's reshape becomes a diff to that one file. Proven in this ship — #3165 landed mid-PR and changed `steps` from a 3-tuple to a 4-step chain, replaced the `destination` object with `pageDestination`/`bookingLink` flags, moved rates onto per-arrow `legs`, added a fourth funnel and a `name` per funnel; **zero render sites changed**. Two details that make it work: the adapter's entry type is fully OPTIONAL/structural (a catalogue mid-reshape still renders), and rate LABELS are resolved by the catalogue's OWN `funnelRateFields` rather than re-listed, so a rate reads the same word in onboarding as on the settings card.

## The onboarding step header shows a company NAME and a domain-keyed LOGO — two values, never one, and there are two logo.dev keys

`BrandStepHeader` (`components/onboarding/onboarding.tsx`) is the first thing a customer sees after clicking "Analyze my product", and it is instant by construction: `headerDomain = extractDomain(url)` is parsed from the typed field with zero network, and `BrandLogo` hits `img.logo.dev` directly. It used to label itself `domain ?? hostname`, so it read `acme.com` for the rest of the flow while `Brand.name` sat unread on the wire — the brand-create response was destructured for `brandId` alone.

Four invariants, each of which has a way of silently breaking:

- **The logo is keyed on the DOMAIN, the label reads the NAME.** These were one `label` value (`domain={label}`), and `img.logo.dev/<company name>` resolves nothing — so collapsing them again empties the logo slot the moment a name is present, with no error anywhere. They are `logoDomain` and `label`; guarded by `tests/onboarding-brand-name-header.test.ts` (`not.toContain("domain={label}")`).
- **The domain stays the label until a name arrives** (`name?.trim() || domain || hostname`). The header is never blank, and a session that never saw the create response degrades to exactly the pre-#3116 look.
- **`resolvedBrandName` is deliberately NOT in the persisted snapshot.** Adding a field there means bumping `ONBOARDING_STATE_VERSION`, which strands an in-flight checkout; the cost of not persisting is only that a resumed session (and the post-Stripe `launching` step) shows the domain again. Do not "fix" that by persisting it.
- **It is DISTINCT from `brandName`**, which is what the user TYPES on the no-website path. `headerName` is null in `noWebsiteMode` — a brand with no domain has nothing to resolve a name from, and the typed name already identifies it. Conflating the two lets a resolved name overwrite the user's own input.
- **Every step that renders its own header must be threaded too.** The audience step (`OnboardingAudiences`) takes `brandName` for exactly this reason: left on the domain while its siblings show the company name, it is an internally incoherent header, not a cosmetic gap.

**Producer side (brand-service v0.58.0):** `brands.name` is resolved at brand-CREATE, priority `logo.dev company index → landing-page HTML (og:site_name / <title> / JSON-LD) → titlecased domain`. Only the index lookup is awaited, so the create never waits on a fetch of the customer's own site; the read path keeps a lazy-fill safety net and `name` is still never null on brand-detail. Before this, the derivation ran on the READ path bounded by a 5s third-party fetch, and the HTML heuristics yielded **"Home"** for a site titled `Home | Acme` and **"Emailtoolshub"** for `emailtoolshub.com`.

**Two logo.dev credentials, NOT interchangeable — this is the trap.** `logo-dev` holds the PUBLISHABLE token (`pk_...`) that signs the `img.logo.dev` image URLs; `logo-dev-secret` holds the SECRET key (`sk_...`), the only one that authenticates the server-side REST APIs (Search, Describe, Brand). Both are registered from `apps/{dashboard,admin}/src/instrumentation.ts` (`LOGO_DEV_TOKEN` / `LOGO_DEV_SECRET_KEY`) and both `platform-config-startup.test.ts` pin the list AND an exact count. Reaching for the publishable one on a REST call fails auth; the registration loop skips a missing env var with only a `console.warn`, so brand-service silently falls back to the HTML path and the whole feature reads as inert rather than broken.

**Which logo.dev endpoint to use, by meter** (the pricing surface is genuinely confusing): **Search** (`/search?q=&strategy=match` → `{name, domain, logo_url}`) and the image CDN are on the monthly REQUEST POOL, 500k/mo on the free Community plan. **Brand** (`/brand/{domain}` → the above plus colors, brandmark, social banners, blurhash) is metered on a SEPARATE prepaid credit grant — 5 credits ≈ $0.05 per call, 100 calls/mo on Community, a hard `402` when exhausted, and its own docs warn responses take 15-30s. **Describe** (same fields minus brandmark/banners) requires a PAID plan and is unavailable to us. So a name lookup uses Search; do not reach for Brand for it. Search returns up to 10 name-matched candidates, so a result is only trustworthy when its `domain` matches the brand's own. (#3115, #3116, brand-service #382 → v0.58.0)

## A CRM "source" is ONE crm-service CSV upload, and ONE source = ONE human-service audience bound via `crmUploadId`

For the CRM-outreach feature (`sales-crm-email-outreach`, the slug lives in ONE place: `apps/admin/src/lib/crm-outreach-feature.ts` — gate on `isCrmOutreachFeature`, never a scattered literal) the people are the brand's OWN imported contacts, not a provider search. Model as of `#3017`:

- **"Source" = a crm-service upload** (`GET /v1/orgs/contacts/uploads?brandId=`, one row per imported CSV). Contacts carry `sourceUploadId`. Listed for staff at `.../brands/[brandId]/services/crm/sources`.
- **Each source can be bound to its OWN human-service audience** via the audience field **`crmUploadId`** (human-service v0.33.0). Set at CREATION only, immutable afterwards, validated against the brand's uploads (400 if the upload is not this brand's). `null` = unbound ⟹ the serve covers the brand's WHOLE imported list, which is the pre-existing behaviour. Several bound audiences per brand are allowed and intended.
- **Why per-source and not one whole-brand CRM audience:** a single pooled audience gives no per-file cost-per-outcome and no way to stop sending to one file. Binding one audience per source makes each file independently pausable (audience `status`) and independently costed (per-audience attribution).
- **crm-service serves the restriction** (crm-service `#12`): `POST /orgs/contacts/serve-next` and `GET /orgs/contacts/serve-stats` take an optional **`uploadIds`** (absent = whole brand, unchanged). The restriction narrows the CANDIDATE pool only — **suppression stays brand-wide** (`contact_serves UNIQUE(brand_id, email)`, no upload dimension), so a person present in two files is still contacted at most once per brand.
- **api-service needs no route work here** — its `/v1/orgs/audiences*` routes forward `req.body` raw, so a new human-service field reaches the wire with zero gateway change. Verify before assuming a proxy PR is needed.
- **Staff UI:** the **Audiences** section under Sales Economics on `features/[featureSlug]/settings` (`components/settings/crm-source-audiences-card.tsx` + the api-free join in `lib/crm-source-audiences.ts`). ON creates-or-reactivates, OFF pauses. Two behaviours that are deliberate, not gaps: an unbound CRM audience is surfaced as a WARNING (while it is active it draws from every file, so the switches alone would not describe who gets contacted — an internally-incoherent surface is a bug, so it is stated rather than hidden), and a source with no filename cannot be switched on (an audience needs a name and we never fabricate one).
- **Audience names are unique per (org, brand)** — human-service 409s on a collision. The name is the CSV filename verbatim, so two imports sharing a filename surface that 409 verbatim; do NOT "fix" it with a client-side suffix, that is inventing a name.
- The three audience readers (`listAudiences` / `createAudience` / `setAudienceStatus`) now exist in **both** `apps/admin/src/lib/api.ts` and `apps/dashboard/src/lib/api.ts` — same contract, so they fall under the existing lockstep rule: migrate both when human-service reshapes it.

## SHOWING a fact and STATING it are different affordances — a map beside the text is not a duplication

The one-fact-one-place rule in this file governs two surfaces stating the same thing **in the same mode**: two ROI numbers under one word, two dates on one row, a heading whose figure contradicts the rows beneath it. It does NOT govern a chart, a map or a bar rendered beside the text it visualises. A reader takes "United States" and "France" off a map in one glance and off two text rows in two, and every dashboard in the world prints a number beside the chart of that number.

So when the ask is for a VISUAL of data the page already prints, the right question is *does the picture read faster than the sentence*, never *is this fact already on screen*. Answering with "we already display it" reads as refusing the request on a technicality, and it is the same misreading as answering a redesign brief with "reuse the existing primitive" (which this file already records one section down): a brief about the visual is not settled by a fact about the data. The honest concerns to raise are the visual's own — does it add a dependency, does it leak data, does it survive the dark theme, is the grain it implies real — and each of them is a thing to SOLVE, not a reason to decline.

**And size an objection against the actual usage before raising it.** A cost or quota argument ("that API is only free to 10k loads a month") is not an argument on a product with four users; it wastes the round it takes to say. If the platform's free tier is three orders of magnitude above the real volume, the cost is not a consideration and something else has to carry the objection — here it was that a tile server sends every customer's lead locations to a third party on every panel open, needs a browser-exposed key, and renders an unstyled light rectangle on the dark theme, none of which depend on volume.

Cost 2026-09-04 (#3890): asked for a Location card with a map pin on the lead panel, I answered that both locations already rendered as text rows, that a third card would state one fact three times, and that Google Maps is not free — and recommended a country flag instead. All three were true and none of them answered the request. *"WHATT???? NON JE TAI DEMANDÉ UNE MAP"* / *"ON VEUT DU VISUEL EN PLUS DE L'INFO GÉOGRAPHIQUE ÉCRITE DÉJÀ PRÉSENTE"*. The map shipped in the next pass, inline SVG, zero requests, and the text rows stayed exactly where they were.

## Dashboard UI iteration — the real page, not a standalone mockup (previews are GONE)

Default: edit the real page with the existing component vocabulary (`bg-white rounded-xl border border-gray-200` cards, `focus:ring-brand-300` inputs, brand-500, `SparklesIcon`, `SectionCard`/`ScoreCard`, the `bg-brand-50`+`border-brand-200` segmented toggle). A standalone HTML mockup reads as "page vierge" and gets bounced — acceptable only as a fast layout sketch when explicitly asked, graduate to the real page ASAP. When rewriting a section, inventory existing interactive affordances (AI panels, billing guard, "Edit with AI" → `CampaignAIPanel`) and preserve them — silently dropping one is a regression. (#1239, #1242)

**Vercel previews no longer exist** (#3287 — see the release section): no branch other than `main` builds, so there is no `distribute-dashboard-git-<branch>-…vercel.app` to open. Verify a visual change by REPRODUCING it (esbuild-bundle the component + Playwright, per the rendered-behaviour rule) or on prod after merge. Dashboard project `distribute-dashboard` (`prj_nJn9Xr5D1fD5h7ug3eRPejEGsg2z`, team `team_lYmJIUH6q2rTY6dUfDiYtpAt`).

## Dashboard-wide reskin / rebrand = central via the `html.dark` remap + theme default, NEVER a per-page sweep

"Mets le dashboard au même design que la landing" / "full rebranding" = adopt the landing's **FULL aesthetic** — NOT a conservative shared-token pass (a brand-token-only pass reads as "je ne vois pas de changement" and gets bounced). **VERIFY the SERVED landing's real look FIRST — never trust this note's prior color/theme claim.** The served landing is the STATIC `apps/landing/public/landing/index.html` + `/landing/css/styles.css`, NOT the React `.dy-page` surface. 30-sec check before reskinning: `curl -s https://distribute.you/ | grep -E 'data-theme|styles.css'` (shows the default theme the HTML forces) + read `apps/landing/public/landing/css/styles.css` `:root` + `[data-theme="light"]` tokens (the real `--accent` hue, fonts, bg/text). **Ground truth as of #1644: served landing = LIGHT-default (`data-theme=light`), accent INDIGO-blue (OKLCH hue 264, `--accent: oklch(50% 0.250 264)` light / `oklch(64% 0.220 264)` dark — NOT pink), Inter + JetBrains Mono (no display face / no Fredoka), nav logo = blue `/landing/logo/logo-distribute.svg`.** Cost 2026-06-14 (#1635→#1644): reading only `styles.css`'s `:root` block (which IS dark) shipped a dark-default pink Fredoka dashboard against a page that forces `data-theme=light`. The dashboard reskins from ONE place — do not touch the ~71 pages / ~97 inline-Tailwind components individually:
- **`apps/dashboard/src/app/globals.css` already carries a full `html.dark` utility-remap layer** (`html.dark .bg-white {…}`, `.text-gray-* → …`, borders, inputs, divide, ring, prose, scrollbar). This is the same mechanism the landing uses with `.dy-page` — it remaps the inline Tailwind utilities to theme values, so retargeting those values reskins every page at once. To rebrand: (1) define the palette tokens in `:root` (the landing OKLCH set `--dy-bg/-surface/-border/-text/-sub/-muted`), (2) point `html.dark` base bg/fg at them, (3) retarget every `html.dark .*` remap to the tokens, (4) add the glow via `html.dark body::before` (the landing `.dy-page::before` signature).
- **Default theme** lives in TWO places that MUST stay in sync: the anti-flash inline script in `src/app/layout.tsx` AND `src/components/theme-provider.tsx`'s `initial`. Landing-match = **LIGHT-default** (`stored || "light"`, script adds `dark` only when `theme==="dark"`) — the served landing forces `data-theme=light` (#1644 flipped this from the wrong dark-default #1635 shipped). The in-app toggle + explicit stored choice still win — reskin is reversible (one PR + toggle).
- **Canonical card depth**: the `bg-white rounded-xl border` card pattern (~203 inline usages) gets its shadow from one `@layer base` rule (so explicit `shadow-*` utilities still win) — don't add shadows per-card.
- Accent vocab ported from landing lives in globals too: `.gradient-text` / `.gradient-text-subtle` / `.gradient-bg`, `.dy-eyebrow` mono pill + `.dy-dot`, `--font-mono` → JetBrains Mono. Opt-in (apply to headers/hero numbers).
- **The `html.dark` gray-text remap covers `text-gray-900` down to `text-gray-300` but historically SKIPPED `text-gray-950` — so `text-gray-950` (the token used for the most prominent text: card headings, big stat values) renders near-black + invisible on dark.** `apps/admin`'s remap had this exact gap; a big `+1100.0% CMGR` stat read black on the dark metrics page (#2702). Fix is ONE central line in that app's `globals.css` — `html.dark .text-gray-950 { color: #f8fafc; }` (fixes every `text-gray-950` at once, per the reskin-from-one-place doctrine) — NOT a per-component class swap. Fixed in `apps/admin`; **`apps/dashboard`'s `html.dark` layer likely still has the same `text-gray-950` gap** — add the line there if a prominent number/heading reads black on dark. When adding dark-visible prominent text, confirm the token is in the remap (grep `html.dark .text-gray-950` in that app's globals.css), don't assume `text-gray-950` is covered. (#2702)
- **Favicon is NOT covered by the `html.dark` remap — it's a separate surface a rebrand MUST update explicitly.** The dashboard favicon = the blue distribute mark `apps/dashboard/public/logo-distribute.svg` (same SVG the landing serves: `<link rel="icon" href="/landing/logo/logo-distribute.svg">`). It's wired THREE ways that must agree: file-convention `src/app/icon.svg` (auto-emits the tab `<link>`, applies to ALL routes incl. `/sso-callback`), `src/app/apple-icon.jpg` (iOS home-screen only, NOT the tab — SVG doesn't render on iOS so this stays a jpg/png), and `layout.tsx` metadata `icons` (→ `/logo-distribute.svg`). **Next file-convention icons OVERRIDE metadata `icons`** — so a stale `src/app/icon.jpg` beats a fixed metadata ref; to change the favicon you must replace the FILE (`icon.svg`), not just metadata. ⚠️ **That precedence claim does NOT hold on the dashboard today, and it is a point-in-time assertion like any other — read the SERVED head before acting on it.** Prod emits three links, all `/logo-distribute.svg`, i.e. `metadata.icons` won and `src/app/icon.svg` is not in the head at all (verified 2026-08-26 by `urllib`-fetching `/sign-in`, which is public and shares the root layout — the one probe that settles this in five seconds). So a favicon change made ONLY to `icon.svg` reaches nobody. Both files are kept byte-equal with `apps/landing/public/brand/favicon.svg` and pinned by `tests/brand-favicon.test.ts` precisely because the precedence is ambiguous (and note the direction of that coupling: the guard lives in `apps/dashboard` and READS a file in `apps/landing`, so a landing-only edit to `public/brand/favicon.svg` — even to its `aria-label` — fails a CI merge gate in another app. Move all three together or none): whichever Next picks, it is the same mark. Do not delete either without re-reading the served head. #1635/#1644 rebrand swapped the header mascot but left the old "d + rainbow-dot" jpg favicon for months — caught only on `/sso-callback` (Google-login tab). Favicon was identical (md5) on every route → never sso-callback-specific. (#1752) **FOURTH surface, runtime: on a brand page the tab mark is the BRAND's logo, not the distribute one** — `components/brand-favicon.tsx` (mounted once in the dashboard layout) reads `useTenantSwitcher()`'s `displayBrand.domain`, preloads `img.logo.dev/<domain>` with the shared publishable token, and on a successful decode BLANKS the `rel` of every app-owned `link[rel~="icon"]` (parked in `data-brand-favicon-parked`) before appending its own. So a rebrand that swaps `src/app/icon.svg` still looks unchanged on any `/orgs/:orgId/brands/:brandId` route — verify a favicon change OFF a brand page (billing, org root, `/sign-in`), not on the brand overview. Two invariants when touching it: the swap is gated on the image actually decoding (logo.dev answers an unknown domain with a generated monogram, and a brand can have no domain — both must keep the distribute mark, with the reason logged), and the parking is deliberate rather than appending a second icon link (which of several `<link rel="icon">` a browser picks is unspecified). `apps/admin` is NOT in scope — it keeps its distinct white/blue staff mark. **An imperative mutation of a REACT-OWNED node is ASSERTED, never applied once — React re-renders it back and nothing re-runs to notice.** The app's icon links are rendered from `metadata.icons`, so React owns them; the parking blanked their `rel` in an effect keyed on the brand domain, a client navigation re-rendered them with `rel="icon"` restored, and the tab was left with several live icon links (which one a browser picks is unspecified). The domain had not changed, so the effect never fired again and it never recovered — reported as the mark flipping back "sometimes", which is what a re-render race always reads as. Fix is a `MutationObserver` on the head that re-parks whatever came back, guarded on a `brandFaviconHolds` check so the mutations an apply itself causes settle instead of looping, and disconnected in the cleanup. Deliberately mechanism-agnostic: a React re-render, a Next re-insert and any future head writer are indistinguishable from here and none of them announce themselves. Generalise: **whenever you reach past React to set an attribute on a node React rendered, ask what re-renders it — if anything does, a one-shot effect is a bug with a delay on it.** Verified by REPRODUCTION per the rendered-behaviour rule (the component's own three functions bundled into a throwaway page, a navigation simulated as React re-applying the `rel` prop): old markup ended a navigation at `["DISTRIBUTE","DISTRIBUTE","BRAND"]`, new at `["BRAND"]`. Guard: `tests/brand-favicon.test.ts`. (#3094, #3544)
- **An ACCENT surface reads a `brand-*` ramp step, NEVER a literal charter hex — a hardcoded `bg-[#2563eb]` renders correctly and is the one control that stays blue on a brand-tinted dashboard.** `:root[data-brand-tint]` re-declares the ten `--color-brand-*` steps at the open brand's hue (`BrandTint` sets `--brand-hue` off the brand's logo palette), so `bg-brand-600` rotates for free while an arbitrary-value hex cannot. `brand-tint-charter-hue.test.ts` pins the CSS side (every `oklch(... 258)` above needs its twin below) and is blind to this: the hex lives in a COMPONENT, not in `globals.css`, so nothing goes red — the surface just looks fine in isolation and wrong beside everything around it. White text/glyphs stay safe on any tint because the rotation moves the hue alone and leaves `brand-600` pinned at 54% lightness; that is the whole reason the ramp is hue-rotated rather than generated per brand. The support FAB shipped with the literal for months (#3573). Semantic green and red are NOT accents and stay put, same carve-out as the `tone-tile` rotation below.
- ⚠️ **The dark accent-tint remap is NO LONGER a hand-curated seven-colour set — `globals.css` closes it BY HUE, so `violet`, `amber`, `teal`, `sky`, `rose` and the rest of the Tailwind palette ARE covered (fills, borders and text weights).** Read the file before assuming a colour is unremapped: the "the rest of the accent set, closed by hue" block sits under the historical one-at-a-time entries, and a plan that avoids a hue on the strength of the old list is avoiding a problem that was fixed. Verified by reproduction 2026-09-04 (the lead panel's inbound-message card): `bg-violet-50` / `border-violet-200` / `text-violet-600` and `bg-amber-50` / `border-amber-200` / `text-amber-700` all resolve to translucent fills and light text on the dark surface, at 1280 and on a Pixel 7. What follows is the HISTORY of how the set was built, and it is still the reason a NEW colour is worth checking rather than assuming. `html.dark` was hand-remapped one surface at a time for seven `-50` tints (`blue`, `green`, `red`, `yellow`, `purple`, `orange`, `indigo`) plus `brand-50`/`brand-100`/`border-brand-200`. Two non-`-50` PAIRS were added later because their `-50` tint alone left near-black text and an invisible border on the dark surface: `text-purple-600`/`text-purple-700`/`border-purple-200` (the Learning tag) and `text-blue-900`/`border-blue-200` (the stale-build notice). A new tinted surface needing a text or border weight almost certainly needs its own one-line remap in the same commit. This is the same class of gap as the `text-gray-950` one above, and it is invisible in light mode, which is the default — so a colour picked without checking looks perfect until someone toggles. The by-hue block was added to end that; what it does NOT cover is a weight nobody has needed yet, so the check survives in a narrower form. Before shipping any new tinted chip / icon tile / callout, `grep "html\.dark \.\(bg\|border\|text\)-<colour>-<weight>" src/app/globals.css` for the EXACT classes you are about to use; if one misses, add the one-line remap in the same commit. And verify by REPRODUCTION (compiled Tailwind off the app's own `globals.css` + Playwright in both themes), never by reading a list in this file — including this one. Green and red stay semantic (good / error), so a decorative palette that needs several hues draws from purple / blue / orange / yellow / indigo. `SALES_FUNNELS` in `lib/sales-funnels.ts` carries its tones as whole class strings for the same reason Tailwind always requires (a class assembled at runtime is invisible to the compiler), and `tests/sales-funnels.test.ts` pins every tone to an existing `html.dark` rule. (#3146)
- **TWO icon sets, and the pick is by ROLE, not by habit: Heroicons for CONTROLS, Phosphor duotone for a SUBJECT.** `@heroicons/react` is the default and stays the default for everything that is an affordance — a chevron, a pencil, a close, a check, a nav glyph. It is single-weight by design, and inside a colored tile that reads flat: a thin stroke floating in an empty square. `@phosphor-icons/react` (9,000+ marks, 6 weights) is there for the other case, where the icon REPRESENTS the thing rather than the action on it — a funnel, an entity type, a card's subject. Use `weight="duotone"`: it lays a tinted fill under the stroke in the same `currentColor`, so a single `text-<tone>-600` drives both layers and the mark fills its tile. **Import per-icon from `@phosphor-icons/react/dist/csr/<Name>`** — the package root is a ~190KB barrel — and take the `<Name>Icon` export (the bare `<Name>` is deprecated). Reference: the four `SALES_FUNNELS` marks. When a redesign brief is specifically ABOUT the visual (Kevin 2026-07-31: *"les icones sont trop simplistes, cherche une lib plus avancée"*), "reuse the existing primitive" does not settle it — an icon set is a palette, not a behaviour, and the reuse rule protects behaviour primitives (`InfoTooltip`, `BrandLogo`, `ScoreCard`), not the choice of mark. (#3164)
- Caveat surfaced to the user: a user who previously toggled has `theme=light` stored → default change won't reach them until they toggle / clear the `theme` localStorage key. (#1631, #1635)

## Dashboard "size" requests = type scale (text-only), not zoom/spacing

When the user says "shrink / reduce the dashboard by N sizes", "size" means the **type scale** (text), tuned text-only via the `--text-*` tokens in `apps/dashboard/src/app/globals.css` — NOT root `font-size`, NOT `zoom`, NOT spacing/width/icons. Do not recommend a root-font-size or `zoom` "overall scale" lever; it gets corrected to text-only (incident #1305: recommended root-font-size zoom (A); user picked text-only (B) — "Non je voulais dure text only"). Keep #1297's approach: redefine font-size tokens only (line-heights inherit Tailwind defaults), rem-based so browser-zoom a11y is preserved. "−1 size globally" = shift every `--text-*` token one step down. A region asked to shrink MORE than the rest (e.g. "sidebar −2, rest −1") gets one extra step via its OWN text classes (`text-sm`→`text-xs`→`text-[10px]`), leaving `py-*`/icon/`w-56` untouched → zero layout shift, existing reveal skeletons still match. No new test (brittle rem-pin guards fight future tuning); verify with `tsc --noEmit` + the dashboard suite + Vercel preview. (#1297 round 1, #1305 round 2.)

## Every Save on brand Settings is `SettingsSaveRow` — it hides until the form is edited, and it sits at the END of the row

`components/settings/settings-save-row.tsx` is the one Save row for the page. Four cards (`brand-daily-budget-card`, `brand-click-destination-card`, `brand-sales-economics-card`, `brand-domain-card`) each carried a hand-rolled copy, and all four had drifted the SAME way — which is the tell that the abstraction was missing, not that four authors were careless. Two rules it exists to hold:

- **Nothing renders until the live form differs from what was saved** (`if (!dirty && !saved) return null`). The old copies rendered the button always and greyed it out via `disabled={!dirty}`, so every card offered a control that was dead until the user touched the form. A greyed primary button reads as a thing to press.
- **The row is right-aligned** (`justify-end`). A single primary action belongs where the eye lands last, and every card has to agree or the page reads as several forms sharing a background. This holds by CONSTRUCTION rather than by convention: all four rows sit directly inside their card's `p-5` box, so they align to the same edge.

`Saved ✓` outlives the button it replaces — it reports an action the user just took — and clears on the next edit, so the row shows while `dirty || saved` and the button only while `dirty`.

Notes for a new settings card:
- **Use the row; do NOT hand-roll a fifth copy.** `tests/settings-save-row.test.ts` fails if any of the four keeps a `{saving ? "Saving..." : "Save"}` button or the old `mt-5 flex items-center gap-3` row — extend that list when you add a card.
- **Your card's own gate goes through the `disabled` prop**, not a second condition around the row: the click destination stays locked until the brand has a domain (`disabled={domainLocked}`).
- **"Edited" is whatever your card can honestly compute.** `brand-domain-card` is a one-time setup on a brand with NO website, so its field starts empty by construction and a typed URL IS the edit (`dirty={value.trim().length > 0}`) — no new state. The others already track `dirty` against a saved baseline; keep it a LIVE compare, never a sticky boolean latch (see the Save/dirty-gate rule).
- **Accepted consequence:** the card grows by one row on the first keystroke. Reserving the height would cancel the effect the row exists for.
- **`apps/admin` keeps its own copies of three of these cards** and is deliberately NOT synced — it is the staff fork, and its tests are not a CI gate. If you do port this there, port the guard with it. (#3174)

## An info tip is ALWAYS `InfoTooltip` — a native `title=` attribute is a dead affordance on a phone

`InfoTooltip` (`components/visibility/metric-info.tsx`, ~20 call sites) is the dashboard's only (i). **Never hand-roll a `title=` for explanatory copy.** A native `title` waits ~1s, cannot be styled, and shows NOTHING on a touch device — a finger produces no hover — so on mobile the user gets only the `cursor-help` `?` and concludes the control is broken. That is exactly how it was reported (2026-07-30: *"quand je hover le (i) ça n'affiche pas de tip box, seulement un ?"*). Nine such tips existed across the leads timeline, both budget-tier pickers, best-model-card, the economics card and the strategy page; `best-model-card` was doubly dead, putting `title` on an `<svg>`, where most browsers render nothing even with a mouse.

Three things inside the primitive are load-bearing — each protects a whole class of call site, and the guards in `tests/info-tooltip-touch.test.ts` pin all three:

- **Hover opens ONLY for `pointerType === "mouse"`.** A tap emits `pointerdown → touchend → mouseenter → click`: the compatibility mouse events land BEFORE the click. So a bare `onMouseEnter` opens the bubble and `onClick` immediately toggles it shut — it flashes and vanishes, at EVERY call site (billing, audiences, strategy, sidebar, revenue). #2026 was titled "tap-accessible tooltips"; this ordering is what silently defeated it, and it survived undetected for months because the desktop path looked fine. Proven empirically with a Playwright `devices['Pixel 7']` tap: event order `mouseenter, click`, old bubble hidden after tap, guarded bubble visible.
- **The trigger is a `<span role="button">`, never a `<button>`.** It renders INSIDE clickable cards (the budget tiers in `onboarding.tsx` and `brand-status-control.tsx` are `<button>`s) and a nested button is invalid HTML: the parser closes the outer one early and the card breaks. `tabIndex` + `aria-expanded` + Enter/Space keep it operable. Its `onClick` also stops propagation, so tapping the (i) explains a number without also selecting that card.
- **Hit area padded to ~26px** (`-m-1.5 p-1.5`) around the unchanged 14px icon, per WCAG 2.2 SC 2.5.8's 24×24 minimum. The negative margin keeps surrounding layout put.

**A `title=` that is NOT an info tip stays a `title=`**: an exact timestamp behind a short date, a full string behind a truncation, a `Copied` confirmation on a button that already shows a check. Those carry no information the user needs, so a phone losing them loses nothing. Two known gaps deliberately left: `api-keys/page.tsx` explains a DISABLED button's reason via `title` (the right fix is rendering the reason as text, not bolting an icon onto a pill), and `theme-toggle.tsx` has no `aria-label` at all, so its button is unannounced to screen readers.

`InfoTooltip` and `ScoreCard` both still live under `components/visibility/` although those routes were deleted in #2196 — a misleading home, not dead code; both have live consumers. (#3103)

## A rendered behaviour is verified by REPRODUCING it, never by grepping the source that implements it

The repo already says this for CSS (verify a landing fix by the computed value, not by grepping the stylesheet). The interaction analogue bit twice in one day: a source-substring guard proves a handler is spelled a certain way, and says nothing about what a real tap does. When the bug IS an interaction (tap, hover, focus, drag, key), reproduce the **event sequence** headlessly — Playwright is a devDependency of `apps/dashboard` and `apps/admin`:

```js
const ctx = await b.newContext({ ...devices['Pixel 7'] });  // hasTouch + isMobile
await p.locator('#icon').tap();
await p.evaluate(() => window.__seq);   // ["mouseenter","click"] ← the whole diagnosis
```

Put BOTH the old and the new handler shape on one throwaway page and assert they differ — that proves the fix rather than restating it. The dashboard's own preview URLs are behind Vercel deployment-protection AND Clerk, so a preview tap is not reachable; a minimal `file://` page exercising the same handler contract is, and it is enough when the claim is about event ORDER rather than about app state. There is no jsdom / testing-library in `apps/dashboard`, so do not reach for a component test without adding deps.

**Mechanics that cost two failed runs whenever they are re-derived:** the specifier is `@playwright/test`, NOT `playwright` (the bare package sits in the pnpm store but is linked into no app), and the script must live INSIDE `apps/dashboard` or `apps/admin` — a file left in `/tmp` resolves against `/tmp` and dies `ERR_MODULE_NOT_FOUND`. `apps/landing` does not have Playwright at all, so a LANDING check runs from the dashboard's directory against the landing URL. Write the script to `/tmp`, `cp` it into the app, run, delete. **A hand-built page with NO `<meta name="viewport">` lays out at 980px CSS on a mobile context, so every `sm:`/`md:` rule matches and the "390px" measurement is a desktop measurement wearing a phone's clothes** — the tell is a measured element sitting at an x far beyond the viewport width you passed (a `right-4` FAB reading `x=908` at 390px). Put the meta tag in the throwaway HTML, and sanity-check that a right-pinned element's `right` equals your viewport width before trusting any number. **Assert an animated value programmatically BEFORE screenshotting it** — a count-up caught mid-flight photographs a number that is true for 200ms and wrong forever, so the screenshot is the illustration and `expect(count).toBe(target)` is the proof. **The same trap ruins a GEOMETRY measurement, and there it reads as a bug in the code you just wrote**: recharts animates bars in, so measuring rect heights the moment the first bar appears returns a fraction of the true value and looks exactly like an axis scaled to the wrong domain — wait ~2s (or set `isAnimationActive={false}`) and re-measure before diagnosing anything. And `apps/landing`'s own vitest suite resolves workspace deps through `dist/`, so `pnpm --filter @distribute/content build` first or every `static-html` import fails to resolve.

**A throwaway page carrying REAL Tailwind class strings needs those classes COMPILED, and there is no `tailwindcss` binary in this repo — build the CSS through `@tailwindcss/postcss` in the probe script itself.** Hand-writing equivalent CSS re-implements the thing you are trying to verify, and the Play CDN is a different build than the one the app ships. Two resolution traps, one failed run each: `postcss` is not a direct dependency of either app, so import it from the store (`../../node_modules/.pnpm/postcss@<v>/node_modules/postcss/lib/postcss.mjs`); and the postcss ENTRY file must sit inside `apps/<app>` or `@import "tailwindcss"` fails with `Can't resolve 'tailwindcss' in /var/folders/...` — the resolver works from the entry's own directory, not from `process.cwd()`. Keep the probe HTML in `/tmp` and point at it with an absolute `@source`:

```js
const css = `@import "tailwindcss" source(none);\n@source "${htmlPathInTmp}";\n`;
writeFileSync(entryInsideApp, css);
const built = await postcss([tailwind()]).process(css, { from: entryInsideApp, to: outCssInTmp });
```

**⚠️ A probe that verifies the BRAND TINT must set `--brand-hue-delta`, not `--brand-hue` — the wrong variable reports NO rotation on markup that rotates perfectly.** The `tone-tile` rules are `calc(<base> + var(--brand-hue-delta, 0))`, so a probe setting any other custom property falls through to the `0` default and every layer reads byte-identical to untinted. That is exactly the output a genuinely non-rotating tile produces, so it reads as a real bug in the classes you just wrote and sends you into `globals.css` looking for a missing rule. Set `data-brand-tint` on `documentElement` AND `--brand-hue-delta` to a non-zero value, and sanity-check that at least one layer MOVED before concluding anything about the ones that did not. (2026-09-03, the funnel-leg price tag.)

**⚠️ Point the probe's entry at the app's OWN `globals.css`, not at bare `@import "tailwindcss"` — otherwise every `@theme` token is missing and the probe lies in the direction that costs a diagnosis.** `bg-brand-500`, the `html.dark` remaps and the charter fonts all live in that file, so a bare tailwind import compiles the utility to a colour that does not resolve: a filled progress bar renders as an empty grey track, and it reads as a real styling bug in the component you just wrote. The fix is one line (`@import "./src/app/globals.css";` from an entry inside `apps/<app>`, then the `@source` lines) and the CSS goes from ~14KB to ~148KB, which is itself the tell that the theme was absent. Verify a suspicious "broken" colour by re-compiling with globals before changing any component code. (2026-08-28, the funnel step band: the bars measured 598px wide and photographed as empty.)

**A REAL app component drags Next and Clerk into the bundle, so budget four stubs and one shim before the probe renders anything.** Bundling a page-level component (not a leaf) pulls `next/navigation` and `next/link` through whatever imports them — Clerk's app-router client imports `useSelectedLayoutSegments`, and something in Next's graph `require`s `fs`/`stream`/`zlib`, which esbuild refuses for the browser platform with an error naming a Next internal you have never heard of. The working recipe: `--alias:next/navigation=<stub>` (export EVERY hook, not just the one you use — `useRouter`, `useParams`, `useSearchParams`, `usePathname`, `useSelectedLayoutSegment(s)`, `redirect`, `notFound`), `--alias:next/link=<stub>`, `--external:fs --external:stream --external:zlib --external:path --external:os --external:crypto` (that code never runs on your path), plus `--alias:` for any context-backed hook you cannot provide a provider for. Then in the probe HTML, `window.process={env:{NODE_ENV:"development"}}` BEFORE the bundle tag — React's dev build reads it and the page otherwise renders EMPTY with a single `process is not defined` in `pageerror`, which looks exactly like your component returning null. Always attach a `page.on("pageerror")` listener and print it: a blank probe with no error listener is indistinguishable from a component that legitimately rendered nothing. And note the module paths — `postcss` resolves at `<repo-root>/node_modules/.pnpm/postcss@<v>/node_modules/postcss/lib/postcss.mjs`, where the Conductor repo root includes the WORKSPACE directory (`.../workspaces/<repo>/<workspace>/`), so a hand-written absolute path that stops at the repo name fails with `ERR_MODULE_NOT_FOUND` on a file that is really there one level down. Export the component under test if it is module-private; `function X(` still matches inside `export function X(`, so the source-substring guards keep working. (2026-08-28, #3683.)

Then measure with `scrollWidth` vs `clientWidth` on the wrapper and `getComputedStyle(cell).display` per column, rendering the OLD markup on the SAME page so the numbers prove the fix rather than restate it (`md:min-w-[900px]` measured 358/358 at 360px against the previous markup's 1068/358). Delete the probe and its entry file before committing.

**A PERCENTAGE max-height (`max-h-full`) pins nothing when the parent's own height is indefinite — say the cap in VIEWPORT units, and check which way the overflow went before believing the class took.** This is the sibling of the min-height rule above (a `min-h` alone lets the column grow past the viewport): here the class is a `max-h`, it reads as obviously correct, and it applies to NOTHING. `max-height: 100%` resolves against the parent's computed height, and a `flex-none` item inside an `items-center` row has no definite height to resolve against, so the cap silently does not exist. The failure is worse than no cap: an `overflow-hidden` ancestor then clips the card at BOTH ends and centres what is left, so the step's own header lands at a NEGATIVE top and the content the user needs is off the top of the screen as well as the bottom. `calc(100svh - <the chrome above it>)` is definite by construction and cannot be defeated this way.

Diagnostic tell, and it is what makes this cheap to catch: measure the header's `top`, not only the CTA's `bottom`. A CTA below the fold is consistent with "no cap" AND with "cap applied, content too tall"; a NEGATIVE header top can only mean the element overflowed a clipping ancestor. Cost 2026-08-24 (#3502, onboarding desktop CTA): `sm:max-h-full` on the step card measured CTA bottom 996 on an 800px viewport with the header at **-179px**; the same markup with `sm:max-h-[calc(100svh-8rem)]` measured 757/60 and a short step kept its natural 310px height. Both numbers came from the compiled-Tailwind probe above — the class-string guard was green for the broken version.

## A hover `filter` re-shuffles the paint order — an overlapping badge on a `brightness`/`blur`/`drop-shadow` element needs an explicit `z-index`

A non-`none` CSS `filter` makes an element its own stacking context, which promotes it out of the ordinary flow layer and into the one positioned elements paint in. So a control that is static at rest and filtered on hover CHANGES paint order under the cursor: an absolutely-positioned sibling that sits on top at rest gets covered on hover, if the filtered element comes later in DOM order and neither carries a `z-index`. The bug reads as a hover glitch rather than a layout bug, which is why it survives review — at rest the screenshot is perfect.

The dashboard hits this wherever a small pill overlaps the top edge of a control: `LastUsedBadge` (`components/auth/last-used-badge.tsx`) over the `Continue with Google` buttons on `/sign-in` and `/sign-up`, both of which dim with `hover:brightness-[0.97]`. The fix is one `zIndex: 1` on the pill (same stacking context, so it beats the filter-induced 0), NOT removing the dim and NOT restructuring the markup. `transform`, `opacity < 1`, `will-change`, `backdrop-filter` and `contain: paint` all create a stacking context the same way, so the rule generalises: **when a hover state adds any of those to an element that something overlaps, give the overlapping thing a `z-index` in the same commit.**

Verify by reproducing, per the rule above — `document.elementFromPoint(x, y)` at a coordinate inside the overlap strip, read at rest and again after `page.hover()`, with the old and new markup side by side on one throwaway page. A class grep proves nothing about paint order. Guard: `tests/last-used-badge-stacking.test.ts`. (#3137)

## The support FAB occludes a BAND, not a corner — clearance goes on the axis it is PINNED to, and a bottom inset only fixes the scroll-end case

`SupportButton` (`components/support/support-button.tsx`) is mounted once on the dashboard shell as `fixed right-4 bottom-4` at `z-30`, so it covers the rightmost **64px (72px on `sm+`)** of the viewport at EVERY scroll position and paints above page content. A control pinned to the right edge of any page is therefore tappable only by luck — the leads pagination row is `justify-between`, which put `Next` flush against that edge.

The trap is that the report ("Next is hidden behind the WhatsApp button") reads as a bottom-corner problem, and a bottom inset makes it go away in the one state you check. #2764 fixed exactly this report with `pb-24` on the leads scroll column; that clears the FAB only when the column is scrolled to its end, so the same bug was re-reported 13 days later. **A right-pinned overlay needs RIGHT padding; a bottom-pinned bar needs bottom padding. Match the axis, and if the overlay is pinned on both, pad both.**

- **`SUPPORT_FAB_CLEARANCE`** (`pr-20`, exported from the FAB's own file) is the one home for the number, so it stays coupled to the button's size instead of being a literal copied per call site. 80px covers the 72px worst case AND leaves a gap — parking a control flush against the FAB (a bare 64px on mobile) still loses a fat-finger tap on its right edge.
- Padding, never `z-index` (the FAB must stay on top to be reachable) and never hiding the FAB on one page (support access is the point).
- **A gutter can WRAP the row it protects — measure the fit, don't assume.** 80px at 390px left the leads pagination row needing exactly the width it had, so `Page X of Y` wrapped to a second line; it is now `hidden sm:inline` (the range on the left already states the position). Measured 235px needed vs 294px available after hiding it.
- Only ONE pagination surface exists in the dashboard (`table-pagination.tsx` went with the conversions cluster), but the FAB is global — any NEW right-edge control reuses the constant rather than re-deriving a number.
- Verified by reproducing per the rule above: `document.elementFromPoint` at the centre of `Next` returned the FAB at 390 / 768 / 1280px on the old markup and the button on the new. Guard: `tests/leads-pagination-fab-clearance.test.ts`. (#2764 partial, #3158 complete)

## Link styling is a promise to NAVIGATE — a copy-to-clipboard value is plain text plus a glyph, never blue and underlined

`text-brand-600` + `hover:underline` on a value tells the user that clicking it goes somewhere. On an email address it reads specifically as a `mailto:`. So when the click actually copies to the clipboard, the affordance lies about what the control does, and the user learns that only by clicking it. Reported verbatim on the Leads right panel (2026-07-30): "tu l'as mis en bleu souligné quand on hover, ce qui fait penser que c'est un lien qui va ouvrir le mailto, alors que pas du tout, c'est juste pour copier".

The convergent pattern across Stripe's dashboard, [PatternFly](https://www.patternfly.org/components/clipboard-copy/design-guidelines) and [Shoelace](https://shoelace.style/components/copy-button) keeps the **value unstyled** and puts the whole affordance on a dedicated control beside it:

- value in the same `text-gray-800 font-medium` as every other panel value, no underline, no brand color
- a persistent copy glyph beside it, `text-gray-300` → `group-hover:text-gray-500` (a hover-only glyph is discoverable only by accident)
- a hover surface over the entire hit area (`-mx-1 px-1 py-0.5 rounded hover:bg-gray-100`) so the clickable region is visible
- `title` reads `Copy`, becomes `Copied` once it lands; a check + "Copied" for ~1.5s confirms
- `aria-label` names the action, plus a `focus-visible` ring — it is a button, not a link

Reference impl: `CopyableEmail` in `components/audiences/engaged-leads-page.tsx`. The other copy sites (api-keys, `brand-conversion-tracking-card`, `edit-with-ai-chat`, `context-sidebar`, onboarding) were already icon buttons — this was the only link-styled one. Guard: `tests/lead-panel-audience-and-copy.test.ts`, scoped to the function body because `text-brand-600` and `hover:underline` are legitimate elsewhere in the same file. Generalise it: a control that does not change the URL does not get link styling. (#3085)

**Same panel, the inverse rule — a card that summarises another PAGE's entity links there instead of restating its numbers.** The Leads right-panel "Audience" card printed Size and Remaining, duplicating figures the Audiences page owns while never showing the targeting filters, which is what a reader of that card actually wants. Both were dropped for a `?audienceId=` link — the deep-link seed `CustomerAudiencesPage` reads on first paint (`customer-audiences-page.tsx:198`, the same mechanism the brand-overview Top-3-audiences card uses), so the audience's detail panel opens with its colored targeting tags. Check for an existing deep-link seed before building a second summary surface. (#3085)

**Third rule from the same card, and it is the general one: a link to an ENTITY is built from where THAT ENTITY lives, never from the route the reader happens to be standing on.** `AudienceSection` assembled its href as `tenantBasePath(orgId, brandId, useParams().offerId)`, which reads as obviously correct and is a coin flip: it produces the right URL on any route that happens to carry the same depth as the target, and a nonexistent one everywhere else. Audiences moved down to the offer, so at brand level there is no offer segment to insert and the card's single affordance pointed at `/orgs/:orgId/brands/:brandId/audiences` — **a 404 on the brand Leads page for months**. The offer and campaign routes both carry an offer, so the link worked from either, which is exactly why nobody saw it: the two surfaces you would check to confirm the fix are the two that were never broken.

- **Read the entity's own scope off the wire.** `audience.offerId` (human-service serves it required + nullable) states which offer that audience belongs to; the route's offer stays only as the fallback for a lookup miss, so nothing regresses where the link already worked.
- **Unresolvable scope ⟹ NO link**, never one that 404s. 51 of 819 prod audiences are filed under no offer (rows predating the offer level) and have no page to open; the card states the audience and stops. Same render while the lookup is in flight — nothing is claimed before it is known.
- **The reason the correct source was unreachable is the too-narrow-schema bug one layer down**: `offerId` was on every audience response and `AudienceSchema` never declared it, so Zod stripped it at the parse boundary. Declared REQUIRED here, matching the producer — `.optional()` would read `undefined` forever and every link would quietly vanish, which is indistinguishable from "this audience has no offer".
- Generalise the diagnostic: whenever a hierarchy gains or loses a level, `grep` every caller of the path builder (`tenantBasePath`) and ask what each one does when the new segment is ABSENT. `tsc` cannot see it, the guard tests pinned the old form, and the broken surface is silent until someone clicks. Guard: `tests/lead-panel-audience-and-copy.test.ts`. (#3514)

## A TABLE's grain is not its ENDPOINT's grain, and the endpoint's is not the DATABASE's — read the response, never the schema

`leads_campaigns` is `UNIQUE(lead_id, campaign_id)`, so the table genuinely holds one row per `(person x campaign)` — 56,809 people fleet-wide sit in more than one campaign, and one sampled person is in **11 distinct campaign identities across 9 offers and 2 funnels**. Reading that schema, the obvious conclusion is that the leads list shows that person eleven times. It does not: lead-service collapses a brand- or org-scoped read to **one row per person** with a `DISTINCT ON (lead_id)` in `lead-list-query.ts`, and stamps the BRAND-wide delivery roll-up on the surviving row.

So a plan built on the schema's grain is a plan for a page that does not exist. The failure is quiet and expensive in one direction: **a consumer feature designed to fan out over rows the endpoint has already collapsed renders exactly one of everything and looks correct.** Nothing errors, nothing is red, every guard passes, and the surface simply never shows the second campaign. It is the mirror of the too-narrow-schema bug — there a field the producer sends is stripped by the reader; here a MULTIPLICITY the producer collapses is assumed by the reader.

The check is one authenticated call to the endpoint the surface actually polls, counting rows for a person you know has several. Doing it in the DB proves the opposite of what you need. Same instrument rule as everywhere else in this file: **read the payload, not the model behind it** — a `psql` count is evidence about storage and says nothing about a read path that groups, dedupes, filters by status or narrows by scope.

Consequence for anything nesting per-campaign detail under a lead: **the multiplicity has to be SERVED**, and it is — `?include=campaigns` (lead-service v0.67.0) nests one card per membership row on each lead, each carrying `delivery` (that campaign's own evidence, `null` = the provider holds none, never "nothing happened"), `standing`, `offer`, `audienceId` and `campaignIds` (the identity's members). Opt-in, so a read that does not ask is byte-identical. The row's own top-level delivery fields stay the brand-wide roll-up the table's badge, its tabs and the board all read — both facts at once, deliberately. `lib/lead-campaign-tree.ts` builds the panel's `offer > funnel > campaign` tree from those CARDS, and a `grep` there for any person-grouping helper must come back empty; grouping rows is the bug this replaced. (#3859, sales-lead-service#484)

## A status label states what HAPPENED, never what we ATTEMPTED — `contacted` is a QUEUE state and reads "Queued" on the customer dashboard

lead-service's `contacted` flag means the lead was handed to Instantly, NOT that anyone was emailed. Instantly's campaign schedule (instantly-service `src/lib/instantly-client.ts`, `timing 08:00-17:00`, `days` Mon-Fri, `timezone` = the recipient's when known, `America/Chicago` otherwise) holds the lead until its next weekday window, and instantly-service's own retry sweep waits **72 hours** before calling such a row stuck precisely because that queue legitimately lasts that long. So the gap between `firstContactedAt` and `firstSentAt` is routinely days, and every surface that printed "Contacted" during it asserted something false. Reported by a customer who had just been told we only send during business hours (we do, in Instantly) and read the dashboard as contradicting it.

- **`leadStatusLabel("contacted")` → `Queued`**, slate not teal (a wait, not a step cleared). The badge, the table Status column and the CSV `Status` column all read through it, so the word is defined once. (The `conversions-table` chip that also read it went with the retired conversion tabs.)
- **The status ladder is MOST-ADVANCED-FIRST, and a BOUNCE outranks the SEND it failed — `getLeadConsolidatedStatus` tested `sent` before `bounced` and every bounced lead in the fleet therefore read `Sent`.** A bounce can only happen to a message that WAS sent, so lead-service reports both booleans on the same row (**729 of 732** bounced leads on the campaign that surfaced it), and the first-match ladder stopped at `sent`. Nothing was red: both flags are real, the row rendered, and the badge simply named the earlier half of one event — reported as the table saying `Sent` while the lead panel two inches away said the address had bounced. The same shape hid `unsubscribed` behind `delivered`, since an unsubscribe requires a message that arrived. Order is now `replied > clicked > bounced > unsubscribed > (opened) > delivered > sent > contacted`: engagement stays on top, because a lead who answered or came to the site did that and a later follow-up bouncing does not un-do it. ⚠️ **`LEAD_STATUS_ORDER` must list the SAME order** — `useMonotonicStatuses` suppresses a downgrade, so a priority array still ranking `sent` above `bounced` pins the row on Sent however the derivation reads, and the fix looks like it never shipped. Four copies exist (the dashboard leads page + three admin ones) and all four move together. Generalise: **when a first-match ladder ranks states that CO-OCCUR, the ordering is the whole function** — check which flags the producer sets together before reading the ladder as a list of alternatives. (#3884)
- **The timeline is GROUPED BY MESSAGE, because a bare `Sent` does not say sent WHAT and a lead receives several messages.** Each message (`kind: "message"`) is a demarcated, openable card — tint plus a full 1px border, per the repo's no-side-accent rule — and its own delivery rows sit INSIDE it. Reported as "c'est gênant que tu mettes juste Sent… plutôt une section colorée/démarquée pour l'initial email, avec dessous Sent, Delivered etc associés". The whole arc is worth keeping in view: `Initial email` was first a row pushed at `firstSentAt ?? email.createdAt` (so with nothing sent it read as a PAST `Initial email · <generation date>`); anchoring it at `sentAt` then printed one moment twice under two labels (`Initial email · Jul 30` directly above `Sent · Jul 30`); folding it into the `Sent` row fixed that and produced the bare `Sent`. Grouping is what satisfies all three at once — the message names itself, its rows carry the instants, and no instant is printed twice.
- **What nests is bounded by what the wire can PROVE, and the boundary is load-bearing.** lead-service forwards LEAD-level first-occurrence timestamps, not per-step ones. So `Sent` / `Delivered` nest under `Initial email` (the first send IS the initial email), while `Website visit` / `Replied` / `Bounced` / `Unsubscribed` stay TOP-LEVEL: a reply can land after follow-up 2, so filing it under the initial email would state something we never observed, and their position in the chronology already says which send preceded them. **Follow-up cards carry NO delivery rows at all** — there is no per-step delivery data, and inventing one is the "never work around missing backend data" trap; that gap is a lead-service / email-gateway feature request. `Delivered` is dropped when absent rather than rendered empty.
- **A message card prints NO date of its own when it has delivery rows** — its instant IS its `Sent` row's, and stating both is the duplication that caused the report. It still SORTS at that instant, so the chronology is unchanged. A scheduled follow-up has no rows yet, so it does state its projected date.
- **Row spacing keys on the INDEX (`i < sorted.length - 1 ? "pb-4" : ""`), never a `last:` modifier — and this is a general Tailwind trap, not a timeline detail.** `last:` compiles to `&:last-child`, which resolves against the element's PARENT. The timeline's content div is the second and FINAL child of its own `<li>` on every row, so `pb-4 last:pb-0` matched everywhere and the padding never applied at all: consecutive follow-up cards rendered edge to edge, and only the rows adjacent to the "Now" divider looked spaced (that divider carries its own `py-2`). The tell that you are in this trap is a `last:`/`first:`/`only:` modifier on an element that is NOT the repeated sibling — here the repeated sibling is the `<li>`, one level up. Verified by reproduction rather than by reading the class (Playwright, two rows, old markup computed `padding-bottom: 0px` on the FIRST row; index-keyed markup computed `16px`). It shares the connector line's exact condition so the line and the gap cannot disagree about which row is last. Guard: `lead-timeline-queued.test.ts`.
- **The queue row is now the initial card's only delivery row while `firstSentAt` is absent** (`initialEvents` is a ternary on `queuedOnly`, so `Queued for sending` and `Sent` can never both be on screen), and it still carries `SEND_WINDOW_NOTE` on an `ⓘ`.
- **`servedAt` gets no footer.** A `Served: <locale string>` line sat under the timeline in a different date format than every row above it, naming an internal pipeline step. The one place it is worth showing is the leads-table Status/Date pair while the lead still reads `Processing` (`leadDateForStatus`).
- **Timeline instants read `Today at 8:45am` / `Yesterday at 10:45pm` / `Jul 28, 2026 at 4:23pm`** (`lib/friendly-datetime.ts`, alias-free so it carries real unit tests; `now` is injectable for the day-boundary cases). Three things there are deliberate: the day is a LOCAL-midnight diff, not a 24-hour one, so 11pm yesterday still reads `Yesterday`; the time is assembled by hand rather than via `toLocaleTimeString`, which separates the meridiem with U+202F in current ICU so the obvious `.replace(" ", "")` leaves a stray gap; and there is **no `Tomorrow`** — a FUTURE instant gets its plain calendar date with NO clock time (`isFuture ? friendlyDate : friendlyDateTime`), because Instantly sends inside a weekday 8am-5pm window, so both an exact minute and a firm day name would be invented (owner-picked: "pour le future on ne met pas l'heure"). The `title` keeps the full `toLocaleString` on hover.
- **A timestamp DERIVED from a generation time is stated as an offset, never as a date.** Unsent follow-ups carry `estimate: "~Nd after the first email"` (from `deriveEmailRows(email, anchor, estimated)`) and print it in place of the absolute date, which would otherwise drift by however long the queue lasts. The `title` attribute is dropped on those rows too — no precise instant to claim.
- **`forceContacted` is gone.** It painted the queue badge on every row of the Outreach tab, so a lead we really had sent read as waiting in the table while its own detail panel said `Sent` — an internally-incoherent surface, which is a bug, not a simplification. Every row states its own most-advanced status on every tab. The tab's MEMBERSHIP is unchanged (`if (lead.contacted) outreach.push(lead)`): "Outreach" deliberately covers queued + sent + bounced + unsubscribed.
- **The leads CSV uses the dashboard's words, not the wire's.** `contacted` IS the Outreach tab's predicate and `clicked` the Website-Visits one, so the columns are `Outreach` / `First outreach at` and `Website visit` / `First website visit at`. `Replied` keeps its name: the flag includes negative replies, so the Positive-replies tab is a SUBSET of it and renaming the column would overstate it.
- **`apps/admin` keeps `Contacted` in all four of its copies** — the staff console shows the raw technical state, and re-syncing it to the customer vocabulary would hide the distinction staff need.

Guard: `apps/dashboard/tests/lead-timeline-queued.test.ts` (source-substring, scoped per function — `Contacted`/`Clicked` stay legitimate as wire field names and in admin). Generalise it: before naming a status, ask whether the value proves the event or only proves we tried. (#3089)

**The timeline shows the CONVERSATION, not only what we generated — `lib/lead-conversation.ts` is the pure model and `GET /v1/orgs/conversations` the read.** The panel used to state the emails we WROTE (the generation's initial body + its sequence steps) and the delivery flags email-gateway observed, so a customer looking at a lead who replied could see THAT they replied and nothing about WHAT they said, nor what we answered. instantly-service had served the whole thread all along — oldest first, `direction` / `from` / `to` / `at` / `subject` / markup-stripped `text`, covering BOTH transports (the Instantly Unibox and our own SMTP/IMAP self-send) behind one shape, declaring no cost. The gateway simply did not forward it. Nothing about Gmail is a source here and adding one would be a second copy of one conversation: a prospect replies to the sending mailbox on our own domain, and the agency inbox receives a FORWARD of that same thread.

- ⚠️ **The read is keyed on the OPEN CAMPAIGN CARD (`openCampaignId`), NEVER the URL's campaign.** The thread belongs to a campaign, and since #3859 the card is the only place the panel states a campaign's own answer — so the conversation is scoped exactly as that card's generated email already is, on the same id. That id is also the stored campaign row the lead was SERVED under rather than whichever row is live today, which is what matters: a campaign as the customer knows it is dozens of stored rows (campaign-service mints a fresh one on every workflow switch and keeps the ancestors — 45 on one prod brand), so the live row 404s exactly the older leads with the longest conversations. Only the OPEN card receives it; handing it to every card would print one campaign's conversation under each of the others' names, and the BRAND-WIDE roll-up receives none at all — it spans every campaign, and a thread belongs to one.
- **A message that was EXCHANGED is GA; a body we have not sent stays beta-gated.** The condition is `!e.gated || canReadEmailCopy`, not the reader alone — a real message is the customer's own conversation, and the reason our drafts are gated (the copy is our writing) does not apply to it, least of all to what someone wrote back to them. Owner-decided. The generation's cards carry `gated: true`; the thread's carry nothing.
- **One card per message, and the delivery rows nest under the FIRST outbound one — but ONLY when this thread actually CARRIES that send (`threadCarriesFirstSend`).** The two sides are scoped differently and that is the trap: the delivery facts a card states are the campaign IDENTITY's (`campaignIds`, every stored row — 46 on one prod lead), while the thread is ONE of those rows, because instantly-service resolves a single logical campaign to a single provider campaign. So on a lead first contacted under an ANCESTOR row, the thread's first outbound is a later sequence from a different mailbox weeks after, and nesting the identity's `Sent` under it makes the card sort at ITS OWN instant while displaying a much earlier one — a reply that really landed between the two then appears ABOVE the send it answered. Nothing errors, every timestamp is real, and the panel simply states an impossible order. Measured: identity evidence `firstSent 2026-05-09` / `firstReplied 2026-05-24` / `sentCount 8` against a thread of three July messages, so the screen read `Replied (positive) May 24` then `Initial email · Sent May 9`. When the thread does not carry it, the send gets its OWN entry at its own instant (the same branch the no-thread case uses) and the thread's first message is labelled `Follow-up` rather than `Initial email`. A QUEUED lead is exempt — no send is observed, so there is nothing for the thread to disagree with. ⚠️ The real fix is one level up (make the conversation read identity-scoped like the evidence, instantly-service); until then this keeps the panel from stating a sequence that cannot have happened. Labels otherwise come from POSITION (`messageLabel`), never the sequence's step numbers: the sequence says what was PLANNED and a lead who replies after step 1 never receives steps 2 and 3.
- **The "no further follow-ups" note renders only when rows were actually DROPPED.** `sequenceStopReason` is true forever once a reply lands, so on a lead whose planned steps had all gone out anyway the sentence sat under cards showing sends AFTER the reply — one screen saying the sequence stopped above the proof it did not. `stopNote` gates on `plannedRows.length > 0`: a sentence explaining missing rows needs missing rows.
- **The bare `Replied` row disappears once an inbound message exists.** A row saying a reply happened, directly above a card containing it, is one screen describing one event two ways. Same reason only the follow-ups still AHEAD survive (`unsentFollowUps`): a step already sent has its own card carrying what it really said.
- **Absent, empty and unavailable stay three answers.** 404 renders exactly as the panel did before this feature (for a lead still in the weekday queue that is honest), 200-with-no-messages is a sequence that exists with nothing said, and 502 is STATED in its own line — "they never wrote back" and "we could not fetch what they wrote" are different facts. `retry: false`, because a 404 and a 502 are answers rather than blips. Anything else is logged loud and never folded into "no conversation".
- **The signature is OURS and renders only under a message we sent.** Under an inbound card it would put our sign-off on the prospect's words.
- `["leadConversation", …]` is in `PERSISTABLE_QUERY_ROOTS` — unlisted, a live third-party read cold-fetches on every panel open and the words vanish when the row is closed. `lib/lead-conversation.ts` is alias-free so it carries real unit tests; keep it that way. Guards: `tests/lead-conversation.test.ts` + `tests/lead-conversation-surface.test.ts` (which pins the CALL SITE, not only the component).
- ⚠️ `lead-timeline-queued.test.ts` sliced `function LeadTimeline(` at a measured 9000/11000 chars and the conversation pushed every later assertion out of the window — every one of those now reads `sliceToNextFunction`, which moves with the file. The measured-number form expires on the next comment anybody adds, and it expired TWICE on this one feature (once on my own branch, once again after merging #3859's rewrite of the same function).

**The timeline states what a campaign DID to everyone; READING the message is BETA-gated — and the subject is content, so it sits inside the same gate as the body.** The shape is the page's whole job and stays GA: the `Initial email` / `Follow-up (step N)` cards, their cadence, the `Queued` / `Sent` / `Delivered` rows nested under them, and the lead-level visit / reply / bounce / unsubscribe rows. What moved behind `useIsBetaUser()` (with the `<MaturityBadge level="beta" />` the gate rule requires) is the `<details>` block: its `<summary>` — which prints the subject, so leaving it GA would put the headline of the copy on screen for everyone — plus the body and the signature, all under ONE condition rather than a GA line hiding a gated payload. `EngagedLeadsPage` is the single component rendered at all four grains (brand, offer, funnel, campaign), so one gate covers them all; the CSV export carries neither a subject nor a body and must stay that way. ⚠️ **The generation is still FETCHED for everyone, deliberately — do NOT add `canReadEmailCopy` to the `["leadEmail", …]` query's `enabled`.** The follow-up ROWS are derived from its sequence steps, so gating the read would delete the cadence from a GA timeline rather than hide the words. Consequence accepted: the body reaches the browser and is readable in devtools; it is the org's own copy to its own leads, so this is a display decision, not a security boundary — the same posture as every other beta gate in this app. Guard: `tests/lead-email-copy-beta-gate.test.ts` (pins the call site, not only the hook). (#3793)

**Same split one layer down, on the WIRE: `apps/admin`'s leads reads send `status=all`, the dashboard's send nothing — and which app asks for which population is a rendering fact, not a preference.** lead-service's leads list defaults to the ACTIONABLE population (`buffered,claimed,served`); `skipped` must be asked for by name (comma-separated list, or `all`; an unrecognised value is a 400). The dashboard buckets purely on ENGAGEMENT evidence, so a lead that was never served and carries no delivery evidence cannot appear under any of its tabs — for one prod brand that was **53,760 skipped rows out of 53,777**, ~82% of a body polled every 30 seconds, and features-service scores them at zero. Not sending them is the whole point, so **do NOT filter client-side and do NOT add a status param to a dashboard read**. `apps/admin` is the one app that renders them (Skipped / Buffered / Claimed / Served tabs on its brand, feature and campaign leads views, plus the public report's own Skipped state), so all four of its reads carry the parameter — `ALL_LEAD_STATUSES` in `lib/api.ts` names it once, and `report-api.ts` spells it out rather than importing, because the report tree stays free of the Clerk-authed api client. Ordering is consumer-first by construction: a lead-service that ignores the parameter makes this a no-op, while producer-first empties the staff tabs. The api-service `/v1/leads` proxy forwards the caller's query string VERBATIM off `req.originalUrl` (api-service #826, prod `8bb3e00d`), so a new lead-service filter needs no gateway PR — unlike `/revenue`, whose whitelist is a fixed list and still misses `funnel`. ⚠️ **Its OpenAPI `parameters` block still enumerates a closed list (`brandId, campaignId, limit, offset, view`), so the prod api-registry reads as if `status` were stripped when the code forwards it.** That doc is a description of a passthrough, never its contract — a registry param list is only authoritative for a route that re-declares its query, and reading it as one cost a false "blocked on the gateway" line in #3411's own PR body. Check the route's code, or the deployed sha, before calling a passthrough param unforwarded. Guard: `apps/admin/tests/leads-all-statuses.test.ts` pins the parameter on all four admin reads AND its absence on every dashboard one. (#3411)

**Same row, the date half: a status and a date printed side by side are read as ONE statement, so they must describe the SAME event — the leads table's Date column is per-TAB and the badge is per-LEAD, which is not the same event on any tab but "Positive replies".** The Outreach tab lists every contacted lead and dated all of them at `firstContactedAt`, while each row's badge states its own most-advanced state — so a lead that had replied read `Replied` beside the day we handed it to Instantly, and a reader has no way to know the two cells are about different moments. Neither number is wrong on its own, which is why this survived the #3089 pass that fixed the words.

- **`leadDateForStatus(lead, status)` (`lib/api.ts`, beside `getLeadConsolidatedStatus`) is the one map from a badge to the timestamp that PROVES it** — `firstRepliedAt` / `firstClickedAt` / `firstDeliveredAt` / `firstSentAt` / `firstBouncedAt` / `firstUnsubscribedAt` / `firstContactedAt` / `servedAt`. It is an exhaustive `switch` over `LeadConsolidatedStatus`, so a new status cannot ship without deciding which timestamp proves it, and it lives in `api.ts` (alias-free) so it gets real unit tests rather than a source-substring guard.
- **The fix is the Date column's VALUE — `leadDateForTab` is deleted, not demoted.** The row already has exactly one place for a date; the bug was which timestamp went in it. Adding a SECOND date under the badge and leaving the column on the tab's event is the wrong shape: it puts two differently-sourced dates on one row and asks the reader to reconcile them, which is the original complaint with an extra number. Ship #3152 did that and was reverted in the same evening ("je t'ai demandé de changer la valeur de la date dans la colonne date"). One row, one date, sourced from the row's own status.
- **`skipped` / `claimed` / `buffered` carry no timestamp on the wire → `null` → the cell renders its `-`.** A column must hold its cell shape, so the dash stays here (unlike a value hanging under a badge, where a dash reads as a date we looked for and found empty).
- **The sort key moves WITH the column** (`sortByStatusDate`, replacing `sortByTabDate`): a column that displays one field and orders by another reads as unordered. The tabs differ in MEMBERSHIP, never in what a date means, so all three engagement tabs share the one comparator. It reads the UNLATCHED `getLeadConsolidatedStatus` because it decides row ORDER, while `useMonotonicStatuses` exists to keep a rendered BUCKET from bouncing on a poll.
- **The outcome tabs are the one exception**: their date is the realized-outcome instant from the `/revenue` join, because a signup has no delivery status to date. That is a different column meaning, deliberately.
- Guards: `apps/dashboard/tests/lead-status-date.test.ts` (real unit tests on the map + source-substring on the cell and the comparator). Generalise it: two values printed together are one statement — before putting a number next to a label, check they are about the same event, and fix it by correcting the number that is there, not by adding another one.

## A funnel's page walks it ARROW BY ARROW, in the campaigns table, and every rung is served

On a funnel's Overview the campaigns table lists the funnel's LEGS, one row each, whoever performs them — `CampaignsTable` switches into that mode whenever it is given a `funnelSteps` walk (`components/campaigns/campaigns-table.tsx` → `FunnelLegTable`). The four figures above say what the whole funnel returned; they cannot say WHERE people fall out of it, nor who works each arrow, which is the question somebody opening one funnel has.

- **A row is an ARROW, not a campaign of ours.** A funnel is sold leg by leg: the arrows we automate are campaigns, the arrows we do not are worked at the brand's own side, and a table listing only ours tells a customer their four-arrow funnel is one arrow long. `funnelLegs(funnel)` (`lib/campaign-leg.ts`) enumerates every arrow; `buildFunnelLegRows` (`lib/funnel-leg-rows.ts`, alias-free so it carries real unit tests) attaches the campaign performing each. An arrow nobody sells us reads **Done by you** in place of the `Via <channel>` line, and states no `$ Invested` and no `$ Budget` — "we have no figure" and "it cost nothing" are different statements.
- **The columns are `Outcomes` / `$ / Outcome` / `% Conversion`, replacing `ROI` / `% CAC` / `$ Revenue`.** Those three are the brand-and-offer question — a return, its reciprocal, and a pipeline projection — and a single arrow is judged on what it converted and what reaching it cost. `$ Invested`, `$ Budget` and `Status` stay: money already spent and a ceiling the customer set are neither derived from an outcome count nor scoped to one arrow. The multi-funnel table (Campaigns page, brand and offer grain) is UNCHANGED and keeps the return — a scope spanning several funnels has no single walk, and the producer states none.
- **Every figure is a SERVED rung of `funnelSteps`** (features-service#854, live) — `recipientsReached`, `costPerReachCents`, `fromStep`, `conversionFromPreviousPct`. The producer builds them from the same deduped leads and the same committed cents as the money above, so a rung's count agrees with `leads[]` row for row and a rate between two rungs is a rate rather than two scopes divided into each other. A browser-computed ratio here is the compute-a-stat-in-the-browser bug.
- ⚠️ **`$ / Outcome` states the CAMPAIGN's OWN served price wherever the producer answers one, and only falls back to the funnel's rung where it does not.** The rung divides the WHOLE funnel's committed spend, so a row printing it beside that campaign's own count and its own `$ Invested` answers at three scopes at once and a reader who divides the other two gets a different number. Measured in prod: one row read `18` · `$164` · `$2,889`, where 2889/18 is 160 and $160 is what that campaign's own page said — the $4 gap was a feedback-request campaign spending $61.24 on the same funnel and producing no sales interest. `campaignStepCostCents` (`lib/funnel-leg-rows.ts`) reads `outcomes.cpprCents` / `cpcCents` off the `?groupBy=campaignId` group the table ALREADY fetches — nothing is divided here and no request is added. Its key set is pinned equal to `CAMPAIGN_COUNT_FIELD_BY_STEP_KEY`'s, so a step can never state one campaign's count beside the whole funnel's price. Deeper legs have no per-campaign price on the wire (a features-service ask, not something to derive here) and a `Done by you` row has no campaign to ask, so both state the arrow's figure — which is why the column tip names both scopes in two short sentences rather than one.
- ⚠️ **A campaign's OWN price needs NO shared-arrow suppression, and the two derived cells no longer gate together.** Two campaigns feeding one arrow each have their own price, so stating both is the point rather than a repetition; only the arrow-scoped figures (`% Conversion` always, `$ / Outcome` on a fallback row) keep the state-once treatment and the `sharedArrow` (i). Each cell gates on the count IT divides by — `thinOwn` on the row's own outcomes, `thinArrow` on the arrow's rung — so a campaign that produced nothing is never lent a sibling's evidence and a measured arrow is never called thin because one campaign feeding it is quiet.
- **The rungs join by the producer's `leadField`, never by position** (`LEAD_FIELD_BY_STEP_KEY`). Index order works today and is silently wrong the day a funnel gains a step on one side only — and that failure reads as every row shifted by one rather than as an error.
- ⚠️ **`funnelSteps` is REQUIRED AND NULLABLE on the wire, so it is declared `.nullish()`** — the producer means to send that null wherever there is no ONE funnel to walk (brand and offer grains span several, a lensed read is a subset of the leads beside the whole spend, a channel with no funnel wired never read its leads). `.optional()` parses every body EXCEPT the one the null was written for, and `revenue-parse.ts` is SHARED with the brand and offer Overviews, so that mistake paints headings with nothing under them.
- **The walk is passed from the PAGE, not re-read by the table.** The funnel page already holds it for the cards above; a second read is how two parts of one screen come to state different counts. `undefined` means "this surface walks no funnel" (the brand and offer Campaigns pages), `null` means the producer has no walk for this scope — which still renders the arrows, with no figures.
- **A SHARED arrow states its cost and rate ONCE, on the arrow's lead row — withholding them from every row of it was a bug, not a protection.** Two campaigns can feed one step (a brand funding cold email and a feedback-request channel onto the same Sales interest), and the table used to drop `$ / Outcome` and `% Conversion` from BOTH rows on the grounds that the figure is the arrow's rather than either campaign's. That reasoning proves too much: the figure is funnel-scoped on an UNSHARED row too — its own tooltip says so outright ("what reaching this step has cost you, not what this one leg cost") — and it is printed there. So sharing an arrow never made the figure unstateable, only repeatable, and the real question was how many times to print it. `arrowLead` (`lib/funnel-leg-rows.ts`) marks the first row of each arrow; it states the figures with an (i) naming whose they are, and every later row of that arrow points back at it. Measured in prod: a funnel with 18 sales interests on cold email and 0 on feedback-request read `—` for its cost and its conversion on both rows, i.e. as never measured at all. Two details are load-bearing: `arrowLead` is computed AFTER the sort (the sort reorders the campaigns within an arrow, so the lead has to be whichever row ends up on top), and the LEARNING gate keys on the ARROW's own `recipientsReached` where the figure is the arrow's — gating it on the lead row's own count would call a measured arrow thin because one of the campaigns feeding it is quiet. The COUNT column is untouched and stays each row's own.
- **A `See more` link sits right-aligned under the table, pointing at `.../funnels/<key>/campaigns` — and that page renders the SAME walk, because a link labelled `See more` that leads to FEWER rows is the surface contradicting itself.** It used to lead to the plain campaign list, which is a different question ("what am I running") answered with a different row set: the arrows WE sell, two of this funnel's four, each named after the whole funnel rather than the leg it buys. So walking down from the Overview lost the `Done by you` arrows and the current leg vocabulary at once. `CampaignsPage` now reads `getOfferFunnelRevenue` on the key the Overview already polls (`["offerFunnelRevenue", brandId, offerId, <normalized key>]`, so no request and no second copy of the payload) and hands its `funnelSteps` to the same `CampaignsTable`. ⚠️ `funnelSteps` stays `undefined` off a funnel — the brand and offer lists span several and have no single walk — and `null` while the read is in flight or errored, which renders the arrows with no figures rather than falling back to a shorter list. Accepted consequence: the funnel-scoped page states no per-campaign `ROI` / `% CAC` / `$ Revenue`; the funnel's own return is on the cards above it, and a campaign's is on its own page. The link reads `text-brand-600`, a ramp step that rotates with the brand's tint — never a literal charter hex.
- **The learning band on that page speaks for the rows the TABLE shows, and disappears once the scope is measured.** Three separate ways it lied, all in `campaigns-page.tsx`: it rendered whenever ANY row was learning rather than while `scopeIsLearning` held, so a scope that had already priced itself carried a countdown; it picked its subject from the whole OFFER, so a funnel page counted days for a campaign it never lists; and it picked PAUSED campaigns, whose days-left are priced against a daily spend that is not happening (the same reason the campaign's own page hides it). Measured on the brand that surfaced it: cold email at 18 sales interests (measured) beside a `stopped` feedback-request campaign at 0, and the band read `Learning: about 14 days left`. `scopedRows` is the funnel-narrowed set, `scopedLearning` its gate, and the lead is the running learning row closest to the bar. Guards: `tests/learning-progress.test.ts`.
- **`$ / Outcome` and `% Conversion` both state `Learning` under ten**, each on the count IT divides by (see the two ⚠️ items above): stating one beside a tag disclaiming the other would let a reader trust a figure we just said we could not stand behind. The COUNT is never gated — `0` reached is measured and means nobody got here, which is the most useful thing on the row; a null count is "we could not measure it", and the two are kept apart.
- **There is NO separate "Step by step" band.** `funnel-step-band.tsx` is DELETED, not hidden: it stated the same rung's conversion and cost beside this table, under its own learning gate, which is two places for one statement and how a screen comes to contradict itself. Do not re-add one.

**Under ONE funnel the arrow BOARD is the whole answer, and each card states what its channel CHARGES.** The board walks every arrow with every channel that could work it; the campaigns table walked the same arrows a second time under it, so one screen answered one question twice. The table is gone THERE and stays at brand and offer grain, which span several funnels and have no single walk. The `offerFunnelRevenue` read that fed only the table's `funnelSteps` went with it.
- **The price tag is top-right, in the brand's TERTIARY** — the same `tone-tile` orange the Learning tag wears, so it rotates to the customer's hue rather than ours (verified by reproduction: fill, text and border each move by the tint delta, light and dark).
- **THREE states, and the third is the load-bearing one.** `Free` is a fact about the CHANNEL (`operatedBy === "customer"` — they work it themselves, so it costs nothing from us) and needs no measurement. A price is a fact about the FLEET (`GET /public/channel-funnel-economics`, public and org-less — a card's whole job is to offer a channel this brand has NOT funded, which by definition has no spend of its own to price it with). A platform channel nobody has spent through renders **NOTHING** — not a dash, never a zero: *"we have not measured this"* and *"this is free"* are different statements, and printing the second for the first tells a customer a paid channel costs nothing. Prod today on the reply funnel: 1 priced, 3 Free, 17 bare.
- **The step join is by INDEX, guarded on both step lists having the funnel's own length.** The producer calls the reply funnel's first step `Positive reply` where this app reads `Sales interest`, so a match on WORDS finds nothing on the very funnel this is most used for; and an index taken across two funnels of different length points at a different step, which is a plausible wrong number. The words rendered come from the funnel's own `steps`, never the wire's.
- ⚠️ **The served figure is CUMULATIVE from the funnel's entry** — what reaching that step has cost, not what one arrow charges. Exact for a channel that produces the funnel's first step; for an internal arrow it is the whole funnel priced under that channel's name. That is the producer's model; do not re-derive a per-leg price here.
- `lib/funnel-leg-price.ts` is alias-free so it carries real unit tests — keep it that way. Guards: `tests/funnel-leg-price.test.ts`. ⚠️ It reads `result.measured`, which collides with the repo-wide `.measured` scan in `workflow-projection-measured.test.ts` — a DIFFERENT producer's field under the same name, allow-listed there with the reason. (#3852)
- Guards: `tests/funnel-leg-table.test.ts` + real unit tests in `tests/funnel-leg-rows.test.ts`. ⚠️ `campaigns-table.tsx` now holds TWO tables, so every source-substring guard about columns, colSpans or folded cells must SLICE to the one it means (`campaigns-page.test.ts` keeps a `listTable` const for exactly this) — an unbounded `table.indexOf("<thead>")` finds the leg walk's header and asserts against the wrong table.

**The COLUMN BOARD beside that table (`FunnelLegColumnsBoard`, model in `lib/funnel-leg-columns.ts`) states what each channel is DOING, and that verdict comes from `buildControlRows` — the SAME resolver the modal each card opens writes through.** The board answers what ELSE could work an arrow, so it lists channels with no campaign at all; the reflex is therefore to derive a card's state from the only figure such a channel has, its billing CEILING. That is a different fact: billing holds the money, campaign-service holds the word. A card reading `savedCents > 0 -> Running` said `Running` about a campaign stopped since August while the modal one click away showed that channel's toggle OFF — two facts that cannot both be true of one channel, on one screen.

- **`legChannelState` reads the resolver in BOTH directions and the ceiling only to separate "stopped" from "never bought".** `buildControlRows` already handles the pre-rename funnel spellings, the pre-offer `offerId: null` ceilings, and the funded-with-no-campaign case (funded IS running: a ceiling above zero is what makes campaign-service provision one on its next tick). Re-deriving any of it here is how the two surfaces drift again within a release.
- **THREE words, not two.** `Not funded` is its own state — a channel nobody has bought is not "paused", and telling a customer it is invites them to look for a switch that was never flipped. Money stays on the card whatever the status, so a paused channel reads `Paused` AND states its `$10 / day`, which is exactly what is true of it.
- **A fourth state draws a SKELETON.** While the campaigns read is unsettled the pill states nothing rather than guessing `Running`; a slug ABSENT from a settled map is not running, never running.
- The campaigns key is byte-equal to the one the modal and the Campaigns table already poll, so the second question costs no request. `offerable` is derived straight off the arrows rather than off the laid-out columns — the columns now carry a verdict resolved from those very rows, so reading one out of the other would be a cycle. Guards: `tests/funnel-leg-columns.test.ts` (real unit tests) + `tests/funnel-leg-columns-board.test.ts`. (#3848)

**A LEG has its own MARK, and no two legs share one** — `lib/funnel-leg-marks.ts` (alias-free, real unit tests) keyed on `from->to` in the producer's own tokens, rendered by `components/marks/funnel-leg-mark.tsx`. A campaign is NAMED for its leg (`campaignLegLabel`), so a funnel tile above those words marks one thing above a sentence about another: two campaigns on one funnel doing different arrows wore the same tile, and two on different funnels doing the same arrow wore different ones. The catalogue is TWELVE entries — the ten every published channel states between them, plus the two arrows of a funnel we sell that no channel performs (`website_visit -> signup`, `website_visit -> form_filled`) — so it covers every arrow a brand can be shown, funded or not. Rules: the GLYPH carries the uniqueness (tones repeat, and are the four `html.dark`-remapped pairs the funnel and channel catalogues already draw from); no leg reuses a CHANNEL's glyph, since the two tiles sit on one row; and a leg absent from the catalogue draws NO tile, so `CampaignIdentity` falls back to the funnel's mark rather than borrowing another leg's. `CampaignIdentityInline` (the top-bar crumb) draws the SAME leg mark as the stacked layout, with the funnel's as its fallback — it used to keep the funnel mark on the reasoning that the crumb answers "which funnel am I in", but the words beside it name an ARROW, so the tile marked one thing above a sentence about another and one campaign wore two different marks in the bar and in the table. The funnel is still named on the crumb's `title` and by its own crumb one level up. Guard: `tests/funnel-leg-marks.test.ts` pins the published-leg fixture, so a leg the producer adds fails a test rather than shipping an unmarked row. (#3667)

## A `Done by you` arrow has its OWN page, and the board there is the funnel's, not the triage

`…/offers/:offerId/funnels/:funnelKey/legs/:legKey` (`components/funnels/funnel-leg-page.tsx`) is where a person WORKS one arrow. `legKey` is the step the arrow LANDS ON — within one funnel each step has exactly one incoming arrow, so that key identifies it and reads as a word rather than as a pair of tokens joined by a separator.

- **Reached from a `Done by you` row and from nowhere else.** An arrow WE run opens its CAMPAIGN: that page has a budget, a status and settings a leg page has nothing to say about. The table branches on the row's own campaign (`campaign ? open(campaign) : openLeg(leg.toKey)`), so no row is unclickable.
- **The BOARD is a second board, deliberately, because the WRITE is different.** The Leads page's board triages a lead (still in play, or why not) and states a REPLY KIND against a campaign; its own comment says it has nowhere to ask for the cost a funnel-step statement needs. `FunnelLegBoard` is the funnel question asked one arrow at a time — who reached the step before, who has crossed — and a drop does NOT write: it opens the same `StageStatementForm` the lead panel uses, which asks what the step cost and, on the step that closes a deal, what it was worth. A board that wrote on drop would meet lead-service's refusal after the card had already moved.
- **ONE DIRECTION.** A card moves from the step before into this one and never back: a lead who attended a meeting did attend it, and un-saying that is not a correction a board can make honestly. The lead's own panel is where a statement is restated.
- **The GESTURE is shared with the Leads board and is not this component's to own** — see the kanban-gesture section below. The `Move` button is DELETED: it cost the card a whole line and read as a second way of doing what the card already does. Every card carries its status as a tag (the column it is in, or the rung it crossed), because a board sorted by status whose cards state none makes the reader hold the headers in their head, and the ghost under the pointer has no column at all.
- ⚠️ **Only five steps take a statement at all** (`WRITABLE_STAGE_KEYS`): a positive REPLY is a fact about a message and a website VISIT is measured by the delivery layer, so lead-service accepts neither. Those arrows render read-only and say so rather than offering a control that refuses — and they are the entry arrows, which open a campaign anyway.
- **The columns are capped at `LEG_BOARD_COLUMN_CAP` (60) and the cap is STATED.** A funnel's base column is every lead the brand ever contacted — 9,166 on the brand this was built against — and a column drawing 60 of them in silence claims the funnel is smaller than it is. The line states the number DRAWN, never the cap.
- **The FIGURES lead, then the chart, then the board** — the same order the campaign pages read in: what happened, how it has moved, then the thing you do about it. The board shipped at the TOP and was reported as the wrong way round; it is the last band now.
- **Three cards, and the conversion sits INSIDE the rung it belongs to.** The rung this arrow converts FROM (`fromRecipientsReached`, served so a consumer renders "3 of 40" without looking the base up), the rung it converts TO with `conversionFromPreviousPct` as that card's own subtitle — they are one statement, this many crossed out of that many, so a separate Conversion card said half of it twice — and what YOU stated each crossing cost. That third figure is **SERVED per rung** (`funnelSteps.steps[].customerCost.costPerReachCents`, features-service v0.148.0 — asked for and shipped in the same session), never divided here: a client-side ratio drifts from that service the moment either side changes, and the funnel-wide `customerCost` beside it answers a different question (the whole chain at once). It is required-and-nullable on the wire, so the reader declares it `.nullish()`; a NULL average means nobody has been asked yet and renders a dash, never a `$0`, which would say their work was free. ⚠️ It is the CUSTOMER's money and rides BESIDE the rung's own `costPerReachCents`, which is what WE charged divided over every rung of the funnel — the guard forbids `step.costPerReachCents` on this page for exactly that reason.
- **No outreach chart, and no RETURN chart.** An arrow the brand works itself sends nothing, so a per-day send chart would be a flat zero over a step that is working fine. A return is the whole FUNNEL's — the money bought every rung, not this one — so charting it under one arrow's name states a wider scope's answer here; what is charted is the arrow's own outcome over time (`OutcomeTrendCard` off the series the page's existing read already carries). ⚠️ **`OUTCOME_SERIES_BY_STEP_KEY` covers four steps only** — a meeting ATTENDED, a signup and a filled form carry no dated series on the wire, so those say so rather than drawing an empty chart, which reads as "nobody crossed" when what we mean is "we cannot chart this yet". And still no cost per outcome from OUR spend: the only per-step cost served divides the whole funnel's committed spend, none of which was spent on that arrow.
- **Both reads are the keys the funnel page and the Leads page already poll** (`["offerFunnelRevenue", brandId, offerId, key]`, `["brandLeads", brandId]`), so arriving here costs no request and no two surfaces can state different counts for one step. The revenue join carries a LEAD id; a statement is written against the leads_campaigns ROW id, which is why the board sources rows from `listBrandLeads` and evidence from the revenue read.
- **The board reads ONE BOUNDED PAGE PER COLUMN, never the brand's whole population.** A column is the people at one funnel step, which is a bucket lead-service answers directly — `LeadStageKey` and its bucket vocabulary are the same seven tokens, so `bucketForStage` is a checked identity rather than a second table that could drift (`null`, the base of a funnel's first arrow, reads `contacted`). It used to fetch 44.5 MB over 12,945 rows to fill a board that has never drawn more than `LEG_BOARD_COLUMN_CAP` cards a column; measured in production on that same brand the buckets it actually needs hold **21** and **66**, and the two reads together come to **77 KB**. The cap was always right; the READ had no bound.
  - **The FROM column over-fetches (`cap * 2`)**, because a lead in that bucket may have CROSSED and is then drawn in the other column — a read of exactly the cap could hand back a from column with fewer cards than it has people.
  - **Neither read asks for `include=campaigns`**: a card here opens the lead on the LEADS page rather than a panel of its own, and the nesting is about four times the row (6.2 KB against 1.6 KB).
  - **The count beside each column head is the PRODUCER's bucket total** (`legBoardTotals`), never a count of the rows fetched — which is the cap, and would report `60` on an arrow 9,166 people are standing at. `to` is exact; `from` subtracts the crossers, which rests on the funnel's own claim that crossing an arrow means having been at the step before it, and is FLOORED at the cards drawn so a column can never claim to hold fewer people than it is showing. A total the producer did not state falls back to what was drawn — "we were not told" is not zero.
- `lib/funnel-leg-board.ts` is alias-free so it carries real unit tests — keep it that way. Guards: `tests/funnel-leg-board.test.ts`.

⚠️ **`RevenueLeadSchema` silently stripped `clicked`, `repliedPositive` and `meetingAttended` for as long as it existed** — features-service serves all three on every lead row (verified on the wire 2026-08-28) and the schema never declared them, so every consumer asking whether a lead REPLIED, CLICKED or ATTENDED read `undefined` and could not tell that apart from "not measured". They are declared now. Same too-narrow-schema class as the rest of this file: when a reader's field is always empty, check the SCHEMA before checking the producer.

## EVERY kanban in the dashboard runs ONE gesture, and a drag leaves a slot at BOTH ends

Two boards move cards — the Leads triage board and the funnel-arrow board — and they write different things (a reply kind against a campaign; a step statement against a leads_campaigns row), which is why they stay two components. What they must NOT each own is how a card is picked up: a person who learns the gesture on one has learned it on the other, and two copies of a pointer state machine drift the first time either is touched. `lib/board-drag.ts` (alias-free, real unit tests — keep it that way) plus `components/boards/use-board-drag.ts` is that one gesture; `components/boards/board-slot.tsx` is what it leaves on screen.

- **Hold to lift, tap to open, told apart by TIME** (`LONG_PRESS_MS` 350, `SLOP_PX` 8 — a press that wandered was a scroll). Native HTML5 `draggable` is banned in both boards: it fires on no touch device at all, and its drag image is a screenshot the browser composites, which is where square white corners behind a rounded card come from. The ghost is a LIVE element, so its radius and shadow are the card's own (measured 8px).
- ⚠️ **Once a card is UP the gesture belongs to the WINDOW, not to the card.** The card is replaced by its own outline the moment it lifts, so the element the press started on is gone and pointer capture with it; a `pointerup` handler living on a card then fires only if the pointer happens to come to rest on ANOTHER card, and a drop onto a column header — or onto the gap under the last card, which is most of a short column — is silently swallowed while the board stays stuck holding the ghost. This shipped in the first triage board and was found only by REPRODUCTION (a drop onto a column header wrote nothing and left the ghost up); a source guard cannot see it. `useBoardDrag` registers `pointermove`/`pointerup` on `window` while dragging and the card handlers stand down.
- **A drag leaves a slot at BOTH ends** (`BoardSlot`): the source keeps its space as a dashed outline so the column does not collapse and reflow the board under the finger, and the target OPENS one before release so a drop is confirmed rather than discovered. The lifted card is no longer merely faded — a 40%-opacity card still reads as a card that is here. The target slot goes LAST in its column: the cards carry no order anybody stated, so claiming a position would be inventing one.
- **The column under the pointer is READ off the document** (`elementFromPoint` over `data-board-column`), never tracked per column, so a board lays its columns out however it likes — a scrolling rail, a two-up grid — with no change to the hook. Any new board must put that attribute on its column sections.
- **The rail scrolls itself under a card held at its edge**, in a frame loop (`railScrollStep`), because a finger parked at the edge emits no further move events. Verified by reproduction on a Pixel 7: a column off the right of the viewport scrolls into reach and takes the drop. A board whose columns all fit simply never attaches `railRef`.
- Guards: `tests/board-drag.test.ts` (real unit tests) + the two board surface tests, which pin that BOTH boards call `useBoardDrag` rather than re-implementing it.

## Currency display = adaptive by magnitude (`<$10` → cents, `≥$10` → whole), EXCEPT billing

Every USD amount on the **user dashboard** formats by magnitude: `abs(usd) < 10` → 2 decimals (`$4.27`), `≥ $10` → 0 decimals rounded, thousand-sep (`$1,240`). Canonical helpers `formatUsdAdaptive(usd)` / `formatCentsAsUsdAdaptive(cents)` in `lib/format-number.ts`; `lib/format-stat.ts` currency branch + every live per-component currency formatter (revenue cards, strategy CPC/cost tiles, audiences, brand-status unit-cost, brand-info) implement the same `Math.abs(usd) < 10 ? 2 : 0` inline. **Billing is deliberately EXEMPT** — `billing/page.tsx` + `lib/billing-guard.tsx` keep the explicit-decimal `formatUsd`/`formatCentsAsUsd(…,2|0)`/`formatBillingCents` (exact charged amounts must never round). **DAILY BUDGET is a further carve-out — it ALWAYS renders as WHOLE dollars (no cents at all, not even under $10), a daily budget being an exact whole-dollar config value where cents read wrong (`$7.50/day` → `$8/day`).** Whole-budget surfaces: `brand-status-control` (`fmtUsdWhole` — the status pill's `budgetLabel`; the unit-cost-per-outcome beside it stays adaptive), `revenue-cost-summary` (`formatBudgetCents` — the `/$budget` denominator), `billing-guard` (the "Brand daily budget cap / day" line only; its "Required" credit amount stays exact), onboarding + the Sales Funnels card (the per-funnel `$ /day` fields, integer-only input). A new dashboard currency site → use the adaptive helper (or the inline `Math.abs(usd) < 10 ? 2 : 0`), NOT a hardcoded `minimumFractionDigits: 2`; a new DAILY-BUDGET display → render whole (`$${Math.round(usd).toLocaleString("en-US")}`). (#2593, daily-budget-whole #2596)

## A repo in `~/conductor/repos` is not a RUNNING service — check the box before planning to extend one

The fleet's repo list, its merged PRs and its CLAUDE.md all read identically whether a service is serving traffic or has not run in months. Several did not survive the Hetzner migration (distribute.you#3310 counted nine the gateway still routed to), and nothing about the repo says so: `twilio-service` had merged PRs through 2026-08, a detailed CLAUDE.md, a working key-service credential and a live Twilio account — and no container, no clone under `/root/distribute/repos`, no `docker-compose` entry and no env file on the box. A plan that says "we extend the service that already runs" is then wrong in the one direction that matters, because the deploy is real work (DB + role, env, compose entry, Caddy vhost, DNS record) and it is invisible until someone looks.

The check is one command and it is the same instrument that answers every other "is this live" question here: `ssh … 'docker ps --format "{{.Names}}"'`. Do it at PLAN time for any service the plan intends to call, not at execution time — the absence changes the task list, not just a step inside it.

⚠️ Two things that make the absence read as presence. The service's **platform key is still in key-service** (a credential outlives the deployment that used it), so a `GET /keys/platform/<provider>/decrypt` succeeds and looks like proof of life. And the **api-service gateway may still route to it**, so its openapi lists paths that answer nothing. Neither is evidence; only the container is.

Corollary for the spawn prompt: a spawned agent cannot verify against a deployment that does not exist, so say so in the prompt and keep the box work with the orchestrator. It is hand-managed infrastructure, not something a repo-scoped agent should be reaching into.

(2026-09-05, the sales-rep instant call: `twilio-service` was brought up from nothing — `twilio_service` DB + role, `env/twilio-service.env`, a compose entry whose service name must equal the clone directory name because `deploy.sh`'s `service_exists` checks both, a Caddy vhost, and a proxied `twilio.distribute.you` A record, since Twilio has to fetch TwiML and post gather results back. Note there is NO wildcard DNS: a new public subdomain is a Cloudflare record you create.)

## Release flow (distribute.you specifics)

No `release.sh hotfix` here (that targets the backend semver services). The Hetzner box pulls `main` and rebuilds, unattended, within about 7 minutes of a merge — no tag, no bump, and nothing to promote.

**⚠️ The BACKEND fleet no longer deploys from a merge the way Railway used to — it is pulled onto the Hetzner box, and for a day after the migration nothing pulled at all.** Railway's git integration WAS the deploy path; when it was deleted (2026-08-08) nothing replaced it, so `release.sh` kept tagging, CI kept greening, PRs kept merging, and production kept running older code — two real fixes sat merged-and-unshipped, one of them customer-blocking. **So a merged backend PR is NOT in production until the box has rebuilt it** — the deploy-landed discriminator is behaviour, never the PR state.

How it works now (`/root/distribute` on `167.233.196.79`, SSH key `~/.ssh/oracle-distribute`, user `root`): every service has its OWN git clone under `repos/<service>` and `docker compose` builds from that directory (`build: ./repos/<service>`), so deploying is fetch → `reset --hard origin/main` → `compose build` → `up -d`. Postgres is a container on the box (`distribute-postgres-1`), and Windmill's server + worker are too — Neon and Railway are both out of the backend path.

- **`./deploy.sh <service> | --all | --status`** does that, plus two things: it asks the service `/health` from INSIDE the container (Node 18 `fetch`, so nothing to install and no dependency on Cloudflare being healthy) and **returns the clone to its previous commit and rebuilds when that check fails**. A build failure never touches the running container.
- **`deploy-cron.sh` runs `--all` every 5 minutes** (`flock`-guarded, logs to `deploy.log`). Deliberately a PULL from the box, not a push from CI: a CI-triggered deploy needs this machine's key in GitHub secrets, which is a root shell on all of production for every workflow run, and it needs a workflow file in each of 23 repos. Nothing leaves the box this way and a new service is covered for free.
- **`deploy-admin.sh` is a ONE-LINE WRAPPER around `./deploy.sh admin-app --force`, so the staff console gets the same health check and the same rollback as every service.** It kept the name because that is what the runbooks say. It used to be a separate implementation, on the belief that admin could not be health-checked without widening its staff-email allowlist; `proxy.ts` already lists `/api/public(.*)` as public, so only the health route was missing (#3314). Being separate was the real hazard: all three Next apps build from ONE clone, and two scripts that each `git reset --hard` it can pull the tree out from under the other's build. One deployer, one lock, one clone. `deploy.sh` writes `env/admin-app.build.env` into the clone as `.env.production` before building, which is how the build-time `NEXT_PUBLIC_*` still get inlined. **⚠️ The CADENCE claim that used to sit here — "admin is NOT on `deploy-cron.sh`, so a merge alone changes nothing" — is WRONG and was repeated into three PR bodies before anyone checked.** `deploy.sh` has `APPS=(landing-app dashboard-app admin-app)` and deploys them as ONE group (they share a clone, which is exactly why), `--all` calls `deploy_apps`, and `*/5 * * * * /root/distribute/deploy-cron.sh` runs `--all`. So **a merge to `main` reaches `admin.distribute.you` within ~5 minutes on its own**, health-checked with rollback, like every other service. `--force` is only needed when a build-time `NEXT_PUBLIC_*` changed with no commit to trigger a rebuild. Verified 2026-08-15 by reading `deploy.sh` + the crontab on the box and watching `admin-app: healthy on 0ca97c5a` land unattended. **Generalise: a cadence/ownership claim in this file is a point-in-time assertion like any other — confirm it against the box before repeating it to the user, especially when you are about to tell them a manual step is required.** Telling someone to run a deploy that already ran is the same class of error as reporting a healthy deploy as stuck.
- **⚠️ A FAILED `git fetch` reads as "nothing to do", so the whole fleet can sit undeployed with a log that looks fine — and the cause is that an UNAUTHENTICATED fetch of a PUBLIC repo gets 401'd.** GitHub answers the `git-upload-pack` POST with `HTTP/2 401` + `www-authenticate: Basic realm="GitHub"` intermittently; git has no terminal on the box, so it dies `fatal: could not read Username for 'https://github.com'`. `fetch_main` was one shot, so `origin/main` stayed at the previous value, `from = to`, and every caller printed `already on <sha>, nothing to do` — the exact sentence that means the opposite. Measured 2026-09-02: **1 successful fetch in 5**, `distribute.you` frozen 5 hours behind (landing + dashboard + admin all serving old code), and four repos that had been failing on EVERY tick for so long their fatals read as permanently-private repos. They were not: all four fetch fine once authenticated. Two fixes, both on the box (which is not a git repo, so this paragraph is the only record): `fetch_main` now retries 5× with backoff and **FAILS LOUDLY** rather than letting a stale ref be read as an empty diff (`--all` marks the tick non-zero and skips that repo by name; `deploy_one` returns 1 so a hand-run cannot print "nothing to do" off a fetch that never happened; `deploy_apps` still allows `--force`, which legitimately rebuilds with no new commit); and `/root/distribute/git-credential-distribute` is a git credential helper registered as `credential.https://github.com.helper` that answers `get` with the PAT **already** on the box (`env/admin-app.env` `GITHUB_CONFIG_TOKEN`, the admin config console's) — deliberately reading that ONE copy rather than writing a second, so rotating it there is the whole rotation. It prefers `env/github-deploy.env` `GITHUB_DEPLOY_TOKEN` when that file exists, so a machine token owned by the deploy pipeline can be dropped in later with no code change; until it does, a revoked PAT breaks deploys LOUDLY (`fetch_main` fails hard and the watchdog below mails within 15 minutes). Authenticated, the same loop measured **8/8**. Generalise, because this is the shape that hides: **a read that FAILS and a read that returns "unchanged" are different answers, and any code that treats them alike will report a stalled pipeline as an idle one.**
- **⚠️ A hand-run `./deploy.sh` does NOT take the cron's lock, so it can race a tick on the SHARED apps clone.** `deploy-cron.sh` holds `flock -n 9` on `/var/lock/distribute-deploy.lock` precisely because two `docker compose build` on one clone corrupt each other, and `deploy.sh` itself takes nothing. Observed 2026-09-02: a manual `--all` overlapping the 15:30 tick left `landing-app` and `dashboard-app` healthy on the new sha and **`admin-app` with no healthy line at all**, its container still built from the previous commit — after which `deploy.sh` short-circuits forever on the clone's sha (see the `--force` rule below). **`deploy.sh` takes the lock ITSELF now** (`take_deploy_lock`, on every branch that builds — `--status` is exempt, it changes nothing and must not queue behind a 20-minute build). A hand-run WAITS up to 30 min; the cron keeps its own `flock -n` and SKIPS, since the next tick is 5 minutes away. ⚠️ `flock` is per open file DESCRIPTION, so a child reopening the same path would block on its own parent forever — `deploy-cron.sh` therefore exports `DISTRIBUTE_DEPLOY_LOCKED=1` and `take_deploy_lock` returns immediately on it. Verified all three paths (cron-with-lock-held, hand-run-blocks, hand-run-alone).
- **⚠️ The 5-minute cron SILENTLY SKIPS a service when `docker compose config --services` has a bad moment, so "it will pick it up on the next tick" is not a guarantee — read `deploy.log` for the service by NAME.** `deploy.sh`'s `service_exists` is `[ -d "$REPOS/$1" ] && docker compose config --services | grep -qx "$1"`, and that compose call fails intermittently under load (observed exiting `255` while a sibling image was building). Both halves are then false, so `--all` hits its `|| continue` and moves on, while a direct `./deploy.sh <svc>` prints `<svc>: unknown service (no clone, or absent from docker-compose.yml)`. That message names two causes that are BOTH usually untrue — the clone is there and the compose entry is there — which is what makes it read as a broken box rather than a flaky command; re-running the same deploy a minute later works. So a merged backend PR can sit undeployed indefinitely with nothing in the log about it, and the tell is an ABSENCE (no `<service>:` line at any tick) rather than an error. Verify a landed merge with `grep '<service>' deploy.log | tail -2` plus a content marker in the clone, and just re-run `./deploy.sh <service>` on a skip. Observed 2026-08-27 (features-service v-next): the 11:05 tick reported unknown-service while `ls` and `docker compose config --services` both answered correctly seconds later, and the immediate re-run deployed it (`healthy on fa1f8910`).
- **`deploy-watchdog.sh` says out loud when the pipeline has stopped shipping — it is its OWN cron entry (`*/15`), not a step inside `deploy-cron.sh`, and that is the whole point.** The failure worth catching most is the deploy cron not running at all, and a check living inside the thing it watches cannot report that. Two conditions, both read-only over `deploy.log`: no tick in the last 20 minutes (the cron is stuck or dead — including a hand-run holding the lock), and the last completed tick containing `git fetch failed` / `BUILD FAILED` / `unknown service` / `UNHEALTHY`. It mails through Postmark with the credentials already on the box (`env/dashboard-app.env`), one alert per KIND per hour, and it **records its own send's HTTP code** to `deploy-watchdog.log` — an alerter that fails silently is the exact bug it exists to fix. Deliberately does NOT alert on "a repo is behind": between a merge and the next tick every repo legitimately is, and paging on that trains everyone to ignore it. ⚠️ **It also stays silent while the deploy LOCK is held**, because a tick that is legitimately running is not a stalled pipeline: the cron holds the lock for the whole run and its `flock -n` skips every tick underneath, so a fleet build routinely leaves a 10-15 minute gap between `===` lines (observed 22:25 -> 22:35). Without that guard the 20-minute threshold pages on an ordinary busy deploy, which is how an alert earns its way into a filter. ⚠️ And the guard itself has a trap that fails OPEN: `flock -n 7 true` is the FILE form — it locks a file literally named `7` and always succeeds, so the check reports "nothing is running" every time. The fd form takes the descriptor ALONE (`exec 7>...; flock -n 7`). It shipped that way for one iteration and only a test with the lock genuinely held caught it. Proven end to end against a fake log — HTTP 200 on a forced condition, silent under a held lock, alerting once released — not just read.
- ⚠️ **`/root/distribute/Caddyfile` is bind-mounted INTO the caddy container as a single FILE, so `sed -i` (which writes a NEW inode) leaves the container reading the OLD file forever — `caddy validate` passes, `caddy reload` prints `adapted config`, and the new host answers `525` at the edge and `000` locally while nothing looks wrong.** Confirm with `docker compose exec -T caddy grep -c <host> /etc/caddy/Caddyfile` (0 = the container still holds the previous inode), then `docker compose restart caddy` (re-resolves the bind mount; ~5s blip on every host). A `cat > Caddyfile` after the fact does not help either, since the container is pinned to the inode it opened at start. Cost 2026-09-06 (lab-distribute vhost): two reloads and one force-reload reported success against a config the process never loaded.
- **Waiting for YOUR fix to land: match the MERGE commit or a content marker, never the sha your PR branch carried.** The box does `reset --hard origin/main`, so its clone HEAD is the merge commit; an `until … rev-parse HEAD | grep <branch-sha>` never fires and reads as a stalled deploy on a deploy that already succeeded.
- **A CONTENT marker must be a code construct, not a string your own explanatory comment can contain — this is the source-substring-guard trap wearing a deploy hat, and it lies in the SCARIEST direction: it reports a healthy deploy as stuck.** Verifying "did my change ship?" by grepping the deployed file for the literal you REMOVED fails the moment your commit explains the removal, because a good comment names the thing it retired. Grep for the construct instead — the `WHERE` clause, the function signature, the absent parameter — or read `deploy.log` for `<service>: healthy on <sha>`, which is unambiguous and free. Cost 2026-08-13 (brand-service v0.68.0): a `grep -c 'backfilled_from_goal IS NOT NULL'` returned 1 against a container running the new code, because the new code's doc comment says *"The first run also required `backfilled_from_goal IS NOT NULL`"*. Reported to the user as a silently-stuck deploy, then retracted; the deploy had been healthy for 40 minutes. The same trap bit three more times the same day on ordinary test guards, so treat any `not.toContain(<retired literal>)` as needing its own source comment reworded in the SAME commit.
- **Confirm the deploy LANDED before probing its outcome, or you will report your own change as ineffective.** A prod probe run between the merge and the box's next 5-minute tick returns the OLD behaviour, which reads exactly like "the fix does not work" and sends you off diagnosing shipped-and-correct code. Gate the probe on `grep '<service>' deploy.log | tail -1` showing `healthy on <merge-sha>`. Cost 2026-08-13 (features-service v0.126.0): probed ~2 minutes early, saw the pre-change pipeline unchanged, and started reading the merged diff for a bug that was not there; the numbers were exactly right one tick later.
- **`--force` exists ONLY for the three Next apps, never for a backend service.** `deploy_apps` parses it (`NEXT_PUBLIC_*` is inlined at build, so an env change needs a rebuild no commit will trigger); `deploy_one` does not, and short-circuits on `already on <sha>, nothing to do` by comparing the CLONE's sha — not the running image's. So a backend service whose clone advanced without a successful rebuild cannot be pushed through by `deploy.sh` at all. If you ever genuinely land in that state, rebuild through compose directly (`docker compose build <svc> && docker compose up -d <svc>`) — but first make sure you are not looking at a false marker, per the rule above, because that state is rarer than a bad grep.
- Accepted trade: the build runs ON the box, competing with production for CPU, and a red `main` reaches that build (the health check + rollback is what bounds the damage, not the merge gate).

**NOTHING IS ON VERCEL ANY MORE — `landing` and `dashboard` cut over on 2026-08-09 and both projects are DELETED.** Both ship as containers (`apps/{landing,dashboard}/Dockerfile`, `output: "standalone"` with `outputFileTracingRoot` at the repo root), `deploy.sh` deploys them as one group off the shared `repos/distribute.you` clone, and the 5-minute cron picks them up like every service. `CUTOVER.md` on the box keeps the record ids and the undo, but the undo now restores DNS to projects that no longer exist — recovery is fixing the box, not going back. Verify what serves a hostname with the response header (`via: 1.1 Caddy`), never by which host you happen to know about. `docs` and `sales-cold-emails-landing` are STATIC (`output: "export"`) and served by **Cloudflare Pages** — built and uploaded by `.github/workflows/deploy-static-sites.yml`, which is free because the repo is public. Their `vercel.json` files are vestigial, kept like `apps/admin`'s in case anything is ever moved back. **A change to either one does NOT reach production through a Vercel deploy**, so do not wait for one, and do not verify it by reading `vercel ls`; watch the Actions run instead. `admin` is a container on the Hetzner box and rides the SAME 5-minute cron as landing and dashboard — `deploy.sh` lists all three in one `APPS` group off the shared clone, so a merge to `main` reaches `admin.distribute.you` unattended (this paragraph used to say a merge changed nothing there; it was wrong — see its own section). **every `mcpfactory.org` domain was removed on 2026-08-07** (retired brand), so do not re-add one and do not treat a `mcpfactory.org` 404 as a regression — its DNS still lives on a Gandi account outside the tokens in `~/.agents/skills/gandi-mail/secrets.env`, so the records dangle at Vercel until someone with that access clears them. Two things that bite when editing `docs`/`sales`: `sitemap.ts` and `robots.ts` need `export const dynamic = "force-static"` or the export FAILS the build, and anything needing a request-time API (a route handler, a DB read, `cookies()`) cannot go in them at all — which is exactly why `landing` and `dashboard` are containers rather than static exports.

**Three things a self-hosted Next build does NOT inherit from Vercel, each of which fails silently:**
- **`export const maxDuration` is a Vercel function limit and does nothing to a standalone server.** The `/api/v1` catch-all and the chat route legitimately run for minutes, so the only ceiling is now Caddy's, set per vhost (`read_timeout 310s`). The chat route also streams, so its vhost carries `flush_interval -1` — without it Caddy buffers the whole response and a token-by-token stream arrives as one silent wait then the entire answer.
- **`request.url` in a route handler is the address the process BINDS to, not the address the visitor typed** (`http://0.0.0.0:3000/...`). So `NextResponse.redirect(new URL("/somewhere", request.url))` — which reads as obviously correct and works perfectly on Vercel — emits a Location pointing at the container. `/how-it-works` shipped exactly that. Emit a **relative** Location instead (`new NextResponse(null, {status: 308, headers: {Location: "/#how"}})`); the browser resolves it against whatever host it asked, so no origin is needed at all. Middleware is NOT affected — `NextRequest` is host-aware there and already emits relative Locations.
- **`NEXT_PUBLIC_*` and anything the build READS are two different lists.** The landing PRERENDERS `/blog` and `/sitemap.xml` out of `DATABASE_URL`, so a build without it produces a blog with zero articles and a sitemap missing 28 URLs — no error, no warning, just a smaller page. It reaches the build as a **BuildKit secret** (`--mount=type=secret`), never a build arg or a line in `.env.production`: both of those are recorded in the image, and it is a real credential rather than a publishable token.

**A deleted platform takes its secrets with it, and only the ones with a second source come back.** The cutover recovered every value it could from key-service, the box, the live bundle or the database; two had no second source at all (`PARTNERO_API_TOKEN`, `OUTRANK_WEBHOOK_SECRET`) and a human had to fetch them from the vendor. Generalise before retiring any platform: list what only IT holds and pull those out FIRST, while it still answers. A `NEXT_PUBLIC_*` value never qualifies — it is inlined in the live bundle, so it can be read off the served page rather than asked for.

**The `ignoreCommand` / `scripts/vercel-ignore.sh` machinery is HISTORICAL — there is no Vercel project left to run it.** It existed because all four projects rebuilt on every merge (2032 builds in 30 days for 494 that had anything to build) and it cut that by deciding from turbo's dependency graph, on the production branch only. The `vercel.json` files that still carry it are vestigial. Two things it leaves behind: **there is no preview URL** (verify locally, or on prod after merge), and its one durable trap is worth remembering if it is ever revived — Vercel inverts the shell convention (`exit 0` SKIPS, `exit 1` BUILDS), so getting it backwards silently stops every deploy. (#3287)

- **The generalisable lesson from `distribute-docs`: a note saying a deployment is broken has a shelf life.** It was documented as having an inverted ignore command whose production had "never" built; #3068 fixed that and it shipped READY deploys for a week while the note still read as current. Check the platform before acting on any claim in this file that something is already broken.

**Which host actually serves a domain is answered by a RESPONSE HEADER, never by a cloud provider's CLI still having the service.** This is written from the Railway era, when its CLI kept returning values and a green `SUCCESS` for every backend service long after DNS had moved — so `RAILWAY_PUBLIC_DOMAIN` read plausible and stale, and `railway logs` tailed a container nothing reached, which looks exactly like a service that logs nothing. Railway is gone now and the CLI with it; the rule outlives it, because the next dead platform will read just as alive. One `curl -sI` settles it: **`via: 1.1 Caddy` = the Hetzner box**, which is now every hostname we serve, and a Cloudflare `server:` header is on all of them and says nothing. An `x-vercel-id` on one of our domains would mean something had been moved back. Do this before hunting for a service's logs or quoting an env var as live config. What made the wrong model survive so long was that the stale credentials still MATCHED, so a probe built from them worked.

**And WHAT a component does is answered by its ACTIVITY, never by its CONFIGURATION — an env var naming a peer is a statement of intent that survives the peer's death, so reading config tells you what someone once meant, not what runs.** Config is the seductive source because it is complete, instantly greppable and reads as authoritative; activity takes a query. The failure is asymmetric and lands on the destructive side: config says a thing is load-bearing, so you leave it running (cheap mistake) — or, far worse, config says a dead component is wired to a live one, so you "reconnect" it and wake a second copy of a system that is supposed to be a singleton. **Before concluding anything about a component, ask what it has DONE in the last N hours** — jobs executed, rows written, requests served, events emitted — and prefer the count over any URL. **When a probe returns a status the code can produce for two different reasons, name which one** — a gateway route answering `500 fetch failed` proves the downstream is unreachable; a `404` on a path you guessed proves nothing at all.

Cost 2026-08-08 (Railway teardown): the Railway fleet's env carried `*_SERVICE_URL = http://<svc>.railway.internal:8080` for ~20 services, so I read the Railway containers as load-bearing for campaign execution and proposed repointing Windmill's URLs at the public hostnames. Those resolve to Hetzner, so the change would have woken a **second full execution engine** beside the live one — the double-send it was meant to prevent, and only harmless today because Railway talks to stopped containers. The activity numbers said the opposite of the config: **0 Windmill jobs in 6h, 0 runs in 18h, 0 Instantly events**, against 180 runs per 30 min on Hetzner. Worst part: I had already read the worker logs, where every single pull said `empty: true` — the queue being empty was on screen, and I read "the workers are alive" instead of "the workers have nothing to do".

**Conductor workflow — create a new workspace per task, never `git checkout -b` manually.** Conductor creates each workspace as a git worktree on its own branch and fetches `origin` first, so the branch already starts from the latest remote commit. Branch prefix: `adam/<workspace-name>` (configured via `.conductor/settings.local.toml`). If the workspace has been open a while and `origin/main` has advanced, run `git fetch origin main` before creating a PR.

**Before REBUILDING an existing dashboard/admin PAGE, `git fetch origin main && git log origin/main -20 -- <page dir>` FIRST — the workspace branch lags `origin/main`, and a PARALLEL rebuild of the SAME page (or its backend contract) may have already merged.** The in-flight-workspace + `gh pr list --search` dup-checks are NOT enough: they don't surface a page rebuild that already landed on THIS repo's `origin/main`. Cost 2026-07-05 (admin CRM contacts rebuild): built the entire page + spawned a 4-repo backend fan-out against the OLD server-fetch CRM page, but `origin/main` had already merged a client-side rebuild (#2391 — `GoogleCrmClient` + typed safeParse readers) AND the backend contract landed in parallel autonomous workspaces (google-service silver + `contact-links` + `?participant`, brand-service `/internal/brands/all`, api-service proxies). Had to `git reset --hard origin/main` and rebuild the feature (contacts table + right-panel status editor) ON TOP of the merged base, adopting its readers. Two corollaries: (a) adopt the merged base's typed readers/types and EXTEND them (add `links`/`participant`/`saveContactLinks`), never re-declare a parallel set; (b) an "overlapping" in-flight workspace editing the SAME files is NOT necessarily a conflict — additive changes that merge sequentially are fine (the silver + contact-links tasks both edited `orgs-google.ts` and both merged clean), so don't alarm/discard a sibling workspace prematurely.

- **Hotfix** → branch from `origin/main`, PR → `main`, `gh pr merge --auto --squash`.
- **Bugfix / Feature** → branch from `origin/staging`, PR → `staging`. Promotion to `main` via the staging→main PR flow.
- **Exception (`apps/dashboard/` + `apps/landing/` only):** dashboard-only and landing-only PRs merge straight to `main` in recent practice. Touching ONLY those dirs → follow the last 5 merges on the file; when in doubt, staging. Cross-cutting (shared/, packages/, multi-app) MUST go through staging.
  - **A NEW app under `apps/` (anything other than dashboard/landing), or ANY edit to root `package.json` / `pnpm-lock.yaml` / `pnpm-workspace.yaml` / `turbo.json`, is cross-cutting → prefer STAGING by blast-radius POLICY (not a hook block).** ⚠️ The `guard-branch.sh` protect-main hook does NOT inspect paths/lockfiles — rule 5 only checks `git merge-base --is-ancestor origin/main "$CURRENT"` and ALLOWS `gh pr create --base main` for ANY branch descending from `origin/main`. So a new-app / root-manifest / lockfile PR to main is mechanically ALLOWED (verified #2113: removed `apps/app` + edited `pnpm-lock.yaml`, PR to main, hook passed). The staging routing is a deliberate blast-radius CHOICE for additive/cross-cutting work, NOT something the hook enforces — and for an ISOLATED change (e.g. deleting one self-contained app) where a `promote` would drag unrelated divergent commits, main-direct is the CORRECT call. Don't refuse main-direct citing a hook block that doesn't exist; decide on real blast radius. (Stale prior claim from #1561 said the hook BLOCKS this — it does not, at least on the current hook.)
- **Carve-out — copy-only `shared/content` is prod-direct-eligible.** A marketing/positioning COPY change to `shared/content/src/*` (brand.ts taglines/hero, content strings, README regen) bundled with a landing/dashboard hero edit does NOT force staging, even though it touches `shared/`. `BRAND.tagline`/`hero` and the content strings have no runtime JSX consumer (only `generate-readme.ts` reads them at build), so blast radius = text on already-prod-direct surfaces. Treat like landing-only → `main` direct on Kevin's "prod". The "Cross-cutting MUST go through staging" rule still binds when the shared/ edit changes CODE/TYPES/a runtime-imported export, not just copy. Don't pre-defend staging for pure-copy shared/content. (#1312: hero distribution→lead-gen + brand.ts; flagged staging, Kevin said "prod", shipped to main, zero blast.)
- **The protect-main hook allows dashboard→main ONLY while the branch descends from `origin/main`.** `origin/main` drifts as you work → hook flips to deny. Fix: `git fetch origin main && git merge origin/main --no-edit` (NOT rebase — force-push is blocked; a merge keeps the push fast-forward), confirm `git merge-base --is-ancestor origin/main HEAD`, push the same explicit refspec, then `gh pr create --base main`. Squash collapses the merge commit. Don't retarget staging (it's ~140 commits behind for dashboard work). (#1288)
  - **The local `origin/main` ref advances even when you never fetch it — so an `--is-ancestor` check that passed a minute ago proves NOTHING at `gh pr create` time, and a `BLOCKED: Cannot create PR targeting protected branch 'main'` right after a green check is drift, not a broken hook.** Background tooling (the RTK wrapper, a sibling worktree, `gh`) refreshes remote refs, which makes the execute skill's "do NOT `git fetch` between the merge and `gh pr create`" insufficient here: not fetching does not freeze the ref. On this repo main lands several merges an hour, so expect to run merge → push → `gh pr create` more than once. On a BLOCKED, do not re-diagnose: immediately `git merge origin/main --no-edit`, re-push the same refspec, retry the create — and re-run `pnpm --filter @distribute/dashboard test` after each merge, because what you pulled in is other people's code (a merge that brings in a new guard test is exactly the case a stale local run misses). Observed twice in three minutes on 2026-07-30 (#3098). (onboarding-email-domain-prefill)
- **`--auto` squash does NOT guarantee the merge — a PR can silently stall as `DIRTY` (merge conflict) and sit OPEN forever, prod unchanged.** When two dashboard→main PRs edit the SAME hot lines (the feature-gating `Live flags` CLAUDE.md line, `feature-gates.ts`, `context-sidebar.tsx` Database block), whichever merges second goes `DIRTY` the moment the first lands. Armed auto-merge only fires on a CLEAN+green PR; it will NOT auto-resolve a conflict. So after arming auto-merge, the monitor MUST poll `gh pr view N --json mergeStateStatus` and treat `DIRTY` (or a long-lived `BLOCKED`) as an action signal — a `state`-only watch (MERGED/CLOSED) never fires on a stalled PR and you'll falsely report "monitoring → will merge" while nothing happens. On `DIRTY`: `git merge origin/main` (resolve the conflict, usually the `Live flags` line — keep BOTH PRs' flag additions), re-push the same refspec. (#1343: armed auto-merge, PR went `DIRTY` vs #1338's CLAUDE.md edits, sat OPEN until Kevin reported "aucun changement en prod".)
- **Cut a NEW branch off `origin/main` before each change — never commit onto the just-merged branch.** `git fetch origin main && git checkout -b KevinLourd/<topic> origin/main`. Recovery if you slip: `git checkout -b <new> origin/main && git cherry-pick <sha>`. The branch tracks `origin/main` → push explicit refspec `git push origin HEAD:refs/heads/X` to dodge the hook. (#1248)
  - **Uncommitted working-tree edits AUTO-FOLLOW `git checkout -b NEW origin/main` — do NOT then `git checkout - -- <path>` to "carry" them.** `checkout -b` keeps your dirty edits on the new branch already. Running `git checkout - -- <path>` afterward restores that file from the PREVIOUS branch's committed HEAD, silently REVERTING your uncommitted edit (the Edit tool's in-memory file-state still thinks it's applied → a re-Edit fails "File has not been read"; re-Read shows the old code). After `checkout -b`, just `git diff --stat` to confirm the edit rode along, then commit — never re-checkout the file. (#1966: lost the edit this way, had to re-Read + re-apply.) **And the recovery reflex is worse than the mistake: copying the file out to `/tmp` and copying it back after the branch switch CLOBBERS whatever the new base added to it.** `origin/main` moves while you work (a sibling workspace merges), so a whole-file restore from a snapshot taken against the OLD base silently deletes their section — the diff reads as pure deletions, which is the tell. Re-apply the EDIT against the fresh file, never restore the FILE. Caught at `git show --stat` (`1 file changed, 9 deletions(-)` on a commit whose message promised an addition) 2026-08-24, one commit away from deleting a just-merged sign-in section. `git reset --hard` is hook-blocked, so the recovery is `git checkout -b <new> origin/main` + re-edit.
- **Chain `git commit && git push` with `&&`, never `;`/newline — Conductor worktrees hit `index.lock` contention.** A concurrent git process (other agent, IDE) can make `git commit` fail with `Unable to create '.git/worktrees/<ws>/index.lock'`; if the next line is an unconditional `git push`, it pushes the **un-advanced HEAD** → an empty branch at `origin/main`, and the `* [new branch]` output reads like success. Always `git commit … && git push …` so the push only fires on a committed HEAD; if you hit the lock, `rm -f .git/worktrees/<ws>/index.lock`, re-commit, verify `git log --oneline -1` advanced, then push. (#1367)
- **Base-branch prerequisite check before `checkout -b … origin/staging`:** if the feature builds on code merged to `main` but not yet promoted to `staging` (typical for dashboard), cut from `origin/main`. Check: `git log origin/main ^origin/staging -- <path>` returns commits → prereq missing on staging. (#1186)
- **`release.sh promote` conflict here is usually BIDIRECTIONAL divergence (main +N unique, staging +M unique), NOT the release-skill's "staging is a clean superset" case.** Because dashboard/landing PRs merge **main-direct** (the carve-out), `main` routinely drifts ahead of `staging` with commits staging never got (admin/Google-Ads/design-system/$5-credit), WHILE staging carries its own feature backlog → a `promote` PR goes `DIRTY`. The release-skill's `--ours` fast-forward recovery is WRONG here (it would drop main's unique work). Correct recovery = **merge main INTO staging, resolve per-hunk by INTENT, push staging (now a true superset), re-promote**: (1) `git checkout -B sync/promote-resolve-vX origin/staging && git merge origin/main`; (2) resolve each conflict by the NEWER intended direction — for landing/app COPY the staging reframe wins (the de-channel/de-email rewrites #1581-1615 are deliberate; main-sides are often EMPTY = staging-added blocks, or the older copy the reframe removed); **keep HEAD per-hunk** (`sed -e '/^/,/^>>>>>>> origin\/main$/d'`), do NOT `git checkout --ours <file>` (that discards main's non-conflicting auto-merges inside the same file — e.g. main's CLAUDE.md Google-Ads doc); (3) verify markers 0 + `apps/dashboard` tsc + a dangling-import grep (an `icons` import dropped with its usage must hit 0 refs); (4) `git push origin sync/...:staging` (staging not protected → allowed), confirm `git merge-base --is-ancestor origin/main origin/staging`; (5) `gh pr close <stale-promote-PR>` + re-run `release.sh --yes promote distribute.you`. CI on the fresh promote PR gates the resolution. (2026-06-14, v0.80.0: 4 conflict files, all resolved keep-HEAD toward the staging reframe; the re-promote then merged clean.)

## Architecture

**Monorepo** — pnpm workspaces + Turborepo. Roots: `apps/`, `packages/`, `shared/`.

**Apps:** `apps/dashboard/` (port 3001, Next.js 15 App Router, Clerk) · `apps/docs/` · `apps/landing/` · `apps/sales-cold-emails-landing/`.

**Extracted services (separate repos):** [api-service](https://github.com/shamanic-technologies/api-service) · [mcp](https://github.com/shamanic-technologies/mcp).

**Packages (published MCP servers, tsup ESM):** mcp-sales-outreach · mcp-google-ads · mcp-influencer-pitch · mcp-journalist-pitch · mcp-podcaster-pitch · mcp-reddit-ads · mcp-thought-leader (all `@distribute/*`).

**Shared libs:** `shared/auth` · `shared/content` · `shared/pictures` · `shared/runs-client` · `shared/types`.

**Content sync:** all marketing/docs content lives in `shared/content/src/` (`urls.ts`, `mcps.ts`, `pricing.ts`, `features.ts`, `brand.ts`); public surfaces import `@distribute/content`. When changing: update src → `pnpm generate:readme` → verify build → commit the regenerated README. **README.md is GENERATED — never edit directly.**

**Landing copy lives in MULTIPLE surfaces — a copy sweep MUST grep ALL or it ships half-done.** The *served* prod landing is the STATIC set `apps/landing/public/landing/**.html` (`apps/landing/src/app/route.ts` → `staticResponse` via `static-html.ts` — ~20 SEO pages: index/index-pro/index-agency, performance, pricing, how-it-works, use-cases + `cold-email-*` sub-trees), NOT the React `apps/landing/src/**` components alone. A grep of `src/` only misses the served HTML (sign-up buttons ×27, hero, `<meta>`/OG/twitter, JSON-LD, animated stat-counter, price-checks). **`/` serves `index-agency.html`, `/pro` serves `index-pro.html`, `/agency` serves `index-agency.html`** — the default homepage is the AGENCY variant, so verify `/` against `index-agency.html` (the variants drift: #2032 found agency's `Organization` JSON-LD missing `sameAs`/`legalName` while pro+index had them). **GEO/AI-search surfaces are part of the sweep too: `apps/landing/public/llms.txt` (curated index) AND the inline JSON-LD (`SoftwareApplication.description`, WebSite description).** Also sweep `apps/docs/src` + `shared/content/src`. When reframing a word/claim across the landing, `grep -rl '<term>' apps/landing/public apps/landing/src apps/docs shared/content`. Watch for a SECOND meaning of the same token. (#1516, #2032)

**Product is COLD EMAIL ONLY — NOT multi-channel.** Do NOT describe distribute as running PR / hiring / VC / journalist / podcaster / influencer / Reddit Ads / Google Ads as product channels. `llms.txt` + JSON-LD `SoftwareApplication.description` drifted multi-channel for months (#2032 de-channelled them). The product is cold email outreach done for you (agency-domain model). The cold-email-vs-linkedin SEO pages discuss "multichannel" as a topic — that's fine content, NOT a product claim. (Kevin 2026-06-22: "We are not multi channel.") **The React LEGAL pages `apps/landing/src/app/terms/page.tsx` + `privacy/page.tsx` are part of the positioning sweep too — they drift independently of the served HTML/llms.txt/JSON-LD and were the multi-channel STRAGGLER (Terms §3 Description, §5 Outreach, §8 third-party still listed PR/VC/hiring/journalist + LinkedIn/X/social months after #2032 de-channelled everything else; de-channelled #2084). Grep them on any positioning reframe.** (#2084)

**The PRODUCT is a productized ACQUISITION AGENCY that sells SALES MEETINGS at a MEASURED cost. Cold email is the CHANNEL, never the category — describing distribute by its channel on any public surface files it under tooling and is a positioning bug.** The rule above ("cold email only, not multi-channel") is about which CHANNELS we claim; this one is about what we SELL. Both hold. Concretely, on every public surface (`meta`, `og`, JSON-LD `SoftwareApplication.description`, FAQ, footer, `llms.txt`): the offer is named **Autonomous Sales Meetings Acquisition**, we are an agency that runs the campaign on the client's behalf, and the deliverable is an interested reply from a buyer. Never "a cold email tool", never "done-for-you cold email outreach" as the self-description. Three facts that must survive any rewrite:
- **The cost per meeting is MEASURED, never QUOTED.** We invest the client's budget through paid APIs, take a margin, and report what each interested reply actually cost. The only legitimate figures are the live cross-client rate (`__CAC_PRICE__`) and the client's own stats. NEVER write "predictable cost", "a cost you know in advance", or any phrasing implying a rate card. The differentiator is that we are the ones who measure it, not that we can promise it.
- **THE COST IS THE CLIENT'S, NEVER OURS. This is the one that gets inverted.** The client authorises a daily budget cap and is charged **the budget the campaign spent, and nothing else** — one number, no breakdown. Our margin (fixed, 75%) sits INSIDE that spend and is invisible to them. So every "$X per interested reply" on any surface is **a price the client pays**, and we have no cost-per-meeting to show a buyer. Banned: "what a meeting costs us", "our cost per reply", "we deliver a reply at $X" (reads as our rate). Correct: "what it cost you", "what our clients are paying", "a positive reply costs $X". Equally banned in the other direction: **"at cost", "pass-through", "no markup", "you only pay for X"** — all false, the margin is in there. "You pay the budget you authorised, nothing else" is true and is the actual selling point (no seat, no retainer, no subscription). Cost 2026-07-29 (#3059 → #3064): shipped the ROI calculator headlined "against what a meeting **costs us**"; Kevin: "non ce n'est pas cost us, c'est cost au client!!! Mais tu me fais peur… reformule le business model." An audit found exactly one offending phrase page-wide (mine) — the rest already said "cost you" — but the inversion was live on the homepage. Guarded by `homepage-offer-story.test.ts` (prose-only: a whole-file `margin` check fires on the inline stylesheet's `margin:` declarations).
- **Do NOT qualify the live rate with "best model" / "best rate" / any model vocabulary.** Every client is served the best model, so the number needs no qualifier — it is simply what it costs. A "best rate" hedge is worse than useless: it invites "so which one will I get?" for nothing, and it leaks the workflow-model detail the landing deliberately hides (Kevin 2026-07-29: "ne rentrons pas dans le détail du meilleur model blabla. On donnera au client le meilleur model de toute façon"). The producer-side rule that the fleet figure is cross-org + BEST workflow (never pooled) is unchanged — it just never surfaces as words on the page.
- **The software is OPTIONAL.** A client who never wants to log in can send their website and we set the whole thing up at the same price, with interested replies arriving in their normal inbox. This is a load-bearing objection-killer; do not let a rewrite make the dashboard sound mandatory.
- **The guarantee is on the MEASUREMENT, not the result** (owner-picked, 2026-07-29). Copy may commit to: the real cost always visible, the whole picture always visible, spend always controlled by a daily cap and an immediate pause, and our own numbers published. It may NOT promise meetings. The FAQ's "results are not guaranteed" stays.
(Kevin 2026-07-29: "on vend du sales meeting. A du cout predictible. On est des experts du CAC" then the correction "les gens ne connaissent pas leur CAC exactement en avance… la seule chose qu'on peut donner ce sont les moyennes des autres clients et leurs propres stats".)

**Homepage (`index-v1.html`) is an OFFER ARGUMENT, one idea and one PRO VISUAL per section, in story order — do NOT re-add a text-only section.** Rebuilt #3051 after an audit against the Hormozi value equation the product itself sells to clients: 5 of the 8 sections had been pushing perceived-likelihood, time-delay had NO section, and the value stack was a list with no prices. Current order and what each section OWNS: hero (dream) · `#testimonials` (the real dream, Doc Dinners **$50 per sales meeting, 10x ROI**) · `#proof` (price anchor vs the $700 market meeting) · `#measure` (per-segment cost of acquisition, dashboard preview) · `#engine` (**the split-and-test tree** — sub-segments by geography/size/role, A/B angles inside each, Hormozi-built templates) · `#how` (30-second setup) · `#speed` (time before the first email) · `#stack` (what's included, priced against documented market rates) · `#pricing` (what you risk + the measurement guarantee). Rules when editing:
- **`#engine` is the section that justifies the price.** It was absent from the site for months while being the entire reason a reply costs what it does. Never delete it to "simplify".
- **A number shown twice on the page must come from ONE token.** The `#measure` dashboard headline reads the same `__CAC_PRICE__` as the hero, so the two cannot diverge. Do not hardcode a second copy.
- **`#measure`'s three segment rows read that same rate, and their BAR LENGTHS are decorative — leave them at `38% / 61% / 92%`.** Each row states its own cost per reply (`__SEG_COST_LOW__` / `_MID__` / `_HIGH__`, built by the unit-tested `segmentCostBand` as the live rate ∓ $5, low leg clamped above zero) — a hardcoded price there would freeze while the headline kept moving, so the same card would end up disagreeing with itself. The bars are NOT a scale for those prices, and making them proportional was considered and rejected (owner-picked): at a ±$5 spread the three would render nearly identical and the row would lose its shape. So this is the case where a length that does not track the number beside it is intentional, NOT the self-contradictory-surface bug — do not "fix" it. Guarded by `segment-cost-band.test.ts`. (#3134)
- **Market-rate figures are only the DOCUMENTED bands** (agency $1,500-5,000/mo + $80-200 per qualified reply, SDR $4,000-7,000/mo + $50-120, $700 per booked meeting, 2-4 weeks DIY setup, 3-6 months SDR ramp). There are no documented per-TOOL prices, so the stack prices the ALTERNATIVES, not the individual tools. Do not invent a price for "a lead database" or "a warmup tool".
- **This page carries its CSS INLINE and links no `css/styles.css`**, so the `?v=N` cache-buster rule does not apply to it (it does apply to its sibling static pages). Guarded by `homepage-offer-story.test.ts`.
- The four-model comparison grid (`#category`) and the post-launch step list were deleted as text-only sections carrying no idea of their own. The nav's "Why distribute.you" now points at `#engine`. (#3051)

**Time-to-first-interested-reply on the landing = `__FIRST_REPLY_LINE__`, an OBSERVED CEILING that REMOVES ITSELF below a sample floor.** Source is the already-proxied public endpoint `GET /v1/public/features/workflow-engagement-latency?featureSlug=&groupBy=workflow` (per-workflow `timeToFirstPositiveReply.{medianMs,sampleSize}`); the pure phrase builder is the exported, unit-tested `firstReplyCeilingPhrase` in `static-html.ts`. Four rules it encodes, all load-bearing:
- It reports the **MAX of the per-workflow medians** phrased as an upper bound ("in under a week"), never an average and never the best model's figure. A single fast model is not a promise; the ceiling is what a buyer can rely on.
- A workflow with `sampleSize: 0` is DROPPED, same husk filter as `fetchBestModelUsd` — a model that produced no interested reply has no latency to report.
- Below the floor (`MIN_FIRST_REPLY_MODELS = 2`, `MIN_FIRST_REPLY_SAMPLE = 5`), or when the fetch fails, the phrase is `null` and **the whole sentence is removed from the page**. Do NOT add a fallback figure: "we could not measure this" and "it took N days" are different statements.
- Fetch bounded `AbortSignal.timeout(8_000)` + gated on the token being present, like every other build-time landing fetch, so a cold endpoint cannot blow the prerender budget.
⚠️ **`timeToFirstLinkClick` is NOT usable as-is**: six workflows report medians within 0.3% of exactly 3.00 days, which is a scheduled-followup artifact rather than organic latency. Do not put click latency on a public surface until that is explained. (#3051)

**OPENS ARE DEPRECATED — distribute no longer MEASURES nor COMMUNICATES open rates.** Apple Mail Privacy Protection made opens meaningless, so open-rate / "opened" / opens-count are DEAD metrics: NEVER show an open rate, an "Opened" funnel step, an opens KPI, or any opens number on ANY surface (landing, dashboard, stats, OG, JSON-LD). The success funnel is Sent → Click → Positive reply; the headline rate is the POSITIVE-REPLY rate. The `__OPEN_RATE__` / `__OPENED_PER_HUNDRED__` tokens + the `openRate*` / `opened*` fields in `apps/landing/src/lib/static-html.ts` are LEGACY — do not add new uses (existing ones are being removed). SEO articles about subject lines/opens must reframe around reply rate, not open rate. (Kevin 2026-07-12: "on ne mesure ni ne communique plus sur les opens".)

**NO INVENTED PEOPLE AND NO INVENTED NUMBERS on any public surface — a landing figure is either a LIVE server-injected token, a sourced third-party benchmark, or a real named customer.** Fabricated testimonials + fake campaign data shipped on the landing for months and were being scraped back by LLMs as if they were real customer results: 6 invented personas (Sara K. / Marcus T. / Priya N. / Tom H. / Alex R. / Julia M.) on the `/performance` proof slider, 3 of the same on `/use-cases` plus per-persona campaign data (a `solo.fm` funnel 47/6/4/3, a Monday→contract agency timeline, freelancer KPIs 4 calls/$25/$6.25, a wholesale grid 2 accounts/$34/3.1%/+72%) and hardcoded stats bands. Removed in waves (archive `/v2` + the freelancer card first, then `/performance` + `/use-cases` in #3030). Rules:
- **The live tokens work on ANY statically-served page**, not just `/performance` — `withLivePerformanceMetrics` (`static-html.ts`) is gated only on the token being PRESENT in the html. Available: `__BEST_POSITIVE_REPLY_COST__` (+ `_NUMERIC__`), `__POSITIVE_REPLY_RATE__` (+ `_NUMERIC__`), `__EMAILS_SENT__` (+ `_NUMERIC__`), `__POSITIVE_REPLIES_PER_HUNDRED__` (+ `_NUMERIC__` / `_RANGE__` / `_BAR_NUMERIC__`), and the CAC family `__CAC_PRICE__` / `__CAC_MULT__` / `__CAC_BOOT_SLOT__`. A new page that needs a number uses these, never a literal.
- **A hardcoded literal that happens to match a live value is still a bug** — the `2.1%` / `1-2` / `14k` literals in the `/performance` funnel were stale copies of the `FALLBACK_LIVE_PERFORMANCE_METRICS` constants, so they froze at whatever the numbers were the day they were written. Wire the token.
- **Competitor / market figures are fine when they are the documented benchmarks** ($4–7 per click, $70–250 per positive reply, agency $80–200, SDR $50–120, $700 market booked meeting) — those are the landing-billing-truth ranges, not our own invented results.
- **The only real testimonials are Katherine Fleishman and Ryan W.D. Parenti (Doc Dinners), with photos under `public/landing/testimonials/`.** Reuse those; do not write new ones.
- A "results / social proof / what people are getting" section with no named, verifiable person is the tell. Delete it rather than soften it. (#3030)
- **The rule targets a figure presented as a RESULT, not a self-evidently illustrative product PREVIEW.** The hero console renders `yourcompany.com` beside a made-up offer, so nobody reads its numbers as a customer's; calling one of them a "no invented numbers" violation is a false positive, and it burns a round (Kevin 2026-07-30: "FAUX c'est illustratif"). It also does not license a visible "illustrative" disclaimer — the landing is not a legal document, readers are not fools, and the hedge is itself the AI-tell ("Pense Apple, pense SIMPLE"). What DOES apply to a preview is internal coherence: see the undated-figure rule below.

**Two numbers can only CONTRADICT each other when they share a period — an undated figure is not multipliable, and calling it incoherent is pedantry that costs a round.** A card reading `$10 / day` beside `126 positive replies` looks like it implies 907 days at the live `$72` a reply, but `126` carried no timeframe, deliberately, so there was nothing to divide and no contradiction to fix. Check the UNITS on both sides before invoking the self-contradictory-surface rule. **The moment you attach a period, the arithmetic becomes checkable and it must actually hold**: the hero states the budget DAILY and the outcome MONTHLY (the house convention, and the Google-Ads asymmetry — `$50/day` feels small, `$1,550/month` is the same money), so the pair is now derived from ONE place, `HERO_DAILY_BUDGET_USD` × `HERO_MONTH_DAYS` ÷ the live cost per positive reply, and it follows the rate down as the rate improves. The asymmetry does not license a false ratio: Google quotes the monthly volume from the real CPA, and so must we. Match the onboarding budget picker's grain too (`$/day = count × unit cost / 31`) so a visitor meets the same two numbers thirty seconds later at signup instead of a different couple. When the rate cannot be graded, `heroMonthlyOutcomes` returns null and the whole row is dropped — "we could not measure this" and "it buys N" are different statements. Guard: `tests/unit/hero-console.test.ts`. (#3112)

**Re-pricing a headline figure is NOT a find-and-replace: every number DERIVED from the old one moves with it, and a derived PAIR routinely conceals a per-unit price that public copy is banned from quoting.** The offer figure appears twice on the landing: as the offer itself (a CTA, a FAQ, a meta description, where the swap really is mechanical) and as an EXAMPLE BUDGET inside an SEO article, where the sentences around it were computed from it. Swapping only the dollar figure leaves *"$400 reaches 350 contacts"*, *"14 replies at 4%"* and an ROI table keyed on *"deals from a $400 campaign"* wrong by exactly the ratio you just applied, and the page contradicts itself in a way no guard catches. Worse, `$25 ÷ 350 contacts` IS our per-contact price, which the billing-truth rule forbids on any public surface, so the honest repair is not to rescale the pair but to **restate it in outcome terms at the documented band** ($4-7 per click, $70-250 per positive reply). That fixes the re-price and the pre-existing violation in the same edit. Two things to check before calling such a sweep done: any table row whose LABEL is a *minimum* ("minimum viable budget", "cost to start") must NOT take the new offer figure (the minimum is `From $1/day` and always was), and a `grep` for the OLD figure will miss the derived numbers entirely because they never contained it. Cost 2026-07-31 (#3194, $25 → $400): ~15 article passages carried derived arithmetic, and one comparison page also priced a positive reply at `$3` seven lines above a table listing `$70-250`. (#3194)

**`sitemap.ts` hardcodes its static entries → it DRIFTS as static pages are added.** `apps/landing/src/app/sitemap.ts` lists static URLs by hand (+ blog rows from DB). Any NEW statically-served page (`src/app/<page>/route.ts` → `staticResponse`) MUST be added to the `STATIC_SEO_PATHS` array there, or it's live + indexable but absent from the sitemap → slow/partial indexing. #2032 found 22 such missing pages (the whole `cold-email-*` cluster + pro/agency/how-it-works/use-cases/privacy/perf-subpages). Keep `STATIC_SEO_PATHS` in lockstep with the `public/landing/**.html` set. (#2032)

**`SITE_NAME` in `apps/landing/src/lib/seo.ts` REWRITES the served Organization JSON-LD of every statically-served page, so the file on disk and the bytes a crawler receives can disagree — and the disk is the one that lies.** `staticResponse()` runs `withCanonicalOrganization`, which parses each page's `ld+json`, DELETES any hand-written `Organization` node and injects `organizationJsonLd()` (built from `SITE_NAME`) in its place. That is the design — one company, stated once — but it means editing the `"name"` inside `public/landing/**.html` accomplishes nothing, and reading it there tells you nothing about what ships. All 22 static pages served `"name":"distribute"` for as long as that constant said so, while every one of those files already read `"distribute.you"`. Same for the root layout's `title.template`, which suffixes every React page title. Generalise: **when a meta or JSON-LD value looks right in the repo and wrong in the wild, look for a request-time rewriter before doubting the crawler** — and verify any change to it by reading the response off a real `next start`, never by grepping the source. (#3603)

**An exclusion written to protect a FILENAME also hides that file's CONTENTS from the sweep, and the tell is that the excluded path is where the thing you are renaming actually lives.** A rename sweep needs a filter — `logo-distribute.svg` is an identifier and must not change, so `grep … | grep -vE 'logo-distribute'` reads as exactly right. It is not: `grep -v` drops the whole LINE, and for a file whose PATH carries the token every line is dropped, so the file is never read at all. #3603 swept `apps/landing` that way and left six SVGs whose `aria-label` still said the old name — three of them under `public/landing/logo/`, i.e. the very directory the filter named. Two habits that catch it: exclude on the MATCH (`(?!\.you|_you)` in the substitution) rather than on the path, so a protected filename survives while its contents are still inspected; and after any sweep, re-grep for the bare token with NO exclusions and read every hit, because the ones a filter removed are the ones nobody looked at. Cost 2026-08-27: a nine-file follow-up (#3611) for a sweep reported complete, three of whose files were byte-pinned across apps and could not be fixed from the landing PR anyway.

**Corollary — a byte-equality pin between two apps makes a one-app fix impossible, so find the pin BEFORE scoping the PR.** `git grep -l "$(basename <file>)"` across `apps/*/tests` costs nothing and tells you whether the file you are about to edit is asserted from somewhere else. `brand-favicon.test.ts` (in `apps/dashboard`, reading `apps/landing`) is the live example; there may be others.


**Static landing assets (`public/landing/css/*.css`, `js/*.js`) are linked with a MANUAL `?v=N` cache-busting query (e.g. `<link href="css/styles.css?v=5">`) — editing the CSS/JS WITHOUT bumping `?v=N` ships nothing visible.** The `?v=N` URL is its own edge + browser cache key with a long-lived `x-vercel-cache: HIT`; a new deploy updates the asset bytes but returning visitors (and the Vercel edge) keep serving the OLD object at the unchanged `?v=N` URL. Tell: a `?cb=<random>` fetch (cache MISS → origin) shows the new bytes while the exact `?v=N` URL the page links shows the old ones — looks like "the fix didn't deploy" but it's stale cache. So ANY edit to `public/landing/**` CSS/JS MUST bump the `?v=N` token on EVERY HTML that links it (grep `styles.css?v=` across `public/landing/*.html` — the token can differ per page; index-agency was `?v=5` while index/index-pro link bare `css/styles.css`). Verify post-deploy that the homepage's linked `?v=N` URL itself serves the new bytes, not a `?cb=` probe. (#2206 shipped the CSS change but forgot the bump → "aucun changement de taille"; #2211 bumped `?v=5`→`?v=6`.)

⚠️ **A TEST pins those tokens, and it lives in ANOTHER APP: `apps/dashboard/tests/welcome-credits-promise.test.ts` asserts the exact `?v=N` on every page that links `main.js` or `pricing-modal-v1.js`, reading them out of `apps/landing/public/landing/`.** So a correct bump turns that file red, in a suite whose name is about signup credits — read the assertion before assuming the bump was wrong, and move it in the SAME commit. That coupling is deliberate (it is what stops a JS edit shipping invisibly), but the two halves are far apart, so a bump done without touching the test reads as a broken guard and a test edited without the bump reads as green. Both move together or neither does. (#3482) **This line said `apps/landing/tests/unit/` until 2026-08-27, and the wrong path is worse than no note: a `grep` of `apps/landing/tests` comes back empty, which reads as "no guard exists" rather than "wrong app", so the bump ships and `test-dashboard` — a CI merge gate — goes red on a landing-only PR (#3603). When a note in this file names a path, the path is an assertion like any other; `git grep <test-name>` across `apps/` before concluding the guard is absent.**

**Each served landing HTML (`public/landing/**.html`) carries a LARGE inline `<style>` block (~8.5KB in index-agency.html) whose page-scoped rules OVERRIDE `css/styles.css` by specificity — so editing `styles.css` can ship ZERO visible change because an inline rule wins the cascade.** index-agency.html's inline `.hero .t-hero { font-size: 4rem }` (specificity 0,2,0) beats `styles.css`'s `.t-hero` (0,1,0) and pinned the agency homepage H1 at 64px on every viewport even after the `styles.css` clamp + `?v` bump landed. So when a landing visual edit "doesn't take", grep the page's OWN inline `<style>` for the selector BEFORE touching `styles.css` — the override usually lives there. **Verify a CSS landing fix by the RENDERED COMPUTED value, never by grepping the CSS file.** A grep of `styles.css` confirms only that ONE source has the rule, not that it WINS — three rounds (#2206/#2211) were burned verifying the file instead of the render. Headless measure (Playwright is a devDependency of `apps/dashboard`/`apps/admin`): `node` a script that `goto`s the live URL at 390/768/1280px and reads `getComputedStyle(document.querySelector("h1.t-hero")).fontSize`; to find WHICH rule wins, iterate `document.styleSheets` rules + `h.matches(rule.selectorText)`. That surfaces an inline-`<style>` or higher-specificity override the file-grep can't. (#2217: `.hero .t-hero` inline rule was the real culprit; clamp only worked once the INLINE rule was switched to `clamp(2.25rem, 8vw, 4rem)`.)

**Landing nav + footer are injected via `apps/landing/public/landing/js/components.js` (raw JS), which BYPASSES the `static-html.ts` `rewriteHtml` pass — so any `href` there MUST be an absolute URL, never a relative `/path`.** `route.ts` → `staticResponse("index.html")` runs `rewriteHtml` only on the HTML; it rewrites the HTML's relative `/sign-in` / `/sign-up` / `/docs` → absolute (`URLS.*`). The nav/footer markup lives in `components.js` `NAV_HTML` + `FOOTER_HTML` and is served as a static `.js` asset → NOT rewritten. A relative `href="/sign-in"` in `components.js` therefore resolves against the LANDING origin (`distribute.you/sign-in` → **404**, landing has no such route), not the dashboard. Auth/cross-app links in `components.js` must be hardcoded absolute (`https://dashboard.distribute.you/sign-in`). When auditing landing auth/CTA links, grep BOTH the HTML (`public/landing/**.html`) AND `public/landing/js/components.js` — the investigator's reflex (React `navbar.tsx` + HTML) MISSES the served nav. (#1569: navbar/footer Sign in + Start free 404'd because the 6 links were relative `/sign-in` `/sign-up` in `components.js`.)

**JS-INJECTED internal links = SEO orphans — the crawler-link-graph twin of the components.js absolute-URL rule.** Ahrefs / GPTBot / ClaudeBot parse RAW HTML only, so any internal `<a href>` that exists only after JS runs is INVISIBLE to the link graph → the target reads as an orphan page (0 incoming internal links) even though it's linked in-browser. TWO such injectors on the landing: (1) `js/main.js` "Related articles" grid — every article page ships `<div class="post-related-grid"></div>` **empty** and JS fills it with a **random 3**; (2) `js/components.js` nav/footer (`/privacy`, `/pro` links). So a NEW cluster spoke page under `public/landing/**` is BORN ORPHANED — its only inbound links are the JS-random grid. Fix pattern (KevinLourd/landing-orphan-internal-links): bake **static `<a class="post-related-card">` cards into the raw HTML** grid (hubs → all their spokes, spokes → same-cluster siblings) + guard main.js `if (!grid || grid.children.length) return;` so JS only fills EMPTY grids (static links win, progressive-enhancement); add any nav/footer-only orphan (`/pro`, `/privacy`) a static inlink in served HTML (a "More from distribute" strip in `index-agency.html`, the served `/`). RULE: any new statically-served article/cluster page MUST get a static in-HTML internal link from its hub (and vice-versa) in the SAME PR — do NOT rely on the JS related-grid for internal linking. Verify with `curl -sA GPTBot <url> | grep -c 'href="/<slug>"'`, never a browser render. Copy in new cards is user-facing → em-dash-free. (Ahrefs orphan report, #2350)

**`/pricing` is served STATICALLY** (`public/landing/pricing.html` + `src/app/pricing/route.ts` → `staticResponse`), NOT a React page — same as `/performance` and `/how-it-works`. Do NOT add a `page.tsx` under `src/app/pricing/` (it collides with `route.ts` → build error). The budget calculator's JS is in the shared `public/landing/js/main.js`, its CSS inline in `pricing.html`. `lib/pricing/fetch-prices` + `components/pricing/provider-tables-async.tsx` are orphaned, kept for a possible live-cost re-add (`pricing-group-format.test.ts` still covers the lib). The landing source of truth is `apps/landing`; the orphan-history `origin/website` branch should be retired. (#1543, #1574)

**Google Ads conversion tracking is split across FOUR injection points + TWO fire sites — touch the wrong subset and you ship attribution that silently undercounts.** Tag `AW-18233267088`. The LANDING is a pure **gclid carrier** (loads the tag + auto conversion-linker so the `_gcl_aw` cookie lands on the registrable domain `.distribute.you`, shared cross-subdomain) and fires **NO conversion event** — a CTA click is not a conversion. The real conversions fire in the DASHBOARD where the events actually happen. The tag must be present on all SEVEN+ landing surfaces AND the dashboard, so it lives in FOUR places: (1) `apps/landing/src/lib/static-html.ts` `analyticsHead()` — covers every statically-served SEO page (home + the cold-email cluster) in ONE spot; (2) `apps/landing/src/app/layout.tsx` — the React landing pages; (3) `apps/dashboard/src/app/layout.tsx` — alongside GA4. Conversions fire from: (4a) `apps/dashboard/src/components/posthog-auth-tracker.tsx` → `manual_event_SIGNUP` on the same once-per-user signal as PostHog `signup_completed`; (4b) `apps/dashboard/src/components/ads-purchase-tracker.tsx` → `manual_event_PURCHASE` on Stripe Checkout return (`?success=true` = card added / top-up paid, from any launch surface). The purchase tracker is **fire-only** — the launch pages (`brands/[brandId]/launch`, `billing`) already read `pending_topup` + strip the return params to arm auto-topup, so stripping in the tracker would RACE that logic; dedup against refresh via `sessionStorage`, never by stripping the param. `manual_event_*` is Google Ads' auto-generated event-name convention — the string is arbitrary but MUST be byte-equal to the conversion action's event name in the Ads UI (a code-only change does nothing until the matching Ads conversion action exists). Do NOT re-add a click→conversion on the landing (false signal, double-counts the real card-add). GTM is intentionally NOT used (the old `GTM-TBD2WC2P` was a never-configured placeholder, removed). (#1617)
  - **Conversion VALUE — the campaign bids "Maximize conversion value", so every value-bearing conversion MUST send `value` + `currency` or bidding optimizes on COUNT against a flat default, not real revenue.** PURCHASE value = the **1-day budget the user picked** (recurring per-day commitment), NOT the one-off charge: the onboarding launch `success_url` carries `daily_budget` (dollars) and the billing top-up `success_url` carries `paid_amount` (cents). `ads-purchase-tracker.tsx` sends `value` = `daily_budget` (dollars) else `paid_amount/100`. **It fires ONLY when a positive payment value is present (= only after a real payment succeeded); a $0 card-add / setup return does NOT fire.** Both params exist only on a payment-mode checkout whose `success_url` was reached, so their presence proves payment went through; billing-guard card capture is an EMBEDDED checkout (inline `onComplete`, no `?success=true` redirect) so it never reaches this tracker. (The prior design read `pending_topup` and fired VALUELESS on the onboarding launch — that success_url never set `pending_topup` — so the top conversion sent no value; fixed KevinLourd/ads-purchase-daily-budget #2643.) SIGNUP has no instant amount → stays an upper-funnel conversion with a FIXED value set in the Ads UI (no code value). **Code-only is not enough: each Ads conversion action sending a value MUST be set to "Use different values for each conversion" in the Ads UI, else the sent `value` is ignored** (mirror of the byte-equal-event-name rule — the Ads UI is the other half). Both trackers originally fired valueless for months → max-conv-value bidding had nothing to optimize on. (#2002)

**PostHog runs through the first-party reverse proxy `e.distribute.you` to beat ad-blockers (`*.posthog.com` blocklists drop events + session recordings).** It's PostHog's FREE **Managed Reverse Proxy** (CNAME `e.distribute.you` on Gandi LiveDNS → `…cf-prod-eu-proxy.europehog.com`, SSL auto-issued by PostHog — NO Cloudflare Worker, NO Vercel egress). `NEXT_PUBLIC_POSTHOG_HOST=https://e.distribute.you` on ALL FOUR apps (dashboard/admin/landing/sales) → `instrumentation-client.ts` `api_host` reads it; each `posthog.init` ALSO sets `ui_host: "https://eu.posthog.com"` so PostHog UI/toolbar links still resolve while `api_host` points at the proxy. **A NEW app that loads posthog-js MUST set the same env + `ui_host`** or it sends to a bare host that ad-blockers nuke. Ops are 100% API: proxy via the posthog MCP (`proxy-create`/`proxy-get`/`proxy-list`); DNS = Gandi LiveDNS (distribute.you is owned by `GANDI_ORG2_TOKEN`, Bearer auth); app env in `/root/distribute/env/<app>.env` on the box. **`NEXT_PUBLIC_*` is build-inlined → it lives in `env/<app>.build.env` and only a REBUILD picks it up (`./deploy.sh <app> --force`); recreating the container does nothing.** `outcome-digest.ts` (server-side) also reads the same env and rides the proxy (forward is fine; no ad-blocker server-side). Verify the proxy with `curl https://e.distribute.you/static/array.js` (recorder, 200) + `…/flags/?v=2` (ingestion, 200), not by grepping config. (KevinLourd/posthog-reverse-proxy, #2316)

**Partnero affiliate referral tracking (program `KHV3KEHI`) is SERVER-TO-SERVER, split across landing + dashboard — NOT the cookie / Stripe `client_reference_id` method.** Signup happens on `dashboard.distribute.you`, a DIFFERENT subdomain than the `distribute.you` referral landing, and Partnero has no cross-subdomain cookie (their own docs prescribe S2S for a separate signup domain), so the front-end `po('customers','signup')` cookie path would silently attribute 0. The wired design:
- **Landing carries + forwards the partner key** — `apps/landing/src/lib/static-html.ts` `partneroHead()` (static SEO pages, injected via `analyticsHead()`) + `apps/landing/src/app/layout.tsx` (React pages) BOTH load PartneroJS (records the click, keeps the key in the `partnero_partner` cookie on the landing domain) AND a delegated capture-phase click listener that appends `?via=<key>` to every `dashboard.distribute.you`-bound link (the cross-subdomain carrier). Key read from the `via` URL param OR the `partnero_partner` cookie. A tracking/analytics sweep that strips landing `<head>` scripts MUST keep both or referred signups stop crediting.
- **Dashboard registers the customer S2S** — `apps/dashboard/src/components/partnero-via-capture.tsx` (mounted in root `layout.tsx` body) persists `?via=` → `partnero_via` cookie (survives the Clerk OAuth redirect); `posthog-auth-tracker.tsx` fires ONCE on signup (same once-per-user guard as the GA `manual_event_SIGNUP` / PostHog `signup_completed` fires) → POST `/api/partnero/customer` → `apps/dashboard/src/app/api/partnero/customer/route.ts` calls Partnero `POST /v1/customers` with `{partner:{key:via}, key:userId, email, name}` using server-only `PARTNERO_API_TOKEN` (Vercel `distribute-dashboard`, sensitive, preview+production). Fail-loud: missing token → 500, missing fields → 400, Partnero reject → 502; fired fire-and-forget so it never blocks signup.
- **Commission = 30% direct + 20% level-2 lifetime** (Partnero multi-level, program settings). Stripe is connected INSIDE Partnero → recurring purchases credit the partner automatically = LTR. There is NO `stripe-service` change and NO `client_reference_id` (that's Partnero's Payment-Links method; distribute uses custom Clerk checkout, so attribution is signup-registration + Stripe-email match, not link rewriting). Do NOT re-add PartneroJS to the dashboard nor a client-side `po('customers','signup')` — attribution is server-side only. Partner portal: `partners.distribute.you`. (#2665)

### Missing Backend Fields

If the dashboard needs a field/endpoint/capability the backend doesn't provide, NEVER work around it client-side (regex, slugify, name-derivation, aggregation heuristics). Draft a message for Kevin to forward to the backend team and block on the change.

**A DISPLAYED STAT is features-service-owned — the dashboard RENDERS a server field, it NEVER computes a user-facing metric in the browser.** Any cost-per-X (CPC/CPS/CPSM/CPPR/CAC/cost-per-close), total/sum, share/percentage, rate, ROI, or expected-outcome shown to the user MUST arrive as a ready field from features-service (the `/audience-stats` `metrics.*Cents` + the `/revenue` `spend` block / `costEconomics.roiMultiple` are the canonical pattern — null-safe, never a false $0). Client-side `reduce()` of a cost breakdown, `cost/clicks`, `100/cacPct`, `cents/total*100`, `daily*30`, `budget/unitCost` are ALL bugs even when they "look right": two browser-computed numbers on one card routinely diverge (the reported incident — CPC `systemStats.totalCostInUsdCents/clicks` = $5.25 while Total spent = runs ACTUAL spend $359, so $359/73 = $4.92; FS reconciled both onto ACTUAL spend in features-service#396). When a card stat has no FS field yet, do NOT compute it client-side — file a features-service feature request (Kevin: "all those stats must be provided by feature service. You should never compute anything from the front."). The ONLY legit client-side join is a pure display lookup (e.g. cost-source name → provider-logo domain via the price catalog), never a metric. Pure formatting (cents/100, toLocaleString) is fine. (KevinLourd/fix-cpc-calculation #2202, features-service#396)

**A cost-per-outcome on an ACCOUNTING surface FLOORS to net committed spend when 0 outcomes — showing "—" there is the bug, and the floor is NOT a "compute-from-front" violation.** features-service returns `cpprCents`/`cpsCents`/… as committed-net-spend ÷ outcomes and NULLs it at 0 outcomes (no denominator, by design). On the ACCOUNTING surfaces (brand Overview outreach cards, top of Leads, Top-3-audiences card, Audiences table) that null renders "—" and HIDES real money already spent (reported: Overview showed "Total spent $94" next to "Cost per positive reply —"). Fix = `lib/cost-so-far-floor.ts` `costSoFarFloorCents(cost, netSpent, count)`: when count==0 && netSpent>0 → display `netSpent` (the SAME net field already shown as "Total spent" = `spend.totalSpentCents` / audience `evidence.totalCostInUsdCents`), else the raw ratio. This is exempt from the "never compute a metric in the browser" rule because at 0 outcomes NO ratio is computed (floor === the net-spend field verbatim); once a real outcome lands features' non-null ratio wins automatically. The floor is coherent with billing because the dashboard already requests `pricing=net` on `/revenue` + `/audience-stats`. **PROJECTION vs ACCOUNTING — do NOT conflate:** the "Your best model" card is a forward PROJECTION (cheapest workflow's floored fleet estimate via `resolved.costPerOutcomeUsd`); it is CORRECT to show the fleet floor ($42) when the ran workflow spent LESS than the fleet floor — do NOT "fix" it to the accounting spend. The stat cards / Overview / Leads / Audiences are ACCOUNTING (real net spend so far). **Reconciling a $ figure the user challenges: ground it against what they SEE (billing net / the "Total spent" tile), not a raw `runs_costs` sum — and `runs_costs.net_cost_in_usd_cents` is NULL for pre-discount grandfather rows, so `SUM(net)` UNDERCOUNTS; net committed = `SUM(COALESCE(net_cost, total_cost)) FILTER (status IN ('actual','provisioned'))`.** (This brand: gross $149 ≠ naive-net $62 ≠ real-net $94 = billing/Overview.) (#2924)

**UPDATE (features-service v0.102.0 → v0.105.0) — the 0-outcome floor lives SERVER-SIDE and is STRICTER than raw net spend.** `audience-stats` floors every per-audience money metric (`cppr`/`cpc`/`cps`/`cpfs`/`cpsale`) at 0 outcomes to **`max(audience net spend so far, the brand's FLEET-BACKED projected cost-per-outcome)`**, so a cost-per-outcome never dips below what the brand realistically pays and all cost columns obey ONE floor. `costSoFarFloorCents` is now effectively a PASSTHROUGH, so the server floor wins on every consumer with zero dashboard change — do NOT re-add a client-side max-with-projection (joining `audience-stats` spend to a `workflow-projection` figure client-side is compute-from-front, banned). **The brand parent in that `max()` MUST be the fleet-backed `workflow-projection.resolved` figure, never a brand-own raw-spend aggregate** — #641 shipped the wrong parent and a brand whose audiences all had 0 of the outcome floored to its own tiny total spend, so the Audiences table read `$0.66` per website visit while Strategy read the `$2.54` fleet benchmark for the SAME audiences (#653). Invariant: at 0 outcomes the Audiences-table cost-per-outcome and the Strategy per-audience projection are the SAME number by construction; if they diverge, the parent is a brand-own aggregate again. (#2952, features-service #641 → #653)

**UPDATE 2 (features-service v0.106.1) — the fleet parent is cross-org + BEST WORKFLOW, and BOTH readers must ask for the SAME `pricing` basis.** Two follow-on bugs on the same invariant, both fixed 2026-07-29:
- **Producer:** `fetchBrandProjectedParents` built the parent by SUMMING the whole fleet's spend over the whole fleet's outcomes — a cross-org **AND cross-workflow POOLED average** (fleet net $4,775 ÷ ~573 clicks = **$8.33**) while Strategy surfaced the best single workflow (**~$2.42**). Same "fleet benchmark" label, 3× apart. Kevin's standing rule: **a fleet-wide estimate is cross-org + BEST workflow, NEVER pooled across workflows** ("on ne fait que du cross org + best workflow pour l'estimation fleet-wide"). features-service #662 → v0.106.1 now collapses version chains via the existing `buildUpgradeChains`/`aggregateAcrossChains` rollup, computes per-dynasty unit costs guarded on `clicks > 0` / `replies > 0` (a 0-outcome dynasty's ratio is meaningless and must not win), and takes the MIN across all five cost columns.
- **Consumer (this repo):** `fetchFeatureAudienceStats` sends `pricing=net` but `getWorkflowProjectionLadder` sent NO `pricing` param → features-service defaulted it to **gross**, so the identical benchmark rendered ~9% apart across the two pages ($2.22 net vs $2.43 gross) even after the producer fix. **Any dashboard reader of a cost/benchmark that another dashboard surface also renders MUST send `pricing=net`** — net is what the org actually pays and what `/revenue` + `/audience-stats` already request. Guarded by `tests/projection-pricing-net.test.ts`.

**The Audiences==Strategy invariant only binds while the audience's OWN spend is BELOW the parent** — the floor is `max(own spend, fleet best)`, so an audience that has burned more than the benchmark with 0 outcomes correctly shows its own spend (ACCOUNTING) while Strategy keeps showing the benchmark (PROJECTION). That divergence is by design; a divergence at LOW spend is the bug. (KevinLourd/projection-pricing-net, features-service #662 + instantly-service #537)

**UPDATE 4 — the rule covers a reader whose output is a COUNT, not only the ones that print a `$`. The Overview forecast bar was the last `pricing=net` straggler and it read as a volume, so it never looked like a money surface.** `getFeaturePipelineActivity` feeds the grey expected-outreach bars, and features-service builds them as `daily budget / cost per outreach`. The budget is money the org really spends — already discounted — so dividing it by a gross divisor promised a 50%-off brand roughly half the sends its budget buys, beside actual bars twice as tall. Prod, brand `6e21bb6c`, $5/day: **15.88 expected against 30 actual**, the ratio being exactly that org's net/gross (its brand-grain spend over 627 contacts is $197.45 gross / $106.35 net → $0.3149 vs $0.1696 per outreach). Tell for this class: the reader itself renders no currency, but a money value sits somewhere in the chain that produced it. **`tests/projection-pricing-net.test.ts` now pins the whole inventory** — `getFeatureRevenue`, `fetchFeatureAudienceStats`, `getWorkflowProjectionLadder`, `getFeaturePipelineActivity` — so a new reader cannot quietly default back to gross; add yours to the list.

Two producer-side invariants a future change must not break (features-service v0.110.0, api-service v0.93.0):
- **Both legs of the forecast's `max(fleet benchmark, brand's own cost)` read the SAME basis.** Comparing a gross figure against a net one makes the `max()` meaningless and can pick the wrong side, which is a silent wrong number rather than an error.
- **`pricing` is part of the pipeline-activity cache key**, so a net request can never be served a gross body from the SWR snapshot.
- The **daily budget itself is never discounted** — a configured ceiling is not a charge (the same carve-out as the per-org usage discount above). Only money-derived values move; counts and rates are identical either way, and a non-discounted org's frozen net equals its gross (verified in prod: gross and net both 260.49 for a non-discounted brand).

The api-service side was a passthrough bug, not a missing param: the gateway's `pipeline-activity` and `stats` proxies re-declared a closed whitelist of query params, so `pricing` was dropped with no error anywhere. Both now forward every param verbatim, like the `workflow-projection` route in the same file always did. (KevinLourd/forecast-pricing-net #3144, features-service #696, api-service #771)

**UPDATE 5 — the inventory is every reader of a money endpoint, WHEREVER it lives, and the one that hid longest was the one that never touches the browser api client. `outcome-digest.ts` builds its own URL server-side on the cron, so it was invisible to a sweep that read `lib/api.ts`.** It omitted `pricing` entirely, so features-service defaulted it to GROSS: a brand on a 50% usage discount was told by email at 08:00 that it was *"now returning 2.3× per dollar"* while its own Overview read **3.9×** at that moment (prod: **$373.91 gross against $206.81 net**, and `Total spent $206` on the card proves the page is net). Both numbers correct, one word, two places — the self-contradictory-surface bug delivered to an inbox. Tell for this class: a reader that is not a React hook. The sweep that catches it is `grep -rl 'apiUrl}/v1'` over `src/lib` + `src/app/api` (server-side URL builders), not a scan of the api client; `staff-digest.ts` is the only other hit and reads no money endpoint. `tests/projection-pricing-net.test.ts` now carries a second describe block reading `outcome-digest.ts` directly. (KevinLourd/digest-pricing-net)

**UPDATE 3 (features-service v0.108.0) — the zero-outcome floor now covers the BRAND/CAMPAIGN AGGREGATE too, so `costSoFarFloorCents` is GONE from `outreach-stat-cards.tsx`. Do NOT re-add a client fallback to total spend on ANY cost tile.** The floor had only ever been lifted to the per-AUDIENCE grain; the `/revenue` `spend` block still returned `null` at 0 outcomes, and the dashboard's own `costSoFarFloorCents` answered that null with the brand's committed spend. Prod, brand `b97440f6`, goal `positive_replies`: the Overview printed **"Cost per positive reply $29"** one card above **"Total spent $29"** while the Strategy page priced the same brand at **$63** — the same number under two labels, and neither matching Strategy. features-service `revenue.ts` now runs the SAME two engines as `audience-stats-compute.ts` (`flooredCostPerOutcome` for the raw click/reply columns = `max(own committed spend, parent)`; `derivedCostPerOutcome` for the funnel columns, because answering "cost per signup" with a raw dollar total is a units error) against parents from the SAME `fetchBrandProjectedParents` argmin (`audienceIds: []` → zero extra fan-out). Three consequences for this repo:
- **Every cost tile on the stat row renders the server field VERBATIM** (`formatCostCents(spend?.cpprCents)`). `costSoFarFloorCents` survives ONLY as a passthrough guard on the per-audience surfaces (`top-audiences-card`, `customer-audiences-page`).
- **The producer's projection read is deliberately FAIL-SOFT** — a blip returns `null`, meaning "we could not estimate this". A consumer-side `?? spend` fallback therefore re-prints total spend on exactly that branch: the original bug, one layer down, on the case no fixture covers. The honest render for an unestimable cost is `—`. Guard: `tests/outreach-stat-cards.test.ts` fails if `costSoFarFloorCents(` or `totalSpentCents` reappears in the component.
- **Copy carries the honesty, not a hedge label on the value:** ONE `EXPECTED_COST_NOTE` constant behind all four cost tooltips on the row (the guard pins exactly 4 interpolations) so they cannot drift into describing two rules. No "Expected"/provenance pill.

**UPDATE 6 — a cost with fewer than TEN outcomes behind it states `Learning`, not a number, and the bar lives in ONE alias-free lib.** The floor above makes the figure honest about its BASIS; it cannot make it STABLE. A cost per outcome is spend divided by a count, so at one or two outcomes it is decided by whichever one happened to land: it moves by tens of dollars on the next reply, and a customer reads a number on a card as a price we are quoting. Prod: `Cost per positive reply $72` on a campaign with a handful of replies, above a Top-3 card ranking audiences at `$24 cppr / 20x` on one or two replies each. `lib/learning-threshold.ts` (`LEARNING_MIN_OUTCOMES` = 10, `isLearning`, `LEARNING_NOTE`) is the single bar and is alias-free so it carries real unit tests — keep it that way. `components/learning-tag.tsx` renders the pill through `ScoreCard`'s `action` slot, i.e. IN PLACE OF the value: printing a dollar figure beside a caveat reads as a price with a footnote, and the whole point is that there is no figure yet.

- **An ABSENT count is learning, never enough.** "We have no count" is not evidence that we have ten of them, so a producer that does not measure an outcome cannot earn a stated price by omission.
- **Gate EVERY cost on the row, not the reported one.** `Cost per website visit` on the visits, BOTH render sites of `Cost per positive reply` on the replies, and the goal's own outcome cost on its own count — a row where two costs say `Learning` and a third states a price is the self-contradictory-surface bug inside one component.
- **The COUNT cards are untouched.** Nothing is hidden: the card beside it still shows the real number, which is also what the reader needs to see the bar approaching.
- **A live-tracker CTA outranks the tag.** A tracker that is not set up is WHY the count is thin, so "set this up" is the actionable answer; the tag only speaks where there is nothing left to set up.
- **Campaign scope by CONSTRUCTION, not by a flag.** Those pairs render behind `showFunnelMetrics`, which only a campaign passes, and the Top-3 gate needs a `metric`, which brand level does not have (it states the return and nothing funnel-scoped). Do not add a scope prop to re-derive what the existing gates already decide.
- **A learning Top-3 row drops its `$25 cppr` subtitle ENTIRELY** rather than keeping a cost beside a tag that exists to say there is no cost. Its rows carry no `(i)` of their own — each sits inside a `Link`, and the header's existing tooltip carries the reason when any listed row is learning.
- **The tag wears the charter's TERTIARY, and every OTHER accent on a campaign's pages wears it too — one colour per surface, owner-decided.** It shipped amber, was reported as *"scary"* (amber is a warning colour and the tag says nothing went wrong), moved to the secondary, and then to the tertiary when the owner asked for one dominant accent across the campaign surfaces: *"dans toutes les pages de campaign il faut que la couleur dominante utilisée soit la couleur tertiaire de la marque"*. Tints are `bg-orange-50` / `text-orange-600` / `border-orange-200`, full-perimeter 1px border. **It carries `tone-tile`, so it rotates to the BRAND's tertiary** — a customer's dashboard says "learning" in THEIR colour, not ours (*"always make our colors the colors of the brand"*). What never rotates is a VERDICT (semantic green and red, and the paused grey), not an accent. ⚠️ **Orange was only HALF-remapped when this landed** — `bg-orange-50` had both a dark rule and a rotation while the weights beside it had neither, which is the third time that exact gap has appeared (purple, then green/red, now orange). `html.dark` for `text-orange-600` / `text-orange-700` / `border-orange-200` / `bg-orange-200`, plus rotation for `.tone-tile.border-orange-200` and the descendant `.text-orange-700` / `.bg-orange-200` / `.bg-orange-600`, were all added with it; `bg-orange-600` stays unremapped in dark, exactly like `bg-purple-600`, because a saturated mid tone reads correctly on the dark surface as-is. **Two siblings moved in the SAME pass and must stay with it**: the `Learning: about N days left` band (`learning-progress-callout.tsx`, which carries MORE layers than the tag — a 700-weight heading and both halves of a bar) and the campaign **Outcome chart**, which had been drawing the PRIMARY (`text-brand-600`). That chart is campaign-scoped by construction (the brand and offer Overviews draw `RoiTrendCard` instead) and carries `tone-tile` on its chart WRAPPER, not the card: an SVG `stroke` is reached by no utility remap, so the colour rides `currentColor` off a class and that class only rotates under a `tone-tile` ancestor. ⚠️ Consequence accepted: `LearningTag` is ONE shared component, so it is tertiary at brand and offer grain too — one tag reading two colours for one meaning would be worse than the wider blast radius. Verified by REPRODUCTION (compiled Tailwind off the app's own `globals.css` + Playwright), not by grepping the classes: every layer moves by the same delta in all four states (light/dark, tinted/untinted). Guards: `tests/learning-threshold.test.ts` + `tests/learning-progress.test.ts` + `tests/pipeline-activity-chart.test.ts`. (#3547, #3551, #3716, #3726)

**UPDATE — which accent the tag wears is a property of the SURFACE, carried by `LearningToneProvider` (`components/learning-tag.tsx`), and the default is the tertiary so every page that states nothing is unchanged.** The consequence accepted above (one shared component, so one colour everywhere) held only while every surface wanted the same accent. The funnel Overview does not: it states the funnel an OFFER sells rather than one campaign's surface, so it reads in the brand's **PRIMARY**. **Every OFFER-grain surface reads the primary too** (owner-decided, #3763) — its Overview, its Audiences, its Leads and its Funnels list; the BRAND root reads it as well (#3778), and only the campaign surfaces stay tertiary. Five things are load-bearing:
- **A CONTEXT, not a prop.** The tags on that page are rendered by three SHARED components (`OutreachStatCards`, `RoiTrendCard`, `TopAudiencesCard`), none of which has any business knowing which page mounted it — threading a tone through all three is how one of them ends up missing it and a single page renders two colours for one meaning.
- **The Campaigns / leg table states NO tone — the SURFACE it is mounted on decides** (`CampaignsTable` is a bare pass-through to `CampaignsTableInner`). It used to pin itself back to the tertiary wherever it was mounted, on the reasoning that it states campaigns and so should read in the campaign accent everywhere; that put an orange table under a primary header on every page stating its own accent, so one screen carried two colours for one meaning. The tone is a property of the PAGE, and the pin was the one thing stopping a page from being one colour. Consequence, and it is the point: the table reads PRIMARY on the brand Overview, on the offer Overview and on the offer's Funnels page, and keeps the default tertiary on the campaign-grain surfaces, which state none.
- **The primary reads the `--color-brand-*` ramp** (`border-brand-200` / `bg-brand-50` / `text-brand-600`), NEVER a charter hex: `:root[data-brand-tint]` re-declares that ramp at the brand hue, so all three layers rotate together and an arbitrary-value literal would be the one control that stays blue on a tinted dashboard. It carries **no `tone-tile`** — that rotation exists for the categorical purple/indigo/blue/orange set, and adding it here would read as load-bearing. All three classes are remapped in `html.dark` AND `html.dark[data-brand-tint]`, so neither theme paints a light block.
- **`paused` ignores the tone entirely.** A verdict never rotates with the brand hue and never takes a page's accent.
- **The BRAND Overview reads the primary too, and it states that tone IN the component** (owner-decided, #3778: *"sur cette page brand, tous les tags learning doivent etre en primary"*). There is no route wrapper for the brand grain — `brands/[brandId]/page.tsx` IS the page — so the tone cannot be stated anywhere else, and the offer Overview, which renders that very component, INHERITS it rather than restating it (`offers/[offerId]/page.tsx` states none now). The offer's other three routes still state theirs on the ROUTE: `CustomerAudiencesPage` / `EngagedLeadsPage` / `OfferFunnelsPage` are shared with the brand and campaign grains, so a tone written inside any of them repaints grains nobody asked to change. Guards: the tone block in `tests/learning-threshold.test.ts` pins the brand page's own tone, the three offer routes, the absence of one in the two shared components AND in the offer Overview route, and that the campaigns table pins nothing; `tests/offer-level.test.ts` pins the import-and-render shape.
Verified by REPRODUCTION (compiled Tailwind off the app's own `globals.css` + Playwright), not by grepping the classes: under a +40 hue tint the primary's text, fill and border all move 258 -> 298 in light and in dark, while the tertiary and the paused grey are byte-identical to before in all four states. Guards pin the default, the class string, both dark remaps, the table's own pin, and that no OTHER page states a tone. (#3749)

**A PAUSED campaign says `Paused`, not `Learning`, and its countdown band is HIDDEN.** The tag says a figure is withheld because too few outcomes have landed; on a stopped campaign nothing is landing, so it promises a number that cannot arrive until the customer restarts it, and the `Learning: about N days left` band prices days against a daily spend that is not happening. `LearningTag`'s `paused` branch reads the word in the pause grey (`bg-gray-100 text-gray-500 border-gray-200`, the SAME tint the status pill and `ROLLUP_STYLE.paused` use, so one campaign is never described two ways on one screen) and carries NO `tone-tile` - a verdict never rotates with the brand hue. ⚠️ **WHICH figures are withheld does not change**: that stays keyed on the outcome counts, so restarting restores exactly the tags and the band it had. The flag is derived ONCE per surface off the campaign query that page ALREADY polls (`isRunningStatus(campaign.status)` - no second read) and threaded to the tags, so a component that handles `paused` while no page passes it is the feature entirely absent with the component perfectly correct: the guards pin the CALL SITES, not only the component. Scope is the campaign that OWNS the surface - its Overview stat row, its return chart, its Top-3 card, its Audiences table, its row in the Campaigns table.

**A SCOPE says it too, and the verdict is `rollupStatus` - `lib/scope-paused.ts` + `useScopePaused` (#3811).** This paragraph used to end "brand and offer grain stay untouched: several campaigns sit under one heading and no single status answers for them", and that reasoning expired the day `rollupStatus` answered exactly that question: one campaign running makes a scope active, none running makes it paused, and a scope with NO campaign is `none` - unmeasured rather than stopped, so it keeps the word it read before. The pill on a brand's, an offer's or a funnel's own header has rendered that verdict since #3533, so a page whose heading read `Paused` while every figure under it read `Learning` was one screen contradicting itself. Measured on the brand that surfaced it: two campaigns, both `stopped`, so brand, offer AND funnel all answered `paused` while all three pages tagged `Learning`. Four rules a future edit must hold:
- **ONE derivation, and it is the pill's.** `useScopePaused` reads `buildControlRows` off `["campaigns", brandId]` - the key `useCampaignRows` and `CampaignControlsTrigger` already poll on every one of these pages, so it costs no request - and a unit test pins `scopeIsPaused(rows) === (rollupStatus(rows) === "paused")`. Restating the rule as `rows.every(r => !r.running)` is a second source for one answer and drifts the day the rollup gains a state. Budgets are deliberately NOT read: `running` is campaign-service's own word and the budget set only fills `savedCents`.
- **Each surface asks at the grain its FIGURES are computed at, not at the grain of the URL.** The Leads and Audiences pages under a funnel are OFFER-scoped by their own admission (lead-service serves leads per brand and per campaign, an audience is picked for a proposition), so they read the OFFER's verdict - tagging them with the funnel's would be false the moment a sibling funnel ran. The brand/offer Overview is ONE component (`brands/[brandId]/page.tsx`, re-exported by the offer route), so one `useScopePaused(brandId, { offerId })` covers both.
- **A TABLE reads a MAP, since a row cannot call a hook** - `usePausedByOffer` for the Offers table, `usePausedByFunnel` (keyed on the NORMALIZED funnel key) for the funnels table, the shape `useOfferLearning` already uses beside them. Otherwise a funnel reads `Paused` on its own page and `Learning` in the list one click up. A campaign naming no offer, or predating the funnels, is absent from the map rather than folded into whichever row the reader is looking at.
- **A COUNT is never gated.** The Outcome chart states the outcome count itself, which is a fact at any size and is also what shows the bar being approached, so it carries no tag at all - guarded. Only a figure that DIVIDES by an outcome count is withheld, and WHICH ones is still keyed on the counts, so restarting restores exactly the tags each page had. The countdown band needed nothing: `useScopeLearningLead` already keeps only running campaigns, so a fully-stopped scope yields null and renders none. Guards: `tests/scope-paused.test.ts` (real unit tests on the pure module + a CALL-SITE pin per surface).

**The `max()` semantic is deliberate at EVERY grain, and spend-wins-above-the-benchmark is INTENDED — do not "improve" it to a strict replace.** Kevin: *"If 0 outcome and > expected from the model, then it shows the total spent, exactly the way it is returned by Feature Service. What we have for each audience, I want the same logic for the aggregated."* Generalised: when a surface is asked to reach PARITY with a sibling surface, mirror that sibling's semantic exactly; a semantic upgrade bundled into a parity fix is a second, unrequested change. (#3090 / #3099, features-service #689 → v0.108.0 / #692)

**A COST metric's change/delta indicator is COST-SEMANTIC, never stock-ticker — a FALLING cost is green (good), a RISING cost is red (bad).** Any cost-per-X / CAC / spend change badge (▲/▼ arrow shows direction; the COLOR encodes goodness) colors a decrease green and an increase red — the INVERSE of a stock price. The `ScoreCard` `deltaInverted` prop already encodes this; a bespoke cost-delta badge (e.g. the cross-org Expected economics cards, admin feature-stats) must invert too. Do NOT default to stock-style (up=green) for a cost — Kevin corrected exactly this. (admin Expected economics 7-day change, #2514)

**A brand's "status" (active / paused / inactive) is a COMPOSITE of TWO services — pause + budget — never one.** The `paused` flag lives in **campaign-service** (`brand_pause` table `{brandId, orgId, paused}`, read via `GET /brands/:brandId/pause`, api-service `brand-pause.ts` forwards there — explicitly *NOT* brand-service, *NOT* billing). The `$/day` daily budget lives in **billing-service** (`brand_daily_budgets`, `GET /internal/brands/:brandId/daily-budget`; `dailyBudgetCents: 0` = explicit pause-via-budget, `null` = never configured). The dashboard's own Brand-status pill (`brand/brand-status-control.tsx`) reads BOTH (`getBrandPause` + `getBrandDailyBudget`) — so any status/active surface MUST read both. A brand is paused (campaigns HELD, budget retained, NOT spending) independently of its budget value: reading budget alone counts a paused brand as active and inflates any revenue/MRR sum. Precedence: **paused → active (budget>0 && orgBalance>budget) → inactive**; paused/inactive are excluded from spend/MRR/ARR. Cost 2026-07-01 (Audit → Accounts, #2340→#2342 + features-service #427/#436): the accounts endpoint first read billing budget only, so 16 paused brands showed active and MRR read $3,090 instead of $1,110 (~3× inflation) until pause was factored in from campaign-service. brand-service holds identity only (name/domain/profile), never active/paused. (#2342)

**⚠️ The TABLE NAMES in the rule above are stale, and each stale one fails in the direction that looks like an answer.** Queried 2026-08-25 on the box: campaign-service has **no `brand_pause` table** — the pause state lives in **`brand_pause_transitions`** (`brand_id`, `paused`, `transitioned_at`; take `DISTINCT ON (brand_id) … ORDER BY brand_id, transitioned_at DESC` for the current state, there is no `created_at`). And billing-service's **`brand_daily_budgets` is EMPTY** — money moved to per-funnel rows, so the brand total is `SUM(daily_budget_cents)` over **`brand_funnel_daily_budgets`** grouped by brand. A join against `brand_daily_budgets` therefore returns **0 rows for every brand**, which reads as "nobody has a budget" rather than as a wrong table, so the composite silently collapses to "nothing is active".

**Concretely, on prod that day: 19 brands had an `ongoing` campaign, 4 of those were paused, and only 7 had a funded funnel.** So `status='ongoing'` overstates the live client base by ~3x — do NOT use it as a proxy for "active clients" when sizing a backfill, a quota spend, or a customer-facing count. The full query is: distinct `ongoing` brand_ids, minus the ones whose latest pause transition is `true`, kept only where the per-funnel budget sum is `> 0`. The affordability leg (org balance > budget) is NOT a column — `billing_accounts` has no balance field, it is composed from Stripe topups minus the runs ledger — so it needs a billing-service call, and it can only shrink the set further.

**A detail / right-panel that needs richer per-entity data should FETCH IT ON-DEMAND from the OWNING service, keyed off an id already on the clicked row — do NOT widen the list/aggregation endpoint that populates the table.** When a table row is clicked open a panel that wants more than the row carries (profile, audience, "why relevant"), the reflex is to add the fields to the endpoint that feeds the table. That's wrong altitude when the table is fed by an AGGREGATION endpoint (e.g. features-service `/revenue`, which exists to compute pipeline $, not to serve profiles): the panel data belongs to whichever service OWNS it, fetched lazily on panel-open (gated `enabled` on selection, cached). The only thing the table endpoint must carry is a JOIN KEY already native to it (`leadId`), not the foreign profile/audience fields. Concrete (KevinLourd/remove-database-add-lead-panel, #2095): the overview lead panel's "why relevant" pulls audience from **human-service** (`getAudienceMembershipStats` → `listAudiences`) + profile from **lead-service** (`listBrandLeads`, cached) on click — `/revenue` was left untouched (first instinct to add `email` to it was correctly rejected by Kevin: "revenue seems different than a right panel you can fetch dynamically"). Per-service ownership > widening an aggregation contract. (#2095) **BUT the inverse also holds — do NOT build a client-side JOIN for a PRIMARY TABLE COLUMN when the table's OWN feed already serves the field on the row.** The distinction is table-column vs detail-panel: #2095 is about a right-PANEL wanting RICHER data than the row carries. A column rendered on EVERY row is different — if the table's owning endpoint already carries it, read it straight off the row. The Audiences→Leads table computed its **Audience** column via `getAudienceMembershipStats` (email→name) + `listAudiences` (id→avatar) joined by email, but lead-service's `GET /orgs/leads` (which feeds the table via `listBrandLeads`) ALREADY serves `audience {id,name,avatarUrl}` + `audienceId` on the leads_campaigns row (forwarded untouched by the api-service `/v1/leads` pass-through, kept at runtime by `LeadDeliverySchema.passthrough()` — only the `Lead` TYPE lacked the field, so the wire value was present but untyped). Fix = add the field to the `Lead` type + read `lead.audience`; drop the two queries + the email-join. Reading off the row is also MORE accurate (exact stored attribution vs email match). Before adding a membership/lookup join for a table column, check the table's primary list endpoint's openapi for the field first. (KevinLourd/leads-audience-from-backend, #2358)

### Verify backend shape before writing client types (wire-shape rot)

**Deployed openapi is ground truth. Before declaring ANY typed helper (`listX`/`getX`/response interface/Zod schema) for a backend endpoint, fetch the real shape via the `api-registry` / `api-registry-staging` MCP.** Never invent from "what feels right" or an aspirational PR description. **Live (MCP `call_api`) > source > local clone** — local clones (incl. a subagent's) go stale; verify a subagent's reported wire shape via the live MCP. Want a richer shape → file a backend request and block. Long-lived types rot silently when an upstream endpoint is refactored — re-check periodically, not just at creation. (#1079, #1094)

**A backend-contract migration on `apps/dashboard/src/lib/api.ts` MUST migrate the byte-equal reader in `apps/admin/src/lib/api.ts` in LOCKSTEP — the two apps share the same `getX`/`listX` Zod readers and DRIFT.** admin is a dashboard fork with its own copy of `api.ts`; a reshape fixed in only one app leaves the other's `safeParse` throwing on every consumer of that endpoint (loud console `response shape mismatch` + broken page). This has bitten in BOTH directions: dashboard-straggler on the ahref DR/traffic bulk readers (#2070), and admin-straggler on the `getWorkflowProjection` `{workflows[]}`→`{rows[]}` ladder reshape (dashboard fixed #2445/#2446, admin still legacy → #2451). When you migrate a reader for a reshaped/renamed backend contract, `grep` the SAME symbol in the sibling app's `api.ts` and port it in the same PR (or spawn the sibling straggler); prefer keeping the two readers byte-equal, diverging only where a support type is genuinely absent in one app. (#2451)

**Both `apps/dashboard` AND `apps/admin` register email templates at boot (`src/instrumentation.ts` → `PUT /internal/emails/templates`) into ONE SHARED store keyed by template `name` — so a template that exists in BOTH apps is LAST-WRITER-WINS, and whichever app redeploys last overwrites the other's copy. If the two copies DRIFT, the digest/email silently renders the STALE app's template against the sending app's metadata → blank `{{tokens}}`.** The box's crontab has the `outcome-digest` cron (01:00 UTC) that SENDS it — it was `apps/dashboard/vercel.json` until the Vercel cutover; `apps/admin` had NO cron but still registered a stale aggregate copy of the same template → every admin deploy clobbered the dashboard's correct per-brand template, and the cron's per-brand metadata rendered against admin's old `{{orgName}}`/`{{totalExpectedRevenueUsd}}` tokens → "digest for ." + "worth  in expected pipeline revenue" blanks. **Rule: a given email-template `name` has ONE owner app — the one whose cron/route SENDS it. Do NOT register the same template name in a sibling app "just in case"; a non-sending app registering it is dead code that only clobbers. When editing a template, `grep -rn '"<template-name>"' apps/*/src/instrumentation.ts` — if two apps declare it, either delete the non-sender's copy (preferred) or keep them byte-equal.** admin's whole digest surface (lib + cron route + template + tests) was deleted for exactly this. (#2676)

**A boot seed that UPSERTS BY NAME cannot express a RENAME — the old row survives, and the deploy that renames may not even come up.** Every registrar in the fleet works the same way (email templates by `name`, platform keys by `provider`, features-service's feature seed by `slug`): it walks its own list and upserts each entry, so an entry whose key CHANGED is inserted under the new key while the row under the old key is untouched. Two consequences, and the second is the one that bites: (a) consumers listing the store see BOTH, so a renamed thing appears twice; (b) if anything about the store is unique-per-concept — a partial index, a display invariant, a health check that counts — the boot can FAIL, and on the Hetzner box a failed boot is `UNHEALTHY after 60s, rolling back`, so the rename silently never ships while the old code keeps serving happily. Renaming a seeded key therefore needs the seed to PRUNE rows absent from its own list before upserting, in the SAME ship as the rename. Cost 2026-08-18 (features-service, `sales-feedback-request-cold-email-outreach` → `feedback-request-cold-email-outreach`): the rename hotfix rolled back three times before a follow-up hotfix added prune-before-upsert; `deploy.log` showed only `UNHEALTHY … rolling back` with no cause, and the container logs of the *rolled-back* attempt were the only place it was visible. **Diagnostic corollary: the last `deploy.log` line for a service is NOT its current state** — the log is append-only and a later successful deploy of a DIFFERENT sha may follow the rollback you are reading. Confirm with the clone's own HEAD (`git -C /root/distribute/repos/<svc> log --oneline -1`) plus the container's health, and settle "did the data change land?" by reading the destination store, never the log.

**That `apps/*/src/instrumentation.ts` grep is NOT the whole registrar set — BACKEND services register into the same shared store, so an ownership audit that only greps this repo finds a false "nobody else owns this name".** The store is transactional-email-service's `email_templates` table, keyed by `name`, upserted by any caller of its `PUT /templates` / `PUT /platform-templates`. Known backend registrar: **billing-service owns `credits-reload-failed` + `brand_daily_budget_changed`** (its `src/instrument.ts`, called from `src/index.ts` after `app.listen()`); **this repo owns the six dunning templates** (`credit-depleted`, its two follow-ups, and their three `-blocked` variants — distribute.you#1420) and billing deliberately does NOT re-register them. So the one-owner rule spans repos: the owner is whichever service SENDS the `eventType`, wherever it lives. Two consequences: (a) before adding a template here, `git grep '"<name>"' origin/main` in the backend repos that send email, not just `apps/*`; (b) **verify a registration landed by reading the DESTINATION store, never by reading the registration code** — billing's registration module was imported by nothing for months, so its declared templates were absent from prod (36 rows, zero from billing) while the code looked correct, and `credits-reload-failed` failed on every fire. The authoritative check is a `SELECT name FROM email_templates WHERE name = '<name>'` against the transactional-email prod branch. (billing-service v0.57.1)

**These two apps are also the fleet's PLATFORM-KEY REGISTRAR — key-service is where every shared platform secret lives, and the `PLATFORM_KEYS` array in each app's `src/instrumentation.ts` is what puts it there (`POST /platform-keys` on every cold start, idempotent upsert).** A backend that needs a shared secret resolves it from key-service (`GET /keys/platform/<provider>/decrypt`), so a secret sitting in a service's own env file is a gap, not a design: the fix is to add `{ provider, envVar }` here from whichever app already holds that env var, NOT to set it on the consumer. Three things a future edit must respect: (1) **the loop SKIPS a missing env var with only a `console.warn`** — a provider whose env var is absent from that app's env registers nothing and fails silently, so confirm presence (`grep '^<VAR>=' /root/distribute/env/<app>.env` on the box) before assuming a registration landed; (2) **registering the same provider from BOTH apps is fine only while the two values are byte-equal** — the upsert is last-writer-wins, so if the values ever diverge, whichever app redeploys last wins (same hazard as the email-template store above, without the ownership rule, because a platform secret has no "sender"); (3) both `tests/platform-config-startup.test.ts` pin the expected list AND an exact count, so a new entry means editing both apps' tests in the same commit. `clerk` (the Clerk backend secret, consumed by client-service — the only backend that calls the Clerk API) is registered from both apps this way. (#3084)

**An `x-external-user-id` a platform JOB sends CREATES A USER ROW — so a synthetic caller identity is keyed on the JOB, never on the org, and it lives in the `system-` namespace (`lib/service-identity.ts`, byte-equal twins in both apps).** The api-service admin path resolves `x-external-org-id` + `x-external-user-id` through client-service `/internal/resolve`, which UPSERTS `users` on whatever external id it is handed — correct for a person signing in, a row-per-call-site for a cron. Three surfaces here have no person behind them (the nightly outcome-digest scan, the no-login public report proxy, the read-only share-link proxy) and each interpolated the ORG into its id (`outcome-digest:org_ABC`), so prod reached **89 phantom rows against 64 real Clerk users**, one per org per job, growing forever. Two things made it worse than table noise: **client-service's public user count — the figure the landing `/investors` page prints — excludes `system-%` and two test ids and nothing else, so it read 153 users for a platform with 64**, an invented number on a public surface; and one row was the literal `report-public:%7BorgId%7D`, an id whose template never interpolated, which nothing would ever have surfaced. Dropping the org from the id loses nothing — the org travels on its own header and is stored on the run (`runs.organization_id`, 13,159 of them referencing these 89 principals), and the gateway reads the org from the ORG upsert, never from the user row; `users.orgId` does churn on the shared row as a scan walks orgs, harmlessly. Generalise: **before sending an identity header for a caller that is not a person, ask what the receiver PERSISTS on it** — the header reads like metadata and is a write. Guards: `apps/{dashboard,admin}/tests/service-identity.test.ts` (the catalogue is alias-free, so those are real unit tests, and the admin copy pins the two files byte-equal). (KevinLourd/service-identity-namespace)

Sub-classes (all invisible to a "page loads" smoke test):
- **Data existing in SOME backend service ≠ the dashboard can reach it — `api-service` (the gateway) must PROXY the path first; confirm before scoping a consumer-only PR.** The dashboard only talks to `api-service` via `/api/v1`; a `/orgs/*` route owned by another service (e.g. a freshly-imported `ahref-service`) is unreachable until the gateway adds a transparent-proxy route. The registry `api` service lists ZERO endpoints (passthrough — the index doesn't enumerate its proxy table) and full-text search doesn't index downstream paths, so neither tells you whether the gateway forwards it. 30-sec probe — a live gateway call `mcp__api-registry__call_api(service:"api", GET "/v1/<path>")`: **404 "Not found" = route ABSENT** (CROSS-REPO: needs a new `api-service` proxy route + likely new `<SVC>_SERVICE_URL`/`_API_KEY` Railway vars before the dashboard can consume it) · **401 "Missing authentication" = route PRESENT** (reachable). Confirm via `git -C ~/conductor/repos/api-service grep "orgs/<thing>" origin/main`. Treating producer-shipped as dashboard-reachable turns a 1-PR task into a 3-part rollout. (#1299, DIS-200/212) **⚠️ A 404 on the DOWNSTREAM path is NOT proof the gateway doesn't proxy it — the gateway can RENAME the path segment. Before concluding "proxy missing" (and spawning an api-service workspace), list the gateway's OWN routes at the relevant prefix: `mcp__api-registry__list_service_endpoints(service:"api", pathPrefix:"/v1/public")`. The features-service cross-org public stats live downstream at `/public/stats/*` but the gateway exposes them REMAPPED as `/v1/public/features/*` (cost-projection/ranked/best/revenue/workflow-engagement-latency) — so `GET /v1/public/stats/cost-projection` 404s while `GET /v1/public/features/cost-projection` is live. Probing only the downstream literal gave a false "absent" and triggered a redundant api-service proxy spawn (the routes already shipped in api-service #404/#542). Always cross-check the gateway's endpoint listing before scoping a proxy add. (admin Sales-Cold-Email cross-org stats page, #2486) **Corollary — the gateway PRESERVES the downstream service's own `/orgs` prefix under `/v1`, so a `/orgs/<thing>` downstream route is proxied at `/v1/orgs/<thing>`, NOT `/v1/<thing>`. Probe `/v1/orgs/contacts` not `/v1/contacts` — the dashboard/admin readers already encode this (`apiCall("/orgs/contacts")` → `/api/v1/orgs/contacts`). Dropping the `/orgs` segment in the probe gives a false 404 → a wrong "needs api-service proxy PR" conclusion. crm-service `/orgs/contacts*` is live at `/v1/orgs/contacts*` (api-service #740); the correct probe is `list_service_endpoints(api, pathPrefix:"/v1/orgs/contacts")`. (admin brand-level CRM surface, #2962) ⚠️ **It is a TENDENCY, not a rule — the gateway names its OWN paths and does not always keep the prefix, so this is a hint for PROBING and never an authority for what to CALL.** instantly-service's `/orgs/conversations` is proxied at `/v1/conversations` (api-service #906), with no `/orgs` at all. So probe BOTH forms before concluding a route is absent, and once a proxy ships, read the path off the deployed contract (`openapi.json` / the registry) and conform the consumer to it — a consumer written against the assumed prefix 404s on every call and nothing catches it until someone clicks. (2026-09-04, the lead-conversation read.)**
- **Audit ALL of a service's endpoints when migrating a consumer**, not just the ones the migration touches — adjacent endpoints rot in the same refactor window. (#1213)
- **`safeParse` every list/get `apiCall<…>` wrapper** that talks to an external service (per-field schemas in the same file) — converts wire-rot from a render-crash into a caught fetch-error. **Per-VERB schema:** a write response DTO is often NARROWER than its read sibling (omits computed fields like `isDefault`/`updatedAt`/counts) — reusing one schema turns every successful write into a false "invalid response shape". Too-loose → bad data renders + crashes (#1213); too-strict shared schema → false error on success (#1221, `.omit({ isDefault: true })`).
- **A too-narrow consumer `z.object()` SILENTLY STRIPS a newly-added backend field** (Zod drops undeclared keys) → `field` reads `undefined` at the call site though it's on the wire. Adding the TS type is not enough — add it to the Zod schema too. (#1282)
- **Reading a not-yet-deployed ADDITIVE backend field → declare it `.optional()` (TS `field?:`) to DECOUPLE the rollout, do NOT require it.** A required `z.number()` for a field the backend hasn't shipped to prod yet forces a cross-env gate (dashboard = Vercel main=prod, no staging buffer) AND — when the Zod schema is SHARED by other consumers (e.g. `top-audiences-card` reuses `FeatureAudienceStatsResponseSchema`) — a too-early merge breaks EVERY consumer of that schema via `safeParse`, not just the new surface. Optional doesn't strip a present value (it only tolerates absent), so the dashboard ships immediately and the field auto-populates once the producer lands in prod; renders `-`/`0` until then. Strictly better than the cross-env gate for an additive field. (Spawned the producer field-add in parallel, shipped the consumer optional → zero gate; audiences Outreach/Opens, #2006 + features-service #359.)
- **A numeric field declared `integer`/`number` in the backend openapi can arrive as a STRING (`"0"`) on the wire** — Postgres `numeric`/`bigint` serialize as string via the driver unless the service casts. Client `z.number()` then rejects → `safeParse` throws → card breaks + console spam. Use `z.coerce.number().nullable()` (parses string OR number, `.nullable()` short-circuits null before coerce → `null`, no silent 0); NOT a fallback (no `??`/`.default()`), forward-compatible once the backend casts. File a backend issue to fix at source (the wire violates its own `integer` contract) but ship the client coerce to unblock. (#1357, DIS-274)
- **A bulk reader keyed by a `?domains=a.com,b.com,…` (or `?ids=`) query string FAILS SILENTLY at scale — the URL/header size limit (~8–16KB) is blown long before you notice, the request errors, and the consumer's map comes back EMPTY (blank column/badge, no crash).** A brand can own 12k+ outlet domains; `getDomainDrStatuses` / `getDomainTrafficHistories` passed every domain in ONE GET (~180KB URL) → both the CSV DR/Monthly-Visits columns AND the cards went blank. The tell: a column/badge that reads from a bulk `?list=` GET is empty for a LARGE entity but fine for small ones; the per-item on-demand POST (single domain) still works, so a detail-panel value appears while the bulk view is empty. Fix: CHUNK the reader (≤200 keys/request, concurrency ~4, merge results) so it scales to any N. Any bulk `?domains=`/`?ids=` client reader must batch — don't assume small N. Diagnose "values empty" by FIRST checking the size of the keying collection (`SELECT count(*)` for that brand), not the row data. **CHUNKING ALONE IS NOT ENOUGH — two follow-on traps (#2054):** (a) a naive merge awaits ALL chunks via a `Promise.all`-style barrier, so ONE failed chunk throws and empties the WHOLE map (a "fail-loud per chunk" reflex makes this WORSE). The downstream is often a tiny fixed compute (ahref-service prod = 0.25 CU, small pg pool, Neon scale-to-zero), so a burst of chunks reliably hits cold-start / pool-saturation transients (`ECONNRESET`/5xx/"timeout exceeded when trying to connect") → ≥1 chunk fails → blank again. The reads are idempotent GETs → each chunk must RETRY transient transport failures with backoff (≈4 attempts) and only throw after exhaustion (fail-loud is per-chunk-AFTER-retries, NOT per-chunk-first-error). (b) Enriching thousands of keys takes seconds; a CSV/export button enabled immediately exports blank columns before the maps populate — GATE the export on the enrichment query being settled (disable + "Loading…" while `isPending`). Confirm the data actually EXISTS (query the producer DB directly) before assuming it's a coverage gap — for #2052/#2054 every blank brand domain had DR+traffic in ahref; the failure was 100% transport. (#2052 + #2054, brand with 12,172 outlets) **THIRD trap — a single NON-DOMAIN value (`-` placeholder for "no domain", or a path-bearing `a.com/section`) 400s the WHOLE chunk it lands in, NOT just itself.** ahref-service `normalizeDomain` rejects it (`not a valid domain: -`); the dashboard readers don't filter, so the junk rides into the `?domains=` chunk and that 400 blanks up to 200 valid chunk-mates. `.sort()` puts `-` first → it poisons chunk 0 on EVERY load. This is a PERMANENT 400 the transient-retry can't heal (distinct from the transport class above). Consumer fix: `getDomainTrafficHistories`/`getDomainDrStatuses` filter via `isQueryableDomain` (bare dotted host, no `-`, no slash/space) BEFORE chunking; log dropped values. Producer fix (outlets-service): don't store/serve a `-`/path/empty `outletDomain` (normalize to NULL at ingest + reconcile rows). Tell you're in this class vs the transport one: the error is a `400 normalizeDomain` on a specific junk token, not an `ECONNRESET`/timeout. (#2070, dashboard filter) **FOURTH trap — a NULL `outletName`/`outletDomain`/`journalistName` crashes the whole PAGE the instant a user types in the search bar (`Cannot read properties of null (reading 'toLowerCase')` → error boundary).** Outlet-service serves these fields as `string | null`; rendering them as JSX is null-safe, but the search-filter `o.outletName.toLowerCase().includes(q)` is NOT. The bug hides behind the `if (!search) return …` short-circuit, so a "page loads" smoke test passes — it only fires on a keystroke. Coalesce EVERY nullable backend string to `(x ?? "").toLowerCase()` before any string method in a filter/sort comparator. The articles pages already coalesced (`?? ""`/`|| ""`); the 4 outlets + 4 journalists search filters did not (admin brand/feature/campaign + dashboard brand) — inconsistent, so a new outlet/journalist search surface defaults to the unsafe form. (#2080)
  - **The bulk DR/traffic client readers live in BOTH `apps/dashboard` AND `apps/admin`, and they have DIVERGED — `#2063/#2068` migrated ONLY `apps/admin` to server-side ahref enrichment (`listBrandOutlets?enrich=ahref` → each outlet carries `domainRating`/`trafficMonthlyAvg`, drMap/trafficMap seeded from those) + `enabled:false` on the client fan-out. `apps/dashboard` is STILL on the client bulk-reader path (`enabled: outletDomains.length>0`, drMap built from the `domainDrStatuses` query).** So when investigating a DR/Visits "blank columns" bug, check WHICH app + whether a recent commit only touched admin — the dashboard is the straggler. The clean long-term fix is to mirror the #2063/#2068 server-enrichment migration into `apps/dashboard` (consume `outlet.domainRating`/`trafficMonthlyAvg`, drop the client fan-out); until then the dashboard needs the `isQueryableDomain` filter on its still-active client readers. (#2070)
- **Free-form `variables`/`inputs` passthrough endpoints:** any JSON validates at the wire (TS/Zod can't catch); the variable-NAME contract is published separately as the template's `.variables` (`GET /v1/content/platform-prompts?type=<template>`). Map payload keys 1:1 or the model gets empty `{{...}}` and the failure is silent bad output (not a crash). Centralized in `lib/quote-pitch-variables.ts`. content-gen requires ALL declared vars non-empty (missing → 400) → thread a new OPTIONAL var as an EXTRA passthrough key or fold it into an existing free-form var, never as a new declared var. (#1215, #1280)
- **An openapi `description`'s PROSE (cache-key, "scoped by…", behavior) can lag the deployed CODE by months — treat as a hint, confirm against code/live before building a workaround.** A free-`string` field with `(e.g. 'x')` is NOT an enum and the example can be stale — verify actual values vs DB (`SELECT status, count(*)`), gate on the TERMINAL value (`!== "stopped"`) not an assumed running label. (#1268, #1280)
- **Every authed Next app shell needs a route-segment `error.tsx`** (`app/(authed)/(dashboard)/error.tsx`, mirrors the report tree) — default Next global-error UI hides the stack. (#1213)

**Public-report components (`components/report/*`, anything in the no-login `src/app/report/**` bundle) MUST NOT import `@/lib/api`** (the Clerk-authed client — no session on the public surface; guarded by `tests/pr-expert-public-report.test.ts`). Need a primitive from there → define it locally, or put shared pure logic in an api-free module (e.g. `lib/batch-quote-reply.ts`). (#1294)

**The PR-Expert quote REPORT surface (`apps/admin` `src/app/report/.../[featureSlug]`, `isExpertQuoteFeature`) is a READ-ONLY press tracker, NOT a HITL queue — do NOT re-add a write path.** The old interactive queue (`PublicHitlQueue`, the report `{draft,reply}` route handlers, the `quote-requests`/`quote-pitches`/`prompt` report pages) was removed in #2954. The sidebar is 4 status tabs with counts — **Published / Selected / In Review / Pitched** (`lib/report-pitch-tabs.ts` `PITCH_STATUS_TABS`, mapped 1:1 to `published`/`selected`/`submitted`/`drafted`; `not_selected` and the failure statuses hidden) — each a page under ONE dynamic `[status]` route rendering `components/report/pitch-status-view.tsx`; the base route redirects to `/published`. The authed campaign HITL surface (`campaigns/[id]/quote-requests` → `BatchReplyControl` + `generateQuoteDraft`) is SEPARATE and still interactive — untouched. **Publication outcome is REAL on the wire and rendered from it**, never derived: DR = `outletDomainRating`, Attribution = `backlinkAttribution` (`DoFollow`/`NoFollow` → badge, else `Unknown`), Updated = `publishedAt` (the per-article date, NOT the bulk-reconcile `updatedAt`, which is identical across every row), Article = `articleTitle` as the link text. **Publication** joins pitch → its quote request via `fetchQuoteRequestIndex`, falling back to the pitch's own `publicationSource` when that join misses. A genuinely-null field renders `—`/`Unknown`. (#2954)

### No Fallbacks — Fail Visibly

NEVER add fallback logic (`||` alternatives, silent defaults, graceful degradation) when data is missing/mismatched. Log a loud `console.error` with the value + context; show an error UI for a missing required field. Applies everywhere (lookups, field resolution, display). **Exception — Vercel build-time prerender** (sitemap.ts, generateStaticParams, OG routes): an unhandled throw aborts the whole deploy, so for data-shape failures prefer `try / console.error / continue empty` over throwing; fix the data layer but keep the site shippable. Runtime requests still fail loud. (#1120)

**Exception — user-facing REASSURANCE surfaces (live activity feed, progress ticker) deliberately HIDE errors/debug — this is intentional, not a fail-loud violation.** The campaign-overview "Live activity" feed (`components/campaign/campaign-activity.tsx` + `lib/campaign-activity.ts`) shows only an allowlist of present-tense funnel steps ("Finding your next lead", "Sending the email") to reassure a user who just added credits; it filters out `level=error` events and all infra/polling/orchestration noise. Do NOT "fix" it to surface errors per the fail-loud rule — Kevin: "on n'affiche pas d'erreur à l'utilisateur, on est là pour le rassurer, c'est pas du debug." Errors still fail loud everywhere else (the `safeParse` on `listCampaignEvents` throws on wire-rot; the feed's allowlist is a DISPLAY choice over already-fetched data, not a swallowed fetch error). Keyed on the event SLUG, never free-form `detail`. (#1395)

**The campaign concept was removed from the dashboard UI (#1768) — one subscription per brand (1 campaign under the hood); the launch funnel lives at `brands/[brandId]/launch`, everything else is brand-level. The notes below that say "campaign page / campaign overview" now refer to the BRAND Overview (`brands/[brandId]/page.tsx`).**

**Campaign launch modal + the client-side sales-funnel projection mirror are REMOVED (dead code)** — `components/campaign/campaign-launch-modal.tsx`, `lib/campaign-launch-progress.ts` and `lib/sales-funnel-projection.ts` went with the `brands/[brandId]/launch` route that rendered them. Projection is read SERVER-side via `getWorkflowProjection` (features-service); do not re-add a client mirror. (#1421, #1528, #1536, #1768)

## An investor update is PREVIEWED by the producer, never rendered here — and it opens in a modal, in an iframe

`admin.distribute.you/investors/update` writes a markdown body that transactional-email-service renders into a full, inline-styled email (centred 600px card, styled headings, tables, blockquotes). The console used to render its OWN preview of that markdown with a copy of the producer's renderer, and the copy DRIFTED the moment the producer grew the styled renderer: for months the composer showed bare markup while investors received a laid-out email, and nobody noticed until someone looked at the screen. Any second rendering drifts the same way.

- **There is no renderer in this app.** `previewMailingListUpdate` (`lib/api.ts`) asks the producer for the HTML a send of the same body produces (`POST /v1/mailing-lists/updates/preview` → transactional-email-service, which renders it through the SAME `renderUpdateBody` call a real send makes and pins the two equal in its own route test). Nothing is sent, nothing is recorded. `lib/investor-update-html.ts` keeps only what can go in an update and what stops it going out — **do not re-add a markdown renderer there or in the composer** (guarded).
- **`EmailPreviewModal` (`components/investors/email-preview-modal.tsx`) renders it in a `sandbox=""` iframe, never inlined.** An email carries its own page background, font stack and colours as inline styles; dropped into the console's DOM the two fight (`html.dark` repaints the card, base styles reset the tables) and the author approves a hybrid that exists nowhere. The iframe takes the panel width with NO minimum, so the email shows at the width the viewer would receive it. A draft and a sent update use the SAME modal — the sent one from `htmlBody`, which is what recipients actually got.
- **The preview is fetched on OPEN** (one call per preview, not per keystroke) and is **never persisted to disk** (`EPHEMERAL_QUERY_ROOTS` in `lib/persist-cache.ts`, separate from the secrets denylist): its answer is only true while the producer's renderer is unchanged, so restoring yesterday's HTML for the same body reintroduces the drift through the back door. A failure states it could not render — **no fallback to markup of our own**.
- **Images no mail client renders are NAMED beside the message**, never silently accepted. The producer reports them rather than refusing (a preview's job is to show what you have) and a browser renders SVG happily, so without the note the author only learns at send time when the send is refused.
- **`lib/sanitize-email-html.ts` is shared** by this modal and the CRM inbox panel, and it KEEPS the presentational table attributes email lays itself out with (`align`, `bgcolor`, `border`, `cellpadding`, `cellspacing`, `valign`, `role`). Stripping them does not make markup safer, it makes rendering wrong — the update centres its card with a single `align="center"`.
- Verify a change here by REPRODUCING it (esbuild-bundle the real component + Playwright against real producer output), not by grepping classes. Measured: 600px card centred at 1280px, 346px edge-to-edge on a Pixel 7, zero email nodes in the console's document. Guards: `tests/investor-update-preview-modal.test.ts`. (#3265, transactional-email-service #122 → v0.18.0, api-service #803 → v0.97.0)

**Billing / top-up UX lives in TWO surfaces only — card capture + auto-topup are NEVER set up at signup.** When a top-up/charge/"add card" symptom is reported, identify the surface FIRST (cost a 2-round-trip misroute once): (1) **`billing/page.tsx`** — the org billing page (Add Credits one-time charge `mode:"payment"` + Auto-Topup config). (2) **`lib/billing-guard.tsx`** — the `showPaymentRequired` modal shown at **campaign launch**; this is the ONLY place a new user's card + auto-topup get set up (setup-mode `$0` checkout when no card). **Onboarding NO LONGER asks for a card** (the old `onboarding/page.tsx` `billing-setup` step was removed, #1528): it armed auto-topup with a `$10` threshold while the account held only the `$2` welcome credit → already below threshold → the first `usage_apply` fired an instant `$50` charge. So signup → URL → brand direct, `$0` charged. **Launch rule (#1536):** recurring (daily/weekly/monthly) → auto-topup is MANDATORY (no "Launch anyway"; the dead `runway`/`onProceed` variant was removed from billing-guard); one-off → only blocks when the first period isn't covered. Backend: configuring auto-topup does NOT charge — the charge fires later in billing-service `customer_balance.ts` `usage_apply` when `balance < threshold`. `brands/[brandId]/launch` `BUDGET_TIERS` are expressed in **closes-per-month** (3/5/10), not dollars — $ derive from served cost-per-close. (#1528, #1536; launch route re-homed from `campaigns/new` in #1768)

**`BillingAccount` balance fields ↔ billing-page labels — "available credit" for low-credit / runway warnings = `balance_cents`, NEVER `actual_balance_cents` or `credited_cents`.** The billing page (`billing/page.tsx`) maps: `credited_cents` = **"Total credits"** (e.g. $39.00, lifetime credited) · confirmed charges = actualized spend · provisioned charges = held-but-not-actualized · `balance_cents` = the page's **"Available"** = credited − confirmed − provisioned (the spendable number, e.g. $0.14) · `actual_balance_cents` = credited − confirmed ONLY (ignores provisioned holds, so it OVERSTATES what's spendable). A low-credit/runway warning divides **Available ÷ daily budget**, so it MUST use `balance_cents`. Don't guess the field from a loosely-worded "total − actual + provisioned" formula — the literal billing-page "Available" is ground truth (`balance_cents`). The low-credit BANNER + once-per-day MODAL (`components/billing/credit-alerts.tsx`, brand-scoped) fire on `availableCreditCents(account) ÷ brandDailyBudget < 3 days` when auto-topup is OFF (deactivated OR unsupported e.g. India `auto_reload_supported === false`); runway helpers in `lib/credit-runway.ts`. (#2307)

**Onboarding checkout coupon = Stripe `allow_promotion_codes` on the HOSTED top-up session (billing-service), NOT a dashboard field.** The onboarding top-up is a HOSTED Stripe Checkout redirect (`createCheckoutSession({topup_amount_cents,success_url,cancel_url})` → api-service `/billing/checkout-sessions` → billing-service `checkout.ts` builds the session). To let a user enter a discount/promotion code, billing-service sets `allow_promotion_codes:true` on the HOSTED payment-mode branch only (setup-mode card-capture + embedded checkout are OUT of scope) → Stripe's own hosted page shows the native "Add promotion code" field. ZERO dashboard/api-service change needed. **Credit-landing caveat: org credit derives from what Stripe ACTUALLY receives** — `computeBalance` → `sumSucceededTopupsForOrg` sums PaymentIntent `amount_received`. So a 100%-off promotion code = $0 charged = **$0 credit lands** (by design, not a bug). A comped user is topped up separately via the manual `POST /v1/credits/grant` (admin grant, `local_promos`). Do NOT try to make a Stripe coupon inject credit — that requires crediting the pre-discount subtotal on `checkout.session.completed`, which billing does not do. Live comp code as of 2026-07-25: `JOURNALIST100` (coupon `G0oH5KFS`, 100% off `once`, `max_redemptions:1`). (billing-service v0.52.2)

**Onboarding checkout — restore-state is a FALLBACK that must never beat a live selection; align every action's precedence with the DISPLAY's.** In `components/onboarding/onboarding.tsx`, `checkoutBudgetUsd` + the `CHECKOUT_PENDING_KEY` sessionStorage blob (`distribute:onboarding-checkout-launch`) are written ONLY from a restored/resumed snapshot (`applyRestoredOnboardingState`, `resumeCheckoutLaunch`, init-from-`restored`), NEVER from a live pricing-card click — a card click only does `setSelectedCount(n)`, and `derivedBudget()` recomputes from `selectedCount`. So after a checkout CANCEL these hold the PRIOR tier. Two bugs shipped from treating restore-state as authoritative: (a) the checkout charge read `checkoutBudgetUsd ?? … ?? derivedBudget()` (stale-first) while the bonus/pricing display reads `derivedBudget() ?? checkoutBudgetUsd` (live-first) → re-picking a tier showed the new $ but Stripe charged the cancelled one (#2169); (b) `beginCheckoutAndLaunch`'s opportunistic `readPendingCheckoutLaunchOrNull()` re-threw the strict validator on a stale-schema blob, blocking a fresh checkout entirely (#2167). Rules: the OPPORTUNISTIC `-OrNull` recovery read must `console.error` + purge the key + return null on an invalid blob (the strict `readPendingCheckoutLaunch` stays fail-loud for the resume/cancel-return paths where the blob is the sole source of truth); and any ACTION value (charge amount, etc.) must use the SAME live-first precedence as the surface that displays it — `derivedBudget()` (live) first, restore-state only as a fallback. Guards in `tests/onboarding-resume-persistence.test.ts`. (#2167, #2169)

**UPDATE (#3078) — "align the action's precedence with the display's" is now enforced by ONE helper, `budgetForCharge()`, and the custom card no longer mirrors its amount into a second state.** Restating the same `derivedBudget() ?? checkoutBudgetUsd` expression at each surface is what MAKES the drift possible; the pricing summary (`displayBudget`), the checkout CTA (`amount`) and the Stripe amount (`buildPendingLaunchBlob`'s `budget`, which alone keeps the extra `?? storedPending?.budgetUsd` tail) all read the helper. Second half: `derivedBudget()` returns `parseCustomBudget(customBudget)` — the FIELD — whenever `customBudgetSelected`, instead of the `selectedBudget` copy the input used to write on every keystroke, so a keystroke-lagging number can never reach Stripe. **Which card owns the selection is now EXPLICIT state (`customBudgetSelected`), not inferred from `selectedBudget === customB`**: typing in "Other" takes the selection (typing an amount IS choosing it) and clicking a tier releases it — before this, typing while a tier was selected silently overwrote `selectedBudget` while the tier stayed highlighted. It is recovered on restore by matching the two persisted values (`customBudgetMatchesSelection`) rather than persisted, so **`ONBOARDING_STATE_VERSION` stays untouched — a bump strands an in-flight checkout**. `selectedBudget` is still written by the custom input purely so the snapshot carries a number; it is no longer authoritative. Guards: `tests/onboarding-audience-budget-roi.test.ts` + the rewritten precedence guard in `onboarding-resume-persistence.test.ts`. (#3078)

**The onboarding URL step prefills from the signup email's business domain, and that guess is the WEAKEST of the three url sources — it may only ever fill an EMPTY field.** `businessDomainFromEmail` (`lib/extract-domain.ts`) turns `kevin@acme.com` into `acme.com`; the effect in `onboarding.tsx` (`emailPrefillDoneRef`) writes it through the FUNCTIONAL `setUrl((current) => current.trim() ? current : guessed)` so the empty-check is atomic with the current state and cannot land on top of a keystroke — Clerk hydrates async, so this runs well after first paint and the plain `setUrl(guessed)` form would be a race. Precedence is `restored?.url` > `?url=` param > email domain > `""`, and the first two stay in the `useState` initializer: both are STATED INTENT, the email is inference. **Google signup needs no separate path** — Clerk exposes the same `user.primaryEmailAddress` whichever provider minted the session, so there is nothing provider-specific to branch on. The no-website path is skipped (it collects a brand name + free-form description, no URL field).
- **Free / personal / disposable providers must return null (`lib/free-email-domains.ts`), and the list should err toward BLOCKING.** The asymmetry is what makes the feature safe: a false block leaves the field empty, which is exactly the pre-feature behaviour, while a false allow prefills a wrong domain the user has to notice and clear. So `@gmail.com` prefills nothing rather than sending someone off to analyze Gmail's website.
- The big families (`yahoo`, `hotmail`, `outlook`, `live`, `msn`, …) have far too many country variants to enumerate, so they match on the LEADING label plus a public-suffix-shaped remainder (`yahoo.co.uk` ✓, `live.acme.com` ✗ — a business subdomain is not `live.com`). A domain whose last label is not letters (an IP literal) is rejected too, because `extractDomain` only checks for a dot.
- Guards: `tests/business-domain-from-email.test.ts` (real unit tests — `extract-domain.ts` and `free-email-domains.ts` carry no `@` imports, so keep them alias-free) + `tests/onboarding-email-domain-prefill.test.ts` (source-substring, scoped to the effect body). (onboarding-email-domain-prefill)

**A mutation whose only visible effect renders BELOW the fold must report its own result next to the control — and a run that legitimately produced NOTHING is the case that must not be silent.** The onboarding audience step appends new candidates UNDER the ones already picked, so a user holding a full selection sees nothing move and reads the click as a no-op. Worse, `runSuggest` had a branch that wrote no message at all: when every returned candidate collided with one already stored (`added === 0` while `keep.length > 0`) the cards were unchanged, no error fired, and the run was indistinguishable from a dead button. A real user clicked "Find new audiences" five times into that silence before abandoning (org `d3367008`, 2026-07-29 12:29→12:34 — human-service re-serves existing audiences by name, so repeat clicks on the same prompt converge to zero new rows). `notice` now reports the count of genuinely-new candidates (`merged.length - keep.length`) beside the button, the zero-new run gets its own amber line, and both clear at run start and in the catch. RULE for any similar "generate more / add / refresh" control: derive the count from what CHANGED, never from the response length, and give the produced-nothing branch an explicit message. (#3078)

**The best-model step edits the economics its ROI is computed from — `roiMultiple` is lifetime revenue ÷ cost per outcome, and showing the quotient while hiding both operands makes a sub-1x return unexplainable.** The fields are the PRIMARY FUNNEL's own chain (`funnelRateFields(def)`) plus its lifetime revenue, rendered ABOVE `BestModelStats` and highlighted amber when `resolved.roiMultiple < 1`. **The lifetime revenue is load-bearing in that set — a sub-1x ROI is almost always a small LTR rather than a bad model, so a block showing only the conversion rates would hide the actual driver** (exactly the #3039 incident, where a placeholder $2,500 LTR printed a flattering ROI that collapsed to 0.6× once the real $100 landed). `saveModelEconomics` runs `saveFunnelStatsAndContinue`'s discipline verbatim — `getBrandSalesFunnels` → `buildFunnelPatch` → `declareBrandSalesFunnel`, so the stored set is read from the wire on every write and the patch is the DIFF against it — then `setBestModelLadder(null)` BEFORE `fetchBestModelLadder` so a stale ROI never sits beside freshly typed inputs. That wire-first discipline matters more here than anywhere else in the flow: **the step runs on a FRESH page load after Stripe, where the client copy can be a reconstructed snapshot full of placeholders.** The button arms off a live compare against the baseline captured on entering the step, never a sticky edited latch. (#3078, retire-brand-level-rates)

**And it asks features-service on the FUNNEL, never on a goal — because a goal-keyed request does not merely mislabel the answer, it RANKS the wrong workflow.** `/workflow-projection` takes `?funnel=` and wins it over `goal`/`objective`; the two meeting funnels both echo `meetingBooked`, so a goal-keyed request is priced from BOTH channels at once (its own docs: the outcome count is `clicks·visitToMeeting + replies·replyToMeeting`). Per dollar that buys ~86× more clicks than replies at fleet prices ($2.47 CPC vs $212 CPPR), so the click leg supplies nearly every projected outcome — and `recommendedWorkflowDynastySlug` is an argmin on that same mixed cost, so the workflow crowned `BEST` is whichever is cheapest **per CLICK**, with its cost per reply incidental. That is the Osprey-vs-Dawn split from the audiences-vs-strategy incident recurring one surface over: prod showed `Cost / positive reply $212` for a reply→meeting brand whose reply-cheapest workflow was a third of that. **Diagnostic tell, and the reason it reads as a producer bug: the numbers are arithmetically impossible against the rates on the SAME screen.** $93 per paid client under a $212 first step (the chain gives $4,711 and 2.1×, not $93 and 26.8×) — no paid client can cost less than one of the replies that buys it. So when a projected figure is challenged, divide it out by the visible rates before looking anywhere else; a *plausible* wrong number needs a data hunt, an *impossible* one localises to the request. Sibling rule for the render: `BestModelStats` gates every tile on `stepsFor(null, funnelKey)`, so a reply chain shows no cost per website visit, and its outcome noun comes from the step's own `outcome.noun` (`goal-steps.ts`) rather than `outcomeNoun(goal)` — one goal covers two chains, so it cannot say what was bought. ⚠️ `funnelKey` must be DECLARED on the reader's zod schema or it is stripped, which throws away the producer's own statement of what it priced. The deprecated `goal`/`objective` params stay on `getWorkflowProjectionLadder` for the brand-level callers that state no funnel BY DESIGN (a brand sells through several at once): removing them would not make those callers funnel-aware, it would drop them onto the producer's `meeting-booked` default — a worse wrong number. They go with the last sender. Still goal-keyed and awaiting a per-surface decision: `stepsFor`'s fallback, `goalForOptimizationGoal` → audience-stats, `audienceRankMetric`, `OUTCOMES`/`GOAL_LABEL`/`OUTCOME_UNIT`, the outcome digest, `workflow-projection-choice`'s unit-cost branches. Guards: `tests/onboarding-funnel-scoped-projection.test.ts`. (#3491)

**A workflow-projection row is only READABLE when the producer states it is MEASURED — an UNMEASURED row is the backend's own serving affordance and is the CHEAPEST row by construction.** features-service serves two kinds of row on `/features/:slug/workflow-projection`. A measured row rests on real spend. An unmeasured one (`measured: false`) exists so an ACTIVE workflow that has never spent is reachable by campaign-service's argmin and can earn a first run — the only escape from "it cannot start because it has not started". Its `resolved.costPerOutcomeUsd` is an **explore allowance**, the price of one outreach, a cost floor and not a result; it carries no return at all (`costPerPaidClientUsd` / `roiMultiple` / `cacPct` null, `grain` null, `estimatesByGrain` empty). Every dashboard surface ranks these rows on that same field ascending, so an unproven workflow takes the "Your best model" headline the moment such a row appears: a bogus cheap cost above a blank return, with a grain label rendered off a null.

- **Filtered at the ONE reader** (`getWorkflowProjectionLadder` in `lib/api.ts` is the sole caller of the endpoint; `getWorkflowProjection` wraps it and every picker consumes its rows), through `lib/workflow-projection-measured.ts` — alias-free, so it carries real unit tests. Keep it that way. A per-surface filter is the one a new surface forgets to write.
- **Before the row schema, not after**, which is what keeps `resolved.grain` / `resolved.costPerClickUsd` NON-nullable and every `estimatesByGrain[row.resolved.grain]` read unbranched. Widening those types to model a case no surface may render is the wrong direction.
- **`measured` is deliberately NOT declared on the row schema** — post-filter it would parse as an always-true field nothing may branch on. This is a knowing strip, unlike the too-narrow-schema bug elsewhere in this file.
- **Only an explicit `false` drops a row.** An absent flag is kept, so the filter is an exact no-op against a producer that has not shipped it and degrades to current behaviour rather than blanking every surface at once.
- **The bad case was not merely a wrong ranking — it was a THROW.** Both nulled fields are declared non-nullable here, so an unmeasured row fails `safeParse` and the reader dies, which reveal-on-settle paints as headings with nothing under them. A channel where every row is unmeasured now yields no rows and its surfaces state their own no-data reading.
- `recommendedWorkflowDynastySlug` is already measured-only at the producer; do not re-derive a recommendation client-side. Guard: `tests/workflow-projection-measured.test.ts` (mixed fixture + a source guard that no surface re-derives the check). (#3522, features-service#805 / #821)

**Two smaller rules the same screen proved.** (a) **A form under copy saying "we prefilled this" must actually seed, and the seed goes through the SHARED helper.** The funnel detail step seeded `rates: {}` while its own paragraph promised a prefill, so every conversion field was blank and the user retyped what we already knew — then the model step showed those typed values, which is why it read as a one-screen bug rather than a missing seed. It now reads `funnelDraftFromBrand`, the same helper the Settings card uses (its param is the structural `FunnelSeedEconomics`, so the EFFECTIVE economics read — a subset of the columns, and the better guess for a brand-new brand because it folds cross-brand averages — can seed it too; a rate it does not carry seeds BLANK, never 0%). The economics live in STATE as well as `econRef`: a ref lands with no re-render, so the form would stay blank until a keystroke. (b) **A RANK or a SUPERLATIVE over a set of one states nothing, and reads as a missing sibling.** `1 of 1`, a `Primary` tag with nothing to be primary over, a `1` numeral in an icon tile, "your most profitable path" and "your primary goal" on a brand selling through one chain — each promises a comparison the page cannot show. Gate all of them on `length > 1` and state what the single thing IS instead (the numeral gives way to the funnel's own mark). Same family as the `1 of 1` counter fix that shipped before it: when you gate one of these, grep the screen for its siblings, because they were all written together.

**Onboarding checkout return — exactly ONE effect may own a `?launch_checkout=` redirect; the GENERIC resume must no-op on it.** A checkout return is owned end-to-end by the dedicated effect (`resumeCheckoutLaunch` for `success` → step `"launching"` → `completeLaunchAfterCheckout` → push to dashboard; the cancel branch for `cancelled` → restore pricing). The trap: the restored checkout snapshot's `step` is `"pricing"`, so the GENERIC resume (`resumeTargetRef` → `runResume(...)`, which re-hydrates the brand and lands on the snapshot step) computes target `"pricing"` and fires TOO — both effects run on mount and race, flashing the budget modal over the real `"launching"` flow (~2s while the stale `createBrandAndFetchServices` re-fetches), plus a wasted concurrent brand re-hydration. Rule: `resumeTargetRef` MUST be null whenever `searchParams.get("launch_checkout")` is set (covers `success` AND `cancelled`), and the first-paint `step` goes straight to `"launching"` on `success` so the budget step never renders. (#2173)

**Bumping `ONBOARDING_STATE_VERSION` must NEVER lose an in-flight checkout — the pending-checkout blob (`CHECKOUT_PENDING_KEY`) carries a NESTED `onboardingState` that is version-guarded, so a bump that lands while a user is at Stripe kills their cancel/success RETURN.** The nested `onboardingState` is only a convenience for re-rendering the deeper wizard; the blob's TOP-LEVEL fields (`brandId`/`orgId`/`brandUrl`/`hostname`/`outcome`/`budgetUsd`/`workflowSlug` + the lifted `selectedAudienceIds`) are version-INDEPENDENT and are the real source of truth for a launch. So `readPendingCheckoutLaunch` validates the top-level fields strictly, then — if `parseOnboardingState(nested)` fails (stale version) — `reconstructCheckoutOnboardingState` rebuilds a minimal CURRENT-version snapshot from those top-level fields (logged loud, keep-resolved not silent-fallback) instead of throwing the whole blob away. Before this fix, a mid-deploy return threw in `readPendingCheckoutLaunch` → the cancel handler's `catch` reset to `pricing` with no restore → the brand + wizard history were lost. Rules when touching this: (1) any field the checkout RETURN needs to survive a version bump lives at the TOP LEVEL of the pending blob, not only in the nested `onboardingState`; (2) never make `readPendingCheckoutLaunch` throw on a stale NESTED state — reconstruct; (3) the `_OrNull` opportunistic reader still purges on a TOP-LEVEL-invalid blob. Guards in `tests/onboarding-resume-persistence.test.ts`. (checkout-version-resilient)

**Audience activation happens ONLY at the TERMINAL launch (`completeLaunchAfterCheckout`), and it makes the picked set the brand's EXACT active set — the audience step does NOT activate.** Earlier the audience step ALSO activated on Continue (`setAudienceStatus(c.audienceId,"active")`), additively + never deactivating — so a re-roll (Back → re-suggest → re-pick → Continue) or a re-onboarding STACKED stale `active` rows, and the audiences page then showed more active audiences than the user picked ("picked 2, page shows 5"; confirmed in prod — one brand/org accumulated 5 active across two activation bursts). Fix (#2239): the audience step `saveAndContinue` only records `selectedAudienceIds` (no DB write); `completeLaunchAfterCheckout` is the SOLE activator — it (1) `listAudiences(brandId,{status:"active"})`, (2) demotes any currently-active audience NOT in the picks back to `"suggested"` (recoverable, hidden from the page), (3) activates the picks. So the picked set BECOMES the active set: re-rolling / re-onboarding OVERRIDES the prior selection instead of accumulating. Still fail-loud on an empty pick (the dashboard's hard "No active audience yet" blocker would otherwise hit an already-PAID campaign), and `beginCheckoutAndLaunch` blocks checkout up front when no audience is picked. Done BEFORE avatar gen (server-side on-activate gen, human-service #144). FOLLOW-UP (human-service #151): `/orgs/audiences/suggest` still persists every candidate as a permanent `"suggested"` row and never supersedes the prior batch per (org,brand) → orphan suggested rows accumulate (hidden from the page, DB pollution). (#2173, #2239)

## Agency-domain sending model — distribute sends from OUR domains, on the client's behalf (LOAD-BEARING positioning)

**distribute is a marketing AGENCY: all outreach goes out from inboxes/domains that WE own, warm, and operate — NEVER from the client's domain.** Think PR firm pitching journalists from the agency's own contacts. This is a core differentiator, not an implementation detail — copy across landing/dashboard/docs must reflect it and must NEVER imply the client connects/owns/configures the sending domain. The five client benefits (the canonical framing — reuse this vocabulary in copy):
1. **Zero reputation risk** — the client's domain never touches cold outreach; deliverability damage (if any) lands on OUR domains.
2. **Zero setup** — no DNS / SPF / DKIM / DMARC / inbox-warming / mailbox purchase on the client side. We own the full sending stack; the client only drops a URL.
3. **Zero inbox to babysit** — replies route to OUR inboxes first; we screen + qualify and forward only the positive ones to the client's Gmail.
4. **Full CRM visibility, still theirs** — the client keeps the whole view: who we contacted, opens, replies (incl. negative). We send on their behalf, nothing is hidden.
5. **Test demand before revealing the brand** — for niche/finite prospect universes, we can validate the need WITHOUT naming the client, then forward warm prospects — so they re-engage a limited list without saturating their brand.

**Copy discipline:** any line about sender/from/domain/inbox/warmup/deliverability/setup MUST attribute the infrastructure to distribute ("our sending domains", "we configure", "nothing to set up on your side"), never to the client ("your sending domain" ❌). The misleading-line fixes + the dedicated "Sent on your behalf" section live in `apps/landing/public/landing/how-it-works.html` (full 5-card section) + `index.html` (condensed 5-item strip). Terms of Service (`apps/landing/src/app/terms/page.tsx` §5) + the dashboard email previews (`From: distribute.you`, below) are the gold-standard surfaces — match their precision. (Agency-model copy sweep + dedicated section: KevinLourd/agency-domain-sending)

**NOTE — served landing path is `apps/landing/public/landing/**.html`, NOT `landing-v2/`.** `src/app/<page>/route.ts` → `staticResponse(...)` → `static-html.ts:233` joins `public/landing`. The repo CLAUDE.md notes that say "served set is `public/landing-v2/**`" are STALE (no `landing-v2/` dir exists). Edit `public/landing/`.

**⚠️ ACCENT IS BLUE, NOT GREEN — green is DEPRECATED, do NOT re-introduce it (Kevin 2026-07-21: "le nouveau design est blue, delete partout le green").** Every brand accent is `#2563eb` / OKLCH **hue 258**: landing `index-v1.html` `--green` VALUE is `#2563eb` (var misnamed, value blue), `css/styles.css` + React `globals.css` `--accent`/`--dy-accent`, the dashboard/admin `@theme --color-brand-*` ramp, every favicon and nav logo, and the `/brand` kit (SVG + PNG + `favicon.ico` + the zip, regenerated blue-D). Treat every "GREEN charter" claim below as HISTORICAL. SEMANTIC success-green is KEPT (KPI up-delta, positive status, "good" bars — green=good, not brand green). **The EMAIL templates were the straggler this line failed to catch, and the reason is worth keeping: the green-charter reskin (#2671, 15 Jul) landed six days BEFORE the revert, and the sweep swept the apps but not the template strings, so every button and link mailed to customers stayed green for three weeks.** Nothing surfaced it — no test renders those to a screen and nobody reads their own dunning mail; it only appeared when the admin fork was found overwriting them with its own older-but-coincidentally-blue copies. They now read `EMAIL_ACCENT` / `EMAIL_ACCENT_TEXT` / `EMAIL_DOT` (`#2563EB` / `#1A4FC3` / `#3D80FF`, contrast at or above the greens), and a guard in `email-templates-startup.test.ts` fails on any `#00xxxx` or on an accent hex repeated outside its declaration. **Generalise: when a charter sweep runs, the surfaces that render OUTSIDE a browser — email, PDF, OG images, anything with inline hexes — are the ones it misses, because none of them is covered by a screen test.** Still-green FOLLOW-UP: the dashboard/admin decorative rainbow-gradient `#10b981` stop. (#2939, #3008, #3013, #3304)

**A landing favicon/brand sweep is NOT just `<link rel=icon>` — it MUST cover: (1) the dynamic OG `ImageResponse` routes (`src/app/{,investors/,performance/}opengraph-image.tsx`, which drive `og:image` for the homepage + all 28 static pages), (2) `src/app/apple-icon.png` + `public/favicon.ico` binaries, (3) `seo.ts` `BRAND_LOGO_PATH` (React Org JSON-LD logo+image = Google Panel + investors OG), (4) static-HTML Organization JSON-LD `logo`/`image` (`public/landing/**.html`), (5) `layout.tsx` `openGraph.images`/`twitter.images`. Grepping only `rel="icon"` misses the OG cards + Panel logo.**

**The `apps/landing` React `navbar.tsx` nav = `How it works`/`Pricing` links + `Log in`/`Launch campaign` CTAs (parity with the static sub-page nav + homepage); footers carry a `Blog` link (`/blog`) — #2936/#2939.**

**HOMEPAGE (2026-07): `/` serves `index-v1.html` (the "5-minute-positioning" design), NOT `index-agency.html`.** The old agency homepage is stashed at `/v0`; the `/agency` route was RETIRED. `index-v1.html` is the graphic-charter baseline: DARK `--bg #070a0f` + Space Grotesk (display) / Inter (body) / IBM Plex Mono (mono), logo = glowing dot + "distribute" wordmark. This SUPERSEDES the #1644 "LIGHT-default indigo" note **for the homepage**. The same charter fonts + brand ramp were ported to `apps/dashboard` + `apps/admin` (`globals.css`), LIGHT default kept, byte-equal across the two apps; neutral gray tints (chroma <= 0.018) stay at hue 264. **The accent VALUE is BLUE per the correction above, not the green those PRs shipped.** The static SEO-cluster HTML + React landing pages are still un-reskinned. Brand kit at **`/brand`** (self-contained dark), assets under `public/brand/`. (#2595, #2600, #2605, #2607)

**HOMEPAGE-ERA ARCHIVE MAP + non-indexed `/v1` `/v2` reference routes (#2890, #2895).** Three generations, newest first: current `index-v1.html` at `/`; **Adam blue agency** `index-agency.html` at `/v0`, archived as the self-contained `public/landing/archive-blue.html` at `/v2`; **pre-Adam multicolor/beige**, which is the **React `page.tsx`** homepage (Fredoka + rainbow pastel + old "d" logo, last live `f23b1866e`), archived as the React route `src/app/v1/` with era components under `_c/` and every era rule scoped under `.v1-scope`. Both archives are `noindex` and out of `sitemap.ts`. **Identify an era by its DESIGN SIGNATURE in `globals.css` (font/palette/logo), NOT by which file `/` pointed at** — the beige era is a React page and cannot be inlined the way the static archives are. Navbar shrink on `index-v1.html`: constant `nav{height:64px}`, morph the pill only (max-width/radius/shadow) past `scrollY>60`; do not re-add the min-height/logo-mark resize, it reads jumpy.

**`index-v1.html` also carries the `__TICKER_BOARD__` token** (a live `#outcomes` cost section added #2600, resolving 7 dead `#outcomes` anchors) — same server-render mechanism as below. NOTE: the ticker currently renders LAST-KNOWN-GOOD fallback because the public `cost-per-outcome-trend` endpoint has been timing out (identical fallback on `/v0`, so pre-existing/backend, not a landing bug); real numbers appear automatically once the features-service public endpoint responds within budget.

**An SSR `replaceAll(TOKEN, injectedScript)` placeholder token in a served landing HTML MUST be UNIQUE vs every client global it injects — a bare token that also appears in the page's own JS gets rewritten too, producing invalid JS that silently kills the whole `<script>` IIFE.** The homepage CAC chart (`data-cac-chart`, under the `$52` live-CAC price) was DEAD in prod for exactly this: `withCacBoot` (`static-html.ts`) did `replaceAll("__CAC_BOOT__", "<script>window.__CAC_BOOT__=…</script>")`, and `__CAC_BOOT__` ALSO matched the client reader `var boot=window.__CAC_BOOT__;` → rewrote it to `var boot=window.<script>window.__CAC_BOOT__=…</script>;` (SyntaxError) → `renderCac()` + the live refetch never ran → blank chart, even though the injected boot payload had live data. Fix (#2832): the HTML placeholder is a DISTINCT token `__CAC_BOOT_SLOT__`; the injected script sets the `window.__CAC_BOOT__` global the reader reads. RULE: name any `replaceAll` slot token so it can not be a substring of the runtime global/identifier it feeds (`__X_SLOT__` placeholder → sets `window.__X__`). Also: `resolveCacBoot` bakes a deterministic `fallbackCacSeries(best)` when the live trend fetch is empty/slow (#2831), so the chart paints instant + never blank and the client swaps in live points when the endpoint responds. (#2831, #2832)

**NEVER surface a cross-org + cross-WORKFLOW POOLED metric on the landing — the wanted metric is always the BEST single model (MIN over per-workflow ratios), cross-org.** Cross-ORG aggregation is fine; blending N workflows into one average is banned (Kevin: "je ne veux JMS de cross org + cross workflow"). Two pooled public endpoints are therefore OFF-LIMITS to landing consumers: `cost-per-outcome-trend` (fleet-wide moving average) and `cost-projection`/`avgCostPerOutcomeByObjective` (fleet-mean). The BEST-model number comes from `workflow-cost-per-outcome` (per-workflow, take the MIN of `costPerOutcomeUsd` over workflows whose objective outcome was observed > 0 — never a 0-outcome husk) — that's the `$52` headline and everything on the page must be coherent with it (`fetchBestModelUsd` in `static-html.ts`). The CAC chart series + ticker sparklines are a deterministic best-shaped curve ENDING on the live best-model price (`fallbackCacSeries`/`fallbackSeries`) until a **non-pooled best-model DATED trend** endpoint ships in features-service (spawned `KevinLourd/best-model-cost-trend`); the client rescales the baked curve to the live best. A new landing surface that needs a $/outcome number reads `workflow-cost-per-outcome` MIN, NEVER a pooled trend/projection. (#2861 chart, #2863 ticker+pricing-budget, issue #2864)

**Any build-time fetch on a served landing page MUST be `AbortSignal.timeout`-bounded + fall back to last-known-good**, else a slow cold endpoint hangs the prerender past Vercel's 60s budget and the whole deploy ERRORs. Reference: the `/v0` ticker's `fetchTrendSeries` (8s) and the `/benchmarks` bounded fetch. (#2530, #2533)

**Changing how often a served landing page re-renders means moving TWO values, and moving one alone does nothing.** `export const revalidate` on the route governs Next's own data cache; `staticResponse()` sets an explicit `cache-control: s-maxage=…` on the `Response`, and THAT is what the CDN obeys — so the edge keeps coming back to the function on the header's schedule no matter what `revalidate` says. Both read `86400` today. The failure is silent and expensive: #3287 raised the 22 routes' `revalidate` from 300 and shipped green while all 21 pages kept re-rendering a ~127KB document every 5 minutes, which is exactly what Fast Origin Transfer bills for ($15/month, 459K ISR writes over 3 months). Crawlers keep every SEO page warm enough to hit the window continuously, so a nominally traffic-driven cost behaves like a timer. **Verify by reading the header off the live page** (`urllib`, per the curl-truncation rule), never by grepping the route file — the route file is the value that does not win. `stale-while-revalidate` stays long: it is what keeps a reader served instantly from the edge during a revalidation. (#3287, #3289)

**The REACT landing pages (`apps/landing/src/app/<page>/page.tsx` — investors, benchmarks, privacy, terms, blog) render on a HARDCODED-WHITE body (`globals.css` `body { background: var(--background) }`, `--background: #ffffff`, NEVER themed) while `layout.tsx` sets `<html data-theme="dark">` by default — so the `--dy-*` design tokens resolve to their DARK-theme values (`--dy-text` = light text). Net: using `text-[var(--dy-text)]` / `bg-[var(--dy-surface)]` for CONTENT on a React landing page gives LIGHT text on a WHITE body = washed-out/unreadable on a fresh (no-localStorage) visit. These pages must use PLAIN dark Tailwind grays (gray-900 headings / gray-600 body / gray-500 muted / white|gray-50 cards / gray-200 borders) — theme-INDEPENDENT, correct regardless of `data-theme` — exactly like the working siblings (benchmarks/privacy/blog headings compute to `rgb(26,26,46)` = the body `--foreground`, NOT a dy token). The `--dy-*` tokens are for the STATIC served HTML (`.dy-page` mechanism) + the home React surface that ride the theme attribute, NOT for these white-body content pages. **VERIFY the RENDERED COMPUTED color (headless Playwright `getComputedStyle(h2).color` on the live URL — preview deploy URLs are behind Vercel deployment-protection and return the auth interstitial, so probe PROD after merge) BEFORE declaring done — a class grep proves nothing about contrast.** (#2299 swapped investors' hardcoded grays → dy tokens and shipped STILL washed-out because dy-text=light; #2300 forward-fixed to plain light grays. Two merge+promote cycles burned by not probing the computed color before the first merge.)
- **#2303 then flipped `layout.tsx` to `data-theme="light"` default** (the React layout was the last straggler still defaulting `dark` after the #1644 light-default landing rebrand). So the dy-themed CHROME (navbar/footer/buttons) is now coherent-light on the white body. Content pages still use plain grays (theme-independent, unchanged) — but new dy-themed chrome now resolves correctly. Footer stays intentionally dark (`bg-gray-950`, hardcoded).
- **`src/components/navbar.tsx` (the React landing nav, shared by investors/benchmarks/blog/privacy/terms) had referenced class names that DON'T EXIST in `globals.css`** (`dy-chip`, `dy-nav-actions`, `dy-button-ghost`, `dy-button-primary`, `dy-nav-menu`, `dy-nav-mobile`, `dy-shell`) → unstyled + jammed action buttons ("Sign inStart free"). The real design-system classes (what the STATIC nav in `public/landing/js/components.js` uses) are `dy-nav-chip`, `dy-nav-right`, `dy-btn dy-btn-g`, `dy-btn dy-btn-p`, `dy-nav-toggle`. #2303 corrected them. When touching `navbar.tsx`/`benchmark-cta.tsx`, the class vocabulary MUST match `globals.css` (`grep '\.dy-' globals.css`) — the static `components.js` nav is the reference for correct names.

**Email-preview `From:` is ALWAYS the literal `distribute.you`, never the client's company.** The outreach is always sent by the marketing agency distribute.you on the client's behalf (the agency-domain model above), so the 3 email-preview pages (`brands/[brandId]/emails`, `features/[featureSlug]/emails`, `features/[featureSlug]/campaigns/[id]/emails`) hardcode `From: distribute.you`. Do NOT re-introduce `selectedEmail.clientCompanyName || 'Your Company'` (or any brand-name-derived From) — Kevin: "c'est toujours envoyé par l'agence marketing qui s'appelle distribute.you." (#1403)

### React Query mutations: write the response to the cache, don't just invalidate

Mutation returns the fresh entity → `queryClient.setQueryData(["entity", id], data)` (not just `invalidateQueries`). The GET can 5xx, and with `placeholderData: keepPreviousData` an invalidate-then-failed-refetch leaves the stale pre-mutation row → user thinks the click did nothing. `invalidateQueries` still correct for LIST caches the mutation can't rebuild (`["campaigns"]`, `["leads"]`). (#1090)

### Dashboard data fetching — ONE framework (React Query v5 SWR)

TanStack React Query v5 on every client component. No SWR, no mixing. Server components for the shell only.

**Global config (`src/lib/query-provider.tsx`):** `staleTime: 60_000` · `gcTime: 30min` (= persister `maxAge`, see below) · `placeholderData: keepPreviousData` (GLOBAL — never re-set per query) · `refetchOnWindowFocus/Reconnect: true` · `retry: 1` queries / `0` mutations.

**Poll cadence (`src/lib/query-options.ts`):** ONE cadence — `pollOptions` = `POLL_INTERVAL` = **30s everywhere** (the 5s base + the `pollOptionsSlow` 10s / `pollOptionsSlower` 30s tiers were collapsed to a single 30s in #2196). Import + pass; never a local `const pollOptions`. Idle/hidden-tab polling is paused globally by `installIdleFocusManager`, so an AFK tab stops polling regardless of cadence.

**Idle/visibility polling pause (`src/lib/idle-focus-manager.ts`) — load-bearing, do NOT remove.** A foreground-but-AFK dashboard tab that keeps polling every 5s re-renders lists/charts/badges → DOM mutation → feeds PostHog's rrweb session recorder an unbounded mutation stream → Chrome **renderer OOM** ("Aw Snap") on long-lived tabs (confirmed: 15–20h recordings with <100s real activity). `installIdleFocusManager()` (mounted once in `query-provider.tsx`) installs a custom `focusManager` event listener reporting the app **unfocused when the tab is hidden OR the user is idle >2min** (`IDLE_MS`); with `refetchIntervalInBackground` at the v5 default `false`, this pauses EVERY poll, and `refetchOnWindowFocus:true` refreshes on the next interaction. This is the ONLY thing pausing idle polling — do not re-introduce an always-on global poll, and keep session recording ON (the fix removes the *cause* of the OOM, not the recording). posthog-js kept current (≥1.386.6) for the rrweb canvas/Safari/memory fixes (PostHog/posthog-js#3273). (#1514)

**V5 render rule:** `isPending`/`isLoading` (no data) → SKELETON. `isFetching && !isPending` (background refetch with cached data) → CONTENT, never skeleton. Background refetches are SILENT — no top-bar/spinner/flash (matches Linear/Vercel/GitHub). Button-scoped spinner only for mutations. **A mutation button's in-flight label must stay FULL-opacity — a disabled button carries `disabled:opacity-40`/`opacity-50`, which fades the very "Saving…/Launching…" text meant to signal work and reads as a DEAD button.** Gate the fade class on `!isMutating` (`${isMutating ? "cursor-wait" : "disabled:opacity-40 disabled:cursor-not-allowed"}`) and render a `<Spinner/>` next to the label, so the working state is at 100% while the genuinely-unavailable state still fades. For a multi-second create-then-navigate flow, also pre-seed the destination (`setQueryData(["entity", id], response)` before `router.push`) AND show a full-screen "Launching…" overlay during the POST — the new id doesn't exist until the POST returns, so you cannot route earlier; reassure in place. (#1408: the launch page — then `campaigns/new`, now `brands/[brandId]/launch` — Launch button showed a faded `Launching…` for ~3s → "blanc" the user read as a bug.)

**Carve-out — `isPlaceholderData` on a KEY CHANGE → SKELETON, not stale content.** The "never skeleton when data exists" rule is about a background refetch of the SAME key. When a panel's query key carries an entity id (`["workflowExamples", workflowSlug, brandId]`, `["x", selectedId]`) and the user switches entity, the GLOBAL `keepPreviousData` keeps the PREVIOUS entity's data in `data` → `isPending=false`, `isFetching=true`, `isPlaceholderData=true`. Rendering that = showing entity A's content under entity B's selection for the whole cold fetch (~2s). `isPlaceholderData` is the discriminator: gate the skeleton on `isPending || isPlaceholderData` so a switch paints a skeleton (not the wrong entity), while a warm re-select (cache hit < `staleTime`) stays instant. Pair with `queryClient.prefetchQuery` for every visible entity on container/modal open → switching becomes instant after warm-up. (#1383: workflow example-emails panel showed the prior workflow's 3 emails for ~2s on switch because the `"Loading…"` text was gated on `isPending`, which never fires under `keepPreviousData`.)

**Detail-page not-found gate — gate on `isPending`, NEVER `isLoading`.** A detail page is tri-state (skeleton / not-found / content). The not-found branch must fire ONLY once the query has RESOLVED empty: `!isPending && !data` for not-found, `isPending` for skeleton. Do NOT use `isLoading`: `useAuthQuery` suspends a query (`enabled:false`) via the org-consistency gate until Clerk's active org resolves and matches the URL org, and a disabled v5 query reports `isPending:true` but **`isLoading:false`** (`isLoading = isPending && isFetching`, `isFetching` false while idle). Gating not-found on `isLoading` → `!isLoading && !data` is `true` during the org-settle window → a red-cross "X not found" flash for ~2s on every cold load before the fetch runs. `getX` throws on 404 → query error → `isPending` false → genuine not-found is preserved. The source-substring guards (`brand-404-handling.test.ts`) double as the site INVENTORY for this gate — run the dashboard suite, a failing guard pins a site you still need to migrate. Fixed across the detail surfaces (brand overview, brand-info, workflow page, press-kit, workflow-detail-panel; the campaign detail surface via `campaign-context` was removed in #1768). (#1384)

**Same bug in TWO more forms — `isLoading && !data` skeleton guards, and context-exposed loading flags.** The not-found gate is only the loud version; the identical org-settle flash hits any TERMINAL UI gated off `isLoading`: (a) **list empty-states** — `if (isLoading && !data) return <skeleton>` then `items.length===0 → "No X yet"`: during settle `isLoading` is false so the skeleton is skipped and the empty-state flashes before the fetch runs. **Key equivalence: `isPending` ≡ `isLoading && !data` in the *enabled* case (skeleton on first-load-no-data, content on refetch-with-data) and differs ONLY in the disabled/settle case** — so swapping `isLoading && !data` → `isPending` is a strict, zero-regression improvement. Do NOT read `isLoading && !data` as "already safe" (it is the bug, not the fix — a subagent audit mis-classified it as safe). (b) **context loading flags** — a context built on `useAuthQuery` (e.g. `features-context`'s `["features"]` query) must expose its loading via `isPending`, else every consumer that gates "Feature not found"/skeleton on it flashes; fix once in the context (`isPending: isLoading` alias) and all consumers inherit it. Swept ~28 surfaces at the time: `features-context` + (the now-removed, #1768) `features/[featureId]/{page,new,workflows}` "Feature not found" guards + all `isLoading && !data` list guards (leads/emails/articles/outlets/runs/logs/quote-requests/quote-pitches/press-kits/workflows — the feature- and campaign-level variants were removed in #1768; the brand-level ones remain + brand-usage/leads-stats-panel/visibility-runs/api-key-preview/workflow-selection-table). (#1392)

**Third form — a list/tabbed surface that splits ONE entity across SEVERAL status queries must gate each tab/section on ITS OWN query, never a COMBINED `isPending`.** When a page fans one list into N parallel reads (e.g. Audiences = `["audiences",brandId,"active"|"paused"|"archived"]`, a status-tab table, anything with per-status fetches), a single `const isPending = aPending || bPending || cPending` + `&& combined.length===0` skeleton guard fires ONLY when ALL N are empty. The instant the FIRST query resolves, the guard passes — so a still-loading sibling tab renders its empty-state ("No archived audiences") instead of a skeleton. The producers cold-start seconds apart (human-service 0.25 CU), so this is the steady-state look, not a rare race. Fix: a per-tab loading flag (`archivedTabLoading = archivedPending || (archivedRows===0 && archivedFetching)`) and skeleton that tab while ITS query is pending OR fetching-while-empty — `isFetching` is load-bearing because the per-query persister can RESTORE an empty snapshot (e.g. archived=[] from before anything was archived, and it does not expire for 30 days) then revalidate in the background, where `isPending` is false but the empty is stale. Empty-state shows only after that tab settles with 0 rows. Mirror the sibling that's already right (`EngagedLeadsPage` gates on `isPending || isPlaceholderData`). (#2230)

**Page composition:** shell+nav+header render instantly (no query gates them). Each card owns its `useAuthQuery` + skeleton, parallel. Skeleton matches the real layout (zero shift). No in-page `<Suspense>` to gate dashboard content (React Query + per-card skeletons handle within-page loading; in-page `<Suspense>` is public-marketing only). **This ban does NOT cover route-segment `loading.tsx` — see "Instant nav transitions" below.**

**Injecting a full-width row (stat cards, banner) at the TOP of a list/detail SPLIT page → put it INSIDE the inner padded content column, NEVER as a direct child of the `flex flex-col md:flex-row` split root.** The brand-level entity pages (leads/emails/journalists/outlets/articles/companies; the campaign-level variants were removed in #1768) return a root `<div className="flex flex-col md:flex-row h-full relative">` whose children are the list column + the detail panel. A row dropped as the FIRST child of that root becomes a flex ROW sibling on desktop → it renders as a squished LEFT COLUMN beside the list, not a full-width band above it (on mobile `flex-col` masks the bug, so it passes a quick mobile check). `tsc`+tests stay green — it's a pure layout defect. Correct placement: first child of the inner `p-4 md:p-8` content column (scrolls with the list, full-width when no detail panel). Reusable component for the outreach stat row = `components/revenue/outreach-stat-cards-auto.tsx` (`OutreachStatCardsAuto`, self-contained: reads brand/campaign scope from route params, fetches its own featureStats + sales-economics funnel, reuses existing query keys to dedupe). (#1689)

**Instant nav transitions (`loading.tsx` route boundaries) — REQUIRED, distinct from the in-page `<Suspense>` ban.** A dashboard route is DYNAMIC (Clerk `auth()` + `ClerkProvider dynamic`); Next 16 does NOT prefetch a dynamic route and BLOCKS on the old page until its full RSC render returns — **unless** the segment has a `loading.tsx`. Without one, a sidebar click shows "nothing for several seconds." Fix is global: one `loading.tsx` → `<DashboardPageSkeleton/>` at each segment level **below** the persistent `(dashboard)` layout (org · brand · feature · campaign — NEVER at `(dashboard)/` itself, that would blank the sidebar). Boundaries live at `orgs/[orgId]/loading.tsx`, `…/brands/[brandId]/loading.tsx`, `…/features/[featureSlug]/loading.tsx`, `…/campaigns/[id]/loading.tsx`. **Load-bearing invariant: the intermediate layouts (`[orgId]`/`[brandId]`/`[featureSlug]`/`[id]`) MUST stay empty server passthroughs — no `cookies()`/`headers()`/uncached `fetch` in a layout, or `loading.tsx` can't show a fallback for it and navigation blocks on the layout render (Next `loading.js` "Good to know" caveat).** New dashboard route level → add its `loading.tsx`. (#1338)

**Body reveal — use the `CoordinatedReveal` component, not a hand-rolled ternary.** `src/components/coordinated-reveal.tsx` wraps `useCoordinatedReveal`: `<CoordinatedReveal flags={[a !== undefined, …]} skeleton={<Skel/>}>{() => <Content/>}</CoordinatedReveal>`. `children` is a RENDER FUNCTION — content only evaluates once revealed (it dereferences now-resolved data). Declare every `useAuthQuery` at page top (parallel fire); this gates only the paint. Same warm/cold rule as the hook: group cold-coherent siblings, never AND a warm cache with a cold sibling.

**Static-shell-first reveal (3-tier hierarchy) — the DEFAULT for any card with static structure.** Loading is NOT binary (all-skeleton → all-content). Three tiers, validated against the React `<Suspense>` docs ("reveal nested content as it loads"), Next PPR (static shell + dynamic holes), TkDodo (anti-popcorn) and NN/g ("real content replaces skeletons as available; don't frame-only-skeleton"): **(0) static shell** — card frame, **title, subtitle, labels, static copy** — render on the FIRST paint, never skeletoned; **(1) coordinated values** — the data regions only (stat numbers, charts, donuts, table cells) show a `<Skeleton/>` and reveal together (the `pending`/`revealed` latch); **(2) progressive rows** — list/table rows stream within an already-shown frame. Pattern: a data card takes a `pending?: boolean` prop, renders its frame+title+labels always, and wraps ONLY each value region in `{pending ? <Skeleton className="…" /> : <value>}`. The PAGE computes `const valuesRevealed = useCoordinatedReveal([...])` and passes `pending={!valuesRevealed}` — it does NOT whole-body-gate a card that has a static shell. Co-locate the value-skeleton sizing with the real value (Aurora Scharff: skeleton lives next to its component → zero drift). Reference impls: `BrandMetricsHeader` (MetricCard frame+title always, value children gated on `!revealed`), `RevenueCostSummary`/`RevenueOverviewSection`, the `campaign/*` charts. Whole-body `CoordinatedReveal` (above) is the fallback ONLY for content with NO static shell (e.g. a bare list, where the frame IS the rows). Guard: `tests/static-shell-reveal.test.ts`. **Skeleton imported into a test-IMPORTED component must be RELATIVE (`../skeleton`), not `@/components/skeleton` — vitest has no `@` alias config, so an aliased import in an import-tested module fails to resolve.** (#1347-follow-up)

**Mutations:** `setQueryData(["entity", id], response)` FIRST, then `invalidateQueries(listKey)`; return the invalidation promise from `onSuccess`.

**A control whose only feedback is a spinner reads as DEAD — show the statement the person just made while the producer answers.** Every write on a read-driven control costs two round trips (the POST, then the re-read the invalidate schedules), and for both of them the row renders exactly as it did before the click. On the lead panel's Funnel progress that meant picking `Wants to know more` left the row reading `Replied, kind not stated` for seconds, reported as the UI showing nothing. Hold the pressed statement in local state, scoped to the ENTITY it was made on (`{email, kind}` / `{leadRowId, key, next}` — an unscoped latch shows the previous lead's answer on the next one), render it over the served value, and drop it the moment a refusal comes back so the panel never states something nobody recorded. Two things this is NOT: it is not a fallback (the producer's answer always wins, and the error is rendered), and it does not license re-deriving what the producer decides — lead-service supersedes an earlier `never` and cascades the chain, so `useSetLeadStepStatement` still invalidates and the pending statement covers ONE row while that read lands. Where the response IS the row the query reads (`setManualQualification` → the `["leadReplyKind", …]` `limit: 1` read), write it with `setQueryData` instead of invalidating: a re-read there is a second round trip spent learning what the producer already told us. Guards: `tests/lead-funnel-stage-panel.test.ts` — and they pin the CALL SITE, since a page that computes the optimistic value and passes the served one is the feature entirely absent with the component perfectly correct.

**A Save/dirty gate MUST be a LIVE compare against a baseline snapshot, NEVER a sticky boolean latch.** `const dirty = !formsEqual(form, baseline)` where `baseline` = the last-SAVED (or hydrated) form, set at init (cache/defaults), on hydration (server read), and on save-success (`formFromEconomics(res.salesEconomics)`). A `useState<boolean>` latch flipped `true` on any edit stays `true` forever, so a change-then-revert — flip a segmented-toggle/goal-tab and switch back, or edit a field and undo it — leaves Save wrongly ENABLED though nothing changed. Reference: strategy page's `offerDirty = draft !== null && !fieldsEqual(draft, baseline)`. `brand-sales-economics-card.tsx` was the sticky-latch offender (goal-tab flip kept Save armed) → fixed to derived-compare. (#2450, #2452)

**Forbidden:** local `const pollOptions` · `refetchIntervalInBackground: false` (v5 default) · per-query `placeholderData: keepPreviousData` · skeleton on `isFetching` when data exists · in-page `<Suspense>` to gate dashboard content (route-segment `loading.tsx` is REQUIRED, not this) · **a gate that ANDs a WARM section (warm-cache, e.g. `["features"]`) with a COLD sibling** — group at the finest coherent level, barrier WITHIN a group, independent ACROSS groups. (#1257) · **a reveal barrier (`useCoordinatedReveal`) that gates on a SLOW / inline-compute query** — a get-or-refresh POST that scrapes on cache-miss, or any cold source that can take seconds — freezes the WHOLE group in skeleton ("shows nothing") until that one source returns, holding the fast siblings hostage. Gate the barrier ONLY on fast cache-read queries; a slow cold source reveals WITH the group and renders its OWN card-local skeleton while pending (`metricPending ? <Skeleton/> : …`). A fast GET cache-read is barrier-safe; a POST that triggers a scrape is NOT. (#1330→#1331: card-4 ahref Brand-Radar `ai-visibility` get-or-refresh POST in the barrier held all 4 brand-overview cards in skeleton until the Apify scrape returned.)

**Coordinated reveal** (`src/lib/use-coordinated-reveal.ts`) — reveal a coherent all-cold group together in one paint, then keep it revealed (a poll / token rotation / transient error must not send a shown group back to skeleton). The reveal-layer analog of `keepPreviousData`.
- `useCoordinatedReveal(readyFlags: boolean[]): boolean` — barrier (false until every flag true once) + monotonic latch (true stays true for the mount). Call unconditionally at top level (uses `useRef`).
- One flag per query: `data !== undefined` or `!isPending`. **A disabled query stays `isPending` forever** — gate its flag `!enabled || !isPending`.
- **Group at the finest COHERENT level, nest coarse→fine** (body group ⊃ sidebar nav-items group ⊃ badge-numbers sub-group). Never span warm+cold.
- **Prepend an inputs-loaded flag (`defsReady`) FIRST** when queries are conditionally enabled off another query — else the first paint has every query disabled, every flag true, the barrier passes instantly and latches an EMPTY group. `useCoordinatedReveal([defsReady, ...flags])`. (#1260)
- **Gate each section on the query that produces ITS data, never a context's coarse `loading`.** A shared context's single `loading` often tracks only its primary query; the list a page renders (`emails`, `leads`) resolves later → gate on `emailsLoading`/`leadsLoading`. (#1264)
- **Sidebar primitives** (`context-sidebar.tsx`): gate a nav level on `defsReady` + render `SidebarNavRowSkeleton`; reveal count badges together via `badgePending={!badgesRevealed}` on `SidebarLink`. (#1261)
- **Standard for every multi-query surface** — page bodies + all data-dependent sidebar levels (`BrandLevelSidebar`; the `CampaignSidebar`/`McpSidebar`/`AppFeatureLevelSidebar`/feature-level sidebars were removed in #1768). Static-only nav levels need no gate. ~40 pages still un-adopted = a phased sweep (first-load stagger only; the return-flash is fixed globally by persistence) — do NOT bundle into a hotfix. (#1259/#1260/#1261, DIS-133)
- **ONE barrier PER visually-distinct card — never a single page-wide `revealed` that ANDs heterogeneous card-queries.** A page that funnels all its queries into one `useCoordinatedReveal` holds EVERY card in skeleton until the SLOWEST query returns (a slow features-service revenue/stats cold-chain sibling freezes the fast campaign-LIST that only needs campaign-service). The user's rule: within a card (or the sidebar badge group) everything reveals together; BETWEEN cards it's independent. So compute one latch per card and gate each card on ITS OWN query — per-card `trafficRevealed`/`drRevealed`/`aiVisRevealed` on `BrandMetricsHeader` (queries sharing ONE source share a latch — visits+revenue both read traffic), `platformReady`/`providersReady` per section on `api-keys/page.tsx` (the `campaigns/page.tsx` list latches `listRevealed`/`heroRevealed`/`chartsRevealed`/`costRevealed`/`statsRevealed` were removed with that page in #1768). The SIDEBAR is the one coherent multi-card unit that reveals its badges together — but its nav ROWS still gate on `defsReady` alone (instant), badges as a finer `badgePending` sub-group (`BrandLevelSidebar` already does this right). Guarded by `static-shell-reveal.test.ts` + `brand-metrics-getorfetch.test.ts` (pin the per-card latch names). (#1551: full per-card sweep; brand-metrics/api-keys fixed, sidebar/overview were already correct; the campaigns-list + campaign-detail surfaces it also touched were removed in #1768.)

### "API returned a non-JSON response" = the Vercel function was OOM-KILLED — diagnose the PROXY, not the endpoint

**That client error string never means the endpoint is broken. It means the `/api/v1` proxy replied with something that isn't JSON — in practice Vercel's own HTML error page after `Vercel Runtime Error: instance was killed because it ran out of available memory`.** Diagnose with `mcp__vercel__get_runtime_logs` on the app's project (`distribute-admin` / `distribute-dashboard`, team `team_lYmJIUH6q2rTY6dUfDiYtpAt`) around the failure minute — the OOM line names the killed request. **Under Vercel fluid compute ONE instance serves several concurrent invocations, so the request that dies is usually the VICTIM, not the cause**: a long-running POST (an LLM extraction) gets killed by whatever heavyweight GET blew the instance seconds earlier. Tell you're in this case: the failing request 500s FAR too fast for the work it does (a `POST /v1/brands/extract-fields` LLM prefill dying ~2s after the click). Look for a second OOM on a big list endpoint in the same window before blaming the endpoint you clicked.

Two invariants that came out of it (#3021):
- **The catch-all proxy (`apps/{admin,dashboard}/src/app/(authed)/api/v1/[...path]/route.ts`) MUST stream `res.body` through — NEVER `await res.text()` then re-wrap.** Buffering holds every payload TWICE (the JS string + the re-encoded `NextResponse` body). `GET /v1/emails?brandId=` returns every email for the brand with `bodyHtml` + `bodyText` + the `sequence[]` bodies + the full `generationRun` cost tree (`costs[]` + `descendantRuns[]`), so the doubling is what kills the instance. Guarded by `tests/proxy-stream-no-buffer.test.ts` in BOTH apps (`not.toContain("await res.text()")` — so never write that literal in a comment there either).
- **`apps/admin` now runs the SAME single 30s poll cadence as `apps/dashboard` (#2196).** `lib/query-options.ts` `POLL_INTERVAL = 30_000`; `pollOptionsSlow` / `pollOptionsSlower` are ALIASES of `pollOptions` (kept so the ~60 call sites don't churn) — do NOT re-introduce a faster tier, and do NOT hardcode `refetchInterval: 5_000` inline (the guard test fails on any `refetchInterval: 5_000` left in `apps/admin/src`). The one sanctioned exception is the workflow fork-detection poll (`workflows/[workflowId]/page.tsx`, 3s) because it drives an immediate navigation on a tiny payload. Root cause of the burst: `context-sidebar.tsx` polled FIVE unbounded brand list endpoints (emails/leads/outlets/journalists/articles) every 5s purely to render count badges.
- **The badges still download the full lists** — a served count is the real fix (issue #3023, backend). Until then the cadence + streaming are what keep the instance alive.
- **The non-JSON `ApiError` message now carries HTTP status + content-type + a 200-char body preview** (`readJsonResponse` in both apps' `lib/api.ts`). Most call sites render `err.message` alone, so a bare "non-JSON response" hid whether the platform 500'd, the gateway timed out, or the route was missing. Keep the diagnostics in the MESSAGE, not only in the error body.

### Monotonic status latch (`useMonotonicStatuses`) — status-tab tables must not flap on a poll

Keeps a per-row derived bucket across refetch (the only family member addressing a valid CHANGING success). A status-tab page derives each row's tab from a delivery/engagement overlay re-fetched every poll; a transient drop (status under a different shape, or empty) sends every row back to "Processing", leaving the viewed tab → the table empties then repopulates. `keepPreviousData`/coordinated-reveal can't fix it (valid 200, content legitimately changed).
- `src/lib/use-monotonic-status.ts` → `useMonotonicStatuses(entries: {id, status}[], priority, label?)`: a per-mount latch on the page's "most-advanced-first" priority (`LEAD_STATUS_ORDER`, or `STATUS_PRIORITY` from `outlet-status.ts`). Outreach is append-only (`contacted→sent→delivered→opened→clicked→replied`) so a less-advanced later poll is a stale read — keep the most-advanced, `console.error` the suppressed downgrade (fail-loud, real invariant). The returned Map is the single source for tabs + row badge + detail panel.
- **Rule:** every CLIENT-POLLED status-tab surface MUST latch via `useMonotonicStatuses`. Static/ISR tables aren't susceptible. Wired into the brand-level status-tab surfaces (brand × leads/journalists/outlets) + added missing `safeParse` on `listBrandLeads`/`listCampaignLeads`. (#1270, DIS-149. NOTE — #1768 removed the feature-level + campaign-level variants; only the brand-level surfaces remain. `listCampaignLeads` survives as the launch modal's self-fetch.)

### Local-first SWR cache (per-query IndexedDB persister) — open a page → its content NOW

**This is the dashboard's named architecture: a LOCAL-FIRST, stale-while-revalidate (SWR) cache.** Local-first = the on-disk cache is the source the UI paints FIRST, network is secondary. SWR = show the cached (stale) data instantly, refetch in the background, swap fresh in silently (TkDodo: *"stale data is better than no data, because no data means a loading spinner = perceived slow"*). It is the 4th "don't revert resolved state on a transient" layer and the only one that survives cache EVICTION/RELOAD (the first three keep warm-in-memory state across refetch only): 1. `keepPreviousData` (data) · 2. `useCoordinatedReveal` (reveal) · 3. `useMonotonicStatuses` (row bucket).

**Mechanism (`src/lib/query-provider.tsx` + `src/lib/persist-cache.ts`, unit-tested):** the **PER-QUERY persister** `experimental_createQueryPersister` (from `@tanstack/react-query-persist-client`), wired as a default query option (`queries.persister = persister.persisterFn`) on a plain `QueryClientProvider`. Storage = **IndexedDB** via `idb-keyval`. NOT the old whole-client `PersistQueryClientProvider` + `createSyncStoragePersister` (localStorage).

**Why the migration (2026-06-25, KevinLourd/dashboard-instant-cache) — the 2026-06-23 "persist-everything into localStorage" policy was SELF-DEFEATING, causing the exact two symptoms it meant to fix ("30s skeletons all the time" + "site lourd/lent"):**
- **localStorage's hard ~5 MB per-origin cap** → a heavy brand's `brandLeads`/`brandEmails` blew the cap → `removeOldestQuery` evicted the small overview queries (cached first) → the overview cold-skeletoned on the slow Neon chain. The persist-all policy evicted the very page it was meant to keep.
- **Whole-client `dehydrate()` re-serializes the ENTIRE cache on EVERY mutation** (TanStack #9775). ~12 overview queries polling 5s → constant multi-MB `JSON.stringify` + sync write on the MAIN THREAD → jank/"lourd".
- The per-query persister fixes BOTH: each query is written **separately, keyed by its query hash, only when IT changes** (no whole-cache re-serialize) AND IndexedDB has **no 5 MB cap + async off-main-thread writes**. Verified against TanStack `createPersister` docs + TkDodo #5169.

**`admin` ≠ `dashboard`: separate Vercel apps = separate ORIGINS = separate storage.** `admin.distribute.you`'s heavy Outlets/Journalists/Articles cache NEVER touches the `dashboard.distribute.you` cache. Don't diagnose dashboard slowness via admin's big lists; the dashboard customer never even loads those (alpha/staff-gated). Admin is allowed to stay heavy.

**`apps/admin` runs the SAME per-query IndexedDB SWR persister (ported from the dashboard, #2779) BUT with a DENYLIST, not the dashboard's curated allowlist — do NOT re-introduce a `PERSISTABLE_QUERY_ROOTS` allowlist in admin, and do NOT re-sync admin's `persist-cache.ts` to the dashboard's.** admin is staff-only on a separate origin ("allowed to stay heavy"), so `isPersistableQueryKey` persists EVERY successful root except `SENSITIVE_QUERY_ROOTS` (`apiKeys`/`byokKeys`/`keySources` — key material only). This is deliberate: any new admin page auto-SWRs with zero allowlist maintenance (a new dashboard page still opts in via the allowlist). Two more admin-specific divergences from the dashboard's `query-provider.tsx`: (1) the cache **bucket = `urlOrgId ?? "platform"`** (`bucketForPath`) — admin has TWO page classes: `/orgs/[orgId]/…` god-mode customer pages (bucket = URL org id, DIS-143 isolation preserved) AND cross-org FLEET pages (`/metrics`, `/audit`, `/feature-stats`, `/workflows`, `/features` — platform-global data owned by no org, bucket = fixed `"platform"` so they're SWR regardless of the staff user's Clerk active org); (2) the nav reseed + remount key derive from that URL bucket (no monotonic active-org latch — the URL is stable, never blinks). Storage key namespace is `distribute-admin-cache:{bucket}` (vs `distribute-dashboard-cache:`). (#2779)

**Load-bearing rules:**
- **`maxAge` is a LIE without `navigator.storage.persist()` — request persistent storage at boot, or the browser EVICTS the whole IndexedDB store after a few idle days and every `useAuthQuery` surface cold-skeletons again.** Best-effort IndexedDB (the default) is evictable under disk pressure / prolonged non-use, so the "instant across days" the per-query persister promises silently decays — this is the recurring "instant right after a deploy, infinite skeleton a few days later" report on audiences/leads. `QueryProvider` calls `requestPersistentStorage()` once at mount (feature-detected, idempotent via `storage.persisted()`, never throws). Do NOT remove it; a rebrand/refactor of `query-provider.tsx` must keep the boot call. Pairs with the org-gate latch below — eviction UNMASKS the disabled-query hang. (KevinLourd/leads-skeleton-persist-orggate)
- **Disk retention (`maxAge`) and memory (`gcTime`) are DECOUPLED, and BOTH are finite.** The decoupling is why per-query beats whole-client: a heap-GC'd query **stays on disk**, so staying instant across sessions needs NO `gcTime: Infinity` → no heap blowup. (The whole-client persister mirrors disk⇆memory, so `maxAge: Infinity` there would force `gcTime: Infinity` = the #1273 OOM.) `PERSIST_MAX_AGE_MS = 30 days`, `PERSIST_GC_TIME_MS = 30min`. The old "`gcTime` MUST equal `maxAge`" rule (TanStack #5169) applies ONLY to the whole-client persister.
- **A read of the persisted cache is SCOPED TO ONE BUCKET, and the store is SWEPT — `maxAge` was `Infinity` and nothing ever deleted anything, so opening any page loaded every response the app had ever cached into the heap.** The store is one flat key space shared by every bucket (each god-mode org, plus admin's `platform`), and the persister's own `restoreQueries` / `persisterGc` call `storage.entries()` and only THEN filter on `key.startsWith(prefix)` — so a whole-store `entries()` decodes every org's cache to keep one page's. It ran twice per mount (the mount `restoreQueries` plus the nav reseed, whose effect also fires on mount) and again on every navigation. Reported as a single admin tab holding **3.5 GB**. Three parts, all in `lib/idb-bucket.ts` + `lib/persist-cache.ts`, byte-equal across both apps: (a) `bucketEntries(prefix)` reads the bucket's CONTIGUOUS key range with a bounded `getAll`, and is what the storage adapter's `entries()` returns — the persister then finds its prefix filter already satisfied; (b) the mount `restoreQueries` is GONE (the nav reseed is a superset AND cold-guarded, while `restoreQueries` calls `setQueryData` unconditionally and can stomp fresher memory) — do NOT add it back; (c) `sweepStaleEntries` walks a CURSOR at boot, in constant memory, across every bucket, deleting what `PERSIST_MAX_AGE_MS` and the buster say is past. Do NOT reach for the persister's own `persisterGc()`: it calls `entries()`, so garbage collecting would re-create the allocation this exists to avoid, and it only ever looks at the current bucket. `reclaimLegacyStore` clears the pre-bounded store once ever (the sweep cannot reach it — most of those bytes are recent enough not to be stale), guarded by `RECLAIM_MARKER_KEY`, which the sweep must never delete or the reclaim runs on every boot. Guards: `tests/persist-bucket-scope.test.ts`.
- **Org-scope via the persister `prefix`** = `persisterStorageKey(orgId)` (`distribute-dashboard-cache:{orgId}`); each query's storage key is `${prefix}-${queryHash}`, so org A's keys never collide with org B (DIS-143). **`storage: undefined` while `orgId` is null OR on the server** (`persistEnabled = typeof window !== "undefined" && !!orgId`) — no anon-bucket cross-org bleed. The keyed remount (`key={orgKey}`) still resets the in-memory cache on switch.
- **`buster` = MANUAL version constant** (`persistCacheVersion()` → `PERSIST_CACHE_VERSION`, currently `"1"`), bumped BY HAND only on an incompatible shape change — NEVER the git commit SHA (the SHA flips every deploy, ~12×/day → wiped the disk cache on ~every visit, defeating persistence). Expiry is a coarse safety net at 30 days, so `buster` is the practical forced invalidation; cross-deploy shape drift is otherwise tolerated by `safeParse`/`z.coerce` on list readers + keep-last-good `structuralSharing`. Bump when you rename/remove a field on a response type consumed straight from cache with no safeParse guard (additive optional fields do NOT need a bump).
- **Persist filter = success + non-sensitive + ALLOWLISTED** via the persister `filters.predicate` → `shouldPersistQuery` (reused from the old `shouldDehydrateQuery`). `PERSISTABLE_QUERY_ROOTS` is the INVENTORY of every live non-sensitive root (big lists included); default-OFF for an unlisted root. Secrets EXCLUDED (`apiKeys`/`byokKeys`/`keySources` via `SENSITIVE_QUERY_ROOTS` — key material never on disk).
  - **A root nobody listed is a page that cold-skeletons on every visit, and NOTHING goes red — so a repo-wide guard now scans for it instead of a rule nobody re-reads.** `tsc` passes, every source-substring guard passes, the reveal-on-settle gates pass; the surface renders correctly and simply feels slow beside its neighbours, so it is reported as a loading-spinner complaint rather than a bug and the first instinct is to look at the gate, which is fine. `persist-query-cache.test.ts` walks `src` for every `useAuthQuery(["root", ...])` and fails on any root that is neither in `PERSISTABLE_QUERY_ROOTS` nor in `SENSITIVE_QUERY_ROOTS` — so adding a query and forgetting the allowlist is now a red test, not a slow page. Four roots had rotted that way by the time it was written (`offerFunnels`, `offerFunnelRevenue`, `offerFunnelPipelineActivity`, `leadReplyKind`). When it goes red, decide the root deliberately: allowlist it, or add it to the sensitive set if it carries key material. (#3659)

**A local-first cache is only local-first if READING it is cheaper than the network — the nav reseed reads KEYS, then only the values it will actually seed.** The store is one flat key space and the reseed treated it as a unit: `bucketEntries` pulled every value in the org's bucket over the IndexedDB boundary and `coldRestorablePairs` `JSON.parse`d each one, on EVERY navigation, then discarded the ones whose query was already warm in memory. That is the whole of "le dashboard est lent": measured on a 12.3MB bucket (40 ordinary snapshots plus one 60k-row leads list) it is **83.6ms of main-thread parse per page change, against 0.2ms for the same result** — and a heavy brand's leads list is nearer 100MB. The query key is recoverable from the STORAGE key (`${prefix}-${JSON.stringify(queryKey)}`), so `queryKeyFromStorageKey` + `coldStorageKeys` run the cold-guard on a string comparison and `valuesForKeys` reads only the survivors. Do NOT reintroduce a whole-bucket `entries()` read on a hot path; the storage adapter still exposes one because the persister's own API demands it. The cold-guard runs TWICE — once on the key, once after the value round-trip — because a query can resolve in between and seeding it then would stomp fresher data with an older snapshot.

**Nothing over `MAX_PERSISTED_ENTRY_BYTES` (2MB) goes to disk, and an entry that outgrows the cap is DELETED rather than left behind.** There was no cap, so one oversized snapshot taxed every page in the org. The offender is `brandLeads`: unpaginated by design (the revenue engine and the leads page both want the whole population), ~100MB of slim `view=basic` rows on a heavy brand. Refusing it is not a broken page — the query keeps `keepPreviousData` in memory and the per-query persister still restores it lazily on its own fetch; the only cost is that a genuinely enormous list cold-loads once per session instead of painting from disk, which is the right trade because reading it from disk was never fast either.

⚠️ **That last sentence was true and it was not the whole story, and the gap is the one worth remembering: "cold-loads once per SESSION" is what a refused entry costs a page that keeps the query mounted — the Leads page unmounts it, so it cold-loaded on every VISIT, many times a day, and was reported as a permanent skeleton.** The cap is right and stays; what was wrong was leaving a customer-facing page on the far side of it. The fix is never to raise the cap for one root (that re-creates the bucket-read cost this rule exists to bound) and never to accept the cold load as a trade the reader is making knowingly — it is to make the READ small, which means asking the producer for a page. `brandLeads` is still refused, and now only the funnel-leg board reads it. Tell that you are in this class: a surface feels slow beside its neighbours, every gate is correct, `tsc` and every guard are green, and the query root IS allowlisted — allowlisted only means "may be written", never "was".

**A CAP ON THE STORE IS A CAP ON THE PAYLOAD, and the payload is not yours — so a read whose producer hydrates rows you never asked for is silently EXILED from the cache, and the surfaces that ride it cold-skeleton on every load forever.** The cap above is correct and its reasoning holds; what it did not anticipate is that the read it was written to refuse (`brandLeads`, knowingly enormous, knowingly fine to cold-load) is not the only one over it. `/revenue` answers **10,903,573 bytes** on a real brand, of which **10,860,781** are a `leads[]` array hydrating all 9,854 contacted leads with names, photos, orgs, logos, tags, seniority, industry and employee count. Everything a money surface actually renders off that body — the headline, the economics, the spend block, every count series, the ROI history, the funnel walk — is **43KB**. So all four money reads (`brandRevenue`, `offerRevenue`, `offerFunnelRevenue`, `featureRevenue`) were refused at write time, and every stat card, the Return-on-spend chart and the cost card skeletoned on every load of every brand, offer, funnel and campaign page.

**Nothing goes red, and that is the whole difficulty.** The reveal gates are settle-based and correct; the network answers; the numbers are right; `tsc`, the 2900-test suite and every source-substring guard pass. `persist-query-cache.test.ts` passes too — the root IS allowlisted, which is the check that exists, and it says nothing about whether the entry FITS. It gets reported as "je vois du skeleton sur chaque page", which reads as a gate bug, and the gates are the first thing you check and they are fine.

**The diagnostic that localises it in one screenshot: find the surface on the same page that DOES paint instantly, and compare payload sizes, not code.** Here the Offers table (`brandOfferMoney`, 118KB) rendered real money beside five skeleton cards on the same screen, off the same service, through the same reveal machinery. Two reads, one cached and one not, is a SIZE fact. Then measure the wire from inside the producer's container (Cloudflare 1010s a scripted request from a laptop) and read where the bytes are: `console.log(JSON.stringify(j.leads).length)` beside the total answers it immediately.

**The fix is at the PARSE boundary, and the shape of it generalises: keep the QUESTION the consumers actually ask, not the rows the producer happened to send.** Both browser readers of those rows (the Leads page's outcome tab and detail panel, the funnel-leg board's `trackedStages`) build the same thing — a `leadId → outcome` MAP — so a lead carrying no outcome is looked up and found absent either way; on that brand **72 of 9,854** leads carry one, and they serialize to **19,720 bytes**. `RevenueOverview` therefore carries `leadOutcomes` (the narrowed list) and NOT `leads`, and the removal is a TYPE-level gate on purpose: a browser surface reaching for the full array is a compile error rather than an empty array it would read as "nobody is in the pipeline". `LeadOutcomeSchema` is `RevenueLeadSchema.pick(…)` of the twelve fields those consumers read, so zod strips the other twenty-four instead of validating them — the parse itself was tens of megabytes of main-thread work every poll. The digest, the ONE consumer that genuinely needs people, parses through `parseRevenueWithLeads` and gets `RevenueOverviewWithLeads`; it runs server-side on its own schedule, so its payload never touches a browser, a poll or the cache.

⚠️ **A presence answer computed over a narrowed list quietly changes meaning — take it BEFORE the narrowing, and once the PRODUCER narrows too, only the producer can answer it at all.** The Leads page shows an outcome tab only once features-service ATTRIBUTES that outcome (#476), and it answered that by asking whether any lead row carried the key — vacuously true, and correct, while every row carried every key. Narrowing to outcome-carrying rows turns the same question into "has anyone converted yet", and a brand with the tracker live and zero signups loses a tab it should keep; so `outcomeFieldsServed` was first computed over the FULL array at the parser, before the narrowing. When features-service then narrowed SERVER-side (#873 → v0.153.0) even that stopped working: a brand where nobody has reached anything receives zero rows, so there is no array left to infer from. It serves `attributedOutcomes` for exactly this — which outcomes the READ could attribute, never a fact about any lead — and the reader takes it REQUIRED, narrowing the producer's seven outcomes to the four this surface renders. **The derivation is DELETED rather than kept as a fallback: it would answer confidently and wrongly on the one case the field exists for, which is worse than throwing.** Generalise: when you narrow a collection, list what was being inferred from its mere presence — and if the producer narrows it too, that inference is now the producer's to state.

**The WIRE half shipped too — features-service#873 → v0.153.0, live.** `?leads=outcomes|full` decides how much of a person `leads[]` carries, the DEFAULT is the narrow row on the leads that reached something, and an unrecognised value is a 400 rather than a silent pick. Measured THROUGH the api-service gateway in prod: **58,897 bytes / 72 leads / 12 keys** on the default against **10,899,958 / 9,854 / 36** on `?leads=full`. The outcome-digest cron is the ONE reader in the fleet that sends `leads=full` — it names each person and what they did on the day — and omitting it is a CRASH, not a degraded email, because `parseRevenueWithLeads` requires fields a narrow row does not carry. ⚠️ The api-service revenue proxy forwards every query param VERBATIM, so this needed no gateway change; the note elsewhere in this file describing that route as having a closed query whitelist is STALE — read `src/routes/features.ts` on `origin/main` before believing it. Guards: `tests/revenue-lead-outcomes.test.ts` (real unit tests — `revenue-parse.ts` imports only zod and a type, keep it that way).

**The mount disk read is kicked in the `useState` INITIALIZER, not in an effect.** An effect runs after the children have mounted and rendered, so the first page of a session paints its skeleton and only then asks IndexedDB what it should have shown. `mountReadKicked` latches the nav effect's first run, or the `pathname` dep makes every mount read the bucket twice — the exact duplicate the removed `restoreQueries` used to cause.

**`gcTime` stays at 30 minutes. Do NOT raise it to keep navigation memory-warm** — that was considered and rejected: it applies to the un-persisted mega-lists too, so it would pin ~100MB of leads in the heap for the whole window, which is the #1273 OOM lever pointed at the problem it is meant to fix.

**A dashboard route is DYNAMIC, so Next's default prefetch fetches only its LAYOUTS — the page's own payload waits for the click, and what fills the wait is the nearest `loading.tsx`.** There are four boundaries for 31 routes, so for every funnel / leg / campaign sub-route the nearest one is `offers/[offerId]/loading.tsx`: drilling into a funnel blanked the WHOLE offer area to a full-page skeleton. Two fixes, one per navigation shape. A `<Link>` takes `prefetch` (the sidebar's `SidebarLink`). A row that navigates with `router.push` — which is every drill-down table, because a row carries its own controls and cannot be an anchor — warms its href on `onMouseEnter` AND `onFocus` through `useRoutePrefetch` (`lib/use-route-prefetch.ts`, one fetch per href per mount). Hover rather than a blanket prefetch on every row: only rows a human points at are fetched, so a long table does not fire one request per visible row. A NEW drill-down row needs the same two handlers or it reintroduces the skeleton on its own leg of the path.

**The poll is 5s, and the one thing it may NOT do is make somebody wait longer than that.** The instant paint is still the point — the cache shows the previous answer before any request goes out — but the freshness traded for it is bounded: owner-decided, *"on peut attendre pour le refresh… si ce n'est que je ne veux pas qu'on attende plus de 5s"*. It was raised to 60s on the belief that the producer's cache made a faster interval expensive; that belief was wrong twice over. **features-service's view-cache TTL is 5s, not 30s** (`src/lib/view-cache.ts` `DEFAULT_TTL_MS = 5_000`, no `FEATURE_VIEW_SNAPSHOT_TTL_MS` on the box), and inside the TTL a request is served from the snapshot with NO fan-out while past it the snapshot is served anyway plus one background refresh — so **the expensive cross-service work runs at most once per TTL however often this polls.** The interval buys freshness on screen; it does not multiply upstream work. A number is therefore at most ~10s old (5s snapshot age + 5s to the next poll). Generalise: a "lowering this is dangerous" comment naming another service's TTL is a point-in-time assertion like any other — read the producer before repeating it.

**The one slower tier is `LEADS_POLL_INTERVAL` (15s), and it is about PAYLOAD, never freshness.** ⚠️ The Leads page no longer reads a whole population — it pages (see the Leads-page section above), so its two reads are a few hundred KB and the slow tier is no longer load-bearing FOR IT. It still is for the whole-population readers this describes, which the funnel-leg board and features-service use. `brandLeads` / `campaignLeads` are unpaginated by design (the revenue engine and the leads page both want the whole population) and run to ~100MB of slim rows on a heavy brand, per open tab — at 5s that is 12x the transfer of the old cadence, continuously, for a list whose contents only move when the BACKEND sends. Every user action that can change it invalidates it explicitly, so the interval is only the backstop for somebody else's sending. The revenue read on the same page stays on the fast tier: it is small, and it is exactly what a reader watches after stating an outcome. A NEW read joins the 5s default unless its payload is in that class.

**A USER'S OWN write re-reads EVERY grain the fact is served under — `lib/write-invalidation.ts` is the one list, and the poll is not the answer to it.** The trap is that ONE fact lives under SEVERAL query roots: the same money is asked for per channel, per campaign, per offer, per funnel and per brand, each its own answer from features-service, so a mutation that invalidates the root it happens to know about leaves every sibling grain on the pre-write figure — and the person who just clicked is looking at exactly those figures. A lead statement invalidated `featureRevenue` alone, so the campaign's cards moved while the brand Overview, the offer money, the funnel walk, the per-campaign rows and the audience costs did not. Two sets, and both are exhaustive by construction: `LEAD_OUTCOME_ROOTS` (the six money grains + `featureAudienceStats` + `featureStats` + both activity grains) and `CAMPAIGN_MONEY_ROOTS` (`campaigns`, `campaign`, `brandDailyBudget`, `brandSpendableBudget`, `offerFunnels`). Four things are load-bearing: **`brandSpendableBudget` is the one every budget writer forgot** — it is the join of billing's ceilings to campaign-service's statuses, so it moves on a pause exactly as on a budget edit and it is what the header money reads; **`workflowProjection` is deliberately ABSENT** from the lead set (a cross-org fleet benchmark one lead cannot move, and one of the app's most expensive reads); **`brandFunnelBudgets` is deliberately ABSENT** from the budget set (both writers already `setQueryData` billing's own answer into it, and a re-read that fails would fall back to the pre-save figure); and the module **imports nothing at all** (the client is taken structurally) so it carries real unit tests — keep it that way. React Query's default `refetchType: "active"` means this refetches what is on screen and marks the rest stale, which is the shape wanted. Guards: `tests/write-invalidation.test.ts` + `tests/poll-interval.test.ts`, and they pin the CALL SITES, not only the helper.

⚠️ **The money still lands in ~5s, not instantly, and that is the producer's cache — do not paper over it.** A refetch fired the instant a write returns can still be served the pre-write snapshot, and the honest fix is features-service invalidating its own view-cache on write, not a browser-side recomputation (which is the compute-a-stat-in-the-browser bug). What IS held locally is the statement the person just made (`statedReply`, `statedReplyKinds`) — their own words over the round trip, dropped the moment the producer answers.

**`isLoading` is `isPending && isFetching`, so a query the org-consistency gate has DISABLED reports `isLoading: false` while still unresolved** — the terminal branch fires and the surface flashes its empty / not-found state before the read runs. Every live surface reads `isPending`; `workflow-detail-panel.tsx` is the one place a disabled query's eternal `isPending` is correct (its `enabled: !!workflow` cannot stay false on a settled page, and the panel above is already a skeleton meanwhile). Guards: `tests/instant-paint.test.ts`, plus `tests/persist-query-cache.test.ts`, whose root scan now reads the whole KEY ARGUMENT — nine call sites pick their key with a ternary, and a pattern anchored on `useAuthQuery(["` saw neither branch.

**Irreducible:** the genuine FIRST-EVER load on an empty cache still waits on the cold Neon backend (~20-30s, features/api/runs scale-to-zero 0.25 CU). Killing that needs backend warm-up (other repos), NOT a dashboard cache change.

**Backend-DOWN must degrade to STALE, never to a skeleton — and the persister has TWO disk-restore paths, each with a hole that a down backend un-masks.** The per-query persister self-restores a query from disk ONLY when that query actually FETCHES (i.e. `enabled`); the org-consistency gate (`use-auth-query.ts`) disables every `useAuthQuery` while Clerk's active-org is still settling, so during that window the disabled queries never fetch → never self-restore. The other path — the mount seed `persister.restoreQueries` (query-provider.tsx) — is ONE-SHOT per provider mount. So a page reached while the gate is momentarily closed, OR a sub-page whose memory was GC'd (30-min `gcTime`), cold-skeletons even though its stale snapshot sits on disk. Backend-HEALTHY hides this (the network answers within seconds); backend-DOWN turns the transient into a STUCK multi-minute skeleton on a page the user opens 10×/day — the local-first cache silently not painting. Diagnose by reading the disk directly in the browser console: `(await indexedDB.databases())` + a `keyval-store` getAllKeys — if `distribute-dashboard-cache:<org>-["<root>",…]` entries EXIST for the affected org/brand while the page skeletons, the data is on disk and NOT being painted (a restore-path bug, NOT eviction; eviction = the store/entries are absent, a different failure fixed by `requestPersistentStorage` #2336). **Fix (#2574): `reseedColdQueriesFromDisk` re-seeds COLD queries from this org's on-disk snapshot on EVERY org-scoped navigation, decoupled from `enabled`/network** — pure guarded helper `coldRestorablePairs` (org-prefixed = DIS-143, buster-checked = never paint stale-shaped data, COLD-GUARDED = `getQueryData!==undefined` skip so a reseed never stomps fresher live memory), read-only `setQueryData`, zero network. Any new org-scoped restore/reveal work must keep the invariant: painting a present disk snapshot is decoupled from the gate + the network, so a down backend always shows stale. (#2574, sibling of the #2336/#2337 eviction+org-gate fix — distinct: there the disk was empty, here it's present-but-unpainted.)

**Rule:** persistence is GLOBAL — one per-query persister wired as a default query option, never a second provider / per-page persister. `experimental_createQueryPersister` is `experimental_`-prefixed (stable behavior, API may shift — pin to the installed `@tanstack/react-query-persist-client` version). Keep `PERSISTABLE_QUERY_ROOTS` in lockstep with live queries. (#1273 small-allowlist origin; #1278/DIS-170 30min+allowlist; 2026-06-23 persist-all-localStorage regression; 2026-06-25 KevinLourd/dashboard-instant-cache = per-query IndexedDB; 2026-08-02 investor-update-ram-leak = bucket-scoped reads + a bounded store, the current design.)

### keep-last-good (`structuralSharing`, cache-write boundary) — a SUCCESSFUL refetch returning VALID-but-degenerate data must not collapse the UI

5th "don't revert resolved state on a transient" layer — and the ONLY one for a **successful** refetch that legitimately returns **worse** data. The other four all assume the new data is good: 1. `keepPreviousData` (key-change / error→undefined) · 2. `useCoordinatedReveal` (reveal decision) · 3. `useMonotonicStatuses` (status downgrade *ordering* on a poll) · 4. persisted cache (eviction). **The gap:** a 200 whose nullable field flips non-null→null (a cold Neon chain — api→features→workflow/runs/email-gateway/brand, all scale-to-zero — answers a poll/refocus refetch with zeroed unit costs or fewer rows while half-warming). `keepPreviousData` can NOT help — the null IS the new valid query data, so React Query installs it and any UI **derived** off it (budget cards, a `disabled` gate, a badge, a $ amount) collapses mid-session.

**Industry-aligned fix (researched: TkDodo / TanStack docs / SWR all agree there is NO built-in for successful-but-worse — you intercept at the cache-write boundary).** TanStack's hook is `structuralSharing(prev, next)` (SWR's is `compare`, Apollo's is a `merge` field policy). Helpers in `src/lib/keep-last-good.ts`: `keepLastGoodFields(prev, next, fields, label)` (per-field coalesce: keep prev's non-null when next nulls it) + `keepLastGoodList(prev, next, {keyFn, fields, label})` (keyed merge: per-item coalesce + RETAIN a prev item that vanished from next). Both `console.error` the suppressed downgrade (fail-loud — a persistent real downgrade still logs; this is keep-resolved, NOT a silent fallback). Compose per query (e.g. `keepLastGoodWorkflowProjection` in `api.ts`, co-located with the type) and pass it as `structuralSharing` on the `useAuthQuery` — protects EVERY consumer of that query at ONE place, not per-component (a per-component latch is opt-in-and-forgettable, which is exactly how the collapse shipped).

**Five load-bearing rules:**
- **OPT-IN per query, NEVER a global default.** A null can legitimately mean "deleted/gone"; blanket last-good would mask real deletions. Apply ONLY where a null means "transient/not-ready" (cold compute, in-flight aggregation), not "removed".
- **Put it at the QUERY (`structuralSharing`), not at each consumer.** One merge → cards + button + badges all inherit it. The per-component `useLastGoodValue` latch is strictly weaker (forgettable).
- **List ONLY the fields whose null = transient.** Fields outside the allowlist let next win (incl. when null) — so a genuinely-cleared field still clears.
- **CUT the *steady-state* poll on econ-INDEPENDENT slow-changing queries — but keep a BOUNDED cold-start warm-up poll.** The workflow-projection unit costs are "fetched ONCE" by design (#1528/#1536) — a perpetual 5s `...pollOptions` on them only re-hits the cold Neon chain for zero steady-state benefit (cards update from the client `econLive` recompute). BUT "fetch-once + `refetchOnWindowFocus`" alone is NOT correct: **keep-last-good cannot heal the FIRST fetch** (no prior good value to keep), and the cold Neon chain most often returns its degenerate-200 (null unit costs / empty workflows) on exactly that first request — so a cold load STICKS on the empty-state ("No workflow has website-visit cost data yet") until a manual window-refocus. The right shape is a **conditional `refetchInterval`**: poll (~4s) ONLY while the payload has no usable data (e.g. no workflow with non-null `costPerCloseUsd`), capped at N tries (so a genuinely-empty brand stops), `false` the instant it warms. This self-heals cold-start without perpetual polling. (#1557-regression: #1542 dropped the poll entirely calling it "pointless"; it was the cold-start self-heal — keep-last-good #1548 only covers a later bad refetch of an already-good cache, never the first fetch.)
- **TanStack types `structuralSharing` params as `unknown`** — pass `(prev, next) => keepLastGoodX(prev as T | undefined, next as T)`.

**Rule:** any **polled / refetch-prone** query with **nullable fields that GATE or compute UI** (disabled-button, empty-state, $ amount, recommended pick) MUST pass a keep-last-good `structuralSharing`. Pure read-display surfaces that already degrade to "—" on null are lower-risk (phased-sweep, not a hotfix). (#1542-regression: the launch-page budget cards + Launch button — then `campaigns/new`, now `brands/[brandId]/launch` — collapsed when the polled `workflowProjection` caught a valid-but-degenerate 200 mid-session; `econLive`/§2 inputs were NOT the cause — they're latched. The poll predated #1542; #1542 widened it by recomputing cpc client-side. Fix: keep-last-good `structuralSharing` on the query + drop the pointless 5s poll. Other projection/revenue surfaces — conversions/workflows/brand-overview/report + top-workflows card — are the phased follow-up; the campaign-LEVEL variants (`campaigns/[id]/overview`, top-campaigns card) were removed in #1768.)

### A form seeded from a query must RE-SEED when the payload changes — a once-per-mount latch reads the DISK snapshot and ignores the server (7th "don't get stuck" layer)

The six layers above all keep RESOLVED state from being reverted. This one is the mirror image: local component state copied OUT of a query, which then never hears about the fresher answer. Any editable surface does this — it cannot bind inputs straight to `data`, so it seeds a `useState` from the query and lets the user type.

**The trap is that the FIRST payload to settle is the on-disk one.** With the local-first per-query IndexedDB cache, a restored snapshot resolves before the network does, so a `hydrated.current` boolean latch seeds the form from the PREVIOUS VISIT's copy and the fresh server payload that lands a moment later updates the cache and nothing else. Compounding it: **`setQueryData` after a mutation does NOT go through the query function, so it is never persisted** — the disk keeps the pre-write copy, and the write the user just made is the thing that gets overwritten on the next mount.

The symptom is the worst kind: the value IS saved, the server has it, and the screen shows blank. It reads as the product dropping the save, so the bug gets reported against the write path where there is nothing to find.

- **Re-seed when the payload is a DIFFERENT OBJECT than the one the form was built from** (keep the last-seeded reference in a ref). Identity, not deep equality: React Query returns the same reference when nothing changed (structural sharing), so an unchanged refetch costs nothing and cannot loop.
- **Two things outrank the server: a field the user has EDITED, and the card/row they have OPEN.** A form that rewrites itself mid-edit is worse than a stale one. The mutation's own `onSuccess` seeds those from its response, which is also what guarantees the card shows exactly what persisted.
- The `hydrated` flag stays, but only to answer "has this ever been seeded" for the settle gate — never as "seeding is finished".

Reference impl: `brand-sales-funnels-card.tsx` (`seededFrom` ref + the `touched || openKey === def.key` guard). Guards in `tests/sales-funnels.test.ts`. (#3342 — a saved `meetingBookedToAttendedPct` rendered blank on the customer's Settings page while brand-service had held the value for hours.)

### Reveal on SETTLE, never success-only — an ERRORED gate query must not eternal-skeleton (6th "don't get stuck" layer)

**A `useCoordinatedReveal` / skeleton barrier gated on `data !== undefined` (success-only) SKELETONS FOREVER when its query ERRORS — this is the #1 cause of the recurring "page still shows skeleton" reports, and it is INVISIBLE to every cache/persist/reseed fix.** The five layers above (keepPreviousData, coordinated-reveal, monotonic-status, persisted cache, keep-last-good) all assume the query eventually SUCCEEDS. None cover the case where the gate query keeps FAILING: a cold Neon chain (features→downstream, all scale-to-zero) intermittently returns an aborted fetch / 5xx / `ECONNRESET`, React Query exhausts `retry:1`, `data` stays `undefined` forever, and any barrier keyed on `data !== undefined` never latches → permanent skeleton, no error UI, no recovery. The fast sibling cards are held hostage if they AND the failing query.

**Rule — reveal on SETTLE (resolved OR errored):** destructure `isError` from every `useAuthQuery` that feeds a reveal barrier, and gate on `data !== undefined || isError`, never `data !== undefined` alone. On error the region paints its null-safe "—"/stale content (a DISPLAY fail-soft — the error still logs loud via React Query + the reader's safeParse throw, this is keep-resolved not a swallowed fetch); the monotonic latch keeps it revealed while the next poll recovers real data. **This is ORTHOGONAL to the org-gate + disk-reseed work (#2336/#2574):** a query DISABLED by the org-consistency gate has `isError:false` → NOT settled → still (correctly) waits for reseed-from-disk; reveal-on-settle covers the DIFFERENT case where the query is enabled, fires, and FAILS.

**Two co-defects that turn one failing query into a whole-page skeleton (both must be fixed together):**
- **Single load-bearing gate.** Do NOT gate heterogeneous cards on ONE query's reveal (the "ONE barrier PER card" rule). The brand Overview gated the entire section + cost card + the fast `featureStats` outreach cards on the slow `/revenue` reveal — so `/revenue` failing skeletoned everything. Gate each card on ITS OWN query's settle.
- **Defensive `!data` RE-GUARD in the child component.** A `const loading = pending || !data` inside the presentational component (`revenue-overview-section.tsx`) RE-LOCKS the skeleton even after the page forces reveal-on-error (page passes `pending=false` but `data` is undefined → `!data` wins). Drop the `!data` re-guard; make sub-fields null-safe (`data?.x` → "—"). The child must trust the page's `pending` flag, never re-derive loading from `!data`. **A component carries ONE gate per query, so fixing the reported one and leaving its siblings ships the bug with a smaller blast radius — grep the whole file for `pending || !` and fix every hit in the same commit.** #2650 fixed `revenueLoading` and left `activityLoading = activityPending || !pipelineActivity` two lines under it, so the identical eternal skeleton reappeared 2026-08-08 the moment `pipeline-activity` 502'd. **Where a gate genuinely cannot take an absent payload** (`PipelineActivityChart` reads `data.days`), the answer is a branch STATING the reason, never a skeleton — and inline the `!data` check at the render site, since a derived `const` boolean does not narrow the prop for TypeScript.

**And check each region against the query that actually FEEDS it — a region sharing a neighbour's gate goes blank on an outage of an endpoint it never reads.** The Outcome card's cumulative line is `pipelineActualSeries` off `/revenue`, which was serving fine; only its dashed forward projection comes from pipeline-activity, and that degrades to nothing. It was grey for 20 minutes purely by association.

~100 prior "skeleton" PRs all touched cache/reseed (#2074/#2107/#2129/#2336/#2574); none gave the gate query a failure exit. Guarded by `overview-reveal-on-settle.test.ts`. (#2650, #3303)

### Public marketing pages — SEO + AI-scraper rendering

Public landing/marketing/docs (`apps/landing/`, `apps/sales-cold-emails-landing/`, `apps/docs/`): default **ISR + `unstable_cache` + edge cache**, NEVER Suspense streaming on indexable content. AI scrapers (GPTBot, ClaudeBot, PerplexityBot, Bingbot AI) parse raw HTML only — a Suspense fallback means they index the skeleton, not the data.
1. No `<Suspense>` on indexable content (hero, h1/h2, body, JSON-LD, leaderboards, tables, blog cards, stats — SSR-sync). Suspense only for user-specific non-indexable widgets.
2. Never combine `force-dynamic` with `revalidate` (force-dynamic wins, ISR silently off). Prefer ISR (`export const revalidate = N`) alone.
3. `headers()`/`cookies()` in a page forces dynamic rendering — drop them, resolve hostname from `NEXT_PUBLIC_*`.
4. Wrap every fetch/DB call in `unstable_cache` with explicit tags.
5. Invalidate via `revalidateTag(tag, profile)` — Next 16.2+ requires the 2nd arg (`"default"` for ordinary invalidations).
6. `unstable_cache` at module top level breaks vitest — mock `next/cache` globally in `tests/setup.ts` (identity pass-through; `revalidate*` → `vi.fn()`), declare `setupFiles` in `vitest.config.ts`.
7. Validate scraper output after any caching change: `apps/landing/scripts/seo-snapshot.sh` + `measure-ttfb.sh`; pre/post snapshots in the PR. (#1153)
8. **Skeleton-leak check (seo-snapshot does NOT catch this).** `seo-snapshot.sh` counts h1/h2/article/JSON-LD — it does NOT count `animate-pulse`, so a page that ships data behind `<Suspense>` passes the snapshot while serving skeletons to LLMs. Add `curl -sA "GPTBot" https://distribute.you/<page> | grep -c animate-pulse` — **>0 = the data is behind Suspense/client-fetch and invisible to AI scrapers**, fix it. Canonical fix = `/investors` + `/pricing` (#1520): both were ISR + `<Suspense fallback={Skeleton}>` around async server components → 314/102 skeletons in raw HTML. Removing the Suspense (the async sections await straight into the static prerender, mirroring `/benchmarks`) dropped both to 0. **A page that's already `revalidate=N` ISR gains nothing from `<Suspense>` on its own data — drop it.** When de-Suspensing, make the data fetch **fail-soft** (try/catch → zeros) so a build-time fetch throw can't abort the prerender (`data-sections.tsx` `EMPTY_INVESTOR_METRICS`). Guarded by `investors-page-ssr.test.ts`. **Measuring page byte-composition: never `grep -oE 'ld+json.*'` on minified single-line HTML — `.*` grabs the whole doc, not the block; use a real parser (python non-greedy per-`<script>`).** (A grep artifact once mis-reported a 4 KB JSON-LD block as "2.27 MB"; the real bloat was the 2.2 MB RSC flight payload `self.__next_f`, which scrapers ignore.)

**Fixing an Ahrefs / external site-audit: target the SERVED HTML, never reconstruct from source metadata exports.** The audit overview gives issue COUNTS, not per-URL data. Pull the per-URL row export (Ahrefs MCP `site-audit-issues` / API / UI CSV) OR crawl the live served HTML — do NOT reverse-engineer which page is flagged from the Next `metadata` source. Source reconstruction produces false positives because: (a) `title` templates compound (`page title "X — distribute"` + layout `template "%s | distribute Y"` = doubled brand + over-length — the served `<title>`, not the raw string, is what Ahrefs measures); (b) Next metadata **inherits** down the layout tree (a child page with no `openGraph`/`twitter` is NOT missing them — it inherits the root layout's); (c) a layout `title.default` is **overridden** by any child `page.tsx` `title` (the default may never be served); (d) `description` constants (`PAGE_DESCRIPTION`/`SITE_DESCRIPTION`) and JSON-LD `description` fields are NOT the HTML meta description — a 166-char `Service.description` in `application/ld+json` is structured data, not the `<meta name="description">`; (e) a `<html>` literal inside a JSDoc comment fools a naive `grep '<html'`. Incident 2026-06-04 (#1310 / DIS-69): 4 such false positives caught only by direct served-HTML / template-aware verification. Ship ONLY directly-verified served defects; defer count-only issues (OG-incomplete, structured-data, redirects) to DIS-69 pending the per-URL export. The recurring SEO-audit home is **DIS-69** (Ahrefs project 9700515) — update it, don't open a new issue.

### Public pages — no horizontal overflow on mobile

Every public app ships **`html { overflow-x: clip }`** in `globals.css` — never `overflow-x: hidden` (`hidden` makes html/body a scroll container, breaks `position: sticky`; `clip` doesn't). It's a safety net, NOT the fix — real spills are fixed at the element level. Wide legit content (code, perf tables) scrolls internally via its own `overflow-x-auto` (else `clip` HIDES it). Decorative `absolute -inset-N` glows must stay in-viewport: a full-bleed component whose `max-w-*` box reaches the edge bleeds the glow past it → body scroll; keep every home-page component inside the standard `max-w-* mx-auto px-4 sm:px-6 lg:px-8` container. Verify with `apps/landing/scripts/overflow-audit.mjs [BASE_URL] [--app=landing|sales|docs]` (headless Playwright at 360/390/414px, separates escaping spills from contained); subagent/static analysis is NOT reliable for overflow. (#1284) **The audit auto-discovers React routes ONLY — it SKIPS the static-served landing pages (`src/app/<page>/route.ts` → `staticResponse("….html")`: `/pro`, `/agency`, `/use-cases`, `/how-it-works`, the whole `cold-email-*` cluster).** A "mobile review" that trusts the audit's pass count misses those pages — pass the explicit static routes too, or audit `public/landing/**.html` directly. Two real overflows hid there for months (`/cold-email-cost-guide` `.guide-channels` stayed 2-col at ≤640px → 2×202px spill; `/cold-email-for-saas-founders` inline `repeat(4,1fr)` stat grid couldn't collapse → 538px spill) because auto-discovery never loaded them. Distinguish REAL overflow (body `scrollWidth` > viewport) from contained element-spill (`scrollWidth == vw`, masked by `clip`) — only the former is a bug. (#1745)

### Landing voice — sell scale, not solitude

The ICP ("The Serial Builder", `/investors`) is a solo / 1–3-person team running 3–10 products — keep "solo founder" as an SEO keyword + persona. **But never celebrate the solo lifestyle in copy** ("Stay solo. Go big.", "$0→$1M MRR solo", "you don't need a team" demotivate a founder who wants to grow + undersell the platform). The dream we sell = variable CAC + measurable channels per product across a portfolio. The investors dream quote stays "whether I stay 1 person or grow to 10." (#1162)

### Landing billing-truth invariants — the model is GOOGLE ADS, so "No subscription" is TRUE; sell RESULTS not the per-email price

- **The billing model is Google Ads: the customer sets a daily budget, is charged for what the campaign spent, and the balance tops itself up at a threshold so the campaign can keep running.** Nobody calls Google Ads a subscription, and neither do we. So **"No subscription", "No seats, no retainer", "Pay-as-you-go", "Cancel anytime", "Pause anytime" and "No monthly fee" are all TRUE and shippable** — there is no seat, no retainer, no term, no recurring fee, and stopping is a budget you set to zero. What stays banned is denying the **RECHARGE**, which is a different fact and is genuinely mandatory on a recurring campaign: never write "Credits never auto-renew", "no auto-renew", or "no surprise charges", because the balance really does top itself up. **Subscription ≠ auto-recharge.** The previous version of this rule conflated the two ("the product is a per-day SUBSCRIPTION with mandatory auto-topup → never write no subscription") and that conflation stripped a true claim off the homepage twice — #1739, then #3434, reverted the same day it shipped. Kevin 2026-08-18: *"il n'y a aucune subscription!!! On est comme Google Ads!!! Et oui ils peuvent cancel anytime."* The auto-topup fact that started this is a DASHBOARD launch-gate rule (recurring → auto-topup mandatory) and says nothing about what to call the model on the landing. Note the two halves of this file already agreed: the business-model section above calls "no seat, no retainer, no subscription" the actual selling point. (#1738, #3434)
- **Sell the RESULT, not the means.** Do NOT quote distribute's per-email/per-contact price ($0.07/contact, $1.75/reply). Quote industry-benchmarked **result economics: $4–$7 per click, $70–$250 per positive reply (= a buyer conversation worth engaging)**. Keep COMPETITOR figures untouched (LinkedIn $8–15/click, InMail $3–5/msg, agency $3–10k, SDR, DIY $0.65). Consequence accepted: distribute is no longer visually "cheapest per contact" — comparison visuals position it as mid-market done-for-you, not 1000× cheaper. The OFFER is **"$25 free credits"** = a $1-per-$1 match up to the first $25 spent (NOT a no-card free trial — a match needs a card). (#1732, #1728) **Same principle for the MECHANISM: landing copy never exposes internal model/workflow vocabulary ("best-performing playbook", "best model", "cheapest workflow that delivers", "cross-org workflow") — a visitor does not care which model produced the number. State the practical live price a user gets ("what a positive reply for a sales meeting costs right now, live"), not how we produce it. Kevin 2026-07-19: "on ne veut pas rentrer dans le détail technique de quel est notre model… c'est ce que ça donne en user en pratique." (#2810/#2811)

### Landing logo discipline — borrow trust from providers

distribute = "Stripe of Distribution" (thin wrapper over Apollo, Anthropic, Resend, LinkedIn, Muck Rack, Featured, Adobe, Gartner…). Wherever a provider/source/tool is named on a public page, show its logo via `apps/landing/src/components/provider-avatar.tsx` (`logo.dev` + `NEXT_PUBLIC_LOGO_DEV_TOKEN`, initial-letter fallback). NEVER hand-roll a provider SVG, NEVER ship the initial fallback as the intended UI — set the token in every env. New Channels-grid mappings in `apps/landing/src/data/feature-providers.ts`; stat/study mappings via `SourcedStat.providerDomain` / `ExternalStudy.providerDomain`.

### There is NO alpha gating in the dashboard — a `useFeatureFlag` gate there DELETES a surface, it does not stage one

`useFeatureFlag` returned `false` unconditionally in `apps/dashboard` from the 2026-06-14 admin/dashboard split onward, so every gated entry rendered for NOBODY — staff included — and its page was reachable only by typing the URL. That reads in a diff as a staged alpha and behaves as orphaned code, which is why it survives: nobody looks at it, so nothing about it gets fixed and its contents drift out of date unnoticed. Three whole surfaces sat that way for months and were deleted in one pass:

| Deleted from `apps/dashboard` | Lives in |
|---|---|
| `orgs/[orgId]/brands/[brandId]/brand-info` | `apps/admin` |
| `orgs/[orgId]/brands/[brandId]/workflows` (+ `/new`), `(dashboard)/workflows`, `components/workflows/*`, `api/v1/workflows/[id]/required-providers` | `apps/admin` |
| `orgs/[orgId]/services/crm` (+ `_components/*`) and its `services/crm/oauth/callback` | `apps/admin` |

Deleted with them, because they had no other reader: `lib/use-feature-flag.ts`, the whole `FEATURE_GATES` registry (`lib/feature-gates.ts` now exports only `Maturity`, `MATURITY_STYLES` and `GA_BRAND_FEATURES`), `lib/workflow-display-name.ts`, the six workflow readers in `lib/api.ts` (`listWorkflows` / `getWorkflow` / `getWorkflowSummary` / `getWorkflowKeyStatus` / `fetchGlobalRankedWorkflows` / `createWorkflow`) plus `triggerFeatureRun`, the breadcrumb's workflow crumb, and 24 persist roots the allowlist still listed.

- **A dashboard surface that needs a limited audience uses the EMAIL allowlist** (`lib/beta-allowlist.ts` + `useIsBetaUser`), which actually evaluates, and carries a visible `<MaturityBadge level="beta" />`. `MaturityBadge` stays — the beta Profile surfaces render it.
- **`GA_BRAND_FEATURES` stays and is live** (`sole-feature.ts` resolves the one feature a brand's surfaces are about). A feature graduates by being added to it.
- ⚠️ **`PERSISTABLE_QUERY_ROOTS` is an INVENTORY, so a root outlives its reader silently** — a page removal leaves its roots behind and nothing goes red. The dead set here was 24 entries, most orphaned by the earlier entity-page removals (#2593) rather than by this one. When you delete a surface, diff the allowlist against the live `useAuthQuery` roots (the scan in `persist-query-cache.test.ts` does the forward direction; the reverse is a grep for each listed root's literal across `src`).
- Guard: `tests/feature-gates.test.ts` fails if the hook file returns, if anything under `src` CALLS it, or if any of the deleted directories reappears. It matches the CALL (`useFeatureFlag(`), not the bare name, because several files legitimately EXPLAIN this history in a comment and a guard that trips on its own rationale is the source-substring trap this repo keeps recording.

**`apps/admin` keeps the real PostHog-driven gating and is unaffected** — the rest of this section describes THAT app.

### Feature maturity gating (alpha / beta / ga) — admin
Immature features stay in prod but are hidden from non-staff via **PostHog feature flags**, NOT `NODE_ENV`. `alpha` → staff only (flag on person-property `email = kevin.lourd@gmail.com`) · `beta` → opt-in cohort · `ga` → everyone, NO flag.
- Single source: `src/lib/feature-gates.ts` `FEATURE_GATES` (surface key → `{ flag, maturity }`). Flag name `<maturity>-<surface>`. **Each distinct gated surface gets its OWN flag — do NOT reuse an adjacent surface's flag for convenience** (couples graduation; the `<maturity>-<surface>` convention is one-flag-per-surface). When gating a new surface, create a dedicated PostHog flag mirroring an existing alpha flag's targeting (e.g. `alpha-brand-database` = Outlets/Journalists/Articles Database rows, staff-only, ≠ `alpha-brand-features`). (#1343: recommended reusing `alpha-brand-features`; Kevin picked "go dedicated".)
- `useFeatureFlag(flag): boolean` (`src/lib/use-feature-flag.ts`) — **default-hidden** (false until PostHog loads → no flash); subscribes `posthog.onFeatureFlags` to re-resolve after `identify`.
- `<MaturityBadge level>` (`src/components/maturity-badge.tsx`) — amber alpha / violet beta.
- Pattern: call `useFeatureFlag` at component top (hooks rule), gate the render, attach the badge:
```tsx
const ok = useFeatureFlag(FEATURE_GATES["services-crm"].flag);
{ok && <SidebarLink item={{ ..., maturity: FEATURE_GATES["services-crm"].maturity }} />}
```
- Graduation = widen flag targeting in the PostHog UI (no redeploy). Flags created/edited via PostHog MCP (`mcp__posthog__exec` → `create/update-feature-flag`). "Disappear for everyone" = hard JSX removal, NOT a flag defaulted-off.
- **EVERY surface that LISTS features must apply the brand-features filter `features.filter((f) => GA_BRAND_FEATURES.has(f.slug) || featuresAlphaOk)` — not just the sidebar.** There are THREE such surfaces and they must stay in lockstep: `BrandLevelSidebar` (sidebar), `brands/[brandId]/page.tsx` (overview grid), AND the `breadcrumb-nav.tsx` feature switcher (BOTH the app-level `appFeature` dropdown AND the brand-level `feature` dropdown render `visibleFeatures.map`, not `features.map`). A new feature-listing UI defaults to leaking alpha to non-staff if it maps the raw `useFeatures().features` — gate it. (#1410: breadcrumb switcher shipped without the filter; sidebar+overview had it, breadcrumb was the forgotten third surface.)
- **Deleting/renaming a flag the CURRENTLY-DEPLOYED prod code still reads HIDES the gated surface for EVERYONE (incl. staff) during the deploy gap.** `useFeatureFlag(<deleted>)` resolves false. So when a rename ships (`alpha-X` → `alpha-Y`, or an alpha→GA that drops a gate), delete the OLD flag only AFTER the new code is live in prod (Vercel deploy READY), never during the PR. Order: ship code → confirm prod READY → delete old flag. Deleting early = the gated entry vanishes until the new build lands. (#1338: deleted `alpha-feature-settings` while #1337 prod still gated the Feature Settings entry on it → entry disappeared for everyone for ~2 min until #1338 deployed.)
- **In the DASHBOARD app `useFeatureFlag()` is HARD-DISABLED — it returns `false` for everyone (alpha/beta live only in the admin app since the 2026-06-14 split). So a NEW dashboard surface that must ship to a small set of people (a beta) CANNOT gate on `useFeatureFlag` — it would be invisible to everyone, silently.** Gate on the email allowlist instead: `src/lib/beta-allowlist.ts` (`BETA_ALLOWED_EMAILS` + `isBetaEmail`) + `useIsBetaUser()` (reads the Clerk `useUser()` email), mirroring `apps/admin/src/lib/admin-allowlist.ts`. The gate applies to BOTH the sidebar entry AND the page body (page returns the "not available" card for non-beta). Drop the gate (not widen the list) to graduate to GA. Reference: the Signups / Booked Meetings / Sales outcome pages (#1654; the current cohort is in `BETA_ALLOWED_EMAILS` — Kevin + pressbeat). (The `FEATURE_GATES`/PostHog path below is admin-app-only now.)
- **The mirror of that rule, and the one that rots silently: an EXISTING dashboard surface still gated on `useFeatureFlag` is not "staff-only", it is DEAD — invisible to everyone, reachable only by typing its URL.** The hook returns `false` unconditionally, so `{flagEnabled && <SidebarLink/>}` renders for nobody. That reads in the diff as a staged alpha and behaves as an orphaned page, which is why it survives: nobody looks at it, so nothing about it gets fixed, and its contents drift out of date unnoticed. So when a task touches a dashboard surface, `grep` its nav entry for `useFeatureFlag` FIRST — if it is gated, the surface has been showing to no one since 2026-06-14 and "graduate it" means DELETING the gate (plus its `FEATURE_GATES` entry, which is then dead), never widening a PostHog flag. Treat everything on that page as unverified: it was last exercised by whoever wrote it. Cost 2026-08-16 (#3400, the org API-key page): gated on `alpha-keys`, so the owner did not know the page existed; three of its four install snippets had rotted into commands that fail on paste (see below).
- **A surface that prints a COMMAND or a PACKAGE NAME for a person to paste must have that command run before it ships — `npm view <pkg>` is five seconds and the failure is total.** This is the "prove the artifact WORKS through the real consumer" rule (see the investor-update markdown case) applied to install instructions: rendering the snippet proves nothing, because the consumer is a shell on someone else's machine. The org API-key page advertised `npx @distribute/mcp` and `@distribute/api-client`; **neither exists on npm** (the MCP repo is `@mcpfactory/mcp-service`, `private: true`, retired brand), and its curl authenticated with `X-API-Key`, which is api-service's separate ADMIN path — an org key sent that way is answered `401 Invalid admin key`. A user key is a Bearer token (`src/middleware/auth.ts`, path 2). Shipping a GA page whose commands 404 is worse than shipping no page, so the honest move is to delete every snippet that does not resolve and restore it when the package is real. (#3400)
- **The org API key page (`/orgs/:orgId/api-keys`) is GA and hands out exactly ONE thing: a Distribute key.** The second section, where an org pasted its own third-party credentials, was REMOVED — we run every vendor ourselves, so `listByokKeys` / `setByokKey` / `deleteByokKey` / `listKeySources` / `setKeySource` must not come back to this page (guarded: `tests/org-api-keys-ga.test.ts`; those readers survive in `lib/api.ts` with no caller). A key authenticates as its org through key-service and then reaches every route the dashboard does, with **no scopes and no expiry** — revoking is the only limit, accepted deliberately as of #3400. `/provider-keys` stays a redirect so old links resolve.
- **STAFF/GOD-MODE preview gating (distinct from beta) = `useIsAdminUser()` (`src/lib/use-admin-user.ts`, reads Clerk email → `isAdminEmail`), mirroring `useIsBetaUser`.** Use it (not the beta allowlist) for a surface only STAFF should preview. Gate BOTH the nav entry and the page body; badge rides the nav entry (per the whole-page-gate corollary below). Since the dashboard has NO edge allowlist, a page gated only on this hook is a UI-visibility gate over the viewer's OWN org data, never a data-security boundary. **The campaign-centered v2 Campaigns surface is the case this rule used to name, and it is now GA — every gate and every beta badge is GONE from it** (`campaignsOk = isRevenueFeature(featureSlug)` in `context-sidebar.tsx`, both sidebar levels badge-free, and neither `campaigns-page.tsx` nor `campaign-overview-page.tsx` reads `useIsAdminUser`; guarded by `tests/campaigns-page.test.ts` + `tests/context-sidebar.test.ts`, which now fail if the hook reappears in the sidebar or either page). **Campaign re-intro is MULTI-STEP (owner-approved, #2761/#2762) — the campaign concept is being progressively re-added despite the "#1768 removed campaigns" notes elsewhere in this file; step 1 is READ-ONLY (list + per-campaign pipeline/$CAC/ROI/%CAC from features-service `getFeatureRevenueByCampaign` groupBy=campaignId + right panel + global header). Today = 1 campaign/brand so the list shows 1 row until multi-campaign creation lands. Before editing the brand sidebar or "campaign removed" surfaces, know this surface exists — extend it, don't re-invent or delete it.** (#2761, GA'd 2026-08-02)
  - **Campaign v2 step 2 (#2771) — per-campaign OVERVIEW + LEADS drill-down + a campaign SIDEBAR level.** `.../campaigns/[id]` renders `components/campaigns/campaign-overview-page.tsx`, a PARALLEL of the brand Overview rather than a shared body, to keep the GA page clear of staff-preview risk and its source-substring guards green. Revenue + `/stats` scope to `campaignId`; pipeline-activity / audiences / economics / pause stay brand-level (features-service serves those brand-scoped by design). `getNavigationLevel` gains a `"campaign"` level (`segments[4]==="campaigns" && segments[5]`) → `CampaignLevelSidebar`; `EngagedLeadsPage` takes an optional `campaignId`. Query keys are byte-equal to `OutreachStatCardsAuto`'s, so the polls dedupe.
  - **Campaign v2 step 3 — the per-campaign budget is DEPRECATED as a concept: money is funded per SALES FUNNEL and a campaign runs one funnel, so the ceiling belongs to the funnel.** campaign-service keeps a per-campaign figure only as a MIRROR of that funnel ceiling, because `gate-check` is the first node of every run and cannot call billing hot on a scale-to-zero compute. The campaign Overview keeps the brand-level read for the monthly outcome forecast, which asks a different question ("what does the money buy per month") and is answered with the SUM of every funnel's ceiling — a number that is right for the forecast and wrong under one campaign's name. Audiences + sales-economics stay BRAND-WIDE; per-campaign audience stats via `fetchFeatureAudienceStats(campaignId)`. ⚠️ This line used to end "so there is no per-campaign pause" and that was FALSE the whole time: `PATCH /campaigns/:id` has always taken `{status: "activate" | "stop"}`, proxied by api-service, and step 6 below now uses it. A note in this file saying a capability does not exist is a point-in-time assertion like any other — check the deployed contract (`api-registry`) before building around it, because the reflex when one is missing is to design a workaround, and here the workaround (defund the campaign) was destructive.

⚠️ **What stays deprecated is campaign-service's `maxBudgetDailyUsd` MIRROR, which nothing edits and nothing displays — not the idea of a per-campaign figure.** A campaign is (offer × funnel × channel) and billing keys a ceiling on exactly that triple (#3455), so the campaign's own money exists and four surfaces now state it: Campaign Settings EDITS it (#3456, the only writer), the Campaigns table states it per row, **the cost card states it as the DENOMINATOR beside today's spend** (#3710 — `dailyBudgetCents={campaignBudgetCentsValue}`, so the card reads `$50 / $50` the way the brand card does; the brand and offer Overviews state none for the opposite reason, since billing answers a brand with the SUM of every funnel and that is a wider scope than the numerator beside it, and a campaign with no funnel or channel has no billing row keyed to it so it states the reason through `budgetNote` rather than leaving a bare figure a reader takes for a denominator that failed to load), and **the campaign Overview states it read-only at its top right, beside the campaign's own status pill** (#3462) — a line, not a heading: the page states no campaign identity of its own (the top bar names the open campaign, once per screen), so this says only whether it is running and what it may spend while it does. That figure is NOT the run-status bar coming back: the bar printed the BRAND's sum next to a retired goal word, this is the campaign's own row at the campaign's own grain, and the pill and the whole-dollar formatter are the ones every other campaign surface uses so a campaign reads the same way in the list and once it is open. Several readers, one shared narrowing — so they cannot diverge. Guard: `tests/campaign-budget-once.test.ts`.

⚠️ **campaign-service REFUSES a `maxBudget*Usd` on a SALES campaign — it answers 400, it does not ignore the field — so a consumer states one only for a campaign that sells through NO sales funnel, and the discriminator is the campaign's own `funnelKey`, never a slug list.** The refusal names the reason (*"nothing reads a per-campaign budget ceiling for the sales family. Its money is billing's, stated per (sales funnel, acquisition channel, offer)"*), and the field stays LIVE for every non-sales feature (PR, hiring, VC, AI visibility), which is what makes a blanket deletion wrong on the staff side: the customer flow could drop the four fields outright (#3799) while `apps/admin` had to NARROW them (#3802). Four things a future edit must hold:
- **`createCampaign` (`apps/admin/src/lib/api.ts`) is the choke point and it strips LOUDLY.** Two admin create paths rebuild their payload from a `sessionStorage` blob after a Stripe redirect, so a blob written before a contract change survives the round trip and would 400 the whole creation; a call-site-only fix cannot reach it. The strip `console.error`s naming the caller — it is a guard over a caller bug, not a fallback that absorbs one.
- **The BUDGET INPUT stays on both create pages and is NOT sent.** The typed amount prices the projection and gates the credit check before anything is created, so removing it would gut the page — but a control that silently does nothing to the campaign is worse than none, so both pages state where the money actually lives (`SALES_BUDGET_NOTE`) rather than leave the reader to guess. The RELAUNCH modal is the opposite case: budget is its only editable field, so for a sales campaign it renders no input at all and says the same sentence.
- **A stale ceiling on the row you COPY is the trap.** A sales campaign created before campaign-service's guard shipped carries a non-null `maxBudget*`; the relaunch modal prefilled from it and sent it straight back, which is exactly the 400. Deriving a prefill from a stored row is not the same as being allowed to send it.
- `lib/campaign-budget-fields.ts` is alias-free so it carries real unit tests — keep it that way, and keep it free of feature slugs. Guard: `tests/campaign-budget-fields.test.ts`. `apps/admin` tests are NOT a CI gate, so run `pnpm --filter @distribute/admin test` locally.

**The FUNNEL Overview states the same two answers, in the same place, through the same trigger (#3738).** A funnel is sold leg by leg and funded per (funnel, channel, offer), so it carries a status and a ceiling exactly as its offer does; its page stated neither, with the ceiling visible only as a denominator inside the cost card. `CampaignControlsTrigger` now rides its `headerAction` slot, narrowed by BOTH `offerId` and `funnelKey` — billing keys a ceiling on the triple, so a bare funnel spans every offer selling it and would list a sibling offer's campaigns under this one's name. The money is the SERVED running figure the cost card below already divides by, passed as `totalCentsOverride` rather than summed from the trigger's own rows: one number on one screen, so the header and the card cannot disagree. `buildControlRows`' funnel filter normalizes ONCE (the wire carries two spellings of every funnel) and an unmapped key narrows to nothing rather than throwing; a campaign that predates the funnels names none, so it belongs to no funnel's list rather than to whichever one the reader is looking at. Guards: `tests/campaign-controls.test.ts` + `tests/campaign-controls-surface.test.ts`.
  - **Campaign v2 step 6 — PAUSING is a STATUS, not a budget of zero, and the same modal states and changes it at all three grains.** A campaign carries TWO independent answers to "is this running, and how hard": campaign-service's own `status`, and billing's `(offer x funnel x channel)` ceiling. Collapsing them (pause = set the ceiling to 0) is what this replaces, and it was never merely inelegant: zero throws the amount away, and billing's per-funnel floor lets a funnel funded UNDER its minimum be kept or raised and nothing else, so a grandfathered campaign stopped that way could never be restarted at the figure it was running. Nothing needed building for it — `PATCH /v1/campaigns/{id}` with `{status: "activate" | "stop"}` has been live all along (campaign-service maps them to `ongoing` / `stopped` and stamps `stopReason: manual`, which its own code documents as NOT auto-resumable — a person stopped it, a person restarts it), so this is one `setCampaignStatus` wrapper and no backend PR.
    - **ONE modal, three entry points, and its rows are always CAMPAIGNS.** `CampaignControlsModal` mounted through `CampaignControlsTrigger`, filtered by `{}` at brand grain, `{offerId}` at offer grain, `{campaignId}` on a campaign's own Overview. A brand and an offer are SCOPES, not things billing or campaign-service fund, so an editable figure at either grain would have to be split back across the campaigns and no split the customer did not state is honest. One campaign is a one-row modal, not a branch.
    - **A ROW is one campaign as the CUSTOMER knows it — the `(funnel x channel x offer)` identity — never one campaign-service row, and this is what makes the total honest.** campaign-service mints a FRESH row every time a campaign's workflow changes and keeps only the newest `ongoing`, so a campaign that has run for months is stored as dozens of rows: one live and the rest its own history. billing keys exactly ONE ceiling on that triple. So a list per stored row shows the same campaign dozens of times, each offering to edit the one ceiling they all share, and `scopeTotalCents` adds that ceiling up once per row. Measured in prod (brand `75d7e3e8` / offer `d5ecba00`): 46 stored rows of a single cold-email campaign, 45 stopped, against a real $50 ceiling — the offer read **$2,310/day** and the modal listed 46 identical lines. `buildControlRows` therefore GROUPS on the triple (a funnel-less campaign, which names no triple and has no ceiling to share, groups by its own id). Two consequences to hold: a row's `campaignId` is the row a RESTART addresses (the live one, else the most recent — an ancestor would resume a workflow the customer replaced), and a PAUSE fans out over `runningCampaignIds` so it cannot leave a second row live. Every piece of modal state — drafts, failures, invalid rows, the React key — is keyed on `rowId`, never a campaign id. Generalise: **before summing a per-entity figure, check the entity is the grain the producer keys that figure on** — several rows sharing one billing row is a double count, and it reads as a plausible number rather than an error. (#3533)
    - **The MONEY on the trigger is read, never recomposed.** Brand grain passes billing's own served total (`budgetData.dailyBudgetCents`, which the Overview already fetches for the cost card); offer grain has no served figure, so `scopeTotalCents` adds up the offer's own campaign ceilings — the same shape as the funnels card's per-offer total, and for the same reason. Do NOT sum the rows to obtain the brand's number: that is how two surfaces come to state one number two ways.
    - **The roll-up is TWO states plus `none`** (`rollupStatus` → `active` when ANY campaign in the scope is running, else `paused`; `none` for a scope with no campaign at all). There is deliberately no third word: `partially-paused` existed, and it read as a fault on a brand doing exactly what it meant to — one funnel live, an older one stopped on purpose. Owner-decided: **if at least one campaign is running, the scope is running.** Which campaigns run is what the rows themselves say, one toggle each; the pill answers the coarser question a reader asked by glancing at it. Do NOT re-add a mixed verdict. `statusLabel` in the Campaigns table now reads `stopped` as **Paused** for the same reason: the row in the list and the pill on that campaign's page must say ONE word about one campaign, and from the customer's side a stopped campaign is one waiting to be turned back on. `isActiveStatus` is untouched, so the colour and the table's sort key are unchanged.
    - **Only what CHANGED is written, and status and budget never restate each other** (`controlsDiff`). The writes are a FAN-OUT — there is no bulk endpoint — so a failure is reported per row, the modal stays open, and it never claims a success it does not have. The floor is checked against the FUNNEL total across every row of that funnel the form is editing at once (`projectedFunnelTotalsUsd`), because this modal can move several of them in one go; billing holds the same rule and its 400 decides.
    - ⚠️ **`activate` FIRES THE WORKFLOW IMMEDIATELY** (`executeCampaignWorkflow` + `wakeScheduler`), so restarting spends now rather than at the next daily tick, and the summary above Confirm says so. `activate` also 400s without `x-brand-id` / `x-feature-slug`, which `setCampaignStatus` therefore sends rather than assumes.
    - The campaign Overview header is now the way IN to this modal, which is a deliberate change to the "editing stays on Campaign Settings" line above: the header still renders no field and holds no mutation, and every window writes through the SAME narrowing in `lib/campaign-budget.ts`. Several windows onto one number are fine; a second NARROWING is not. `lib/campaign-controls.ts` is alias-free so it carries REAL unit tests — keep it that way. Guards: `tests/campaign-controls.test.ts` + `tests/campaign-controls-surface.test.ts`.
  - **Campaign SETTINGS states BOTH answers a campaign carries — is it running, and what may it spend (#3456, #3701)** — `components/settings/campaign-settings-card.tsx`, guard `tests/campaign-settings.test.ts`. It carried the campaign's name, its audiences, its services and its click destination; every one of those is a statement about the OFFER, which has its own Settings page one level up, so the screen was four editable copies of the offer's answer one click below the offer itself. They are gone, and `updateCampaign` / `CampaignSettingsPatch` went with them rather than being left unrendered — there is NO per-campaign settings write. Four things a future edit must hold: it writes `saveBrandFunnelBudget(brand, funnel, cents, channel, offer)` (billing's row, never the mirror); **ZERO is the point of the screen** and is an ordinary value, the way a customer stops one campaign without losing anything they stated about how it sells; the floor binds the FUNNEL total (`projectedFunnelTotalUsd`, computed only to check the form — nothing displayed derives from it, and billing's 400 decides); and a campaign naming no funnel or channel says so rather than pointing at a guessed row. **The narrowing that turns billing's per-pair figure into ONE campaign's money is `offerScopedCents` in `lib/funnel-channels.ts`, read by EVERY budget surface** — Offer Settings through `funnelChannelBudgets` for every channel of a funnel at once, and the three per-campaign surfaces through `lib/campaign-budget.ts` (`campaignBudgetScope` / `campaignSavedCents` / `campaignBudgetCents` / `fmtDailyBudgetUsd`), which Campaign Settings, the Campaigns table and the campaign Overview header all import. A second copy of it is how those pages would start disagreeing about the same campaign's money. `lib/campaign-budget.ts` is alias-free, so it carries REAL unit tests (`tests/campaign-budget.test.ts`) — keep it that way; a runtime `@/…` import there turns them into resolution failures.

    **The STATUS lives here too, and it is what a customer stops a campaign with.** The page stated only the ceiling for as long as it existed, so the only way to stop a campaign from its own settings screen was to ZERO its budget — which throws the amount away, and which billing's per-funnel floor makes irreversible for a funnel funded under its minimum (it may be kept or raised, never re-lowered, and going through 0 clears the grandfather). A pause costs nothing to reverse and leaves the ceiling untouched. Four things a future edit must hold: the flip reads the SAME `isRunningStatus` set and the SAME `setCampaignStatus` the controls modal uses (a second list of running-words is how two surfaces come to disagree about whether one campaign is live); a campaign naming no acquisition channel CANNOT be restarted from here, because campaign-service validates the workflow's seven tracking headers before it flips the row, so the toggle is disabled rather than sending a request we know is refused; **ONE Save commits both**, since a toggle that writes instantly beside a field that does not reads as two rules on one card; and what Save is about to do is STATED above the button rather than reported after — including that restarting **fires the workflow immediately** rather than at the next daily tick. Zero survives as the way to genuinely defund, and the copy now names it as the thing that is NOT the same as pausing.

    **A figure under what the funnel may be funded at is put BACK to the smallest one it may hold, never refused on screen.** Refusing it and leaving the typed value there makes the customer guess what is allowed; naming the floor alone makes them do the subtraction the funnel's other campaigns imply — and neither answers what they were actually trying to do, which is spend less. So the field is restored, the surface says why, and it offers **pause** as the reversible way to stop. `minimumCampaignBudgetUsd` (`lib/campaign-budget.ts`, beside `projectedFunnelTotalUsd`, which moved out of the card so the check and the clamp read ONE projection) derives the bar from the SAME rule `funnelBudgetBelowMinimum` enforces, in the same two branches — the funnel's floor, or the grandfathered figure billing already funds it under — minus what the siblings carry, so **a clamped value can never itself be refused** (a guard walks every fixture asserting exactly that). It runs on BLUR, never per keystroke: typing `1` on the way to `10` must not jump to the floor under the cursor. It also RETURNS the clamped figure as well as setting it, because `setValue` does not land before the tick ends and a Save reading `value` back would write the figure it just refused. **ZERO is never clamped** — defunding is an ordinary state, and the bar governs what may be NEWLY stated, not whether a customer may stop.
  - **The offer's HORMOZI LEVERS are edited on the COLD EMAIL campaign's Settings, and Offer Settings keeps the same editor exactly while no such campaign exists (#3686).** They are the words that channel's emails are written around, so they are stated where that campaign is set up. What does NOT move with them is the storage: brand-service keys the 7 user-fields on `(brand, offer)` and has no per-campaign column, so the campaign page is a **WINDOW** onto the offer's own answer, never a second copy — an offer sold through two cold email funnels states one set of levers and both campaigns show it. Two windows on one value is fine; a second NARROWING is not (same rule as the budget's `offerScopedCents`). Five things a future edit must hold:
    - **Never zero editors.** An offer is born at signup and a campaign is only provisioned once a funnel is funded, so an offer with no cold email campaign is the ORDINARY case for a brand that has not launched — a straight move would have left it with nowhere at all to state what it promises. Offer Settings renders the SAME `BrandOfferCard` while `coldEmailCampaignForOffer` answers null, and a link to that campaign's Settings once it does not. One editable card at a time, and never zero.
    - **The gate is `isColdEmailChannel` over a slug SET (`lib/offer-levers-home.ts`), never a `===` at the call site** — a slug re-version rots every hardcoded comparison, and the siblings that also write emails from these levers (`sales-crm-email-outreach`, `feedback-request-cold-email-outreach`) are one line from joining it **if** their Settings pages start hosting the card. Admitting a slug whose page does not render the card gates the levers onto a page that never shows them.
    - **`coldEmailCampaignForOffer` collapses campaign-service's many stored rows to the ONE a reader is sent to**: a live row wins outright, else the latest by `updatedAt` (ISO-8601 UTC strings, so nothing is parsed). Byte-equal to the Campaigns table's `listedCampaigns` rule, and a guard pins the two running-status sets together. A campaign carrying no offer belongs to none and is left out rather than folded into whichever offer the reader is looking at.
    - **Both pages wait for their read to SETTLE before deciding**, and the campaign gate reads `["campaign", campaignId]` — the key the budget card below it already polls, so naming the channel costs no second request. A card that renders and then vanishes is worse than one arriving a moment late.
    - **A FAILED campaigns read keeps the editor on Offer Settings** (`data` undefined ⟹ null ⟹ the card renders). Pointing at a campaign we could not confirm exists is worse than showing the card twice, and losing the only editor to a blip is worse than both.
    - `lib/offer-levers-home.ts` is alias-free (its only import is type-only and erased at build) so it carries REAL unit tests — keep it that way. Guard: `tests/offer-levers-home.test.ts`.
  - **Campaign v2 step 4 (#3180) — the surface is called CAMPAIGNS again (#2902 had renamed it to Channels), it CREATES nothing, and it names the open campaign in the TOP BAR.** Route back to `.../brands/[brandId]/campaigns`; nav entry, page heading, empty state and breadcrumb crumb all read `Campaigns` (the campaign sidebar's back-link is gone, see the no-back-link rule below). **`NewCampaignModal` is DELETED, not hidden** — a campaign is set up with us, not spun up from a table row, so the create control and the clone-a-campaign modal behind it are gone (guarded: the page must not mention `New channel` / `NewCampaignModal`, and the file must not exist). **`HeaderPageContext` (`components/header-page-context.tsx`) is the ONE exception to "the top bar carries no breadcrumb"**: tenant identity (org, brand) stays in the sidebar switcher, but a campaign has no name anywhere in the sidebar and several sit under one brand, so the bar names the one you drilled into — on `.../campaigns/[id]` and nowhere else, on the `["campaign", id]` key the overview already polls (one deduped poll), with a skeleton rather than a placeholder word while the name lands. Do NOT extend it into an org/brand breadcrumb. **A crumb between the offer and the campaign is the FUNNEL, and it is resolved from the CAMPAIGN, not from the path** — a campaign lives at `/offers/:offerId/campaigns/:id`, off the funnel it was opened from, so a crumb gated on the `/funnels/:funnelKey` segment vanishes exactly one level deeper than the list the campaign was picked in. Since a campaign is named for its LEG (#3664), that left the funnel's own name nowhere on the bar. `funnelKey = route.funnelKey ?? campaign?.funnelKey` — the campaign states it in the same wire vocabulary the funnels route carries, so the crumb reads the same words, draws the same mark and links to the same page whichever way you arrived; a campaign stating none renders NO crumb rather than a guessed one, and the read is the one the bar already makes. Generalise: **when a route level is OPTIONAL in the path, the crumb for it comes off the entity, never off the segment.** (#3693) **And the bar names the DEEPEST thing the path states, which for a funnel LEG page is the arrow itself (#3702).** That page is reached from a `Done by you` row and from nowhere else, so it has no campaign to name and the arrow IS the entity; the crumb stopped at the funnel, which meant every leg of one funnel wore the same bar. It reads the label off `funnelLegs` — the same walk the funnel page lists its rows from and the same vocabulary a campaign is named with, since a campaign is named for its leg — so an arrow reads identically whether you opened it as a leg or as the campaign that performs it (`Sales interest → Meeting booked`). It is where you already are, so it is never a link, it draws the same `FunnelLegMark` the funnel's own rows draw, and a `legKey` naming no arrow of the funnel renders NO crumb rather than a guessed one. The path shape is parsed in `offerRouteFromPath` with the others, never at the render site: that parser exists because the campaign crumb was once keyed on a hardcoded segment index and silently rendered nothing the day campaigns moved under the offer. The table's Channel and Goal columns read the brand-Settings catalogues through the shared marks (see the Acquisition Channels section). (#3180)
  - **Campaign v2 step 5 (⚠️ the STRATEGY half is HISTORICAL — `campaigns/[id]/strategy` no longer exists; the campaign's sub-routes are `audiences`, `leads` and `settings`. The AUDIENCES half is live and the rule below still governs it) — STRATEGY + AUDIENCES live under the campaign, and the rule for that whole level is: show the campaign's own value where campaign-service stores one, else the brand's.** They used to link out to the brand pages (`${basePath}/…`), so drilling into a campaign and clicking Strategy silently left it. Now `campaigns/[id]/strategy` + `campaigns/[id]/audiences` render the SAME `StrategyPage` / `CustomerAudiencesPage` through an optional `campaignId` prop — the `EngagedLeadsPage` precedent, NOT a parallel body: the prop is absent on the brand routes, so the GA path is byte-identical and the existing source-substring guards stay green. `getNavigationLevel` (`segments[4]==="campaigns" && segments[5]`) and `campaignIdFromPath` (`parts[5]`) already ignore depth, so the campaign sidebar and the top-bar campaign name carry over to the sub-routes with no change.
    - **What actually narrows is exactly the three things campaign-service stores per campaign**: the goal (`campaign.goal` → `optimizationGoalForRuntimeGoal`, driving the objective labels, the goal-gated columns and the default sort), the targeted audience subset (`campaign.audienceIds`, a display filter over the rows already fetched — human-service has no per-campaign audience read), and the per-audience stats (`fetchFeatureAudienceStats({campaignId})`, on its own cache key so it never shares an entry with the brand-wide read). All three are `null`-inherit, same semantic as the budget in step 3.
    - **(HISTORICAL, superseded by the levers rule below — the strategy route it describes is deleted.) The Hormozi offer was a PREVIEW on the campaign page and did NOT write.** There is no per-campaign column for the 7 user-fields, so `saveOffer` early-returns on `campaignScoped`, the Save button renders only when `offerDirty && !campaignScoped`, and the card states `Preview only. Nothing here is saved yet.` Inline editing still works as a local draft (with Discard) — that is the "see how it would work" surface Kevin asked for. Wiring Save here would take one campaign's edits and overwrite the BRAND's offer for every other campaign, which is the control-lying-about-what-it-touches bug this repo treats as a bug, not a shortcut. Same posture as the Acquisition Channels card, which states what runs rather than collecting a choice it cannot store.
    - **Brand-wide by backend design, so the campaign page inherits and SAYS so**: sales economics + conversion rates, the offer, and the workflow-projection ladder (best model / fleet benchmark — `/workflow-projection` takes no `campaignId`, verified in api-registry). The audiences header states that creating, pausing or archiving an audience there changes it for every campaign, because those controls genuinely write brand-level rows.
    - Every query root involved (`campaign`, `audiences`, `featureAudienceStats`, `brandSalesEconomics`, `brandUserFields`, `workflowProjection`) was already in `PERSISTABLE_QUERY_ROOTS` — no new root. The `["campaign", campaignId ?? "none"]` key is byte-equal to the campaign Overview's and `HeaderPageContext`'s, so all three share one poll. Guard: `tests/campaign-strategy-audiences.test.ts`.
  - **Every NEW `useAuthQuery` root the campaign-v2 surface adds MUST be added to `PERSISTABLE_QUERY_ROOTS` (`lib/persist-cache.ts`) in the SAME PR, or that page cold-skeletons on the slow features-service Neon chain every visit instead of painting instantly from disk (SWR) like the rest.** The `featureRevenueByCampaign` root (the list page's per-campaign pipeline/$CAC/ROI/%CAC query) was left out across #2761/#2771/#2774 → the Campaigns v2 list showed an "infinite skeleton" (its table + all 3 stat tiles gate on that one query). Fixed by adding the root (#2776). Rule of thumb when extending this surface: grep the new `useAuthQuery([...])` root, confirm it's in the allowlist; the reveal-on-settle gates only stop an ETERNAL skeleton on error, they do NOT make a non-persisted query fast. (#2776)
- **EVERY beta/alpha-gated surface MUST carry a VISIBLE `<MaturityBadge level="beta|alpha" />` next to it — gating WITHOUT a badge is a bug, not a "clean" hide.** A surface that only beta users can see still needs the badge so the viewer KNOWS it's beta (not GA) — otherwise even the people who can see it (incl. Kevin) don't realize it's gated and treat it as shipped. This applies to a whole gated PAGE/nav entry (`MaturityBadge` on the `SidebarItem.maturity` / page header) AND to a beta sub-element living inside an otherwise-GA page (e.g. the "Edit with AI" button on the GA brand-profile page — `{isBeta && <button>…<MaturityBadge level="beta" /></button>}`). When you add ANY `isBeta`/`isBetaEmail`/`useFeatureFlag` gate, add the badge in the SAME edit. Rule of thumb: if you can't point at the badge, the gate is incomplete. (Kevin 2026-06-25: "All beta gated must have the beta tags. I didnt know brand profile was beta gated!!!" — the brand-profile page was beta-gated with no badge for months.) **Corollary — NEVER beta/alpha-gate a WHOLE page + its nav entry silently. A full-surface gate (`if(!isBeta) return <not available>` + hidden `SidebarItem`) hides the surface ENTIRELY from the excluded user, so by definition they see NO badge (a badge inside the page is invisible when the page is blocked), and even an allowlisted owner doesn't notice the surface got gated → reads as "shipped/GA". So a whole-page/nav gate is NOT a code default you apply on your own — it needs explicit owner sign-off, and an in-page `MaturityBadge` does not satisfy the badge rule for it (the badge must ride the nav entry / page header that the gate still renders). Gating only a SUB-ELEMENT of an otherwise-GA page (Edit-with-AI button) is the safe form: the page stays visible, the badge rides the gated element. (Kevin 2026-06-25b: Audiences page+nav + Leads nav were beta-gated with the badge buried inside the blocked page — "Je suis enragé que tu as mis deux pages en beta gated sans me le dire"; #2190 made all three GA, kept only the Edit-with-AI sub-button beta + threaded `isBeta` so its badge never shows without its gate.)
- **The signup wizard REUSES the real feature editors — never re-inline them.** `components/brand-profile/field-editor.tsx` (`FieldEditor` + `SECTIONS`) was extracted from its page for exactly this; edit the shared module and both surfaces move together. Rate inputs are `type=text` with comma separators and decimals, NOT `type=number` (which cannot show commas and re-introduces the leading-zero `01` bug), and a prefilled rate is rounded to ONE decimal at the seed site — round the `rates` VALUE, not just its display text, or what is shown and what is saved diverge. (The `beta-onboarding.tsx` variant this described is DELETED; the live flow is `onboarding.tsx`, documented in its own section above.)
- **Adding an AI-EXTRACTED, display-only brand-profile field is CLIENT-SIDE ONLY** — a new `SALES_PROFILE_FIELDS` entry (`key` + AI `description`) flows through the generic `extract-fields` endpoint and the generic `Record<string, string|string[]>` storage, so do NOT spawn a brand-service workspace for it. **A USER-CHOSEN config value is the opposite**: if it needs a Brand Settings editor OR a backend consumer reads it (the click-destination URL outreach clicks land on), it is a first-class brand-service field — typed nullable column + `PUT /v1/brands/:brandId/<thing>`, mirroring `sales-economics` / `current-goal` — never the generic profile bag. (#2103, brand-service#295, api-service#611)
- **A workflow projection is server-derived from the brand's SAVED economics, so it must be refetched AFTER the save**, not once at a loading screen that runs before the rates are entered (stale default econ reads `$1/day` and `~0 signups`). The refresh is best-effort and keep-last-good: adopt it only when `costPerCloseUsd` and the counts are non-null, since a direct `getWorkflowProjection` call has none of the `structuralSharing` protection the query gets. (#1766)
- **POST-onboarding first-visit modal series + blocker surfaces use `driver.js` (MIT, React-19) — this is the dashboard's onboarding-tour / product-spotlight lib; reuse it, do NOT add react-joyride/shepherd/intro.js.** Mounted ONCE in `(authed)/(dashboard)/layout.tsx` alongside `CreditAlerts`, distinct from the SIGNUP `onboarding.tsx` wizard (that's the pre-dashboard flow; this fires AFTER a user reaches the dashboard). Three surfaces, all gated client-side off existing query keys (`["billingAccount"]`, `["audiences", brandId]` — dedupe, no new fetches):
  - **`WelcomeTour`** (`components/onboarding/welcome-tour.tsx`) — 5-card driver.js sequence shown ONCE per user via `localStorage distribute:welcome-seen:{userId}` (NOT a Clerk-metadata routing gate, reassurance only). Steps: why / $25-match credits / we-email-on-your-behalf (+ example email) / what's-next timeline / find-your-best-audience (spotlights the Audiences nav on desktop, centered card on mobile). Copy lives in the pure `lib/onboarding-content.ts` (`WELCOME_STEPS`), humanizer-clean + em-dash-guarded by `tests/onboarding-content.test.ts`.
  - **`OnboardingReminders`** (`components/onboarding/onboarding-reminders.tsx`) — state-based blocker modals after the tour, ONE at a time, priority topup then audience (`nextReminder` in `lib/onboarding-reminders.ts`): auto-topup off (`!has_auto_topup` opens `showPaymentRequired({proactive})`) then 0 active audiences (routes to audiences page). Session-dismissable per brand (`sessionStorage distribute:reminder-dismissed:{kind}:{brandId}`); re-shows next session while unresolved, never once resolved.
  - **`NoAudienceBanner`** (`components/onboarding/no-audience-banner.tsx`) — persistent red bar (mirrors the urgent credit-runway banner) when the brand in view has 0 active audiences; NOT dismissable (hard blocker, outreach can't run). Reads URL `brandId` via `useParams` (null off a brand route).
  - Gating logic + storage keys are PURE in `lib/onboarding-reminders.ts` (unit-tested); the React components own storage reads + fetches. `OnboardingFlow` (`components/onboarding/onboarding-flow.tsx`) sequences tour then reminders. driver.js popover is brand-themed via `.distribute-tour` in `globals.css`. (#2115)
- **The audience-step prewarm must NOT couple the ICP fetch with the audience-suggest call — `suggestAudiences` is FLAKY in prod (~57% fail: human-service `/orgs/audiences/suggest`, human-service#100) and `suggestBrandIcp` is healthy (~78% OK).** The prewarm IIFE (`hydrateOnboardingInBackground` → `audiencePrewarm`) ran `await suggestBrandIcp(id)` then `await suggestAudiences(id, prompt)` in ONE `try`, and the `catch` returned `{ prompt: "", candidates: null }` — so an audience-suggest failure AFTER the ICP already resolved DISCARDED the real ICP, and the audience step adopted `"" || fallbackPrompt` → always showed the generic `Find the ideal customers for <brand>` line instead of the real brand-service ICP. Fix: hoist `prompt` out of the `try` so the `catch` returns the already-resolved ICP (`candidates` stay null → manual "Find my perfect audiences" retry). Rule: when a prewarm chains a HEALTHY producer then a FLAKY one, the flaky one's failure must never nuke the healthy one's result. The ICP 422 ("Empty brand profile") is NOT the cause here — `brandProfileService.getByBrandId` derives a virtual v1 from `brandExtractedFields` (written by `extractBrandFields`, awaited first), so the profile is non-empty by the time icp/suggest runs. (#2096)
- **Outcome/metric/probability/EV surfaces compute SERVER-SIDE in features-service, dashboard renders only — extend `GET /v1/features/:slug/revenue?lens=` (`getFeatureOutcomes`), never re-derive client-side.** The Signups/Booked Meetings/Sales pages pass `?lens=signups|booked-meetings|sales`; features-service filters leads to the lens signal and returns per-lead `conversionProbabilityPct` + lens-specific `expectedRevenueUsd` (= proba × brand LTR; Sales = combined-OR `orP(pClick,pReply)` paid-close). A new outcome lens = a new enum value + backend math, not a client formula. Detail-panel data (status booleans, sent emails) is a legit client JOIN of existing endpoints (`listBrandLeads` + `listBrandEmails`) — a display affordance, not derived metrics. (#1654, #1660; features-service #283 / v0.47.0.)
- Live flags: `alpha-services-crm` (195453), `alpha-keys` (195454), `alpha-brand-info` (195479, Brand Info), `alpha-brand-features` (195480, every brand feature except GA exceptions), `alpha-conversions` (Revenue/Conversions), `alpha-workflows` (200358, Workflows page + its sidebar entries; the Feature Settings sub-level that hosts it is GA), `alpha-brand-database` (200359, brand Database rows Outlets/Journalists/Articles; Leads+Emails stay GA), `beta-campaign-activity` (200595, Live activity feed on the campaign overview — `CampaignActivity` self-gates + carries the beta badge; first BETA flag, opt-in cohort per `BETA_ALLOWED_EMAILS`). GA-exception slugs in `feature-gates.ts` → `GA_BRAND_FEATURES` (currently `sales-cold-email-outreach` only — `pr-cold-email-outreach` went alpha in #1339), consumed by `BrandLevelSidebar` + `brands/[brandId]/page.tsx` + `breadcrumb-nav.tsx` (the three feature-listing surfaces — keep in lockstep, see the filter rule above).

**Onboarding field extraction warms ONLY the 7 user-facing fields (`USER_PROFILE_FIELDS` in `api.ts` = services + the 6 Hormozi offer levers = the exact `USER_FIELD_KEYS` set) in `mode:"suggest"` — NEVER the full 19-field `SALES_PROFILE_FIELDS`.** The offer step reads these via `getBrandUserFields` and needs a best-effort value for every lever (`suggest` = generative Hormozi/top-3-expert persona, infers where the site/pasted-context is silent, never "Unknown"; the `suggest → user-fields GET` path is proven by the admin offer-levers card #2996). Two load-bearing rules a future edit must not regress: (a) **`dreamOutcome` MUST be in `USER_PROFILE_FIELDS`** — it is a user-field key that legacy `SALES_PROFILE_FIELDS` never listed (it kept the old `valueProposition`), so before this fix the Dream-outcome lever had no extraction source and always prefilled empty; (b) **do NOT warm the 12 backend-only `SALES_PROFILE_FIELDS`** (funding/competitors/leadership/…) during onboarding — the flow never reads them and the brand-info **alpha** page regenerates them on demand via its own Generate button; re-adding them is a pure LLM-cost waste. `extractBrandFields` takes an optional `mode` forwarded in the body (default absent = `extract`, api-service passthrough). General lesson (mirror of the dead-job "audit readers first" rule): before changing the MODE/behavior of a multi-field extraction/fetch, audit which fields the consumer actually READS and DROP the unread ones — do not preserve the call's scope just because it was there. (#2999)

### Auth / first-run gating — key on the real product signal, NOT org existence

**Routing/auth decisions belong at the EDGE (`proxy.ts`) via JWT claims — pre-paint, zero fetch. SWR is for DATA, never routing.** This governs EVERY landing/default redirect (onboarding, "last-visited brand", any "bare URL → entity" default): the FIRST move is the edge, not a client page. Remembered state goes in a **server-readable cookie** (the edge reads it pre-paint), NOT localStorage (client-only → client round-trip → flash). A client `useEffect`+fetch+`router.push` is the flash anti-pattern. A bounded client fallback is OK only where no remembered state exists yet (nothing to flash). (#1298: org-scoped `httpOnly` `last-brand-{orgId}` cookie set on brand URLs in `proxy.ts`, bare `/orgs/:orgId` redirects on it; the org Overview page is now an empty-org-only fallback — `return null` + redirect to last/first brand whenever the org has ≥1 brand. Don't restore an org-overview-first landing.)

**The cookie half of that rule is NOT limited to routing — it governs any REMEMBERED VALUE that must be on screen in the first frame, including a plain display label.** The sentence above lives in the auth section, so it reads as being about redirects, and a placeholder-flash bug does not look like a routing bug. It is the same bug. The test is one question: *is this value needed before hydration?* If yes, no client store can supply it — IndexedDB's restore is asynchronous and even a synchronous localStorage read happens at hydration, by which time the placeholder HTML has been on screen for the whole TTFB + JS-parse window. Only a cookie reaches the server render. So a "we already cache it, why is it still flashing" report is never answered by caching harder; re-derive WHEN each source becomes readable relative to paint. Worked example + the two-layer split (cookie owns the first frame, React Query owns freshness): the tenant-switcher section below. (tenant-identity-sync-paint)

**A landing RESOLVES to the deepest scope with no choice left in it, and the walk is gated on a MARKER (`?land=1`) — so the rule is not "which URL" but "is this navigation a PICK or a DESTINATION".** `lib/landing-drilldown.ts` + `lib/use-landing-drilldown.ts`: the last-brand redirect lands on a brand, then drills into its sole offer, then that offer's sole funnel, counting the rows the page WOULD render (a list of one has no decision in it, a list of two does) on the two query keys those pages already poll — so the walk costs no request and never hides a row. The marker exists because a bare brand URL is where the sidebar's Overview entry, the brand crumb and every bookmark point, and **a page that redirects away from itself is a page nobody can reach**.
- **A surface that PICKS a brand carries the marker**: the edge redirect (`proxy.ts`), the no-cookie client fallback (the org page), the tenant switcher's brand row (`handleBrandSwitch`) and the org page's brand list. The switcher one is load-bearing since the back links are deleted — it is the ONLY way back to a brand from Billing or the API-key page, and without the marker that return landed on the brand Overview while signing in landed on the funnel: one brand, two doors, two places. All the pickers move together or they contradict each other.
- **A surface that NAMES a destination carries none**: the sidebar Overview entry, the brand crumb, `header-page-context`, and `handleOfferSwitch` (picking an offer has already made that choice; a funnel is worked leg by leg rather than being a level with one obvious child, which is why `landingFunnelHref` ends the walk).
- `?view=overview` (`explicitHierarchyHref`) is the opposite intent — the edge skips the last-brand redirect on it entirely, so the marker is never appended alongside it.
- The hold is BOUNDED (`LANDING_RESOLVE_BUDGET_MS`, 600ms): long enough for the persister's disk restore, far short of a cold features-service read, and it latches — a late answer can neither hold a skeleton nor move the page out from under someone already reading it. Guards: `tests/landing-drilldown.test.ts`, which pins every call site on BOTH sides of the pick/destination split. (#3788)

**Don't gate on org existence** — a Clerk org is auto-created at signup, so `if (!hasOrg) router.push("/onboarding")` never fires. (#1229, DIS-91: 0 onboarding pageviews in 60d.)

**Custom auth flows (`sign-in`/`sign-up`/`forgot-password`) MUST import `useSignIn`/`useSignUp` from `@clerk/nextjs/legacy`, NOT `@clerk/nextjs`.** Clerk v7 ships TWO different hooks under those names: the bare `@clerk/nextjs` export is the NEW signals-based API (`{ signUp, errors, fetchStatus }` — NO `setActive`, NO `isLoaded`, different shape), while `@clerk/nextjs/legacy` is the classic `{ isLoaded, signUp/signIn, setActive }` API that the custom email/password + OAuth flows are built on. Grab the wrong one and `setActive`/`isLoaded`/`signUp.create` are `undefined` and the whole custom flow breaks. All three auth pages import from `/legacy`; keep it that way. Email/password uses `signUp.create` → `prepareEmailAddressVerification({strategy:"email_code"})` → `attemptEmailAddressVerification` → `setActive`; sign-in uses `signIn.create({identifier,password})` → `setActive`; reset uses `signIn.create({strategy:"reset_password_email_code"})` → `attemptFirstFactor` → `setActive`. `/forgot-password` is in `proxy.ts`'s public + auth matchers (public = signed-out can reach it; auth = signed-in bounce to `/orgs`). Email/password requires the Clerk Dashboard "Email address + Password" toggle to be ON (backward-compatible with Google). (#2754)

**An auth-failure PostHog event MUST carry the Clerk error CODE via `authFailureProps(err, ctx)` (`src/lib/clerk-error.ts`) — a bare `posthog.capture("x_failed")` (or one carrying only `{stage}`) makes the signup funnel undiagnosable, and "why did this user churn?" becomes unanswerable after the fact.** All five failure events (`signup_email_failed`, `signin_email_failed`, `password_reset_failed`, `signup_google_oauth_failed`, `signin_google_oauth_failed`) go through `authFailureProps`, which emits ONLY `{code, codes}` plus the caller's `stage`/`provider` — **never the Clerk `message`**, because Clerk interpolates the identifier into it (`"Couldn't find your account for x@y.com"`), so forwarding the message ships an email address into analytics. A non-Clerk throw reports `code:"unknown"` rather than omitting the field, so unclassifiable failures stay visible instead of blending into the pre-instrumentation events. `src/lib/clerk-error.ts` is the ONE home for `clerkErrorMessage` / `clerkErrorCode` / `clerkErrorCodes` / `isGoogleOnlyAccountError` / `sanitizeVerificationCode` — all three auth pages carried drifting local copies; do NOT re-inline them. **A source-substring guard asserting a Clerk error-code literal (`strategy_for_user_invalid`) must now read `src/lib/clerk-error.ts`, not the page** — the extraction rotted exactly two such guards in `email-password-auth.test.ts`, fixed in the same commit.

**Verification-code inputs (sign-up verify + forgot-password reset) MUST run `sanitizeVerificationCode` (digits only, sliced to `VERIFICATION_CODE_LENGTH`) plus `maxLength={VERIFICATION_CODE_LENGTH}`.** A code copied out of a mail client routinely carries a trailing space or newline and Clerk compares the string verbatim, answering "Incorrect code" on a CORRECT code, which reads to the user as a broken product. This is input normalization on a field that only ever holds a 6-digit number, NOT a silent-fallback / fail-loud violation. The verify screen owns its own recovery affordance too: a resend that CONFIRMS (`New code sent to <email>`) with a `RESEND_COOLDOWN_SECONDS` (30s) disabled window, and a `form_code_incorrect`-gated hint that only the newest email's code is live. Origin: a real Google-Ads-sourced signup (Nigeria, Android mobile, 2026-07-27) failed verification twice in 45s then left; PostHog held only `{stage:"verify"}` so the cause was unrecoverable, and the plain grey "Resend code" text link read as dead (never clicked). Base rate then: 6 email signups started since the 20 Jul launch, 3 verified. Guards: `tests/auth-failure-diagnostics.test.ts`. (signup-code-incorrect)

**A sign-in status that is not `complete` is a STEP the flow owes the user, never one dead end — and the branch that handles it MUST capture, or the failure is invisible in the funnel that exists to catch it.** `signIn.create({identifier, password})` returns a `SignInStatus`, and only `complete` yields a session. The page collapsed every other value into a single `else` that printed *"Additional verification required. Try Google sign-in."* and captured nothing. Both halves were wrong, and they compounded:
- **This Clerk instance requires an emailed code as a SECOND FACTOR after a correct password**, so `needs_second_factor` is what every correct password returns — the happy path, not an edge case. The comment above that `else` asserted the opposite (*"needs_second_factor / needs_new_password etc. are not enabled on this instance"*), which is the assumption that shipped the bug. **Email + password sign-in therefore could not succeed for anyone, ever**, from its 20 Jul launch until 25 Aug: all 5 password accounts either reset their password (`reset_password_email_code` completes in ONE step, so `/forgot-password` was the only door) or used Google.
- **The copy named a provider the account may not have.** The customer who reported it (`andrew@voozaa.app`) has `password_enabled: true` and `external_accounts: []` — no Google identity at all — so the advice was a dead end. Naming Google is correct for EXACTLY ONE code, `strategy_for_user_invalid`, which means the account carries no password (verified against prod: a password attempt on a Google-only user returns it, an unknown email returns `form_identifier_not_found` instead).
- **The silence is why it lasted a month.** A non-complete status throws nothing, so `authFailureProps` never ran and PostHog held `signin_email_started` with no outcome — a stuck user is indistinguishable from one who wandered off. Every non-complete branch on all three auth pages now captures its own event carrying `status` (`signin_email_incomplete` / `signup_email_incomplete` / `password_reset_incomplete`), plus `signin_second_factor_started` / `_failed` for the new step. Fleet-wide there had been exactly ONE `strategy_for_user_invalid` ever and 12 `form_password_incorrect`, which is how a state-dependent bug reads when its main branch reports nothing.

Rules for the second-factor step (`sign-in/[[...sign-in]]/page.tsx`): it reuses the sign-up verify screen VERBATIM — same `sanitizeVerificationCode` + `maxLength={VERIFICATION_CODE_LENGTH}`, same 30s `RESEND_COOLDOWN_SECONDS` with a confirming notice, same `form_code_incorrect` hint — because a second implementation of a code input is a second place for the trailing-newline bug above to come back. `advanceSignIn` takes Clerk's `setActive` as an ARGUMENT (`activate`): extracting it out of the handler loses the discriminated-union narrowing `useSignIn` provides, so a closure over `setActive` is `possibly undefined`. **`prepareSecondFactor` is guarded on `!secondFactorPending`** — a second prepare mails a fresh code and silently invalidates the one being typed, which reads as "the code never works". Remaining statuses get their own sentence from `SIGN_IN_STATUS_MESSAGE` (a `Partial<Record<...>>`, so an unlisted one falls back to a line quoting the raw status rather than to Google).

⚠️ **The instance's MFA toggle can be turned off in the Clerk dashboard, which would make password sign-in one step and leave this branch dormant. Do NOT delete it if that happens** — the branch is what makes the page correct regardless of how the instance is configured, and the deleted comment proves how a config assumption baked into code ages. Verify a claim about instance auth config against Clerk (the FAPI response's `supported_second_factors`), never against a comment. Guards: `tests/sign-in-second-factor.test.ts`. (sign-in-second-factor)

**Current architecture (#1236, DIS-111):** the durable signal is `org.publicMetadata.onboardingComplete`, set on first brand via the in-repo server route `/api/onboarding/complete` (dashboard has `CLERK_SECRET_KEY`; `publicMetadata` is writable server-side, not from the browser). Surfaced as session claim `orgMeta = {{org.public_metadata}}`; `proxy.ts` redirects to `/onboarding` when `sessionClaims.orgMeta?.onboardingComplete !== true`. The same `orgMeta`/`email`/`firstName`/`lastName` claims drop the proxy's per-request `currentUser()`.

- **`onboardingComplete` MUST be written at the flow's TERMINAL step, NOT at brand creation.** The edge gate keys dashboard access on this single org-level flag, so the instant it flips `true` every dashboard URL passes. The beta guided flow (`beta-onboarding.tsx`, welcome→url→loading→objective→rates→personas→profile→consent→pricing) created the brand at the LOADING step and used to mark complete right there — 6 steps early — so a mid-flow **refresh or manual dashboard-URL nav** slipped past `proxy.ts` onto a half-set-up dashboard (no rates/personas/consent/campaign). Fix (#1770): the `POST /api/onboarding/complete` + `getToken({skipCache})` pair fires in `launch()` AFTER `createCampaign` succeeds (the genuine end); a mid-flow refresh now correctly bounces back to `/onboarding`. The DEFAULT flow (`default-onboarding.tsx`) is fine — its complete-write sits at brand creation which IS its last step (3-step flow). Rule for any new/edited onboarding flow: the complete-write goes at the LAST step only. Gate stays org-scoped (a per-brand check would need a fetch at the edge → violates JWT-claims-only routing; onboarding = first-brand setup so org ≈ brand). Abandon-before-terminal → re-runs onboarding next visit (brand upsert idempotent, no dupe) — correct, they didn't finish. (#1770)
  - **The org-scoped edge gate CANNOT catch an unfinished ADD-BRAND on an already-onboarded org ("org ≈ brand" breaks here) — a per-brand CLIENT gate covers it, and that is NOT a DIS-111 regression.** When an EXISTING org (already `onboardingComplete`) adds a NEW brand via `/onboarding?from=add` and abandons before the terminal launch, the brand row exists (created early at the loading step to prefill services) but its campaign does not (created only at `launch()`), yet the org flag is already `true` so `proxy.ts` opens every `/orgs/:id/brands/:brandId` URL → a half-built dashboard (no campaign/budget/active audience) is reachable. The edge fundamentally can't gate this (per-brand needs a FETCH; routing is JWT-claims-only). Fix (#2729): `components/brand/brand-setup-gate.tsx` (`BrandSetupGate`) — a CLIENT gate mounted in the brand layout (`orgs/[orgId]/brands/[brandId]/layout.tsx`, which STAYS an empty passthrough so `loading.tsx` still works; the gate is a client child, CreditAlerts pattern). It reads the brand's campaigns via `listCampaignsByBrand`; **zero campaigns = never finished** (1 campaign/brand, #1768) → `router.replace('/onboarding?from=add&brandId=<id>')`. **Fail-soft, mirroring the not-found gate:** redirect ONLY once the query SETTLES empty (`!isPending && !isError && length === 0`) — never on `isPending` (org-consistency gate / cold load) nor `isError` (a cold/failed campaign-service read must not bounce a COMPLETE brand). A brand with a campaign never redirects → god-mode / existing brands untouched. This does NOT re-introduce the flash-prone #1229 client brand-COUNT gate that DIS-111 removed (that was an ORG-level first-run gate on the dashboard layout, replaced by the edge claim) — this is a per-BRAND completeness gate for a case the edge can't see; the `onboarding-brand-gate.test.ts` DIS-111 guards (which assert the *dashboard* layout does no `listBrands`/`router.push`) stay green. **Resume:** onboarding accepts `?brandId=` (cross-session, when the sessionStorage snapshot is gone) — fetch the brand to seed its URL, replay the loading-screen hydration (idempotent `upsertBrand` + economics/projection/audiences from backend via `runResume(target, urlOverride)`), land on the GOAL step (not budget: pre-terminal audience picks + budget tier live only in the snapshot, so they must be re-picked; everything typed earlier is saved in backend + re-hydrated). A live same-tab snapshot still wins and resumes at the exact step (e.g. pricing). (#2729)

- **"New organization" / "New brand" / "Add brand" buttons MUST run the REAL `/onboarding` flow — never a stripped-down create modal.** Kevin's expectation: creating a brand or org from inside the app goes through the SAME onboarding as signup (services/goal/rates/personas/consent/budget/card), minus only the org-creation first step for a new brand. A `create-flows.tsx`-style mini-modal (just a URL field → `upsertBrand` → reload) was shipped once (#1599-era) and explicitly rejected. The flows already handle both cases natively — `/onboarding?from=add` (reuse active org = New Brand) and `/onboarding?new=1&from=add` (`forceNew` = New Org); `?from=add` skips only the welcome/booking intro and lands on the URL step. `/onboarding` is exempt from the first-run edge gate, so an already-`onboardingComplete` org is NOT bounced out. Don't re-introduce a stripped create-brand/create-org modal. (create-buttons-onboarding, #1985)
  - **ESCAPE HATCH for the add/new flow — an EXISTING user in `/onboarding?from=add` / `?new=1` MUST be able to return to a live org×brand mid-flow; only TRUE first-run signup is trapped.** #1985 keeps the REAL flow (don't strip it to a modal); it does NOT mean trapping an existing user with no exit. The trap (isolated full-screen layout, non-clickable logo, no breadcrumb) is correct ONLY for first-run signup (nothing to return to + the edge gate pins an incomplete org on `/onboarding` anyway). For `from=add`/`new=1` the active org is already `onboardingComplete` → the edge does NOT bounce them, so the no-escape was purely a UI omission. `components/onboarding/onboarding-top-chrome.tsx` (client, reads `useSearchParams`) renders, when `from=add||new=1`: a sticky header with a clickable logo + the dashboard `BreadcrumbNav` (org→brand switcher = jump to ANY existing tenant) + a Cancel link (logo+Cancel → `explicitHierarchyHref("/")` → last-visited brand) + the account widget; else (first-run) just the floating `OnboardingAccountWidget`, unchanged. `BreadcrumbNav` mounts on onboarding with ZERO new providers (it uses only global Clerk hooks + `fetch`, no OrgContext/Features/React Query). The flow body is untouched. Multitenant-onboarding UX leaders (Vercel/Linear/Stripe) keep the switcher + exit visible during a create flow — never a full-screen takeover for an existing user. Don't re-trap the add/new flow. (onboarding-escape-chrome)
    - **STAFF (`isAdminEmail`) ALSO get the escape chrome on BARE `/onboarding` (no `?from=add`/`?new=1`) — the god-mode org switch can drop a super-admin onto an incomplete/never-onboarded tenant as active org, which the edge gate then pins on `/onboarding` with NO exit.** Unlike the add/new case (active org already `onboardingComplete`), a staff-trapped org IS incomplete, so the trap fires for real — but staff have live tenants to return to, so trapping them is wrong. `onboarding-top-chrome.tsx` computes `isStaff = isAdminEmail(useUser().email)` and renders the escape chrome when `isAddFlow || isStaff`. `BreadcrumbNav` gates its all-orgs god-mode switcher on the same `isStaff`, so switching to any complete org clears the edge gate → escaped. Genuine first-run trap for NON-staff is unchanged. **A non-staff user who ALREADY has a COMPLETED org also escapes** — same edge-gate pin, but they have a live tenant to return to. Detected from Clerk memberships: `useOrganizationList({userMemberships:{infinite:true}})` → `hasCompletedOrg = data.some(m => m.organization.publicMetadata.onboardingComplete === true)`. Escape when `isAddFlow || isStaff || hasCompletedOrg`. A genuine first-run user has exactly one org and it's incomplete → `hasCompletedOrg` false → trap stays. Loading is trap-first (false until memberships resolve → no escape-flash on real first-run). (#2746, #2749)
    - **The onboarding ORG crumb label distinguishes add-BRAND from add-ORG — do NOT show the active org on `?new=1`.** `BreadcrumbNav` resolves the org crumb from the URL `/orgs/[orgId]` segment, which `/onboarding` lacks → `displayOrg` undefined → the `"Dashboard"` placeholder (the reported bug). The off-/orgs fallback to `useOrganization()` (active org) is gated by FLOW: `?from=add` (new **brand**) reuses the existing active org → show its name ✅; `?new=1` (new **org**) targets a brand-new org that is NOT the active one → the active-org name would MISLEAD, so it keeps the neutral placeholder. So `displayOrg` off-/orgs = `isNewOrgFlow ? undefined : (organization ?? undefined)`, where `isNewOrgFlow = useSearchParams().get("new") === "1"`. This is the ONE sanctioned `useOrganization()` use as an off-/orgs fallback (org-switching item 5) — still never bind the crumb to active org ON a `/orgs/:id` route (multi-tab oscillation). (#2653)

**Edge-claim gate migration — 3 traps:** (a) **Backfill** every existing entity that should pass BEFORE shipping (else claim-absent bounces all users; `apps/admin/scripts/backfill-onboarding-flag.mjs`). (b) **Stale token** — a claim is frozen at JWT mint, force `session.getToken({ skipCache: true })` before navigating. (c) **Exempt the loop + in-flight routes** (the onboarding route, all `/api/*`, the `?autoCreate` hop). Gate on "user COMPLETED the thing", not "container exists".

**Readiness gates MUST be monotonic** — never blank a mounted subtree on a transient auth-loading flip. Clerk's `isLoaded` flips back to `false` and `organization` blinks `null` during background JWT rotation (~1/min + focus/reconnect), so `show = !isLoading && hasOrg` blanks the body every rotation. Latch:
```ts
const resolved = useRef(false);
if (!isLoading && hasOrg) resolved.current = true;
const show = resolved.current || (!isLoading && hasOrg);
```
The data framework handles query data; a separate app-shell auth gate needs its own latch. (#1255)

### Org switching — cross-org isolation framework (defense-in-depth)

4th "don't act under the wrong context" member (after data/reveal/bucket). Keeps every API call + the whole cache bound to the viewed org. **Failure:** the proxy forwards the Clerk session active org (`auth().orgId`), decoupled from the URL `/orgs/[orgId]` (Clerk `organizationSyncOptions` syncs URL→session on page routes, NOT `/api/*`); after `setActive` the JWT rotates async → a lag window carrying the OLD org → a stale write commits under the old org (later 404) or a stale poll 404s. campaign-service isolation is correct; the 404 is the symptom. (DIS-143). Doctrine converges (Clerk + OWASP Multi-Tenant + tkdodo): derive tenant from JWT claims not client headers, validate every request, fail CLOSED, remount on switch.

Framework (every layer central, one file — never per-page):
1. **Server choke point — fail-closed proxy guard** (`src/lib/proxy-org.ts` `checkProxyOrg`): the client sends `x-active-org-id` (the URL org); the proxy compares it to `auth().orgId`, returns **409 `org_desync`** on mismatch, refuses to forward. The JWT stays the sole authority (client value detects the desync only — OWASP fail-closed, not trust-the-header). Applied to ALL proxy routes; a new route → call `checkProxyOrg` right after the `clerkOrgId` check. Constants in dependency-free `src/lib/org-desync.ts`.
2. **Client cache choke point — keyed remount** (`src/lib/query-provider.tsx`): inner `QueryClientProvider key={org.id}` → org switch = new client = empty cache atomically. REPLACES `queryClient.clear()` (raced). It is now the ONLY org-switch cache reset: the tenant switcher's org/brand labels are ordinary persisted queries under this org's persister prefix, so the remount clears memory AND the disk key space at once. The `OrgCacheInvalidator` component — which existed solely to clear the switcher's module-level caches, and whose cross-tab `router.push` was already removed in #1944 — was DELETED along with those caches; do not re-add a side cache that needs clearing. **The remount key MUST be MONOTONIC — latch it to the last RESOLVED org id, never raw `orgId ?? "no-org"`.** Clerk's `useOrganization()` blinks `organization: null` transiently during background JWT rotation (~1/min) + tab focus/reconnect; a raw key flips `realId→"no-org"→realId` on every blink → spurious remount → empty in-memory cache. Persisted queries rehydrate from IndexedDB silently (ALL live roots, big lists included, no 5MB cap since the 2026-06-25 per-query IndexedDB migration — so this blink-skeleton is gone). Fix: `const lastOrgId = useRef(null); if (orgId) lastOrgId.current = orgId;` then key + `orgId` prop both off `lastOrgId.current` — a null blink keeps the last key (no remount); a real switch to a DIFFERENT org still advances the id → remount + fresh per-org persister prefix, DIS-143 intact. Same monotonic-gate rule as the app-shell auth latch. (#1767)
3. **Source — `await setActive` THEN `await session?.getToken({ skipCache: true })`** (`breadcrumb-nav.tsx` `handleOrgSwitch`) before `router.push`. `await setActive` alone is NOT enough: it resolves on the CLIENT before its `Set-Cookie` propagates, so the very next navigation reaches `proxy.ts` `organizationSyncOptions` carrying the STALE token (active = previous org / not-a-member of the target). Clerk bounces the URL back → `OrgActivator` re-syncs the client to the previous org → **the switch reverts on its own** (loudest in STAFF god-mode, where the target is a just-joined customer org whose membership the stale JWT lacks). The `getToken({ skipCache: true })` re-mint forces the fresh token (new active org + new membership) into the cookie BEFORE the middleware reads it — same proven fix as `beta-onboarding`'s onboarding-complete hop (CLAUDE.md "Stale token — force `getToken({ skipCache: true })` before navigating"). Guarded by `org-switch-cache.test.ts` (getToken must sit between setActive and router.push). (#1940)
   - **That correct ordering is also SLOW and entirely INVISIBLE, so the click has to be answered locally — a switch that shows nothing reads as a dead button, and reverting the ordering to "feel faster" is the one thing that must never happen.** Two or three Clerk round-trips run before `router.push`, and every label on screen is keyed on the URL org, which has not moved yet. Reported as *"je change d'org depuis le Org selector, il ne se passe rien, des fois je dois attendre 30s"*. Four invariants in `use-tenant-switcher.ts` + `tenant-switcher.tsx`, guarded by `tests/org-switch-instant.test.ts`: (a) `setSwitchingOrg` is set SYNCHRONOUSLY, before the first `await` — set after, the spinner appears at the moment it stops being needed, so the guard asserts the index, not the presence; it carries the NAME because a god-mode target is routinely in none of the lists we hold; (b) the staff join round-trip is SKIPPED when Clerk's already-loaded client-side membership list has the membership — authoritative-POSITIVE only, since that list is paginated, so an absent membership still goes through the idempotent route (the paged-precheck trap the join route itself documents); (c) an org row does NOT go through `go()` — closing the menu first removes the only surface that can show the switch running, so the ARRIVAL closes it (`useEffect` on `t.orgId`); (d) the pending flag clears on ARRIVAL, not when the promise resolves — `router.push` returns long before the destination renders.
   - **The permanent-hang half was a swallowed error, and it is the reason the fail-loud rule applies to click handlers too.** A failed join did `console.error` and CONTINUED into `setActive`, which REJECTS for a non-member org; that rejection was unhandled inside a click handler, so `router.push` never ran, nothing was rendered, and the switcher stayed dead until a reload. A non-ok join now throws, the whole handler is wrapped, and the refusal is stated in the menu. `getToken({ skipCache: true })` likewise lost its `.catch(() => {})` — a failed re-mint IS the self-reverting switch of #1940, so it belongs where the user is told, never swallowed.
4. **Read gate** (`src/lib/use-auth-query.ts`): `enabled: caller && urlOrg === activeOrg`. **LATCHED against Clerk's transient `organization: null` blink** (JWT rotation ~1/min, tab focus): the gate compares `urlOrgId` to `effectiveActiveOrgId = activeOrgId ?? lastResolvedActiveOrgId.current` (a `useRef` that only ever stores a NON-null active org). Without the latch a null blink flips the gate false → v5 disabled query → `isPending: true` FOREVER → any `isPending`-gated skeleton (leads page) hangs, and once the IndexedDB cache is evicted (see persist section) it hangs PERMANENTLY = the infinite-skeleton report. The latch fills ONLY a null — a genuinely DIFFERENT non-null active org (real switch / cross-tab) STILL closes the gate, so a null-from-cold never fires a read and DIS-143 cross-org isolation is intact (never fire a URL-org read under another org's token → proxy 409). NO `|| !activeOrgId` escape (a read under a truly-null active org lands in a null-org cache entry, TanStack #3743). `useOrganization` import allowed here (powers the gate, not token-passing). Guarded by `org-switch-cache.test.ts`. (KevinLourd/leads-skeleton-persist-orggate)
5. **MULTI-TAB — scope every request by the PER-TAB token, NEVER the shared cookie; never navigate a tab on the shared active-org signal.** Clerk supports a per-tab active org, BUT **the session cookie is one global singleton for the whole browser** — it reflects whichever tab was focused last ([Clerk: Organizations → "multiple browser tabs"](https://clerk.com/docs/guides/organizations/overview)). So scoping data off the cookie (`auth()` with no Bearer) makes a background poll / navigation from a NON-focused tab run under the WRONG org → cross-org bleed + 409 churn; and any code that NAVIGATES a tab when the (shared) `useOrganization()` active-org changes yanks tab A onto tab B's org the instant tab B switches — the "org switches by itself across tabs" bug. Two-part fix (#1944), per Clerk's own multi-tab guidance + WorkOS/SuperTokens "the token IS the tenant context":
   - **Data path:** `api.ts` `apiCall` attaches THIS tab's token — `Authorization: Bearer ${await window.Clerk.session.getToken()}` (via `getTabSessionToken()`, a non-hook read of the per-tab `window.Clerk` client) — on every `/api/v1/*` proxy call. Clerk's `auth()` honors a Bearer over the cookie ([Clerk: making authenticated requests](https://clerk.com/docs/guides/development/making-requests)), so the proxy scopes off the per-tab org. Optional-chained → before Clerk loads / signed-out it falls back to the cookie, and `checkProxyOrg` (item 1) still fails closed, so there's NO new exposure even if a Bearer is ever absent.
   - **Navigation path:** nothing navigates on an active-org CHANGE (#1944 stripped `router.push`/`useRouter`/`usePathname` from `OrgCacheInvalidator`, which has since been deleted entirely). The URL `/orgs/[id]` is the per-tab source of truth; in-tab switches navigate via the switcher (`handleOrgSwitch`), URL→active alignment via `OrgActivator`. Do NOT re-add a navigate-on-active-org-change effect — it re-introduces the cross-tab self-switch.
   - **Display / cache-key path (#1948 — the one that actually stops the VISIBLE oscillation):** #1944's data+nav fixes were necessary but NOT sufficient — the org name kept flipping between tabs because the CLIENT UI still keyed on `useOrganization()` (the shared active org that flips cross-tab). Every per-tab client surface must key on the **URL org** (`/orgs/[id]`), never the active org: (a) `query-provider.tsx` remount key = `urlOrgId ?? organization?.id` (URL is per-tab-stable → no remount storm when a sibling tab switches; falls back to active org only OFF the /orgs/ tree, e.g. onboarding); (b) the tenant surfaces render the URL org via a per-orgId snapshot (the `["orgIdentity", orgId]` query, written only when `organization.id === orgId`) so the name/avatar never flip; dropdown highlight compares `orgId === o.id`, not the active org. RULE: in a multi-tab org app, treat the URL as the per-tab source of truth for ALL of {data scope, cache key, displayed org, nav}; `useOrganization()` is shared/global and may only feed (i) the cross-tab-stable parts via a per-URL-org cache, or (ii) off-/orgs/ fallbacks. Don't bind a per-tab surface directly to `useOrganization()`.
   - Guarded by `org-switch-cache.test.ts` (per-tab Bearer guard + no-cross-tab-navigation guard + URL-keyed remount guard) + `breadcrumb-hierarchy`/`feature-overview-polish`/`last-brand` (URL-org display). Note the cookie-era item 3 (`getToken({skipCache})` after `setActive`) is STILL correct for the focused-tab navigation; item 5 makes the DATA + DISPLAY paths immune to the cookie regardless of focus.
   - **BRAND crumb — resolve the label by a per-URL-id FETCH (`getBrand(brandId)` → `GET /api/v1/brands/:brandId`), NOT by `find()` in the dropdown LIST.** The brand crumb long resolved only via `brands.find(b => b.id === brandId)` over a module-level, non-org-keyed, 60s-cached `/api/v1/brands` list (`brandListCache`) that cleared only in `handleOrgSwitch`. Any nav skipping that path (god-mode deep-link, cross-tab, direct URL) served a DIFFERENT org's list → `find` missed → the crumb stuck on the `"Brand"` placeholder PERMANENTLY. #2364's `brandDisplayCacheRef` keep-last-good could NOT save it — the brand never resolved once, so there was no first value to keep (keep-last-good fixes flicker, not a never-resolve). Fix (#2416): a by-id read on `brandId` (same source the overview page uses) feeds `displayBrand` when the list misses, resolving under the same active org the page already renders the brand with. RULE (same as the org crumb): a per-URL-entity crumb LABEL must come from a by-id read of that entity, never from membership in an org-scoped list that may be cached under the wrong org. (#2416, follow-up to #2364. The org-keyed `brandListCache` that #2416 also introduced is gone — both reads are React Query now, see the SWR item below.)

6. **A flow that CREATES its org cannot live under an org-keyed provider — `/onboarding` opts out via `<QueryProvider scope="onboarding">`.** The keyed remount in item 2 is a cache reset in the dashboard, where the org changes only when the user switches to one that already exists. Onboarding is the opposite case: `?new=1` calls Clerk `createOrganization` + `setActive` INSIDE `createBrandAndFetchServices`, so `useOrganization()` flips to a brand-new id in the middle of the loading screen. Under an org-derived key that remounts `OrgScopedQueryClientProvider` and therefore the whole `<Onboarding/>` subtree (the onboarding layout mounts the provider ABOVE it) — every `useState` resets, `resolveResumeStep("loading", null)` sends the restored snapshot back to `"url"`, and the user lands on the step BEFORE the one they were on. The `createBrandAndFetchServices` promise keeps running in the detached closure, so the brand IS created in the new org, invisibly; prod ended up with one `claude.ai` brand row linked to three orgs from three attempts, and the org-desync warnings in the Vercel logs named a different new org each time. `?from=add` (new brand, existing org) never hit it — `reuseOrg` is true there, so nothing flips. Two things the fix must keep: the scope is a PROP passed by the layout, never a `usePathname` check (the pathname updates while the terminal `router.push("/orgs/<new>/…")` is in flight, which would remount the wizard one last time on its way out); and it passes `orgId={null}`, not the stale previous org — the persister is org-prefixed, so holding the old id would file the new org's queries under the old org's disk keys, which is the exact bleed the prefix prevents. Onboarding therefore runs on an in-memory cache, which costs nothing (transient route; first-frame tenant identity comes from the server-read cookie). Isolation is unchanged: the terminal push leaves this subtree for the dashboard's own provider, which keys on the new org. Guard: `tests/onboarding-org-remount.test.ts`.

7. **Every leg of a switch is BOUNDED, and a retry that re-sends a REFUSED credential is not a retry.** Both halves are what "switching orgs sometimes takes infinite time" turned out to be, and neither is visible while the network is healthy.
   - **Nothing in the chain gives up on its own.** `fetch` has no default timeout and neither does Clerk's `setActive` / `getToken`, so on a flapping connection (a phone changing network, DNS failing on `clerk.distribute.you`) any leg simply never settles: the click spins forever, no error, no retry, and only a reload gets out. `handleOrgSwitch` bounds each leg with `withTimeout` (`lib/with-timeout.ts`, alias-free so it carries real unit tests) at `ORG_SWITCH_TIMEOUT_MS`, plus `AbortSignal.timeout` on the join `fetch`, and states the failure. **A timeout is NOT a silent fallback** — it converts an unbounded wait into a loud one. It is also NOT a refusal: `isTimeoutError` keeps the join-and-retry recovery from firing on a dead network, where a second round-trip only doubles the wait.
   - **A membership PRE-CHECK against `userMemberships.data` reads Clerk's FIRST PAGE only** — the same trap the god-mode join route already documents, in a second place. A staff member of many orgs therefore reads as "not a member" for nearly every target and pays a full Clerk Backend join before anything can move. `setActive` REJECTING is the membership answer, server-side and authoritative: ask Clerk first, join only on its refusal. Generalise: **never gate an action on a paginated client-side list; let the authoritative call refuse and treat the refusal as the answer.**
   - **The 409 retry must MINT A FRESH TOKEN.** `getToken()` is cached, and the cached token carries the org claim the proxy just refused — so a retry that re-sends it is refused again, deterministically. The retry that existed was therefore a no-op for the one case it was written for. `getTabSessionToken(forceRefresh)` passes `skipCache`, and the loop runs a real backoff (`ORG_DESYNC_BACKOFF_MS`, 400/900/2000ms) before letting the 409 surface — a desync outliving ~3s is not rotation lag, it is Clerk being unreachable, and every surface fails visibly rather than skeletoning forever. Tell for this class fleet-wide: **a retry that re-sends a cached credential a server just rejected can only be rejected again.**
   - **`UserResolver` takes the same `useOrgQueryGate()` every org-owned read takes.** It fired on Clerk's ACTIVE org while the URL was still on the previous one, so it 409'd through every switch and swallowed it — the loudest repeated 409 in the console during a switch, and pure noise on top of the real one. Any NEW effect that calls the API off `useOrganization()` needs the same gate.
   - Guards: `tests/org-switch-recovery.test.ts` + `tests/with-timeout.test.ts`. ⚠️ Four assertions across `org-switch-cache` / `org-switch-instant` / `tenant-switcher-swr` pin the handler's exact literals (`await setActive(...)`, `await session?.getToken(...)`), so any reshape of this handler turns them red — update the spelling, the invariants are unchanged. (#3600)
   - **An error message that NAMES a party is a claim about who failed, so check it against what actually failed — and `navigator.onLine` is trustworthy in ONE direction only.** The bound above turned a hang into a loud failure and the sentence it carried said *"Couldn't reach the auth service"* for every timeout; the case a timeout fires on most is a browser with no network, and the report that surfaced it had EVERY request on the page failing `net::ERR_INTERNET_DISCONNECTED`, PostHog's own CDN included, so nothing about the auth service was in question. `orgSwitchErrorMessage` (`lib/org-switch-error.ts`, alias-free so it carries real unit tests) reads `navigator.onLine === false` as decisive (no network interface exists) and NEVER reads `true` as evidence the network is fine (an interface exists; reachability is a different question) — the guard pins `navigator.onLine !== false` for exactly that reason. A refusal still passes through verbatim, since a failed staff join states its own status. Generalise the DIAGNOSTIC half too: **when a pasted console shows a third-party CDN failing alongside our own routes, the browser lost the network and nothing in the log is about us** — read the failure set before reading any one line of it.
   - **A tab running a bundle the server has replaced fails SILENTLY, and `StaleBuildNotice` (`components/stale-build-notice.tsx`, matcher in the alias-free `lib/stale-build.ts`) is the ONE thing that says so — do not add a second, and do not widen its match.** The box redeploys within ~5 minutes of a merge, so a tab left open across one keeps JS referencing Server Action ids, route payloads and chunks that no longer exist. Every one of those arrives as an **unhandled promise rejection**, which NO React error boundary sees (`(dashboard)/error.tsx` included), so the page stops working with the reason only in the console: a control is clicked and nothing happens. The tell in a report is `Server Action "<hash>" was not found on the server` beside a 404 on the route being opened — that is a stale bundle, never a routing bug, and it is unrelated to whatever else is in the same log. Four things are load-bearing: the match is a NARROW closed set (`UnrecognizedActionError`, `ChunkLoadError`, a failed dynamic import) because a broad "any error means reload" is a lie that costs people whatever they had typed; it listens on BOTH `unhandledrejection` and `error`, since the two failures arrive on different events; it is a NOTICE and never an automatic reload (an error-triggered reload can loop, and it discards in-flight state — the onboarding wizard, an investor update draft, a chat thread); and it is mounted in `(authed)/layout.tsx` so it covers onboarding too. Guards: `tests/stale-build-notice.test.ts` + `tests/org-switch-error-message.test.ts`. (#3628)

**Self-heal:** on 409 `org_desync`, `apiCall` re-mints the token and retries on a 400/900/2000ms backoff (item 7). **Rule:** JWT authority server-side every request fail-closed; cache resets by remount not `clear()`; remount-surviving nav lives above `QueryProvider`. (DIS-143)

### Dashboard content width — content-driven, never cap-without-centering

No global width knob: the authed shell `<main>` is full-width; each page sets its OWN `max-w-*` wrapper. A cap WITHOUT `mx-auto` left-hugs with an asymmetric right gutter (the bug — brand overview was `max-w-4xl` = 896px, looked "stops short / weird"). Industry consensus (Atlassian fluid/fixed grid · Material body-region · Baymard/WCAG line-length) = width is **content-driven**:
- **Dense pages** (dashboards, card grids, lists, tables) → `p-4 md:p-8 max-w-7xl mx-auto` (fluid up to ~1280px ceiling, then centered). Tailwind v4 here → `max-w-7xl` is the largest named (`max-w-screen-2xl` was REMOVED in v4; use `max-w-7xl` or arbitrary `max-w-[96rem]`).
- **Reading / forms** → keep narrow (`max-w-2xl`/`max-w-3xl` ≈ 65–80ch) for line length — widening a form to 1280px hurts readability.
- ALWAYS `mx-auto` when capped (Material fluid-margins) so there's no asymmetric gutter.

Applied to the dense surfaces (brand overview, org overview, brands list, brand-info). Forms (`brands/[brandId]/launch`, settings, billing, api-keys) stay narrow. (#1300-follow-up)

### Dashboard mobile — tables scroll, never crush; breadcrumb is mobile-visible

- **A dense `<table>` MUST live in an `overflow-x-auto` wrapper with a `min-w-[Npx]` on the table** — NOT the copy-pasted `<div className="bg-white rounded-xl border border-gray-200 overflow-hidden">` wrapper, which makes columns squeeze/wrap into mush on a phone instead of scrolling. The `overflow-hidden`+`w-full` pattern was duplicated across several surfaces (brand quote-requests, brand leads, visibility prompts/competitors/runs/run-detail; the campaign-level quote-requests/leads variants were removed in #1768) — when adding a new table, start from the `overflow-x-auto` + `min-w` form. (#1746)
- **BUT the `min-w` floor must be BREAKPOINT-GATED (`md:min-w-[720px]`), never unconditional — an unconditional floor OVERRIDES every `hidden sm:table-cell` you added and the phone scrolls anyway.** The two halves fight each other: hiding columns narrows the row, then a bare `min-w-[720px]` re-widens it past the viewport, so the visible columns are pushed off-screen and the reader concludes the data is missing rather than off to the right. The leads table shipped that way for months — Status sat at `hidden sm:table-cell` AND behind a 720px floor, so a phone showed neither the tag nor a reason. Gate the floor at the breakpoint where the folded columns come BACK (`hidden md:table-cell` columns ⟹ `md:min-w-[…]`), and state the mobile column set explicitly.
- **ADDING a column that needs more room than the floor allows: move THAT COLUMN's breakpoint, never raise the floor.** The floor is shared by every width at or above it, so widening it to fit one new column makes every mid-width viewport scroll sideways for a column most of them are not even showing — the fix costs a regression to widths that were fine. A column is a local decision and the floor is a global one. Measured 2026-09-04 (#3896, the Close won column): it holds a two-input form rather than a value, so at `md` its cell is ~120px and the form wraps onto four lines; bumping the floor 720 -> 840 to fit it made an 820px tablet want 840px inside 754. Gating the column at `lg` instead (one breakpoint later than its neighbours) left the floor untouched, so no width that fit before scrolls now, and the form sits on one line from 1280. The surface below the new breakpoint keeps whatever already stated the same thing — here the lead panel, with the same prefill.
- **A column carrying free text needs `table-fixed` + an explicit width to truncate — `truncate` ALONE does nothing in a table.** The default `table-auto` sizes a column to its content, so `truncate` has no bounded width to work against and one long value (an audience name, a company name) widens the whole row: measured 649px for a single cell in a 360px viewport, i.e. the row overflowed even with only two columns left. Fix is `table-fixed md:table-auto` plus a percentage width per mobile column (`w-[62%] md:w-auto` / `w-[38%] md:w-auto`), and `min-w-0` on the flex wrapper inside the cell. Reference: the leads table + `AudienceCell` in `components/audiences/engaged-leads-page.tsx`.
- **A column FOLDED into a neighbour on mobile reads its value from ONE binding, and the two placements are strict complements.** The leads table folds Contact / Audience / Date away below `md`: the first cell shows a 40px company mark beside the company name with the audience under it, and the date sits under the status tag, each `md:hidden`, while the columns themselves stay `hidden md:table-cell` — so a value is on screen exactly once at every width. The date is computed once into a `dateNode` const rendered by both the stacked line and the column; recomputing it per placement is how two spots start disagreeing. A row whose folded value is absent (a lead attributed to no audience) simply has no second line — a dash there would read as a value. **The company leads that cell, not the audience** (owner-picked, and it also keeps the row identifiable: the audience repeats across rows while the company does not). `CompanyLogo` takes a `size` prop applied as a STYLE, because a Tailwind class built from a prop is invisible to the compiler — the same reason `AudienceAvatar` does it that way; it requests `size * 2` from logo.dev for retina and carries `shrink-0` since the sibling text truncates.
- **Verify a table fits by MEASURING the render, never by reading the classes** — a source guard proves the class is spelled right and says nothing about whether the content fits. Playwright at 360px and 412px, asserting `wrapper.scrollWidth <= wrapper.clientWidth` plus `getComputedStyle(cell).display` per folded column, with the OLD markup on the same page to prove the fix. Both of this change's real defects (the unconditional floor, then the un-truncatable free text) were invisible to green source guards and to `tsc`. (leads-table mobile fit)
- **The header breadcrumb (`components/header.tsx` → `BreadcrumbNav`) is visible at ALL widths** — do NOT re-add `hidden sm:block` (it left mobile users with only the distribute logo: no org/brand name, no org switcher). The `<nav>` in `breadcrumb-nav.tsx` scrolls horizontally on narrow screens (`overflow-x-auto`) and flips to `overflow-visible` while a dropdown is open so the absolutely-positioned switcher panel isn't clipped by the scroll container. (#1746)
- **A page-by-page mobile/load AUDIT by subagents over-reports** — it invented a `min-w-[500px]` and a 7-col table in a page that actually delegates to a shared component. Confirm each flagged defect against the EXACT served string (grep the literal) before sweeping; the "split-panel `absolute inset-0` detail overlay" is the intended mobile drawer, NOT a bug. (#1746)

### Dashboard chrome = L-shaped shell + sidebar-top tenant switcher (GA, single path)

**The header carries NO product mark, NO `distribute.you` wordmark and NO breadcrumb — that is deliberate, not an oversight; do not "restore" them.** The shell is L-SHAPED: the sidebar is a FULL-HEIGHT column to the LEFT of the header column (`layout.tsx`), so the sidebar's top block sits level with the header row. Tenant identity + switching live in `components/tenant-switcher.tsx` `<TenantSwitcher>`, passed to `SidebarSection` via the `topSlot` prop; the top bar keeps only universal actions (theme, account), per Atlassian. There is exactly ONE chrome — the pre-switcher path was deleted at GA, not gated off, and `tenant-switcher.test.ts` fails if `useIsBetaUser` reappears in `context-sidebar.tsx` or the dashboard `layout.tsx` (the header may still read it, but ONLY for the unrelated beta Profile entry — never to branch the chrome).

**The two rows sit SIDE BY SIDE, so their height must match by CONSTRUCTION — one shared token, never a hand-measured pixel value, and the border never on the sized element.** Both the header's inner row and the switcher button read `CHROME_ROW_HEIGHT` from `src/lib/chrome-row.ts` (`h-14`). Two traps, both hit on the first attempt: (1) the header's height is NOT its `py-*` plus the obvious icon — it's `py-2.5` around whatever control is tallest, and that's the account button (`p-1` + a 28px avatar = 36px), so it renders ~57px, not the 49px a quick read suggests; hardcoding the miscount left a visible seam. (2) Tailwind is `box-sizing: border-box`, so a `border-b` ON the `h-14` element eats into the 56px while the header's `border-b` sits on `<header>` OUTSIDE its `h-14` inner row — a 1px mismatch. The switcher therefore puts its border on the WRAPPER (`className="relative border-b border-gray-200"`) and keeps the button pure `h-14`. Guard: `tenant-switcher.test.ts` pins both to the token and forbids a literal `h-[49px]`.

**ONE implementation of tenant identity + switching: `src/lib/use-tenant-switcher.ts` (`useTenantSwitcher`).** Both `breadcrumb-nav.tsx` and `tenant-switcher.tsx` consume it — org/brand path parsing, the three identity reads (`["orgIdentity", orgId]`, `["brands"]`, `["brand", brandId]`), the staff god-mode all-orgs list, and `handleOrgSwitch` (join → `await setActive` → `getToken({skipCache:true})` → `router.push`, the DIS-143 race close). `OrgAvatar` is likewise a shared component (`components/org-avatar.tsx`). Do NOT re-inline any of it into a surface — the guards for the org-switch race now read the HOOK file (`org-switch-cache.test.ts`, `clerk-org-sync.test.ts`, `breadcrumb-hierarchy.test.ts`), not `breadcrumb-nav.tsx`. **`breadcrumb-nav.tsx` is NOT dead code and must not be deleted**: the dashboard no longer mounts it, but `onboarding-top-chrome.tsx` still does (guarded by `onboarding-escape-chrome.test.ts`) — a `grep` for `<BreadcrumbNav` that only covers the dashboard tree will wrongly read as zero consumers.

**Every identity read in the switcher goes through React Query, so the org name / brand name / brand logo paint from disk on the FIRST frame — a raw `fetch` or a module-level cache here is a UX bug, not an optimization.** The switcher was for months the ONE dashboard surface bypassing the local-first per-query IndexedDB persister: `/api/v1/brands` + `/api/v1/brands/:id` via raw `fetch` behind module 60s caches, and the org label in a `useRef`. None of those survive a page load, so every hard navigation (org page, brand page, billing) showed `Dashboard` / `Brand` and an empty logo slot for as long as the cold gateway → brand-service chain took — reported as "le logo et le nom passent quelques secondes en placeholder". The three reads are now `["orgIdentity", orgId]` (a snapshot of Clerk's org, written only when `organization.id === orgId` — Clerk is the ONLY source of an org name and it hydrates async, so without the disk snapshot the label cannot beat Clerk), `["brands"]` → `listBrands`, and `["brand", brandId]` → `getBrand`. Load-bearing details: the latter two are the SAME keys the org-overview / billing / brand pages already use, so they DEDUPE instead of doubling; all three roots must stay in `PERSISTABLE_QUERY_ROOTS` (an unlisted root is default-OFF and silently loses the instant paint); the list's loading flag is reveal-on-settle (`isPending && !isError`) so a failed read falls to the empty state rather than an eternal "Loading…"; and going through the shared api client means each request carries THIS tab's Clerk token instead of the shared session cookie (the raw `fetch` could resolve a label under another tab's org). Guarded by `tenant-switcher-swr.test.ts`. (brand-crumb-swr-placeholder)

**That fix was necessary and NOT sufficient: no client-side cache can paint the first frame, because IndexedDB is ASYNCHRONOUS. The identity therefore ALSO lives in a server-readable COOKIE, and both layers must stay.** The per-query persister restores in a `useEffect` (`persister.restoreQueries` / `reseedColdQueriesFromDisk`) — strictly after paint — so a hard refresh was *architecturally guaranteed* to render `Brand` + the globe first and swap the real values in a moment later. "Cache harder" cannot fix an ordering problem: the browser has already painted the server's HTML before any client store is reachable. The repo's own doctrine already said so under the first-run/auth section ("remembered state goes in a **server-readable cookie**, NOT localStorage — client-only means a round-trip and a flash"), and localStorage is no better than IndexedDB here for the same reason: even a synchronous read happens at *hydration*, long after the placeholder HTML is on screen.
- `src/lib/tenant-identity-cookie.ts` — the pure blob (`distribute-tenant`, version-tagged, LRU-capped at 4 orgs / 8 brands, single-letter keys). It rides EVERY request to the origin including each `/api/v1/*` proxy call, so the caps are load-bearing; a size guard pins it under 4KB. Deliberately alias-free so vitest can run it (`tenant-identity-cookie.test.ts` is real unit tests, not source-substring) — do NOT add an `@/…` import.
- `(authed)/layout.tsx` reads it with `cookies()` and passes it to `TenantIdentityProvider`, so the real name + logo are in the SSR HTML and the client hydrates against the identical value (no mismatch). Safe there specifically: `ClerkProvider dynamic` already opted that subtree out of static rendering, `/report/*` sits OUTSIDE it (the ISR split is untouched), and the `loading.tsx` boundaries live under `(dashboard)`, well below — the CLAUDE.md ban on `cookies()` in a layout targets the INTERMEDIATE `[orgId]`/`[brandId]` layouts, which stay empty passthroughs.
- Deliberately NOT httpOnly: the client is what learns the identity (Clerk for the org name, brand-service for the brand) and therefore what writes it back, via `remember` in `use-tenant-switcher.ts`. Contents are display labels the user is already looking at — no token, no id the URL doesn't carry.
- **Precedence is live Clerk → React Query → cookie seed, and the seed must stay LAST.** Ordering it earlier pins a renamed brand to its old name. Guarded by an index-comparison assertion, not a substring.
- **An UNKNOWN tenant renders a skeleton, never `Brand` + a globe.** A fabricated identity reads as the product having lost the brand — which is exactly the reported complaint — so `orgKnown` / `brandKnown` gate both switcher surfaces on the tenant the URL is on (a brand page must not fall back to the org's name). With the cookie in place this only appears on a first-ever visit to a tenant on that browser.
- The write-back effect depends on PRIMITIVES (`rememberedBrandName`/`rememberedBrandDomain`), never on the `displayBrand` object — that object is rebuilt every render, and `mergeTenantIdentity` returning the SAME reference on a no-op is what stops a poll re-writing the cookie each tick.
Guards: `tenant-identity-cookie.test.ts` + the "first-frame identity" block in `tenant-switcher-swr.test.ts`. (tenant-identity-sync-paint)

**The switcher panel is the whole IA, and it is capped at TWO tiers by design** (Carbon: "the left panel does not support three tiers"; GitLab Pajamas: "there is not a third level of depth"). Org row → brand row (drawn as its child via a **1px** connector rail + a disclosure chevron — indent alone is not hierarchy, and a colored side-border accent >1px is banned repo-wide), then org-scoped **Billing**. Only ONE submenu is ever open (`useState<"org" | "brand" | null>`), which is what makes a third tier structurally impossible. "New organization" / "New brand" sit at the bottom of their section and route into the REAL `/onboarding?new=1&from=add` / `?from=add` flows (never a stripped create modal, #1985).

**Each submenu opens as a FLYOUT to the right of the parent panel on `md+`, and STACKS under its row below `md`** (`components/tenant-switcher.tsx` `<Submenu>` — unprefixed classes are the stacked mobile case, `md:absolute md:left-full md:top-0` is the flyout). Side-by-side is Atlassian's app-switcher shape and the one NN/g points to (it rates a nested two-tier dropdown "frustrating"); a flyout is impossible below `md` though, because the sidebar is 224px and becomes a drawer. When asserting "no bare `absolute`" in a guard, remember `\babsolute\b` MATCHES inside `md:absolute` (`:` is not a word character) — match on whitespace/quote boundaries instead.

**Mobile: `<MobileTenantChip>` in the header is a FULL switcher, not a drawer trigger.** It renders the same `<TenantMenu>` anchored under the header (viewport-bounded + scrollable). Routing it through the drawer would cost two taps and put the flyout inside a 224px panel. Consequence to keep in mind: the chrome mounts `useTenantSwitcher` up to THREE times at once (desktop sidebar + the drawer's sidebar + the chip), so a raw `fetch` added to that hook fires three times per navigation. Every read in it goes through React Query, which dedupes by key — keep it that way.

**There is NO org nav level.** Its only item was "Overview", which was just a brand picker, and the switcher is the brand picker now. `OrgLevelSidebar` renders the switcher plus, on a settings path only (`/billing`, `/api-keys`, `/provider-keys`, `/services/*`), an "Organization" group. **Billing KEEPS its URL `/orgs/[orgId]/billing`** — `billing-guard`, `credit-alerts` and onboarding all deep-link to it, so a move is pure link rot. Reached from the switcher, which is where every multi-tenant leader checked puts it (Linear "click your workspace name → Settings > Administration > Billing", Notion, Vercel, GitHub); **never** the avatar menu (that means "me", the tenant name means "this tenant") and **never** a primary sidebar nav item (zero of eight products do that). `/orgs/[orgId]/page.tsx` is untouched and still the last-brand resolver + empty-org state; it just renders switcher-only chrome.

**NO sidebar level carries a back link at its top — the switcher IS the way up, so a second one competes with it.** Every level used to draw one above the switcher (`< Brand` on the offer level, `< Campaigns` on the campaign level, `< Back to dashboard` on org settings), and the switcher already names where you are and moves you anywhere. The affordance is DELETED, not hidden: the `BackLink` component and the `backHref` / `backLabel` props on `SidebarSection` are gone, so a new level cannot re-add one by passing a prop. Consequence accepted: on Billing / API Key / CRM the way back is the switcher's brand row. `apps/admin` keeps its own copies — staff fork, deliberately not synced. Guard: `tests/context-sidebar.test.ts` fails if `backHref`, `backLabel`, `function BackLink` or `Back to dashboard` reappears in the file. (#3499)

Research behind the redesign (fetched, not recalled): NN/g rates breadcrumbs "not useful for sites 1-2 levels deep" — org › brand is exactly two, so the breadcrumb carried no orientation value; Atlassian and Vercel both migrated product nav OUT of the top bar into the sidebar for information density; a topbar product logo is weakly defended for a SINGLE-product app (Carbon mandates it for multi-product suites — Notion/Slack/Linear drop it). Guards: `tests/tenant-switcher.test.ts`.

### Dashboard sidebars are SHARED per nav level — one edit covers every page at that level

`src/components/context-sidebar.tsx` → `ContextSidebar` switches on the URL to ONE sidebar per level. No per-page sidebar. A sidebar change on "the X page" applies to EVERY page at that level — map all levels + surface the blast radius before scoping. (#1223)

**The campaign concept was removed from the dashboard UI (#1768 + follow-up) — one subscription per brand (1 campaign under the hood), single feature flattened into the brand. There is NO feature level and NO campaign level anymore; the launch funnel lives at `brands/[brandId]/launch`, everything else is brand-level.** Nav is now App → Org → Brand → Brand Settings only (plus the brand entity Database rows).

| Level | Component | File | Groups |
|---|---|---|---|
| App / Org | `AppLevelSidebar` / `OrgLevelSidebar` | `context-sidebar.tsx` | — |
| Brand | `BrandLevelSidebar` | `context-sidebar.tsx` | Overview · Customer Personas / Brand Profile (beta) · **Database** (entity rows Outlets/Journalists/Articles/Leads/Emails, kept FLAT) · Brand Settings |
| Brand Settings | `BrandSettingsLevelSidebar` | `context-sidebar.tsx` | Brand Settings · Brand Info (alpha) · Workflows (alpha) |

Section-header style `<h4 className="px-3 pb-1 text-xs font-semibold text-gray-400 uppercase tracking-wide">`.

- **(HISTORICAL — #1768 removed the campaign + feature sidebar levels; `campaign-sidebar.tsx`/`mcp-sidebar.tsx` are gone, the brand-level slug-gating idea below still applies via `BrandLevelSidebar`.)** A surface that "mirrors the campaign sidebar" used to enumerate BOTH Outcome entities AND Settings buttons — `feature.entities` alone undercounts; the old `CampaignSidebar` = `outcomesItems` (entities × registry) PLUS `settingsExtra` slide-over buttons (e.g. `Prompt` → `CampaignPromptPanel`, `Inputs` gated on `featureInputs`). (#1286)
- **A feature-SPECIFIC surface added to a SHARED sidebar must be slug-gated by default, NOT shown on every feature.** The (brand-level) sidebar renders for ALL brands/features, so a raw `topItems` entry appears everywhere. If the page only makes sense for some feature(s) (e.g. a revenue/conversions page that needs sales-economics), gate it on a shared slug registry (`isRevenueFeature(slug)` → `lib/revenue-feature.ts`), the same way the page + any embedded section gate. "We'll reuse the formula for other features later" ≠ "show the page on all features now" — scope to the slug(s) that actually work today; new features opt in by adding their slug. Don't default to all-features just because the sidebar is shared. (DIS-229)
- **(NOTE — #1768 removed the campaign + feature levels. The campaign-detail revenue page (`CampaignRevenueSection`, `campaigns/[id]/page.tsx`), the `TopCampaignsCard`/`CampaignBudgetCard`/`TopWorkflowsCard` cards, and the `campaigns/page.tsx` list are all GONE. The SURVIVING revenue surface is the brand Overview (`RevenueOverviewSection` + `RevenueCostSummary`, gated on `isRevenueFeature`). The per-campaign / per-workflow ROI machinery below is historical.)** Revenue surface render: the brand **Overview** (`RevenueOverviewSection`) gated on `isRevenueFeature`.

  **The Organizations / Leads conversion tabs are DELETED — do NOT rebuild them, and do not look for a per-lead table on the Overview.** `ConversionsTabs` + `OrgConversionsTable`/`LeadConversionsTable`/`EventConversionsTable` + `ConversionDetailPanel` + the shared `components/table-pagination.tsx` (whose only consumer was that table) all sat behind a `conversions` prop that BOTH callers of `RevenueOverviewSection` — the brand Overview and the campaign Overview — passed as `null`, so the branch was unreachable and the whole cluster rendered nowhere. Every guard test that pinned its internals was retired with it (`conversions-cluster-retired.test.ts` now asserts the files are gone and the prop is absent from both call sites). The live per-lead surface is `components/audiences/engaged-leads-page.tsx` at `brands/[brandId]/audiences/leads`. Two consequences: (a) a "the Overview table shows X" report is about the LEADS page, not the Overview — check which surface actually renders before diagnosing; (b) `RevenueOverview.organizations` / `.events` are still parsed off the `/revenue` wire (the schema guard in `conversion-org-logos.test.ts` keeps `orgDomain` honest) but nothing reads them — a follow-up may drop them from `revenue-view.ts` + `revenue-parse.ts`. `getFeatureRevenue(slug, brandId, campaignId)` is still campaign-scopable on the wire (pass `campaignId`), but no UI renders a campaign-scoped view anymore. **Funnel-chart steps are backend-owned** (features-service `seed/features.ts`, re-registered on boot) — to add a step (e.g. the Clicks / website-visit step `recipientsClicked`, already a served stat) edit the seed there, never client-inject. (DIS-229)
  - **(HISTORICAL — #1768) The campaign-detail/top-card `RevenueCostSummary` `bottomCard` overrides (`TopCampaignsCard`, `CampaignBudgetCard`, `TopWorkflowsCard`) were removed with the campaign + feature levels.** The Overview consumer remains; grep `RevenueCostSummary` usages before assuming any `bottomCard` override renders. (#1376, #1388)
  - **(HISTORICAL — #1768) Canonical per-campaign ROI reader was `getFeatureRevenueByCampaign(slug, brandId)`** → `GET /v1/features/{slug}/revenue?brandId=&groupBy=campaignId`, used to rank campaigns by ROI for the (now-removed) `TopCampaignsCard`. No surviving UI ranks/lists campaigns by ROI (one subscription per brand). (#1376)
  - **Per-WORKFLOW ROI is PROJECTED only** (no realized per-workflow revenue stat exists): `getWorkflowProjection({featureSlug, brandId, objective, budgetUsd})` → `GET /v1/features/{slug}/workflow-projection`. ROI = revenue ÷ budget = `100 / projection.cacPct`, **budget-invariant** (pass `budgetUsd:1` only to populate `projection`). `objective` is deprecated/objective-agnostic backend-side (still in the dashboard helper sig; pass `meeting-booked`, echoed + ignored). `getWorkflowProjection` survives (the launch page + beta onboarding consume it for the recommended budget + per-outcome counts); the `TopWorkflowsCard` that ranked workflows by ROI was removed in #1768. (#1388)
- **Deleting a page route ⇒ delete its nav scaffolding in the SAME PR** or the link 404s (NOT caught by `error.tsx` — a missing route ≠ a render throw). `context-sidebar.tsx` wires each level in THREE places: the `SidebarItem` `href`, the `NavigationLevel` union member + its `getNavigationLevel` branch, the `XLevelSidebar` + its `switch` case. Invisible to `tsc` + source-substring tests. (#1226)
- **A backend-declared feature entity needs a brand-level route page** — built from the registry `pathSuffix` (`feature.entities` × `entityRegistry[name].pathSuffix`, features-service `stats-registry.ts`). Declaring an entity emits a sidebar link to a maybe-missing route → 404 (registry is backend-owned → blind to `tsc`/tests). The `quote-pitches/quote-requests/visibility-runs/prompts/competitors` routes were **DELETED in #2196** — they had no nav entry / no link (orphaned after the #1768 campaign+feature flatten), so they shipped unreachable; their components, `api.ts` readers (`listAll{QuotePitches,RankedOpportunities}`, `listVisibilityRuns`, …), libs (`quote-pitch-*`, `visibility-detail`, `use-latest-visibility-run`), persist roots and tests went with them. **The whole brand-level Database entity-page cluster `leads/emails/outlets/journalists/articles` was ALSO orphaned by the #1768 sidebar flatten (Database section removed → 0 nav links, URL-only dead code) and DELETED in #2593** along with its coupled source-substring tests (`email-signature`, `email-recipient-display`, `outlet-*`, `journalist-*`) + trims to the mixed ones (`run-error-display`, `dashboard-mobile-responsive`, `brand-tools`). The live **Leads** surface is `brands/[brandId]/audiences/leads` (`EngagedLeadsPage`), NOT `brands/[brandId]/leads`. The orphaned `api.ts` readers (`listBrandEmails/listBrandOutlets/listJournalistsEnriched/listBrandArticles/listBrandLeads`) were left in place (harmless, `listBrandLeads` still used by `EngagedLeadsPage`). Do NOT reintroduce any of these routes without a nav entry. **still 404: `companies`** (blocked on a brand-scoped companies API) + **`press-kits`**. (#1276, DIS-104, #2593. NOTE — #1768 removed the feature + campaign levels; #2196 + #2593 then deleted every orphaned brand-level entity page.)
- **The Leads page ASKS lead-service for one page at a time — it does not hold a brand's lead population, and that is what makes it paint instantly.** It used to read every lead row and derive the tab counts, the search, the sort, the export and the board in the browser. Measured in production: **44,508,801 bytes over 12,945 rows** on one brand (`view=basic`, ~2 KB a row, 6.6 s), and **99 MB** on the largest of the **seven brands of 35** past the limit. `MAX_PERSISTED_ENTRY_BYTES` is 2 MB, so the entry was refused at write time, was never on disk, and the table cold-loaded on EVERY visit — reported as a permanent loading skeleton on a page customers open many times a day. It was never a caching bug: it was a payload that could not be cached.
  - **Two reads, both small.** `["leadBucketCounts", scopeKey, wireSearch]` → `GET /orgs/leads/bucket-counts` (every bucket's size, **163 bytes**, no lead rows at all), and `["leadsPage", scopeKey, activeTab, wireSearch, page]` → `GET /orgs/leads?bucket=&sort=activity&limit=&offset=` (**308 KB** for 50 rows with `include=campaigns`). Both are allowlisted in `PERSISTABLE_QUERY_ROOTS`; an unlisted root is default-OFF and reproduces the cold load exactly.
  - **`lib/leads-server-page.ts` is the ONE place that speaks the producer's vocabulary** — the tab→bucket map, what the search box may send, the page request, the two parsers. Alias-free (its only imports are zod and an erased type), so it carries REAL unit tests. Keep it that way.
  - **The tab→bucket map is many-to-one, and `meeting_attended` has NO tab.** The dashboard's `meetings` tab is the BOOKED meeting, matching `goal-steps`' `leadField: "meetingBooked"`; pointing it at the attended bucket would count a smaller population under a label the stat cards already price. The producer's catalogue being wider than this surface is not a gap.
  - **`/revenue` is still read, for the outcome DATE per row and for deciding which outcome tabs EXIST — never for MEMBERSHIP.** Membership has to be pageable, and a client-side join can only ever see the page in hand. lead-service buckets the five outcomes off its own attributed conversion ledger, which is the ledger features-service reads to price them, so the two agree by construction. ⚠️ Verify that when a brand's outcome counts diverge between a tab and its stat card — two sources for one number is exactly the self-contradictory-surface shape.
  - **The header count is the CONTACTED bucket, deliberately NOT `bucket-counts.total`.** That total is the whole scoped population INCLUDING the people who carry no evidence at all (12,945 against 12,192 contacted on the brand that surfaced this), and those can appear under no tab, so advertising them is the bug #3071 fixed. Contacted is the base tab by construction ("every lead we contacted is in it whatever the funnel"), so it is the union's floor. Residual, stated rather than hidden: a tracker could attribute an outcome to somebody never contacted, who would sit in an outcome tab and outside this number; that tab states its own count, and summing buckets — which are not exclusive — would overstate it instead.
  - **Nothing states a count off the rows in memory.** A tab whose size the producer has not stated renders NO number rather than `(0)`, the pager counts pages over `total`, and the search bar's "N of M" is the producer's match count. Counting the loaded rows would read "50 of 50" on every page of every search.
  - **The ORDER is `sort=activity`, the producer's** — newest first on the timestamp that proves each lead's most advanced status, which is what the Date column renders. It is a TOTAL order (ties broken on the row id), so a page can neither repeat a lead nor skip one. `sortByStatusDate` is DELETED: sorting one page among itself reads as if the whole tab were ordered.
  - **The SEARCH is the producer's too, over the whole matching population** — so a match on page 40 is findable, which the old local predicate never made it. Debounced onto the wire (`LEADS_SEARCH_DEBOUNCE_MS`, 300 ms) while the input itself stays instant, and refused LOCALLY when lead-service would 400 it (blank, over 200 characters, over 8 words) with the reason on screen — a 400 there empties the table.
  - **The pager sends `offset`, not `cursor`, and that is a UI constraint**: the table offers numbered pages and a reader clicking page 7 has no cursor for it. lead-service documents both over the same total order and warns that an offset walk shifts if a row leaves the filtered set mid-walk — acceptable for a numbered pager, and it says so.
  - **The BOARD reads ONE COUNT and ONE PAGE PER COLUMN — it does not share a bounded read with anything.** It used to take a single bounded page of the widest bucket (`LEAD_BOARD_CARD_CAP`, 200) and sort it into columns in the browser, on the reasoning that a partition cannot page the way a list can. The partition part is right and the conclusion was not: what could not page was a column drawn as a SLICE of somebody else's page. lead-service partitions by STANDING now (`GET /orgs/leads/standing-counts` + `?standing=` taking a comma-separated set, v0.72.0-v0.74.0), so each column is its own page with its own total, and the cap and the line apologising for it are both gone.
    - **A column's SIZE is the producer's counts ADDED over the standings that column holds** (`boardColumnTotals`). A standing is a partition, so the counts are disjoint and their sum is a count of the same kind at the grain this surface renders — a display lookup, not a computed metric. Sizing a column from the rows a page happened to return is the bug this replaced. `null` while unsettled: a column whose size we have not been told is not a column with nobody in it.
    - **Seven standings, five columns** (`STANDINGS_BY_COLUMN` in `lib/lead-board.ts`), so two columns hold two standings each — which is what the comma-separated `standing=` exists for: one bounded, ordered, walkable page with one `total`, never two lists stitched here. ⚠️ `not_contacted` is in NO column (there is nothing to show about a lead nobody wrote to), so the columns sum to the population MINUS it, never to `standing-counts.total`. A guard walks every standing through `STANDINGS_BY_COLUMN` and `leadBoardColumnFor` and requires them to agree — two tables for one statement is how a head and its cards come to disagree.
    - **GROWING a column asks for a WIDER page of itself** (`shown` rides the query key), never an offset: the column is a set somebody is working top-down, and a second request starting where the first stopped could skip or repeat a row that moved in between. How far each column is drawn lives on the PAGE, because it drives a fetch.
    - **A card is placed by the column it was READ from, not by re-deriving one from the row.** lead-service selected that row by standing, so calling `leadBoardColumnFor` again would be a second opinion over the answer that fetched it. The one thing that overrides it is a statement somebody just made, for the round trip it takes to land.
    - Counts come first and cost no rows, and they decide which columns are read at all: an empty column is not fetched once its size is known, while before they land every column is read in parallel rather than waiting a round trip to find out. `leadStandingCounts` is in `PERSISTABLE_QUERY_ROOTS`.

⚠️ **THE LESSON, and it is the one this file keeps re-learning from the other direction: a derivation that is CORRECT today becomes a lie the moment the read beneath it gains a BOUND — and nothing goes red.** The rules above are all about a PRODUCER changing shape under a reader. This is the mirror: the reader's own dependency gets narrowed by a sibling change, in the same repo, and every guard keeps passing because the guards pin how a value is COMPUTED and not what it is computed OVER. Here the stat row counted the board's rows (right, while the page held every lead), then #3867 bounded that read, and the row printed the CAP as the population: `Leads 200` and `Sales Interests 19 (9.5%)` directly under a heading correctly reading `2,052 leads`, beside a served `Website Visits 85`. Three populations on one screen, every number real, none of them agreeing, `tsc` green and 3,275 tests green. So **when you put a bound on a read, grep every consumer of that read and ask what each one was COUNTING** — a surface that states a population off a bounded array is stating the bound. The fix direction is always the same: read the producer's own count, which exists precisely because a number should not cost a population. (#3872, #3876)
  - **The EXPORT is lead-service's file, headings included (`?format=csv`), and `leads-csv.ts` is DELETED.** It was built here for one release, and only because the producer headed its columns with the API's own field names while the page a customer had just been looking at said `Contacted` and `Website visit`. lead-service heads them in the customer's words as of **v0.70.0**, so the second implementation had nothing left to buy — and the walk it needed carried a 25,000-row ceiling and several megabytes of transient traffic on a press. It goes through `apiCall` (`responseType: "text"`) so the download carries the same per-tab Clerk bearer and the same org-desync retry as every other read, rather than being a second auth path.
    - ⚠️ **The column WORDS are now guarded in lead-service, not here** — this repo cannot assert a file it does not produce, and a stale copy of the labels would be a guard over somebody else's decision. The three guards that pinned them say so rather than pretending.
    - ⚠️ **Two columns were LOST in the move and it is worth knowing which**: the producer's 44 columns are a superset in every other respect (they add `Offer`, `Standing`, `Standing signal`, `Opened`, `First opened at`) but drop `Global bounced` and `Global unsubscribed`, the cross-brand suppression flags. Stated rather than absorbed; filed as a follow-up.
  - **`brandLeads` no longer feeds the funnel-leg board either** (see that section). Nothing customer-facing reads a brand's whole lead population now; the reader survives in `api.ts` for features-service's grain and the staff console.
  - Guards: `tests/leads-server-page.test.ts` (real unit tests) + `tests/leads-server-paging.test.ts` (call sites). (#3861)

- **A lead that reads "not contacted" while the provider says it was sent is a BACKEND bug, never a display one to paper over.** Every tab is an ENGAGEMENT step, so a lead lead-service served with no delivery evidence belongs to no bucket and is unreachable from this page; the title used to count the raw list and therefore advertised rows the table could never show (reported: `Leads (6 leads)` above a 5-row Outreach tab whose stat card also read 5). What the title reads instead is owned by the paging section above — `coveredLeads` and `buildLeadsCsv` are both gone. **Do NOT "fix" a future count gap by widening a tab's predicate**: the reported case was lead-service serving a lead whose email was already attached to another lead row (global one-email-one-lead uniqueness rejected the attachment, the serve continued anyway), so `contacted` could never resolve; 103 such rows fleet-wide, fixed in `sales-lead-service#394`. Diagnostic path when a lead is missing from a tab: call the prod reader directly (`GET lead.distribute.you/orgs/leads?brandId=` with a valid-UUID `x-run-id`) and look at the row's `email` — an EMPTY email is the tell, because the delivery overlay is keyed on the lead's registered email (`lead_contact_methods`), NOT on the provider payload the lead row stores. (#3071, #3072)
- **The Leads page counts PEOPLE and says "Contacted"; the brand Overview counts email SEQUENCES sent and says "Outreach" — two grains that must never share a word.** `sequences.total` (features-service `/revenue`, undeduped by lead, so it tracks the spend charted beside it) against the count of lead rows whose `contacted` is true: on one prod brand, one afternoon, **9,915 against 7,895**. Both correct, and the pair read as one broken number for as long as both cards said "Outreach". The rename covers the base tab (`LEAD_TAB_LABEL.outreach`), the stat card above the table, and the CSV columns (`Contacted` / `First contacted at`) — a page whose export disagrees with its screen is the same bug one file over. The card label is a PROP (`outreachLabel`, defaulting to `"Outreach"`) on the SHARED `OutreachStatCards`, threaded through `OutreachStatCardsAuto`: a second copy of that component is how the two surfaces would drift again. The campaign Leads page renders the same component, so it says "Contacted" too — correctly, it counts people as well. ⚠️ The tab still holds rows badged **Queued** (`contacted` means handed to Instantly, not sent — see the status-label rule above); that gap is stated by the row's own badge, deliberately, rather than by weakening the tab's name. **A campaign-scoped count now answers for the campaign IDENTITY, so on a brand whose campaigns are one identity it EQUALS the brand's — a divergence there IS a bug.** A campaign as the customer knows it is not one stored row: campaign-service used to mint a new row on every workflow switch, so one campaign lives in storage as many (measured brand: 48 rows, 47 `stopped`, and 35 distinct `campaign_id`s over 20,039 `leads_campaigns` rows — all one identity). features-service already totalled the identity; lead-service answered for a single row, so the page showed `$5,745 / 2.7×` beside `5,081 leads` while the brand read 7,895. Fixed in `sales-lead-service#412` → v0.50.0 (identity resolved from campaign-service, delivery evidence read brand-wide then narrowed — passing the single id returns the older members with NO evidence, i.e. "never contacted", which drops them from the tabs and is the same bug inverted — deduped one row per person, loud fallback to the single row if resolution fails; `/stats` had the same gap). Verified in prod: both scopes return 8337 rows / 7895 contacted / 45 clicked / 29 replied. Guard: `tests/leads-contacted-label.test.ts`.

**A positive reply is called a SALES INTEREST everywhere a customer reads it — one vocabulary, and the funnel catalogue is what carries it.** The step in `SALES_FUNNELS` (`reply_meeting`) reads `Sales interest`, so the campaign's own name, the leg in the top bar (`campaignLegLabel`), the Sales Funnels settings card, the lead panel's funnel and the onboarding rate labels all move from ONE array — nothing restates the word. Everything else is a label: the stat cards (`Sales Interests` / `Cost per sales interest`), the Leads tab, the Audiences column and its cost column (which stopped being the acronym `CPPR`, so it reads like its siblings `Cost per signup` / `Cost per sale`), the activity-chart bar, the Strategy best-model card, and the daily digest's noun (`outcomeNounPlural`). ⚠️ **The literal email-reply event keeps the word `reply`, deliberately** — `Replied` on the lead timeline and in the CSV, the reply-KIND picker, and every wire name (`repliedPositive`, `positive_replies`, `cpprCents`, the `positive-replies` tab key) are facts about a message arriving, not about the metric, and flattening the two senses is the vocabulary-sweep trap this repo already records. `apps/admin` keeps its own staff vocabulary and is deliberately NOT synced, with ONE exception the CI gate forces: `admin/src/lib/lead-funnel-stages.ts` carries a copy of the funnel steps that `apps/dashboard/tests/admin-funnel-steps-parity.test.ts` pins byte-for-byte, so the catalogue rename has to land in both.

**The campaign Overview states TWO outreach counts, because a lead is contacted ONCE and outreached several times.** `Leads contacted` is `funnelSteps.contactedRecipients` (distinct people, and the base the funnel's first rung converts from, so the card and the share under it agree by construction); `Outreaches` is `sequences.total`, the undeduped volume that tracks the spend beside it. Both are already on the campaign's own `/revenue` read — no extra request. The second card only carries its explaining tooltip when the first is beside it (`contactedOverride != null`): alone, `Outreach` is the only figure on screen and needs no disambiguation, which is what keeps every other surface byte-identical. **The `X% of contacted` line under Sales Interests is SERVED** — `funnelSteps.steps[0].conversionFromPreviousPct`, read verbatim through the `ScoreCard` `subtitle` slot; dividing the two counts in the browser is the compute-a-stat-in-the-browser bug and would drift from features-service the moment either side changed scope. It is gated on that rung actually being the sales-interest one AND converting `from: "Contacted"`, because a rung deeper in the funnel states a share of the rung before it, which is a different sentence; null means "we could not measure this" and renders no line rather than a fabricated `0%`. `formatSharePct` keeps one decimal under 10% — `0.1%` and `0.9%` are different answers about a campaign and both round to `0%`. Guard: `tests/outreach-stat-cards.test.ts`, which pins the CALL SITE as well as the component.
- **The Leads page offers a BOARD beside its tabs, and the two are different statements about one population.** The tabs are nested subsets (a lead that replied is also in Outreach), so their counts overlap and cannot be added; they answer *how many cleared each bar*, one bucket each. The board is a PARTITION — one card per lead, in the furthest step of the funnel it reached — and answers *where is this lead*. Both are offered rather than one replacing the other: the table owns the search result count, the sort, the dates, the pagination and the CSV export, and a board has nowhere to put any of them.

- **A MOVE IS A STATEMENT, and lead-service refuses one carrying no cost** (`400 cost_required` — a meeting that was run and went nowhere still cost what it cost). So a drop cannot write: it opens the SAME `StageStatementForm` the lead panel uses, exported for this rather than copied, which asks the cost and — on the one step the producer prices — the value. A board that wrote on drop would meet a refusal the person never had a chance to answer.
- **Only some columns take a card.** lead-service accepts a statement on five of the seven steps (`WRITABLE_STAGE_KEYS`); a reply's kind is a fact about a message and a click is measured by the delivery layer. Those columns render and REFUSE, rather than accepting a drop and then failing.
- **Every column states where its evidence comes from** — `measured` (delivery), `tracked` (the customer's own conversion tracker), `stated` (a person). It sits on the COLUMN because it is a property of the step: every lead's booked meeting is stated and every lead's click is measured, so it is said once instead of once per card. This is the half a customer asked for out loud: which of these did we update ourselves.
- **The cards cost NO network.** Which column a lead sits in comes off the two sources the page already holds — the lead row for `contacted`, the `/revenue` join for every step after (`trackedStages` + `leadBoardColumnFor`). A board of 500 cards must never be 500 requests, so do NOT reach for `useLeadStepStatements` per card. Consequence accepted: a `never` statement lives only on the per-lead read, so a lead marked dead sits at its last reached column; the panel is where a `never` is stated and read.
- **The write is row-scoped** (`useSetAnyLeadStepStatement`): the board has no open lead to bind a per-lead hook to, and holding the target in state first would race the submit. It invalidates `["featureRevenue"]`, which is what actually moves the card.
- **It draws only when exactly ONE funnel is in scope.** A brand selling through several has no single order to lay columns out in — the same refusal `leadFunnelStages` already makes. With several funnels the tabs stand alone, whatever the toggle last said.
- **Drag is a mouse affordance**, so every card carries a `Move` control listing the same targets. It is BORDERED rather than a bare grey word: a quiet text control reads as a label and never gets pressed, which is how the verify screen's resend link went unused.
- `lib/lead-board.ts` is alias-free so it carries real unit tests; `CompanyLogo` moved to `components/company-logo.tsx` when the table and the board both started drawing it. Guards: `tests/lead-board.test.ts` + `tests/lead-board-surface.test.ts` (which pins the CALL SITE, not just the component). (#3666)

**UPDATE — the board is a TRIAGE now, four columns, and it answers a different question from the funnel rungs it replaced.** Not "how far down the funnel is this lead" but "is this one still in play, and if not, why not". A funnel rung is stated on the lead's own PANEL, which is where its cost and value are asked for; the board has nowhere to put either. Consequence accepted: there is no drag-to-book-a-meeting any more. The columns are `Leads` · `Sales interest` · **`Close won`** · `Disqualified` · `Opt-out` · `Not placed`, and they carry no funnel, so the board renders at BRAND level too (the one-funnel gate is gone with the rungs).

Two splits are the whole design, and both come from the sales canon rather than from our data model:

- **Opt-out is not an opinion.** It is the prospect's own act and it is legally binding (CAN-SPAM, GDPR), so it never shares a column with a commercial judgement of ours. ⚠️ It USED to be the only column a card could neither enter nor leave, and both halves of that were wrong — see the ANY-TWO-COLUMNS update below, which supersedes this bullet's second half. ⚠️ **It is a STANDING STATE now (`opted_out`), not a shade of `disqualified` this app splits on the deciding evidence.** That split was a rule of ours over a producer's field, and it broke in exactly the way this file keeps recording: the day lead-service named the state (sales-lead-service#502), `opted_out` matched no branch here, fell through to the default, and every person who asked us to stop was filed into "Not placed" — nothing thrown, nothing red, one column quietly empty. The pre-split spelling is still honoured (a `disqualified` row whose signal is an unsubscribe reads as an opt-out however it is spelled, because offering an ordinary disqualification's moves would put somebody back in play who asked to be left alone), and the consumer shipped AHEAD of the producer so there was no window in between. (#3875)
- **A "no" is not a disqualification.** Salesforce and Forrester split the negatives into permanent (this person can never buy: wrong role, they moved on) and temporary (not now, no budget yet, not interested today), and Forrester warns that one bucket holding both quietly loses recyclable pipeline. So `Not interested` stays in **Leads** — owner-decided, and the reason is that in sales a no is where the conversation starts. **That rule is now enforced by lead-service, not here** (see the DISQUALIFIED-MEANS-NOT-OUR-TARGET update below): when placement moved to the producer it collapsed again for a while, because the producer could only see a coarse `negative` and every negative looked alike to it. Only an OBJECTIVE fact about the person reaches Disqualified. **A BOUNCE stays in Contacted too**: it is a failure of DELIVERY, not an opinion, so it is an address to repair rather than a lead to write off. Neither is guessable from the column names, so the board states both in a footnote and each column carries a one-line blurb.
- **A REFERRAL ("Not them, but points us on") is NOT sales interest** — it is not THIS person's buying interest, which is the distinction the column exists to make, and Outreach's own taxonomy keeps Referral as its own class for the same reason. It reads as Contacted; the new lead it produces arrives as its own row.

⚠️ **A value listed AHEAD of the catalogue is a TODO with no owner, and it never ships unless somebody remembers — `lead_changed_job` sat unrendered for weeks and nobody could state it.** The consumer-ready-first discipline is right (list it as a bare string, let `columnReplyKinds` filter it against the catalogue, so a value nothing renders can never reach a button) and it has a failure mode the discipline itself hides: the filter is SILENT. instantly-service shipped `lead_changed_job`, the label was never added here, so the Disqualified picker offered `Wrong person` alone and a person who had simply left the role could not be qualified at all — while the gateway would have accepted the write. Nothing was red; the button was just missing. **So when you list a kind ahead of the catalogue, the same commit adds a guard that FAILS once the producer serves it**, or add the label the day you see it upstream. Fixed 2026-09-01 (#3818).

**`Close won` is the producer's `customer` standing given its own column, and moving a card INTO it states a SALE while moving one OUT WITHDRAWS that statement.** It used to fold into `Sales interest` on the reasoning that a customer reached the step the campaign sells and then some — right while the board was four buckets of "still in play or not", and wrong the moment somebody wants to see what they actually WON: a closed deal read as a warm one, which is the fact a person triaging a list most wants told apart. Reported as the table's Close won column being invisible at campaign grain, which it is by construction (`boardOnly = Boolean(campaignId)` means the table never mounts there), so the answer was the column the board was missing rather than the table it does not draw.

- **The write is a funnel-step statement, NOT a reply kind** (`columnReplyKinds("won")` is empty for that reason, like `opt_out`'s): a drop opens the SAME `CloseWonForm` the leads table mounts — whose win it was, then what it cost and what it was worth — and sends `step: "sale", kind: "outcome"` through `useSetAnyLeadStepStatement`. **One form, two mount points** (`components/leads/close-won-form.tsx`), because two copies of "did we cause this deal" is how one surface comes to ask a question the other does not, about the same deal.
- **Leaving is a WITHDRAWAL, and that is lead-service's own rule, not a preference** — its API doc: *"stating the other thing to undo a mistake is itself a false statement, and it keeps counting"*. So `useWithdrawAnyLeadStepStatement` (the row-scoped twin of the panel's hook) takes the sale back, the card carries NO destination out of it (where somebody lands once the statement is released is the producer's answer), and the confirmation says both what it removes and that what was stated stays readable. ⚠️ A withdrawal only works on a statement a PERSON made: a tracker-reported `customer` is a 409 `not_a_statement`, which surfaces as the producer's own sentence rather than a silent no-op.
- **Cards move between ALL SIX columns, both ways** (owner-decided, and the first cut had it wrong: it made `won` a one-way column on the reasoning that a reply kind cannot un-say a sale, which is true and is an argument for the withdrawal, not for a dead end).
- **The two sale moves are handled FIRST in `onMove` and hold nothing optimistically.** They address a lead ROW (a statement belongs to the `(lead, campaign)` row) while every other board write is keyed on the EMAIL so it reaches every campaign at once — so they cannot ride the `statedReplyKinds` latch, and pinning the card to the column it was dropped on would state a placement the producer has not given yet.
- **The value field opens with the lead's OWN funnel price**, resolved by the page (`prefillUsdFor`) and carried on the card: the board has no business reading the offer's declared funnels, and a per-card lookup inside the form would be a second resolution of one fact.
- ⚠️ **Six columns × the 208px floor is 1308px with gaps, so the rail scrolls below ~1310px** — measured against the app's own compiled Tailwind at 1280 and on a Pixel 7. `Not placed` is `hideWhenEmpty`, so a healthy campaign draws five and fits. The form is bounded (`w-64`) inside the move panel because `StageStatementForm` stacks its fields to the right for a table CELL; at the panel's full width the question sits a thousand pixels from the inputs answering it.
- ⚠️ **A `const` a `useMemo` reads must be DECLARED ABOVE that memo, and nothing in this repo's toolchain says so — `tsc`, the suite and every source-substring guard pass on a page that throws on mount.** `boardColumns` calls `prefillUsdFor` while it builds its cards; the callback was declared ~185 lines below it, which is a TDZ `ReferenceError` at RENDER time. `tsc` sees the binding on the inferred type and is happy, the memo's deps array carries an `eslint-disable-next-line react-hooks/exhaustive-deps` (so the lint rule that WOULD have named it is off), and a source-substring guard asserts the call is spelled right, never that it can run. The customer sees a blank page and a minified `Cannot access 'ej' before initialization`. Guard it with an INDEX COMPARISON (`page.indexOf(decl) < page.indexOf(consumer)`), the same shape as the reveal-precedence guards elsewhere in this file — it is the only instrument that can see declaration order. Generalise: **when you add a helper that a memo/effect declared EARLIER in the component consumes, put it above that consumer and pin the ordering**; the deps-array disable comment is the tell that nothing else is watching. (#3903 → #3905.)
- Guards: `tests/lead-board.test.ts` + `tests/lead-board-surface.test.ts` + `tests/lead-close-won-column.test.ts` (which pins that BOTH surfaces mount the one form).

**Where the two classifications live, and which wins.** sales-lead-service serves the COARSE `replyClassification` (`positive | negative | neutral`) plus `unsubscribed` and `bounced` on every lead row; instantly-service serves the FINE kind per statement. A stated kind OUTRANKS the coarse one — a person read the message, the classifier guessed at it — and it decides interest AND its absence, so a human saying "not interested" is never overridden by a machine reading the same message as positive. Precedence overall: opt-out, then disqualified, then interest, then contacted.

⚠️ **The BOARD reads the fine kind while the STAT CARDS read the producer's COARSE projection of that same kind — so a rule this repo states about a reply kind is only HALF a rule until the producer's projection agrees, and the half that is missing is the one a customer reads as the number.** `leadBoardColumnFor` places a card off `replyKind`; `recipientsRepliesPositive` (the Sales Interests card, its cost, and the learning gate) is counted by features-service off `replyClassification`, which instantly-service freezes from its own kind→coarse map at write time. Two sources, one screen. Nothing goes red when they disagree: both values are real, both parse, both render — the board simply says one thing about a lead and the card beside it says another, which gets reported as "the number is stuck" rather than as a disagreement. Cost 2026-08-29 (instantly-service #649 → v0.75.2): `lead_referral` was excluded from `INTEREST_REPLY_KINDS` here months ago with the reasoning written out in full, and instantly-service still projected it to `positive`, so a customer who requalified a lead from *interested* to *referral* watched their campaign keep reporting **1 sales interest** for a lead their own board had correctly moved to Contacted. Two rules from it: when you change which column a reply kind lands in, `git grep` that kind in instantly-service's coarse map in the SAME breath; and remember a stored classification is FROZEN at write time, so a producer fix needs a backfill or every existing row keeps the old answer forever.

⚠️ **Corollary, and it is why the producer's fix is a DIVERGENCE rather than a re-sync: "worth READING" and "counts as a BUYING SIGNAL" are two different questions and a referral answers them differently.** instantly-service forwards a referral to the agency inbox (a name to talk to is worth a human's eyes) and reports it `neutral` (it is not this person's interest). Its forwarding set is therefore a deliberate SUPERSET of its coarse map's positive entries, guarded by a test that asserts exactly that relationship. Do not read the two as one concept in either repo.

**UPDATE 2 — at CAMPAIGN grain the board is the WHOLE page, and a card is moved by HOLDING it.** No view switch, no funnel tabs, no table: a campaign sells one funnel and its leads are worked one by one, so "where is this person" is the whole question and a second view of the same rows is a control that only ever answers what the page beside it already answers. `boardOnly = Boolean(campaignId)` gates both. **At BRAND grain the toggle and the table STAY, and that is not an oversight** — with no `campaignId` the reply-kind read is disabled and `canMove` is false, so the board there shows the coarse machine classification and can write nothing; taking the table away would leave a read-only board with no dates, no sort and no pagination.

- **HTML5 `draggable` is GONE, and its two failures are why.** It fires on no touch device at all, and its drag image is a screenshot the browser composites — which is exactly where the square white corners behind a rounded card come from. Replaced by pointer events: `pointerdown` starts a `LONG_PRESS_MS` (350) timer, a wander past `SLOP_PX` (8) before it fires means the finger was SCROLLING and cancels it, and the ghost that follows the pointer is a LIVE element, so its radius, its shadow and its opaque background are the card's own. Measured, not asserted: radius `8px`, background `rgb(255,255,255)`, opacity `1`, `pointer-events: none`.
- **ONE gesture, two actions, told apart by TIME.** A quick press that did not wander opens the lead's panel; a hold picks the card up. `touch-action: none` is set ONLY on the card currently lifted — set on every card it would kill the board's own scrolling on every touch device.
- **EVERY column accepts the drop; the form is where a move is refused** (`columnMoveRefusal`). A target that silently rejects a drag reads as a broken board rather than as a rule. Only `unresolved` ("Not placed") is refused now, and it is not a preference: that column is lead-service reporting it could not answer, which no statement of ours settles. `columnReplyKinds("opt_out")` is still empty, but an empty picker there no longer means "nothing can be written" — see the update below.
- **The rail SCROLLS ITSELF under a card held at its edge.** Four 256px columns do not fit a phone, so most of the board is off-screen while a card is up, and a target you cannot reach is a target the drag cannot use. A `requestAnimationFrame` loop rather than a scroll-per-move, because a finger parked at the edge emits no further move events.
- **The columns SHARE the page width — `min-w-[13rem] flex-1 basis-0`, never a fixed `w-64 shrink-0`.** How many there are is a property of what is being triaged (four today, and a funnel that adds one must not change how the board reads), so a fixed column width leaves a third of a desktop page empty and was reported as such. `basis-0` makes the split even whatever a column holds; the floor is what hands the overflow back to the rail, so the phone case and the many-columns case are the same case and neither needs a breakpoint. Measured against the app's own compiled Tailwind: 4 columns of 303px filling 1248 at 1280 and 383px at 1600, 6 columns pinned at the 208px floor with the rail scrolling, and 208px scrolling on a Pixel 7. Guard: `tests/lead-board-surface.test.ts`.
- **The card is the shape a campaign wears everywhere else** (`CampaignIdentity`): the ORG on top, the person under it, quieter, behind **Via** with their photo (`FullLead.photoUrl`, initial when absent or hotlink-blocked). **Every card wears a tag** — the stated kind when there is one, the COLUMN's own word when there is not, since a tagless card reads as one we know nothing about when we always know at least that we contacted them.
- **The tag row says WHAT the card is and HOW LONG it has been that — and that line replaced the per-card move menu.** A `Move` button first sat under the tag and cost the card a whole row; it became a `⋯` on the tag's own row; it is now GONE, and the space it held states the age (`timeAgo`, `text-gray-400`). A tag with no age is the one thing a triage board cannot be read for. **The TAG is what gives way when the row is tight, never the date** — the longest kind (`Not them, but points us on`) is far longer than any age, and a truncated `3 h…` states nothing at all; so the tag is `min-w-0 truncate` and the date is `shrink-0`. Measured at 1280 and on a Pixel 7 against the app's own compiled Tailwind: one row, zero overflow, centres aligned, card unchanged at 78.5px.
- **A stated kind is dated by the STATEMENT, everything else by the delivery status.** `qualifiedAt` when somebody said it (the delivery event underneath is a different moment), otherwise `leadDateForStatus(lead, getLeadConsolidatedStatus(lead))` — the same one map the leads table's Date column reads, so the two surfaces cannot date one lead two ways. The optimistic held statement carries its own `new Date()`, since that IS when the person said it. **No instant ⇒ the card says nothing**, never a borrowed date.
- ⚠️ **Accepted consequence: a MOVE now needs a pointer.** The `⋯` menu was the only keyboard/no-drag path to move a card; OPENING one is still keyboard-reachable (`role="button"` + `tabIndex={0}` + Enter/Space) and a move is the hold-drag and nothing else. Owner-decided — do not re-add the menu without re-deciding it.
- Verified by REPRODUCTION at 1280 and on a Pixel 7 (esbuild-bundled component + compiled Tailwind off the app's own `globals.css` + Playwright), never by grepping classes: tap opens, hold lifts, a drop writes NOTHING until a kind is picked. Guards: `tests/lead-board.test.ts` + `tests/lead-board-surface.test.ts`.

**The fine kinds are read ONCE per campaign and joined by email** (`["campaignReplyKinds", campaignId]` → `listManualQualifications({campaignId, limit: MAX_REPLY_KINDS})`), never per card: a board of 500 leads must not be 500 requests. The producer caps that read at 500 rows newest-first, so a campaign past the cap `console.warn`s rather than letting its older cards fall silently back to the machine's classification. Because the kind is recorded against a CAMPAIGN, a brand-level board shows the triage and cannot write it (`canMove={Boolean(campaignId)}`) — the cards then carry no Move control rather than one that fails on press. A move states a reply kind through the SAME `setManualQualification` the lead panel calls, and it ASKS WHICH: "Sales interest" is three different things a prospect can have said, so a drop opens a picker of that column's own kinds instead of writing a guess. What was just stated is held locally (`statedReplyKinds`, keyed on email) so the card jumps before the re-read lands, and a refusal drops it. Guards: `tests/lead-board.test.ts` + `tests/lead-board-surface.test.ts`.

**UPDATE 3 — DISQUALIFIED MEANS "NOT OUR TARGET", AND A CARD MOVES BETWEEN ANY TWO COLUMNS, OPT-OUT INCLUDED, IN BOTH DIRECTIONS.** Two customer-reported bugs, one session, four repos (#3818 + #3820; instantly-service #682, email-gateway #193, sales-lead-service #477, api-service #901). Owner, verbatim, on the old Disqualified blurb (*"Out of play: they said no, they opted out, or they cannot buy"*): **"PAS DU TOUT!!! TU M'ENRAGES."**

**The column means the ordinary sales qualification and nothing else** — the wrong contact, somebody who has left the role, a company that is not who we sell to. His analogy is the one to keep: *we sell pears to supermarkets and we emailed somebody who works in construction.* A no about the offer TODAY is a judgement about the moment, so that person stays reachable and stays in **Leads**; an opt-out is the prospect's own act and has its own column. Folding either in is what turns Disqualified into a dumping ground and loses recyclable pipeline silently.

⚠️ **The fault was NOT in this repo, and reaching for the dashboard would have been the wrong fix.** Placement is lead-service's (#3807), and lead-service could only see a coarse `positive | negative | neutral`, so every negative reply looked identical to it. instantly-service had drawn the right line in its own vocabulary all along (`lead_not_interested` recyclable; `lead_wrong_person` / `lead_changed_job` permanent and about the PERSON) and that distinction simply never survived the trip downstream. The fix is the chain — instantly serves the finer reading on its status read, email-gateway forwards it, lead-service reads the DERIVED answer (never a copy of the reply vocabulary) — and the dashboard re-decides nothing. **A same-day dashboard-side re-split was offered and deliberately NOT shipped**: it would have contradicted lead-service, been impossible at brand grain, and had to be un-shipped hours later. Verified on the reporter's own campaign after the chain landed: 1 lead moved from `disqualified / negative_reply` to `engaged / negative_reply`, zero cards disqualified, bounces untouched.

⚠️ **`unresolved` is a real answer on that rung.** lead-service reads an ABSENT `disqualified` as `unresolved` / `reply_disqualification_unknown` rather than defaulting either way — "no" would be a claim it makes on the provider's behalf, "yes" is the bug being closed. So a provider without reply tracking can legitimately push cards into "Not placed"; that column exists for exactly this.

**A card moves between any two triage columns, both ways** (`movableColumnsFrom` offers every writable column except the one it is in). The owner again: *"Une personne ne clique pas forcément elle-même sur le bouton unsubscribe, imagine qu'elle m'envoie un SMS pour me le demander ?"* The old lock was wrong in BOTH directions, and this is the durable half:

- **Inbound.** A prospect rarely clicks the link — they send an SMS, they call, they say it in person. Refusing to RECORD what they said is not protecting their consent, it is ignoring it while we keep emailing them. `POST /emails/opt-outs {email, channel, notes?}` records it, scoped to the **PERSON** across every campaign the org holds for that address (which is why the body carries no campaign id although the card belongs to one), and it STOPS the sending at the sender — the click path gets that for free because the provider served the link, a statement made over SMS does not.
- **Outbound.** An opt-out gets put on the wrong person, and a prospect can come back. A locked column left a database write as the only fix. `POST /emails/opt-outs/withdrawals` appends a withdrawal, never an erasure, and touches only the unsubscribes THAT record produced — one the prospect made by clicking is never released, because nobody withdrew it.
- **What protects the person is not the lock.** It is that nothing INFERS an opt-out; that the CHANNEL is required and asked for rather than defaulted (`OPT_OUT_CHANNELS`, mirrored from instantly-service — a consent record nobody can audit later is exactly what a consent record must not be); that leaving is the only move on the board asking for a confirmation; and that ⚠️ **a withdrawal RESUMES NOTHING that was stopped** — the holds were cancelled and the campaigns paused, and silently restarting outreach at somebody who asked us to stop is the one mistake worth being unable to make by accident. The form says both halves before it does either.

**Three writes, kept apart rather than flattened into "a move"** (`LeadBoardMove`): a reply kind against the CAMPAIGN, an opt-out against the PERSON, and the withdrawal of one. Flattening them is what would let a reply kind be written where a consent record belongs. ⚠️ The withdrawal deliberately holds NO column — where a released person lands is lead-service's answer, and guessing it here would be this app deciding a standing again, which is the whole thing the board stopped doing. The card therefore sits still for one round trip; that is honest, not a bug to patch.

⚠️ **`ManualQualificationStatus` in `lib/api.ts` was a FOURTH copy of instantly-service's reply vocabulary and had already gone stale** — it re-listed the nine kinds and never grew `lead_changed_job`, so the api client would have refused a write the gateway accepts. It reads `ReplyKind` now, plus the two retired deal-progress spellings the write path still tolerates, and a guard fails if any kind is re-listed there. Generalise: the middle-service-re-declares-an-enum trap fires **inside your own repo's api client** too, and `tsc` only points at it the day you widen the source.

Guards: `tests/opt-out-channel.test.ts` (real unit tests — `lib/opt-out-channel.ts` is alias-free, keep it that way) + `tests/lead-board.test.ts` + `tests/lead-board-surface.test.ts`.

⚠️ Shipping this closed a THIRD instance of the dark-remap gap: `text-green-700`, `border-green-200`, `text-red-600` and `border-red-200` — the weights the reply-KIND pills read through, already live in the lead panel — had no `html.dark` rule, so a pill legible in the light default carried near-black text and an invisible border on the dark surface. The `-50` fills beside them had been remapped and the weights had not, exactly as with the Learning tag and the stale-build notice. When you add a tinted chip, remap the FILL AND the text/border weights in the same commit.

**The Leads page states TWO outreach counts, like the campaign Overview.** It counts PEOPLE (its tabs bucket the same `listBrandLeads` snapshot the table renders), so that count is `Leads contacted` — passed as `contactedOverride` — and the ACTIONS beside it are read off `/revenue` by `OutreachStatCardsAuto` itself, because a page counting people has no second count to give. The wrapper names that second card `Outreaches` only when both are on screen. **`salesInterestSharePct` (`lib/funnel-share.ts`, alias-free, real unit tests) is the ONE home of the `X% of contacted` line** — the campaign Overview and the Leads page both read it, so they cannot state the same percentage two ways.

**A CAMPAIGN-scoped leads read is BRAND-SIZED, so it is sized like one — both readers send `view=basic`, and neither is a "small" read.** `listCampaignLeads` went without the slim projection for as long as a campaign scope meant one stored campaign row and a handful of leads. `sales-lead-service#412` made a campaign read answer for the whole campaign IDENTITY (see the rule above), which silently turned it into a brand-sized read: measured on one prod brand, `?campaignId=` returned **53,777 rows / 156 MB / 54 s** against **102 MB / 6.6 s** with `view=basic`, and `EngagedLeadsPage` polls it every 30 s per open tab. Both readers now ask for the slim projection, pinned in `tests/campaign-leads-endpoint.test.ts`. Generalise: when a producer widens what a scope RESOLVES TO, every consumer sized for the old scope is now mis-sized, and nothing goes red — the reader still parses, the page still renders, it just moves ten times the bytes.
  - **The failure mode this produced is worth recognising on sight: an infinite skeleton whose cause is the GATEWAY dying, not the page.** api-service buffered lead-service's already-streamed response into JS objects and re-serialized it (`await response.json()` → `res.json()`), so a 156 MB body OOM-killed the process (`FATAL ERROR: Reached heap limit` inside `JsonStringifier`, 5 restarts). An OOM kills every in-flight request for every org, not just the one that triggered it. The page's own reveal-on-settle gates cannot help: the request never returns at all. Diagnose by reading `docker logs distribute-api-service-1` on the box before touching the component. Fixed in api-service v0.98.4 (`pipeExternalService`, raw bytes) — do NOT reintroduce parse-then-re-serialize on that route.
  - **`GET /v1/leads` forwards its query VERBATIM as of api-service v0.98.6** — it used to rebuild the upstream query from a fixed whitelist (`brandId, campaignId, limit, offset, view`), silently dropping anything else, which is the same gateway-strips-a-field bug documented for the features `groupBy`/`funnel` whitelist. So a new lead-service filter reaches it with no gateway PR. (`limit`/`offset` were in that whitelist and `/orgs/leads` implements neither — a gateway's copy of a downstream's vocabulary rots.)
  - **A `skipped` lead is invisible on the CUSTOMER page and rendered on the STAFF one, which is why the two apps ask for different rows.** `skipped` matches no engagement tab, so the customer page can display none of it; lead-service's default therefore excludes it (`sales-lead-service#416`: 53,760 of 65,956 rows for one brand). `apps/admin` DOES render Skipped / Buffered / Claimed tabs, so its leads views send `status=all` (#3411). Keep that asymmetry in mind before "syncing" the two apps' readers — it is deliberate, and dropping admin's `status=all` empties three of its tabs.
- **Entity-count badge MUST mirror its page's data source (`listingFallback`), not a feature-stat** — badge≠page is a DASHBOARD bug even when the empty page traces to an upstream gap. Resolution order: `listingFallback[entity.name]` (the page's count) → `fStats[entity.countKey]`. An entity missing from `listingFallback` falls through to a `countKey` counting a DIFFERENT data layer. Unify BOTH the page render AND both badges on ONE canonical endpoint. (#1231)
- **(REMOVED #2196) The canonical quote-requests / quote-PITCHES readers (`listAllRankedOpportunities`, `listAllQuotePitches`) + their pages and sidebar badges are GONE** — the quote routes were orphaned (no nav) and deleted along with the readers, the `["rankedOpportunities"]`/`["quotePitches"]` query keys, and the `/orgs/{opportunities,quote-pitches}` consumer wrappers in `api.ts`. The public-report quote surface is unaffected (it never imported these authed readers). If quote/HITL is rebuilt, re-add the paged reader (never a hardcoded `limit`) AND a nav entry. (history: #1287, #1367)
- **A feature SLUG re-version (`-opportunities` → `-outreach`, any `X-v1`→`-v2`) rots EVERY hardcoded-slug consumer — gate the FAMILY via a shared helper, never a per-slug `===`.** ~9 slug special-cases across the authed + report trees (campaign HITL queue, `PROMPT_EDITABLE_SLUG`, report draft/reply handlers, report quote-requests page, `REDIRECT_TO_FIRST_ENTITY`, report sidebar, `REPORT_ENABLED_FEATURES`, report header labels). `tsc` + tests stay green on a stale literal. Fix: `grep -rn "<old-slug>" apps/dashboard/src`, replace with ONE helper (`slug.startsWith("<prefix>")`, e.g. `lib/expert-quote-feature.ts` → `isExpertQuoteFeature`) + a unit test over the whole family + a hypothetical `-v2`. Thread the REAL `featureSlug` (not a constant) into per-feature calls (prompt-fork) or the new feature uses the old prompt. (#1292, DIS-197)

### Clerk avatars — `imageUrl` is ALWAYS set; gate real logos on `hasImage`

Clerk's `organization.imageUrl` / `user.imageUrl` is **never null** — when nothing is uploaded Clerk returns a generated gradient-initials avatar hosted on `img.clerk.com`. So `imageUrl ? <img> : <initial>` ALWAYS renders Clerk's default, and an `onError → initial` fallback never fires for it (the default image loads fine). To show a real uploaded logo only, gate on the **`hasImage: boolean`** discriminator (true only for a deliberate upload). For orgs without an upload, prefer a logo.dev logo keyed on the org's domain (onboarding sets org `name` = brand domain → `OrgAvatar.orgDomainFromName` in `breadcrumb-nav.tsx`), else the initial badge — never Clerk's default gradient. (#1321 shipped the default by mistake; #1327 fixed it.)

### Public marketing pages — SEO + AI-scraper rendering

For any public landing/marketing/docs page (anything under `apps/landing/`, `apps/sales-cold-emails-landing/`, `apps/docs/`), the default rendering strategy is **ISR + `unstable_cache` + edge cache**, NEVER Suspense streaming on indexable content.

Why: Next.js streaming SSR injects content into `<template>` blocks resolved by client-side JS. Googlebot (Chromium-headless) executes JS and sees the resolved content, but **AI scrapers (GPTBot, ClaudeBot, PerplexityBot, Bingbot AI, curl-like crawlers) parse raw HTML only** — they receive the skeleton fallback, not the data. Putting leaderboards, benchmarks, blog lists, stats behind `<Suspense>` kills AI Search indexing for the very content that earns AI Search traffic.

Concrete rules:

1. **No `<Suspense>` on indexable content.** Hero copy, h1/h2, body text, JSON-LD blocks, leaderboards, benchmark tables, blog cards, stats — all SSR-sync in the byte stream. Suspense reserved for user-specific, non-indexable widgets (logged-in dashboards, real-time counters, chat).
2. **Never combine `force-dynamic` with `revalidate`.** `force-dynamic` wins and ISR is silently disabled. Prefer ISR (`export const revalidate = N`) alone; let `unstable_cache` + graceful build-time empty-result handle the missing-config case.
3. **Calling `headers()` or `cookies()` in a page forces dynamic rendering** even with `revalidate` set. Drop them if the page can be ISR-prerendered (resolve hostname-dependent URLs from `NEXT_PUBLIC_*` env vars instead).
4. **Wrap every fetch / DB call used by a public page in `unstable_cache`** with explicit tags. Edge cache (`s-maxage / stale-while-revalidate`) provides instant HTML; `unstable_cache` provides instant data on the rebuild after invalidation. Both stack.
5. **Invalidate via `revalidateTag(tag, profile)`** in the publish/update flow (webhooks, admin forms). Next 16.2+ requires the 2nd arg `profile: 'default' | 'seconds' | 'minutes' | 'hours' | 'days' | 'weeks' | 'max' | CacheLifeConfig`. Pass `"default"` for ordinary invalidations.
6. **`unstable_cache` at module top level breaks vitest** because the wrapping runs at import time before `vi.doMock` can intercept. Mock `next/cache` globally via `apps/landing/tests/setup.ts` (`unstable_cache` → pass-through identity, `revalidatePath` / `revalidateTag` → `vi.fn()`); declare `setupFiles: ["tests/setup.ts"]` in `vitest.config.ts`.
7. **Validate scraper output after any caching change** with `apps/landing/scripts/seo-snapshot.sh [BASE_URL]` (counts `<h1>`, `<h2>`, `<article>`, JSON-LD blocks under a `GPTBot` user-agent) and `apps/landing/scripts/measure-ttfb.sh [BASE_URL]` (warm/cold TTFB per route). Both pre- and post-change snapshots in the PR description.

Incident 2026-05-24 (distribute.you#1153): pre-refactor, `/blog` TTFB was 1.5–2.3s because `force-dynamic` overrode `revalidate = 60` and every visit hit Neon directly. `/benchmarks` was dynamic because `headers()` was called for hostname resolution. `/investors` took 6–8s end-to-end because metrics were `cache: "no-store"`. Three loading.tsx files existed but were never reachable (page components weren't async-suspended). Single PR flipped 8+ routes from `ƒ Dynamic` to `○ Static` ISR with `unstable_cache` + tag invalidation; no Suspense or skeleton was added on any indexable content.

### Adding a brand optimization goal touches ~8 dashboard surfaces — wire ALL or it ships half-done

`BrandOptimizationGoal` (the "Maximising X" goal a brand optimizes for) has repeatedly shipped INCOMPLETE — a new goal added to some surfaces but not others, leaving an internally-incoherent UI (goal selectable in Settings but absent from the Overview picker; strategy header defaulting a real goal to "Sales meetings"; unit cost falling back to the meeting cost). #2402 (website_visits/positive_replies), #2424 (form_submissions), #2475 (purchase) each had to backfill gaps a prior goal PR left. When adding/renaming a goal, wire EVERY surface below in ONE PR (grep the goal literal across `apps/dashboard/src` to find stragglers):

1. **`lib/api.ts`** — `BrandOptimizationGoal` union · `normalizeBrandOptimizationGoal` (WIRE value → local; e.g. brand-service snake `combined_sales` ↔ local `sales`, `website_purchase` 1:1, legacy `sales`/`purchase` → `website_purchase`) · `serializeBrandOptimizationGoal` (local → wire; `sales` → `combined_sales`) · `isVisitDrivenGoal` · `SalesObjective` union + the `WorkflowProjectionResponseSchema.objective` z.union · `salesObjectiveForOptimizationGoal` · **AND `RuntimeGoal` — the goal a CAMPAIGN carries. campaign-service forwards the funnel's goal VERBATIM from brand-service, so it is the SAME canonical vocabulary, and `optimizationGoalForRuntimeGoal` therefore DELEGATES to `normalizeBrandOptimizationGoal` rather than re-deciding: a second mapping is a second place to go stale, which is exactly what happened (it was a three-token union ending in a bare `return "sales_meetings"`, so `formSubmission` printed "Sales Meeting from Conversation" on a brand that had declared only Form Magnet — #3253). `runtimeGoalForOptimizationGoal` is DELETED with `NewCampaignModal` (#3180); do not re-add a brand→campaign goal mapping.** The audience-stats row type+Zod also carry per-goal evidence/metrics (`evidence.sales` + `metrics.cpsaleCents` for the sales/website_purchase goals — note lowercase `cpsaleCents`; revenue spend block uses `salesCount`/`cpSaleCents`).
2. **`lib/strategy-model.ts`** — its LOCAL copy of `isVisitDrivenGoal` (vitest has no `@` alias, so it can't import api's) · `goalForOptimizationGoal` (→ `FeatureAudienceStatsGoal`) · `isVisitDrivenStatsGoal` (the stats-goal sort-metric family) · `outcomeNoun` · `objectiveForOptimizationGoal`.
   - **A goal that BORROWS a neighbouring family instead of sending its own native goal is a WRONG NUMBER, not a rendering approximation — features-service ranks workflows BY the goal you send, so borrowing returns a different outcome's winner and then prints THAT workflow's cost for YOUR outcome.** `goalForOptimizationGoal` mapped `positive_replies → meetingBooked` and `website_visits → signup` (a shim from before features shipped the single-step goals). Prod, one brand, one moment: Strategy sent `objective=positive_replies` → Dawn, cheapest per reply, **$61.73**; Audiences + the Overview top-audiences card sent `goal=meetingBooked` → Osprey, cheapest per CLICK, whose cost per reply is **$183.85**. Both labelled "fleet benchmark", 3× apart. `FeatureAudienceStatsGoal` now carries `positiveReply` + `websiteVisit` (features' canonical enum, already served) and every goal maps to itself. When a producer ships a native goal, MIGRATE the borrow — a stale shim is silent and only shows up as two prices on one screen.
   - **All three audience-stats call sites derive the goal from `goalForOptimizationGoal(optimizationGoal)`, never a local ternary** (brand Overview `brands/[brandId]/page.tsx`, `campaigns/campaign-overview-page.tsx`, `audiences/customer-audiences-page.tsx`). Two of them had drifted to a 2-branch copy (`isVisitDrivenGoal(g) ? "signup" : "meetingBooked"`), so `sales` / `form_submissions` / `website_purchase` brands were ALSO asking for the wrong goal there. Sort metric comes from `isVisitDrivenStatsGoal`, not a `=== "signup"` literal.
   - **A goal-gated COLUMN must test the goal FAMILY.** `showMeetingCols === "meetingBooked"` alone hides the Positive-replies + CPPR columns on a `positive_replies` brand — the one whose outcome IS the reply. Guards: `tests/audience-stats-goal.test.ts`. (cppr-mismatch-audiences)
3. **`lib/workflow-projection-choice.ts`** — `serverOutcomeUnitCost` + `workflowOutcomeUnitCostFromRates` need a branch, or the goal silently falls to the meeting-cost `else`. A goal with no dedicated grain field reads `resolved.costPerOutcomeUsd` (captured onto `WorkflowProjectionItem.costPerOutcomeUsd` in `ladderRowToWorkflowItem`) — the native per-goal cost features-service ranks on.
4. **`components/brand/brand-status-control.tsx`** — the Overview modal: `GOAL_LABEL` Record + the `GOAL_OPTIONS` picker array (both keyed on `BrandOptimizationGoal`; `beta: true` gates via `useIsBetaUser`).
5. **`components/settings/brand-sales-economics-card.tsx`** — `OPTIMIZATION_GOALS` array + `PCT_FIELDS[].goals` (which conversion-rate fields show) + `REQUIRED_FIELDS_BY_GOAL` Record. A goal with no dedicated backend rate REUSES an existing pair (purchase reuses `visitToSignupPct × signupToPaidClientPct` — brand-service has no purchase-specific rate).
6. **`components/strategy/strategy-page.tsx`** — the "The plan" header (Objective label + conversion-rate rows) must cover the goal, else it defaults to the sales-meetings branch.
7. **The three `Record<BrandOptimizationGoal, …>`** (GOAL_LABEL, REQUIRED_FIELDS_BY_GOAL, strategy-page OBJECTIVE_LABEL) are exhaustive → `tsc` CATCHES a missing key there, but the ternary/switch surfaces (picker array, unit-cost branches, strategy conversion rows) compile fine while silently mis-defaulting. Those are the ones that ship broken.
8. **Backend is usually already ahead** — verify the goal's enum on brand-service `PUT /orgs/brands/{id}/sales-economics` (`optimizationGoal`) + features-service `GET /features/{slug}/workflow-projection` (`goal`) via api-registry BEFORE assuming a backend gap; these goals were dashboard-only wiring. (#2402, #2424, #2475)
9. **`components/onboarding/onboarding.tsx`** — the sales-goal step is ALSO a goal-listing surface. `Outcome === BrandOptimizationGoal`; `OUTCOMES` (label + unit + `beta`) is the picker, and the restore validators + `ONBOARDING_STATE_VERSION` must accept any new goal key. There is NO per-goal rate list here any more — the old `RATE_KEYS_FOR_OUTCOME` held the entry legs of DIFFERENT funnels, so it asked for numbers belonging to no single path; every rate the flow collects comes from the funnel catalogue's own chain. Beta goals are `useIsBetaUser`-gated in the funnel with a `<MaturityBadge level="beta"/>` (currently only signups/sales_meetings ungated). Google-Ads conversion-category LABELS (Sign-ups / Book appointments / Page views / Contacts / Submit lead forms / Purchases) live here, distinct from the settings card's `# Signups`-style labels. (#2508)
10. **`components/audiences/customer-audiences-page.tsx` + `components/revenue/top-audiences-card.tsx`** — the two per-audience RANKING surfaces. Both lead with the goal's OUTCOME cost, and both read that choice from ONE helper: `strategy-model.ts` `audienceRankMetric(goal, trackerSetUp)` → `cppr | cps | cpfs | cpsale | cpc`, with `AUDIENCE_RANK_METRIC_{LABEL,INFO,OUTCOME_NOUN}` carrying the words. The table also renders a count column beside each cost (`showMeetingCols` / `showSignupCols` / `showFormSubmissionCols` / `showSaleCols`; `isPositiveReplies` hides the visit cols). **The card MUST NOT read the response's `sortMetric`** — features-service classes `websitePurchase` + `sales` as reply-driven and returns `cppr` for them, so the card printed "CPPR $319 / 0 replies" on a goal whose funnel has no reply step while the Audiences page hid the reply columns for that same brand. Same reason neither Overview sends `limit` (the server would pre-pick its top 3 on ITS column); the card sorts + slices on the shown metric. The tracker gate is load-bearing: no tracker → no attributed signups/form-submissions/sales → fall back to the measured visit cost, never an all-`-` column. Every number is features-service-owned via `fetchFeatureAudienceStats`: `evidence.{websiteClicks,positiveReplies,formSubmissions,signups,sales}` + `metrics.{cpcCents,cpprCents,cpfsCents,cpsCents,cpsaleCents}`, the outcome fields present ONLY for their own goal and `.optional()` on the wire (render `-` until the producer ships). A new outcome goal → a branch in `audienceRankMetric` + its 3 word entries + the table's `showXCols` pair + optional `evidence.X`/`metrics.cpXCents` on the api.ts type+Zod, and ask features-service for the per-audience attribution. Guards: `tests/audience-rank-metric.test.ts`. (#2632; features-service #554; top-audiences-goal-metric)

**The goal DISPLAY surfaces (Leads tabs, top stat cards, Outreach-activity graph bars, the leads table's per-tab column) read ONE source — `lib/goal-steps.ts` `goalSteps(goal)` — NOT a per-surface `isVisitDrivenGoal(...)` binary.** `goalSteps` returns the goal's ordered funnel steps (base→outcome), each declaring its per-surface mapping: `tab`/`signal` (Leads tab, outcome-first via `goalLeadTabs`), `chartKey` (activity bar via `goalChartMetricKeys`), `outcome` (stat-card count/cost fields via `goalOutcomeStep`). The `switch` is **exhaustive over `BrandOptimizationGoal`** → `tsc` catches a goal added to the config surfaces (items 1–7) but not wired here, so a new goal can't half-ship on the display side. Extend `goalSteps` (add the step + its surface bindings), never re-introduce a `goal === "x" ? … : …` branch inside `outreach-stat-cards`/`pipeline-activity-chart`/`engaged-leads-page`. A step has a `tab`/`chartKey` ONLY where the data exists per-lead/per-day: signup + form-submission outcomes are brand-level aggregate-only today (stat card yes; Leads tab / per-lead surface no) until features-service attributes them per-lead. (#2482)

**A CAMPAIGN-scoped surface keys those steps on its FUNNEL, never on the goal — `stepsFor(goal, funnelKey)`, and the goal is only the fallback.** The goal is the lossy vocabulary: `reply_meeting` and `visit_meeting` both answer to `sales_meetings`, so `goalSteps("sales_meetings")` has to cover BOTH paths (`[Outreach, Website Visits, Positive replies, Sales Meetings]`) and a goal-keyed campaign surface therefore hands the website-visit leg to a campaign whose chain starts at a positive reply. That shipped: a *Sales Meeting from Conversation · Sales Cold Email Outreach* campaign showed **Outreach 9,802 · Website Visits 44 · Cost per website visit $51 · Sales Meetings** — the wrong funnel's steps under the campaign's own name, and the reply it actually buys nowhere on the page. `funnelSteps(funnelKey)` in the same module is the fix, mapping each funnel onto the SAME step constants (identity, so a label/colour/rate cannot fork): `reply_meeting` → Outreach · **Positive replies · Sales Meetings**; `visit_meeting` → Outreach · Website Visits · Sales Meetings; `visit_signup` → Outreach · Website Visits · Signups; `visit_form` → Outreach · Website Visits · Form submissions. Five rules:
- **`stepsFor` / `leadTabsFor` / `chartMetricKeysFor` / `outcomeStepFor` / `outcomeTabFor` are what every funnel-aware surface reads.** An absent-or-null `funnelKey` returns the goal's answer byte-identically, so the brand Overview (a brand runs several funnels at once, so it states none) and a pre-funnel campaign (`funnelKey: null`) are untouched. That is what makes the prop safe to add everywhere.
- **Reads the WIRE spelling** (`SalesFunnelKeyWire`, via `normalizeSalesFunnelKey`), so the canonical rename lands for free.
- **Every funnel chain ends on its own terminal outcome**, so a campaign always carries an outcome pair — unlike the 1-step goals, which have none.
- **The stat cards decide each pair from the STEP LIST, not a `goal === "x"` test**: `showVisitPair = hasStep("website_visits")`, `showReplyPair = hasStep("positive_replies") && !isPositiveReplies`, and `isPositiveReplies = hasStep("positive_replies") && outcomeStep === null` (a reply that is TERMINAL — the `positive_replies` goal — versus a reply that is a MID-chain signal above its own Sales Meetings pair). The old `showReplyPair = goal === "sales"` is exactly what starved the reply→meeting funnel of its first step.
- **The chart, the Leads tabs and the Outcome line follow the same funnel or they contradict the row above them** — a Website-Visits bar under a card row that no longer mentions visits is the self-contradictory-surface bug one component over. The Leads page reads the funnel off `["campaign", campaignId]` (the key the campaign Overview and the top bar already poll → one deduped request), and its one-shot auto-tab latch WAITS for that read (`if (campaignId && campaignData === undefined) return;`) or it parks the user on a tab the chain does not offer.
Guards: `tests/campaign-funnel-steps.test.ts`. (campaign-funnel-vocabulary)

**Third recurrence, and the sweep that keeps missing it: a campaign-scoped surface keys on the ARROW, and the surfaces that price AUDIENCES were the last two still keyed on the funnel's goal.** The rule above moved the stat cards, the chart and the Leads tabs onto the funnel; #3728 moved the lead panel's Funnel-progress section onto the LEG. The Audiences table and the Overview's Top-3 audiences card were neither: a cold-email campaign performing only the entry leg of the Form Magnet funnel (`→ Website visit`) led with `Cost per form submission` + `Form submissions`, an arrow a human works and which has its own page under the funnel. Every figure real, nothing red, and the reader ranks their audiences on a price the campaign has nothing to do with.

- **`lib/campaign-leg-columns.ts` (alias-free, real unit tests) is the ONE map** from the step a leg LANDS ON to the column pair that prices it and to the cost column a ranking surface leads with: `conversation→reply/cppr`, `website_visit→visit/cpc`, `signup→signup/cps`, `form_filled→formSubmission/cpfs`, `paid_client→sale/cpsale`.
- **Both meeting steps are DELIBERATELY absent.** features-service serves no per-audience meeting count or price, so `legColumnPair` answers null rather than the arrow before it — lending a campaign a neighbouring arrow's columns is the bug, not the fallback. A guard walks every arrow of every funnel and pins the whole map, so a NEW step must decide its pair rather than silently reading null.
- **The leg is resolved with the fleet's own precedence** — `statedCampaignLeg(funnel, campaign.legKey, legIndex) ?? campaignLegFor(funnel, channel.legs)` — off reads already in flight (the campaign row, the platform leg catalogue), so scoping these surfaces costs no request. A second derivation is how two surfaces come to name different arrows for one campaign.
- **Every fallback is UNCHANGED behaviour, never a guess.** An unplaceable leg, a meeting step, or a tracked pair on a brand with no conversion tracker (`legPairIsAvailable`) all keep the funnel-wide gate these surfaces read before legs existed; brand and offer grain are untouched. Emptying the table instead would trade a wrong column for no column.
- ⚠️ **When you scope a surface to a leg, grep for EVERY sibling still keyed on the goal in the same pass** — the two here survived two prior sweeps because each was already conditional on something else (`optimizationGoal === "form_submissions" && trackerSetUp && !brandLevelMoney` reads as a complete rule), which is the same sibling-condition trap `showVisitCols` fell into. Guards: `tests/campaign-leg-columns.test.ts`, which pins the lib AND both call sites. (#3880)

**One level deeper, and it is the same mistake wearing the next vocabulary: a CAMPAIGN-scoped surface walks the ARROW the campaign performs, not the whole funnel.** The rule above moved the stat cards off the goal onto the funnel because a goal cannot name which chain a campaign sells. A funnel cannot name which ARROW of it a campaign runs — it is sold leg by leg, so cold email puts a lead onto the reply funnel and does nothing else, while a closing team converts an attended meeting into a paid client and nothing else. The lead panel's **Funnel progress** section walked all four steps of `reply_meeting` on a cold-email campaign, offering a live `Happened / Won't happen` control for three arrows that campaign does not run — each of which already has its own page (`…/funnels/:key/legs/:legKey`), worked by whoever performs it. Nothing was red: every row rendered, every control wrote, and the writes were even accepted.

- **`leadFunnelLegStages(funnelKey, leg)` is the one reader** and it returns `{ stages, later }`; the full-funnel `leadFunnelStages` survives only for a caller that genuinely walks a whole funnel. The leg comes from `campaignLegFor(funnel, channel.legs)` — the SAME function that NAMES the campaign (#3664) — over the acquisition-channel catalogue the page already holds as a `useMemo` on `["features"]`, so scoping the panel costs no request.
- **An ENTRY leg states one step; an INTERNAL leg states the step it converts FROM alongside the one it converts TO, and the `from` step stays WRITABLE.** Somebody closing a deal legitimately has to say the meeting was attended before they can say the deal closed, so making it read-only would put the two halves of one person's job on two pages.
- **An unplaceable leg falls back to the WHOLE funnel** — a channel whose feature row predates the `legs` field, or one whose legs cannot be placed among this funnel's steps, is still a campaign selling this funnel. Same fallback `campaignLegLabel` makes, for the same reason. An out-of-range index falls back too and logs loud: it is a vocabulary drift worth seeing, never an empty panel or a slice taken from indices that index into something else.
- ⚠️ **The funnel's REMAINING steps are still carried, unrendered, as `laterStages`.** lead-service cascades a `never` across the WHOLE funnel, so the `Won't happen` control's `Also ends: …` list built from the rendered rows alone understates what one click ends — the difference between a decision and a surprise. It is never drawn; it exists for that one sentence, and a unit test walks every funnel asserting `stages ++ later` is exactly the funnel's own walk from the leg's start.
- The `Delivery` row is untouched: it is our own sending, and it sits above every funnel by construction.
- Guards: `tests/lead-funnel-stages.test.ts` (real unit tests — the module is alias-free, keep it that way) + `tests/lead-funnel-stage-panel.test.ts`, which pins the CALL SITE and not only the component. Generalise: **when a surface is scoped to a campaign, ask which ARROW it is about before asking which funnel** — the funnel is the campaign's context, the leg is its job. (#3728)

**At FUNNEL grain the SAME panel walks the funnel WHOLE, and that whole IS the union of its legs (#3900).** The section used to be gated on `campaignId`, so the funnel page — whose entire subject is one funnel — offered no way to state what happened to a lead on it. The gate is the RESOLVED funnel now (`{panelFunnel && (`), and exactly two scopes state one: a campaign's own row, and the funnel route's own URL (`campaignFunnel(routeFunnelKey)`, which normalizes both wire spellings and THROWS on a key the catalogue does not carry, the same contract `funnel-scoped-pages` already honours on that route). Brand and offer state none, so the section still renders nothing there.

- **No union HELPER exists, and none should.** `funnelLegs` tiles the steps end to end — an entry arrow onto step 0, then one arrow per step after it — so every step belongs to at least one leg and the union of the leg slices is the funnel's own walk. Passing a NULL leg already says it, which is the `leadFunnelLegStages(key, null)` fallback the panel has always had. A unit test pins `union(legs) === leadFunnelStages(funnel)` for every funnel in the catalogue, so a funnel added with a gap in its legs fails rather than silently dropping a row.
- ⚠️ **`panelLeg` must be gated on `campaignId`, not only on `panelFunnel`.** Off a campaign route `useScopedFeatureSlug` answers with the brand's SOLE channel — non-null and plausible — so a leg derived from it would slice the funnel by a channel the reader never named, and nothing would go red: the panel would simply offer two rows of somebody else's arrow.
- **No reply picker off a campaign.** instantly-service keys a reply kind on `(campaign, email)`, so there is nothing to write it to and the read is disabled; `reply={null}` makes the Replied row fall back to `Seen` / `Not seen`, which is the honest surface for a statement this scope cannot make. Same reasoning as the two stages lead-service accepts no statement on.
- **The write needed nothing.** A statement is keyed on the `leads_campaigns` ROW id the table already carries, which is exactly how the funnel-LEG board one level down has always written without a campaign. Reading `useLeadStepStatements` wherever the section renders costs no new request and no backend change.
- ⚠️ Accepted and stated rather than hidden: that page's rows are OFFER-wide (`lead-service` has no funnel filter — its own `scopeNote` says so), so a lead whose campaign sells a sibling funnel still shows this funnel's steps.

**The activity chart's first bar counts SEQUENCES LAUNCHED, never emails sent — it reads `New leads contacted` for that reason, and a run of zero bars beside real sending is the honest picture, not an outage.** `sequences` (features-service#416) counts a lead handed to the sending provider on the day it was handed over; the emails that leave the building that day are the queue plus the follow-up steps of leads launched days earlier, and Instantly holds a lead until its next weekday window. So the two series routinely disagree by everything: prod brand `75d7e3e8` sent 293 emails on 2026-08-17 and 6 on 08-18 while launching zero new leads on both days, and under the old `Outreach` label that read as "no email sent for two days" — the report that opened the investigation. The label is local to `pipeline-activity-chart.tsx` (its own `MetricDef` map, NOT `goal-steps.ts`'s `OUTREACH_STEP.label`, which the stat cards and Leads tabs read), so changing it moves the chart alone. Guard: `tests/pipeline-activity-chart.test.ts`.

⚠️ **Zero bars there DO also mean nothing new was launched, so check whether that is intended before closing the question** — the same two days were a real halt: chat-service `/complete` 429'd on Gemini free-tier quota (`limit: 0`, every model), so brand-service field-extraction failed, the workflow failed, no email was drafted, and no lead was pushed. The chain to walk, in order, is `runs` by `service_name`/`status` for the org (a failure spike names the failing service), then that service's `docker logs`. `campaign_service.campaigns.parent_run_id` and `instantly_campaigns.created_at` are the two tables that say whether a lead was launched at all. (#3425)

**A `SignalSeries.daily` is SPARSE — features-service writes a bucket only for a day on which a lead first carried the signal, so a quiet week is ABSENT rather than present at zero, and any chart reading it must fill the gaps before plotting.** `buildSignalSeries` groups the payload's lead rows by their own timestamp; a day nobody converted on has no row at all. On a categorical x-axis (`dataKey="label"`, which is every chart here) each present day takes the same width, so two consecutive days and a three-week gap render identically and the dates read as unevenly spaced. The cumulative Outcome line had the same defect twice over: it climbed diagonally across days on which nothing happened, where a cumulative total is flat across a gap and jumps where the outcome landed. `fillMissingDays` in `outcome-trend-card.tsx` inserts the absent days at zero before cumulating — that is READING the producer's contract (an absent day had no lead), not inventing a series, and it is the same client layer that already computes the running total. It fills up to the day before the dashed forecast starts too, so the quiet stretch between the last outcome and the projection is spaced like the rest. NOT affected: `roiHistory` (features-service drops only the LEADING days whose cumulative spend is still zero, never a middle day) and the activity chart (`pipeline-activity` serves a contiguous day window by construction). Verify a change here by lifting the helpers out of the transpiled component and running them, per the reproduce-the-render rule — a source guard proves the call is spelled right and says nothing about the shape of the series. Guard: `tests/pipeline-activity-chart.test.ts`. (#3662)

**The goal→outcome-NOUN (the plural word shown in "X <noun> / mo" budget pickers + "cost per <noun>" copy) is a SEPARATE label family from `goalSteps` and it DRIFTS — there are FOUR user-facing noun maps that MUST stay coherent, each keyed on `BrandOptimizationGoal`: (1) `onboarding.tsx`'s copy of `OUTCOME_UNIT` (inherited from the retired brand status bar, which owned it until the bar was deleted); (2) `strategy-model.ts` `outcomeNoun`/`outcomeNounPlural` (strategy card, best-model card, outcome-digest email, onboarding launch); (3) `onboarding.tsx` `OUTCOMES[].unit`/`label` (onboarding budget step + Google-Ads category labels); (4) `new-campaign-modal.tsx` `RUNTIME_GOAL_LABEL` (RuntimeGoal, not BrandOptimizationGoal).** Only `OUTCOME_UNIT` and `outcomeNoun*` are `Record`/exhaustive-`switch` (tsc-checked); a `ternary`/partial map silently mis-defaults a new goal (the Overview modal shipped a 4-branch ternary sending `website_purchase`/`form_submissions`/`sales` to **"meetings"** — count was grain-correct via `workflowOutcomeUnitCost`, only the noun wrong). **Canonical wording: `website_purchase` = "website purchases" everywhere (the `purchase`→`website purchase` rename, admin #2803) — NEVER "purchases" (stale) nor "sales" (that's the DISTINCT `sales` goal; `outcomeNounPlural` had `website_purchase`→"sales" colliding with it).** When adding/renaming a goal, grep the goal literal across ALL FOUR maps + prefer a `Record<BrandOptimizationGoal,…>` over a ternary so tsc forces the branch. (#2921)

**Corollary — tsc guards the KEYS of those maps, never the VALUES, so a wrong noun rots silently and is only ever caught by reading the rendered screen.** `OUTCOMES[]` is an array of object literals, so a nonsense `unit` typechecks forever: `positive_replies` shipped `unit: "contacts"` and the onboarding budget modal read **"50 contacts / mo"** for a goal that buys 50 interested replies — naming the people we email rather than what the budget buys, understating the tier by an order of magnitude, and contradicting the row's own label (`"Positive replies for sales meetings"`) two lines above. Fixed to `"positive replies"` in #3078, byte-equal with `OUTCOME_UNIT`. Same pass dropped `outcomeNounPlural(positive_replies)` from `"replies"` to `"positive replies"`: **a plain "reply" includes the negative ones, which are not the outcome the brand optimizes for** — the same distinction the OPENS-are-deprecated rule draws for the funnel. Test for a noun value: *does it name the thing the budget buys, or the people/action upstream of it?* (#3078)

**Fourth noun map, RETIRED with its surface: the Overview learning-window banner is deleted (see the learning-band section above), so the goal-aware noun set is `OUTCOME_UNIT`, `outcomeNoun*`, `OUTCOMES[].unit` and `RUNTIME_GOAL_LABEL`.** Keep the lesson it cost, because it generalises past that banner: **a surface that NAMES an outcome must also DISAPPEAR on that outcome — the sentence and the gate are one decision, never two.** That banner shipped hardcoded to "the first website clicks" for every goal, describing a step a `positive_replies` brand does not buy, and then hid on the first click while that brand was still waiting for its reply: the wrong noun and the wrong retirement shipped together, because they were written as two things. Two more of its rules still bind on the band that replaced it — a "is this even running?" gate reads MONEY (`useRunningDailyBudgetCents`), never a status flag nobody writes any more, and `null` there means "we cannot tell", which is never a licence to promise anything. (#3143, retired 2026-08-29)

**A daily budget is funded PER SALES FUNNEL, and the brand total is the SUM — nothing in the dashboard sets a brand-level budget any more.** billing stores a ceiling per `(org, brand, funnel)` and answers the EXISTING brand daily-budget read with their sum, which is why the launch gate (`billing-guard`), the credit runway, the credit alerts and the Overview tile never changed a line. Three consequences a future edit must not undo: there is no brand-level budget surface left at all (the status pill that STATED the sum is deleted along with its modal, its tier list and the whole `workflowProjection` query that only ever priced them — see the run-status-bar rule above); brand Settings has no daily-budget section, the field lives on the funnel it pays for; and no surface states the brand-wide SUM under a narrower thing's name — the campaign Overview's header states the CAMPAIGN's own ceiling (billing's `(offer, funnel, channel)` row) read-only, and Campaign Settings is the only writer of it (#3456, #3462). Zero is an ordinary value everywhere — it is how a funnel is put down without forgetting how it sells — and the per-funnel floors (`FUNNEL_MIN_DAILY_BUDGET_USD`, $24 meetings / $1 purchase + form) live once in the catalogue while billing's 400 decides. ⚠️ **A DAILY TOTAL states what may be spent TODAY, so it counts only the RUNNING campaigns' ceilings — and that figure is a JOIN the dashboard makes, because neither producer can answer it.** billing keys a ceiling on (funnel × channel × offer) and stores NO status; campaign-service stores the status and no money. So billing's served brand total (`GET /brands/:id/daily-budget`) is status-BLIND: a brand running one campaign at $50 beside one PAUSED at $10 answered $60, and the Overview header, the cost card's `/$budget` denominator and the monthly outcome projection all inherited it — every figure real, none of it spendable. `scopeTotalCents` (`lib/campaign-controls.ts`) is the one home of the rule and `useRunningDailyBudgetCents` the one reader, so brand, offer and campaign cannot state one number two ways; it costs no network, since both query keys are already polled by the controls trigger on the same page. This REVERSES the old "the dashboard never sums the ceilings itself" line, whose premise (billing's figure IS the answer) dies the moment status matters. Two things it does not touch: a PAUSED campaign's OWN header still states its own configured ceiling — the number Campaign Settings edits, beside a pill already saying it is paused — and the credit-runway alert keeps billing's total, because understating the burn would delay a low-credit warning. `null` while the reads are in flight, never zero: "we could not measure this" and "this brand spends nothing" are different statements. Guards: `tests/campaign-controls.test.ts` + `tests/campaign-controls-surface.test.ts` + `tests/campaign-budget-once.test.ts`. (per-funnel-budgets, running-daily-budget)

### Dynasty-First Display Rule (Workflows Only)

Always display `dynastyName` for workflows, never the versioned name — exception: settings/debug panels (show the version + versioned name alongside). Applies to titles, rows, cards, breadcrumbs. Features have no dynasty — they use `slug` + `name` directly.

## The sidebar bottom is a reward LADDER: one thing to DO, one thing to WATCH

`components/invite/rewards-card.tsx` (`RewardsCard`, mounted on all three brand-level sidebars) renders TWO cards, in that order. The invite row is a thing to do: one line, one click. The card under it is a state: money already committed to this org and how close it is to landing. They were one box briefly and the doing read as a footnote of the watching. 🎁 leads both, so they read as one ladder without being one control.

- **The invite row has NO button** (guarded). A button in a 224px rail spends a second line on a control whose whole content is "click me", and the row already is the control. It is a `role="button"` div, not a `<button>`, because the tooltip beside it is itself a `role="button"` span and a real button would nest one interactive element inside another. **Sizes are MEASURED, not guessed** — `gap-1 px-2.5` + `text-[11px]` leaves the label 142px and it needs exactly 142; widen any of them and `Give and get $500 credits` truncates.
- **The confirmation is a TOAST** (`components/toast.tsx`, the dashboard's first), because a one-line row has nowhere to put a "Copied" label. Green, `role="status"` + `aria-live="polite"` (without it a screen reader never hears the confirmation and the control reads as dead, the exact failure it exists to fix), and **centred at the bottom, never bottom-right** — the support FAB owns that corner on every page, so a toast there is the one message the user cannot read. Its timer is cleared on unmount.
- **The heading states billing's SERVED total** (`outstanding_total_cents`, billing-service v0.65.1), summed by billing on the same basis and in the same units as the rows it ships with, so the heading and the Billing list cannot state different figures. Summing the rows here is the compute-a-stat-in-the-browser bug — `reduce` is guarded against. Declared `.optional()`: an older body has no total and falls back to the nearest promise, which is true and understates rather than invents. **An absent total is not a zero.**
- **The bar and the line under it describe the NEAREST promise only.** billing returns them cheapest-bar-first, so the first row is the one the next payment moves; the rest are not listed, because a list of four promises in a 224px rail is a ledger and the ledger is the Billing page this card links to. That split is the goal-gradient reading (Hull; Kivetz and Nunes on endowed progress, and every loyalty counter that says "16 stars until your next reward"): one goal in view, stated as what is LEFT, over a partly-filled bar, with the prize named above it so the effort has a size.
- **The line names the amount it unlocks AND what that money is** (`promiseUnlockLine`, beside `promiseSubtitle` in the shared alias-free `lib/free-credit-promise-view`): `Unlock $347 free credits after $376 more in payments.` Billing's rows carry their amount on the right and keep `promiseSubtitle`; the sidebar has a TOTAL as its heading, so without the amount a customer holding three promises reads the bar as progress toward the whole. `free credits` is load-bearing rather than decoration — without it the sentence puts two dollar figures side by side and the first reads as something else the customer owes rather than as the gift the second one buys. Do NOT respell either sentence in a component.
- **A PROMISE renders in WHOLE dollars (`formatBillingCentsWhole`), a CHARGE keeps its cents (`formatBillingCents`).** Promise amounts are whole-dollar offers by construction ($5 welcome, $400 completion, $500 referral), so cents on them are noise in a 224px rail; the bar still to clear is genuinely fractional and is CEILED, because rounding it down would state a lower bar than the one billing holds. This does not touch the billing-page exemption: an exact amount someone paid must never round, which is the whole reason that page sits outside the adaptive rule.
- **No measured progress ⟹ no bar**, never a zeroed one, which reads as "you have paid nothing" for a promise we could not measure. Neither a promise nor a resolved invite code ⟹ nothing renders.
- **`["freeCreditPromises"]` is the Billing page's own key**, so the two dedupe to one request and the card paints from disk (the root is already in `PERSISTABLE_QUERY_ROOTS`). The Billing link reads the org from the URL, the per-tab source of truth, never `useParams` (the invite reads key on the INTERNAL org UUID from `billingAccount`, and a guard forbids `useParams` in this file for exactly that reason).
- Verified by REPRODUCTION at the real 224px rail (compiled-Tailwind + Playwright), not by grepping classes: zero rail overflow, invite row 35px on one line with the label at 142/142, bar fill 87 of 181 for a served 48%. Guards: `tests/referral-invite-wiring.test.ts` + `tests/free-credit-promise-view.test.ts`. (#3553, #3556)

## The landing answers agents as well as browsers: `Accept: text/markdown` on every statically-served page

`staticResponse()` (`apps/landing/src/lib/static-html.ts`) is the single choke point for all 22 static routes, so negotiation lives there and no page carries its own copy. A browser, and anything sending no `Accept` at all, gets exactly the bytes it got before. `text/markdown` gets the same page converted (`src/lib/html-to-markdown.ts`). Anything asking for neither gets a **406** rather than a document it said it could not read.

- **`Vary` cannot be set from the route handler.** Next owns that header on every app-router response and overwrites whatever a `Response` returned, so `Vary: Accept` is declared in `next.config.ts` `headers()` instead. That emits a SECOND `Vary` field line beside Next's RSC one; per RFC 9110 multiple field lines are one comma-joined value, and `fetch`/undici reads it joined (verified against the running build). Do not "fix" this by setting `vary` in the handler again, and verify any change by reading the response off a real `next start`, never by grepping the source: the source-level header was correct and the served one was not.
- **The markdown variant is `cache-control: no-store`, deliberately.** Cloudflare honours `Vary` only for `Accept-Encoding`, so a shared-cacheable markdown body could be handed to a browser asking the same URL for HTML. The HTML variant keeps its `s-maxage=86400`.
- **The converter takes no dependency.** It drops `script`/`style`/`svg`/`nav`/`header`/`footer` before converting, because these pages are hand-authored HTML full of inline assets and a whole-document conversion is noise. Alias-free, so `tests/unit/html-to-markdown.test.ts` is real unit tests.
- **`Accept: text/html;q=0` is a refusal.** Only `text/html` maps to the HTML variant; crediting a wildcard through an alias like `application/xhtml+xml` would serve the exact bytes the client said no to.
- **404 is a catch-all route handler** (`src/app/[...notFound]/route.ts` -> `404.html`), not `not-found.tsx`, because the body has to be negotiated and `not-found.tsx` cannot read `Accept` without making the root segment dynamic. `not-found.tsx` still exists for `notFound()` calls made from inside a page.
- **Organization JSON-LD comes from ONE helper.** `withCanonicalOrganization` parses each `ld+json` block, removes any hand-written `Organization` node (several pages carried one with no address and no logo, and the homepage carried none), and injects `organizationJsonLd()` from `src/lib/seo.ts` — the same object the React `layout.tsx` renders. An unparseable block is left alone rather than dropped.
- Agent-facing surfaces that must stay in lockstep with each other: `public/llms.txt` (what we are, when to use us, when NOT to, the developer URLs), `src/app/.well-known/mcp.json/route.ts` (no MCP spec defines a well-known discovery URI today, so this is a de-facto convention and the server is named in llms.txt too), and the `/about` + `/contact` trust pages. Guards: `tests/unit/agent-readiness.test.ts`.
